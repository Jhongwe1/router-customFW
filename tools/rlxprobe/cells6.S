/* cells6.S -- GENERATED by tools/tcpay.py from tools/isa-toolchain.tsv.
 * DO NOT EDIT.  Edit the table.
 *
 * One thunk per row.  The thunk's whole job is to put the SENTINEL in
 * $v0 in the instruction immediately before the load, so that a store
 * which reads $v0 too early has a known value to read.  For a compiled
 * variant that instruction is the `jal`'s delay slot; for a control it
 * is written in line.  Both are the same shape and that is the point.
 *
 * $a0 = destination word, $a1 = source word, $a2 = the sentinel.
 */
#include "rlxdefs.h"

	.set	noreorder
	.set	noat
	.set	nomacro
	.text

/* c_lock -- POSITIVE CONTROL. Hand-written lw nop sw with this payload's own operands. A correct build reads the loaded value; if this row does not, nothing below is attributable. */
	.globl	rlxp6_c_lock
	.ent	rlxp6_c_lock
rlxp6_c_lock:
	move	$v0, $a2
	.globl	rlxf_c_lock
rlxf_c_lock:
	lw	$v0, 0($a1)
	nop
	sw	$v0, 0($a0)
	jr	$ra
	nop
	.end	rlxp6_c_lock

/* c_open -- NEGATIVE CONTROL. Hand-written lw sw. isa-hazard.tsv lu_sd_d0 with these operands. Shows this payload can observe the wrong answer at all; if it reads lock the table is void. */
	.globl	rlxp6_c_open
	.ent	rlxp6_c_open
rlxp6_c_open:
	move	$v0, $a2
	.globl	rlxf_c_open
rlxf_c_open:
	lw	$v0, 0($a1)
	sw	$v0, 0($a0)
	jr	$ra
	nop
	.end	rlxp6_c_open

/* v1 -- THE INSTRUMENT'S OWN CONTROL. Every payload that has ever executed on this die was built exactly here. A refutation of this row is a refutation of probe4 and probe5 as well. */
	.globl	rlxp6_v1
	.ent	rlxp6_v1
rlxp6_v1:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v1
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v1

/* v2 -- The smallest step off the exposed side. mips2 adds the load interlock to the architecture and nothing else this fragment can use. */
	.globl	rlxp6_v2
	.ent	rlxp6_v2
rlxp6_v2:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v2
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v2

/* v3 -- The march CLAUDE.md's Never-table bans by name. This is the row that turns a rule this project follows into a rule it has demonstrated. */
	.globl	rlxp6_v3
	.ent	rlxp6_v3
rlxp6_v3:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v3
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v3

/* v4 -- rsdk-1.3.6-4181 at its own core. The release that built every kernel this project has booted. */
	.globl	rlxp6_v4
	.ent	rlxp6_v4
rlxp6_v4:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v4
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v4

/* v5 -- THE SAME RELEASE at the other march. With v4 it separates the flag from the release. */
	.globl	rlxp6_v5
	.ent	rlxp6_v5
rlxp6_v5:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v5
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v5

/* v6 -- rsdk-1.3.6-5281 at the OTHER release's core. With v7 it is the second half of that separation. */
	.globl	rlxp6_v6
	.ent	rlxp6_v6
rlxp6_v6:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v6
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v6

/* v7 -- rsdk-1.3.6-5281 at its own core. R2c column two. */
	.globl	rlxp6_v7
	.ent	rlxp6_v7
rlxp6_v7:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v7
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v7

/* v8 -- rsdk-1.5.5-5281 gcc 4.4.5 at 4181. R2c column three at the exposed march. */
	.globl	rlxp6_v8
	.ent	rlxp6_v8
rlxp6_v8:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v8
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v8

/* v9 -- rsdk-1.5.5-5281 at its own core. R2c column three. */
	.globl	rlxp6_v9
	.ent	rlxp6_v9
rlxp6_v9:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v9
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v9

/* v10 -- The fourth Lexra core name in TC-15's non-exposed set. Nothing has ever been built at 4281 in this project. */
	.globl	rlxp6_v10
	.ent	rlxp6_v10
rlxp6_v10:
	addiu	$sp, $sp, -8
	sw	$ra, 0($sp)
	jal	rlxf_v10
	move	$v0, $a2		/* delay slot: the sentinel, one instruction before the callee's load */
	lw	$ra, 0($sp)
	nop			/* this payload's OWN load-use pad -- d0 is open on this die */
	addiu	$sp, $sp, 8
	jr	$ra
	nop
	.end	rlxp6_v10

