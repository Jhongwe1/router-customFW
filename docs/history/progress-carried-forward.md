# PROGRESS.md § Carried forward — rows as they stood before a rewrite

Text moved here verbatim, one row per section, when a carried-forward row was
rewritten to its current state (`CLAUDE.md`, *Documents*: a state row is rewritten,
never appended to; its history moves here). `tools/docmove.py` proves each move.

## `CI-5`, as it stood until 2026-09-23 (the one hundred and fifth segment)

| # | Question | Owning gate |
|---|---|---|
| `CI-5` ⊘ | 🔄 **2026-09-10(第五十三段):`n` 11 → 17,帶一格都沒動,而這個區塊存在的第一個理由是這一列的**尾巴**還寫著 `n = 11`。** 量,`citime record 34397301668`(`db5741c`,BIG3 **957**)之後 `citime stats --since 2026-09-08T20:43:13Z`:**n=17,945..960 = 15 s,中點 952.5,半幅 7.5,± 0.787 %,中位數 956**,`A7` 91.0..96.7 %。`segments` **3/3**,併池正控制兩條都發射(`A8` 31.778 % 對上限 5.0、`A9` 167.61 sd 對下限 8.0 指名 `e79ff63`)。🔴 **`± 0.787 %` 這個百分比與 n 無關**:`945..960` 的中點 952.5、半幅 7.5,`7.5/952.5 = 0.7874 %`,所以帶沒動時它三段都一樣 —— 而**那正是為什麼尾巴那個 `n = 11` 讀起來像是最新的**。這一列有十一個日期區塊,最新的在**頭**,而 `spec-check` 的 `C12` 就是為 `Next after this` 的同一個排序陷阱寫的。**以後從 `citime stats` 導,不要從這一列的任何一端抄。**⚠️ `suite_cost` 同一批點是 977..1049 = 72 s、± 3.55 %,**不要拿它當帶**。 🔄 **2026-09-10：`apt` 的紀錄從 650 s 推到 **2,524.0 s**（4十一段）。** 量 `gh api repos/.../actions/runs/34372452520/jobs`：那一步 **2,524.0 s = 42.07 min**，是本列下面記的 650 s 的 **3.88×**；而**同一個 workflow 檔、相鄰兩個 run 的同一步是 30.0 s 與 24.0 s**（**84× / 105×**）。🟢 **而這一次有一個 650 s 那一次沒有的讀數：apt 完成之後的 suite 那一側被量了**—— `test-console-capture-mutants` 在那個 run 上跑 634.5 s，在相鄰的 run 上是 **775 s 且成功**，所以**整個落後量就是那一次 apt，沒有第二個成因** —— 這正是本列主張的那句話（wall-clock 不是 suite 成本）的第三個資料點。🔴🔴 **而那個讀數是一個外部來源壞掉的前沿，不只是一個極值。** 同一晚 17:30Z 與 17:39Z 兩個 run（`eb9ee6c`、`edacc59`）的 `lint` 與 `instruments` **都死在 `apt`**，exit 100，錯誤是 `E: Failed to fetch https://dl.google.com/linux/chrome-stable/... Hash Sum mismatch` —— runner 映像預裝的 Google Chrome apt 來源供應損壞的索引，**兩次獨立重現**。**16:16Z 花 2,524 s 而成功，74 分鐘後同一步直接失敗** —— 逐 step 的計時在它壞掉之前就看到了。🟢 `text` 兩次都通過，而那是跑每一道主題是已追蹤 `.md` 的檢查的 job。⚠️ **修法是晚一點重跑，不是改這個 repo 裡的任何東西。**　⚠️ **而那個 run 永遠不會進 `citime` 的帳**：它的 `text` job 已 failure，而 `citime check` 只數 successful run。 🔴 **CI 的 wall-clock 不是 suite 成本的訊號，而這一次是量到的而不是提醒。** 量 2026-09-03，`gh run view --json jobs` 逐 job 逐 step 拆三次執行：`33595236873`（09-02 05:34）wall 14:32、`instruments` exec 9:20、其中 `apt` **0:24**；`33657418793`（09-02 16:50）wall 9:35、exec 9:21、`apt` **0:24**；🔴 `33657603686`（09-02 16:52，**與前一次相隔 118 秒**）wall **19:59**、exec **19:43**、`apt` **10:50**。**`apt` 26 倍，這個 repo 一個字都沒改**；suite 那一側逐項不動（`test-console-capture-mutants` 三次都是 6:01）。`census` 的 `queued=19:49` 是它 `needs:` 別人，是結構不是延遲。⚠️ 上一段開場就是把 14m32s 讀成「suite 慢了五分鐘」（實際是 9m20s ＋ 五分鐘排隊），所以這一列存在的目的不是「以後小心」，是**把那個警告換成一個有數字的讀數**。**兩個可做的（都還沒做）**：① 在 `.github/workflows/ci.yml` 的 `apt` 步驟上加 cache 或 `--no-install-recommends`，把最大的一項從 10 分鐘壓回半分鐘；② 或者不要壓，但**在任何寫上去的地方都不要引用 run 的 wall-clock**，只引用逐 step 的數字。🔴 **先做②，因為①會改 CI 而 CI 現在是綠的，而②不花任何東西**。🔄 **2026-09-03（第二十九段）：兩次新的 run，五個資料點，而第五個抓到這一列自己用錯的量。** 這一列（與那一段的第一版寫上去）都拿 `instruments` 的 **exec** 當「suite 側」的代表 —— **而 `exec` 是所有 step 的和，`apt` 在裡面**，所以 `33657603686` 的 1183 s 之所以是 1183，正是因為它的 `apt` 是 650 s；它不能同時當「suite 側穩定」的證據。**承載這個宣稱的量是 `exec − apt`。** 量，五次三天：**536／537／533／533／523 s**（`apt` 24／24／650／29／35），全距 14 s ≈ **±1.3 %**。🟢 **結論沒有倒，緊度從 ±0.4 % 收到 ±1.3 %。** 🆕 **而今天兩次相隔 29 分鐘、母體只差兩個散文檔的 run 逐 step 拆開，是比逐 job 更強的證據**：每一支 suite 都在 ±5 s 內（`test-spec-check-mutants` −5、`test-console-capture-mutants` −3），**而 `apt` 自己 +6** —— 逐 job 只能說「總和穩定」，逐 step 能說「每一支都沒動」，而這一列要的是第二句。**所以 ② 要收窄**：不只是「不要引用 wall-clock」，是**不要引用任何含 `apt` 的合計**，包括 `exec`。🔴 **2026-09-06(第三十六段):這一列自己停在 n=5 停了三段,而那三段一共加了七個點——全部只寫進 `LOG.md`,沒有一個進到這個擁有者。** 這正是這個 repo 自己的規矩(一個狀態一個擁有者)在自己身上失效:`LOG.md` 是紀錄不是擁有者,而每一段都在紀錄裡把 n 加一,沒有人回頭看這一列。**補齊,母體 n=13**：536／537／533／533／523／532／529／532／533／532／530／525／**534** s。全距仍是 **14 s**(523–537)、中點 **530**、半幅 **7**、**±1.32 %**——🟢 **從 n=5 到 n=13 帶寬一格都沒動**,而第 12 點(`34013708455` 的 525)與第 13 點(`34020901033` 的 534,就是這一段自己上機半那個 commit 的 run)都落在既有全距內。⚠️ **第 13 點是在同一段裡收的**,因為那個 run 在收工寫紀錄的時候跑完了;`472d70f`／`670f29b`／`6d3204d` 三個 run 留給下一段點清 —— 這一列不寫死「這一段有 N 個 run」。⚠️ **判定 `apt` 的規則不變**:步名等於 `apt` 或以 `Run sudo apt-get` 開頭,不用子字串;量到的 `apt` 是 25 s／36 s,而 `text`／`lint`／`census` 三個 job 沒有 `apt` 步驟。🟢 **順帶一個兩側都有讀數的結論**:`census` 的 `merge the captures` 與 `census` 兩步在 GitHub 上是 **0.0 s 綠**(兩份 run 都 7/7),在這張桌子上是紅——**所以「桌面那兩紅是結構性的」現在是量到的,不只是寫下的** 🔄 **2026-09-06(第三十七段,同日第四段):四個點入帳,而重新導出它們的過程在這一列自己身上找到三個缺陷,三個都是同一類 —— 規則寫在散文裡,而每一次動手的手都做了不一樣的選擇。** 🟢 **四個目標值全中**:`34021008582`=**533**、`34021165444`=**534**、`34021286020`=**536**、`34021447809`=**535**。🔴 **① 這一列寫著「`text`／`lint`／`census` 三個 job 沒有 `apt` 步驟」——假的,而且是被這一列自己寫下的規則抓到的。** `lint` 有一步 `Run sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck`;量,導出來的 44 個 run **一個都沒有例外**(5–22 s,零個 0),而 `git log -S'apt-get' -- .github/workflows/ci.yml` 全歷史只回一個 commit `970f041` —— **`apt` 進到 `ci.yml` 的那一次**,所以這句話從寫下的第一天就是假的。🔴 **② `apt` 的標註中途換了母體。** 歷史那五點的 24／24／650／29／35 逐項等於 `instruments` 自己的 apt;而這一段開場交辦的四點 22／22／23／39／24 逐項等於 `instruments`＋`lint`(16+6／16+6／17+6／31+8／19+5)。兩個都是對的量測,量的是兩件事,而被寫在一起。**①正是讓②看不見的原因**:相信 `lint` 沒有 apt,「run 的 apt」和「instruments 的 apt」就是同一個數。🔴 **③ 更貴的一個:`exec` 的定義也中途換過,而它動到的是這一列引用的數字本身。** 量,對那五個 run 兩種算法各算一次:`33595236873` wall 560／sum 556、`33657418793` 561／558、`33657603686` **1183／1180**、`33747027566` 565／562、`33749622915` 559／558。**這一列寫的 536／537／533 逐項等於 `wall−apt`,而 533／523 逐項等於 `sum−apt`** —— 同一個五點序列裡兩種 `exec`。(1183 這個數這一列自己引過,所以前三點是 wall 沒有疑義。) 🟢 **所以規則從散文變成程式:`tools/citime.py` ＋ `tools/ci-suite-cost.tsv`。** 它從 `gh` 導出、一行一個 run,`instr_apt_s` 與 `lint_apt_s` **分兩欄**所以②不可能再發生,而且**永遠不印裸的 `exec`** —— `exec`／`apt`／`exec−apt` 三個一起印,引錯是一個選擇而不是一個意外。五個控制:`A1` 缺時間戳的 step 拒絕整列(當成零會讓 job 變短、讀起來像 suite 變快)、`A2` apt 規則一次都沒發射的 run 拒絕、`A3` **把子字串規則會多抓的 step 逐一印出名字**(在這個 repo 是 `capture dir`／`replay-capture …`／`merge the captures`／`test-console-capture …` —— *capture* 裡面就有 *apt*)、`A4` job 集合不同的列不許併池、`A5` 沒成功的 job 拒絕。自我測試 **9／9**,含負控制 `P9`(把 apt 規則掏空,整套必須壞掉);`P8` 在寫的時候真的抓到一個 bug —— `read_tsv` 把自己的欄位標題當成資料列。 🔴 **導出來的母體不是一個母體,而這是這一列今天最大的一件事。** 量,44 個成功的 run(2026-08-31→09-06)併起來是 **523..551 = 28 s、±2.61 %**。做一次單一變點掃描,**而且對「我是看過資料才選切點」付了代價** —— 統計量取所有切點的最大值,置換檢定 20,000 次:變點落在 **2026-09-01T14:27:11Z**,兩側平均差 **12.643 s**(左 n=16 平均 543.75,右 n=28 平均 531.11),**p = 0.00070**。⚠️ **原因未定**:那個視窗裡只有兩個 commit,一個只加一張卡片,另一個加 145 個 `bench/` 檔 —— 都不會讓 suite 快 12.6 s,而且方向是變快。runner 那一側是剩下的候選,而這個 repo 量不到它。 🟢 **變點之後的帶寬,n=28:523..536 = 13 s、中點 529.5、半幅 6.5、±1.23 %。** 🟢 **而把這一列自己的 17 點改到同一個基準上,答案一模一樣。** 只有前三點要改(wall→sum:536→532、537→534、533→530),改完是 **523..536 = 13 s、中點 529.5、半幅 6.5、±1.23 %** —— **逐位數等於那 28 點導出來的帶寬**。兩個母體,一個是每段手挑一個 run、一個是 GitHub 上全部的成功 run,在同一個定義下對到小數點。 ⚠️ **所以「從 n=5 到 n=17 帶寬一格都沒動」這句話要換掉。** 帶寬停在 14 s 不是因為 suite 穩,是因為**三個點高了 3–4 s,而高的原因跟 suite 無關**;改對之後是 13 s,而且不再是巧合 —— 它等於獨立導出的那一個。**這一列以後引用 `citime stats` 的輸出,不抄數列**,而 `citime check` 會點名 GitHub 上沒有列的 run,所以「停在 n=5 停三段」從結構上不會再發生。 🆕 **這一段自己的 run 是 `34027603554`(`47dfb2a`)，四個 commit 一次 push。** 🔴 **而它紅了，紅在 `test-file-modes`：`tools/citime.py` 回到 `100644`。** 這一段已經修過一次，而拆 commit 前的 `git reset` 把那次 `--chmod=+x` 一起退掉了，`git add` 又把 `core.fileMode=false` 下的舊模式抓回來 —— **`CLAUDE.md` 寫過的那個陷阱，同一個檔案踩兩次，第二次是我自己把自己的修正推翻的**。⚠️ 本機 sweep 的 `test-file-modes` 在凍結樹上跑過並且綠，因為 `git reset` 發生在 sweep **之後** —— **一個在正確狀態下取得的綠結果，不能拿來證明它之後的樹**。⚠️ 這一列不寫死「這一段有 N 個 run」——記下 run id 與導出方法，數量交給下一段點清，而導出方法現在是 `tools/citime.py record <run-id>`，`citime check --last N` 會點名 GitHub 上沒有列的 run。 🟢 **修好之後 `34028209788`(`e4e7fd4`) 四個 job 全綠，`text` 51 步。** 🟢 **而 `citime` 第一次用在真實資料上就讓一個控制發射了**：`A5` 拒絕了那個失敗的 run（一個中途停下的 job 的時間不是成本），綠的那個記進去是 **535**。🟢 **變點之後 n=29：523..536 = 13 s、±1.23 %**，帶寬沒動，新點在帶內。 🔄 **2026-09-06(第三十八段，同日第五段)：四個 run 入帳，而驗證它們的過程在這一列自己身上找到一個沒有任何地方寫下來的慣例。** 🟢 `citime check --last 40` 點名的四個跟交辦的一字不差：`34028703137`=**527**、`34031008830`=**534**、`34031168702`=**535**、`34031232174`=**531**，四個全部落在 523..536 帶內。**527 用 `jq` 走一條完全不碰 `citime` 程式碼的路獨立重算一次，548 − 21 = 527，相同**；五個 run 都重算了，五個全相同。 🔴 **而那個慣例是這個：變點那一列自己歸哪一側，整個 repo 一個字都沒寫。** `33519565710`(`24f5e26`、529) 的時間戳就等於切點；排置檢定把它放在**右側的第一個**(左 n=16 平均 543.75)，而我用 `>` 切得到 n=32、擁有者說 33。**重現出擁有者自己的 n=16 平均 543.75 才是「我拿到它的慣例」的控制。** 🟢 **所以慣例從散文變成程式：`citime stats --since changepoint`**，加上 `P10` 這個控制（邊界那一列屬於從它開始的那一段，並把錯的慣例寫在旁邊）。輸出：**變點之後 n=33，523..536 = 13 s、中點 529.5、半幅 6.5、±1.23 %** —— **帶一格都沒動**。以後引用工具輸出，連切片都不手算。 🔴 **`citime` 自己的 A3 docstring 也錯了，而 `P3` 看不到**：它寫「在這個 repo 上是四個」而工具自己每一次都印**六個**——那四個是 `_fixture_normal()` 裡的名字，漏掉的兩個 `test-console-capture`／`test-console-capture-mutants` 是真的 step。**一個建在假資料上的案例檢不了一句關於語料庫的宣稱。** 🔴 **這一段自己的 run 交給下一段點清**，導出方法是 `citime record <run-id>` 加 `citime check --last N`。**已知的：`34040804067`（`f8d5d96`）與 `34041174474`（`45f151f`），兩個都在收工前跑完了，所以結果記在這裡而不是交出去**——而這一列仍然不寫「這一段有 N 個 run」，因為記錄自己的 CI 結果會再產生一個 run。 🟢 **`A5` 在真實資料上第二次發射**：`34040804067` 被拒絕，`A5 job 'text' conclusion='failure'` —— 一個中途停下的 job 的時間不是成本。（它紅在 `spec-check`，缺陷是 `FW-42` 的擁有者欄指錯，已修，`45f151f` 全綠。） 🔴 **而綠的那個是這條帶建立以來第一個掉出去的點：`34041174474` = 538，帶是 523..536。** 開場白說掉出去是一個發現，所以它被拆開了。🟢 **逐 step 對最近的參考點 `34028209788`（`e4e7fd4`，535，同一天、同一個 job 集合）：貢獻 503 秒的那三支一秒都沒動** —— `test-console-capture-mutants` **361 → 361**、`test-console-capture` **84 → 84**、`test-deskchan` **58 → 58**。整個 **+3** 落在五個短步驟上，每一個剛好 ±1 秒：`checkout` 3→4、`upload-artifact` 1→2、`test-isa-probe` 0→1、`test-spec-check-mutants` 25→26、`test-rtkimage` 1→0。🔴 **那是整秒量化的形狀，不是 suite 的形狀。** 讀，GitHub 的 step 時間戳是 `%Y-%m-%dT%H:%M:%SZ` —— **整秒**，而 `suite_cost` 是 `instruments` 十六個 step 扣掉 `apt` 之後**十五個整秒項的和**，所以兩次執行差幾秒可以完全來自真實時間落在各自那一秒的哪裡。⚠️ **這使帶的外緣不可靠，而不是使帶無效**：半幅原本 6.5 s，而十五項各 ±1 s 的量化噪聲與它同一量級。🟢 **它正好證實這一列自己早就寫下的那句話** —— *逐 step 的證據比逐 job 的更強，因為它顯示沒有任何一支 suite 在動* —— **以後引用逐 step，帶只當粗篩。** 新的帶（`citime stats --since changepoint`，不抄數列）：**n=34，523..538 = 15 s、中點 530.5、半幅 7.5、±1.41 %**。 🔄 **2026-09-07（第三十九段）：第三個 run 入帳，而這一列最大的一件事是「帶的外緣不可靠」從一句話變成一個分解。** 🟢 **點名的是 `citime check --last 40`**（37 個成功、50 列有紀錄、1 個缺），唯一缺的 `34043834567`（`adbf9c9`）**用 `gh` ＋ 真 `jq` 1.7 走一條完全不碰 `citime` 程式碼的路獨立重算：`instruments` exec 582 − apt 50 ＝ 532，而 `citime record` 印的是`suite_cost=532 (instr 582 - apt 50; lint apt 6)` —— 逐位數相同，連兩個中間項都相同**。`A3` 的語料庫宣稱同時被獨立確認：我的 `jq` 數到 7 個命中、**6 個相異名字**（`capture dir` 在兩個 job 各一次），與工具去重後印的六個相同。新的帶：**n＝35，523..538 ＝ 15 s、中點 530.5、半幅 7.5、±1.41 %、平均 531.51、中位數 532.0** —— 帶一格沒動，新的點落在中位數上。 🟢 **`A5` 第三次發射，而獨立路徑看到它比自己寫的理由更寬。** `34040804067` 被拒絕：失敗的是 `text`，`census` 是 **skipped**，而 `instruments` **16 個 step 全成功**、它自己的 exec−apt 是 **535**、落在帶內 —— 一個完整的量測被丟掉。docstring 寫「一個中途停下的 job 的時間不是成本」，那句話描述的是 `text`，不是被拒絕的那一列；**那一列被拒絕是因為一列是一個單位**（`text_s`／`census_s` 會變成看起來像量測的數）。規則對，理由描述的不是它做的事 —— 與 `A3` docstring 同一個形狀，**不改行為，只改 docstring**。 🟢 **分解，量 2026-09-07，對 51 個 run 的 `gh` JSON 逐 step 重算。** `instruments` **16 個 step、51 個 run、step 名稱序列只有 1 種**，所以 `A4` 說的「一個母體」在這裡是量到的而不是假設的。**從原始 step 列重算的 suite_cost 是 n＝35、523..538 ＝ 15 s、中位數 532，與 `citime stats` 逐位數相同** —— 這條帶的第三條獨立導出路徑。🔴 **三支主導的 suite（`test-console-capture-mutants` ＋ `test-console-capture` ＋ `test-deskchan`）佔成本 94.4 %，合起來的全距只有 497..504 ＝ 7 s、±0.70 %；其餘 12 個 step 佔 5.6 % 的成本，卻佔 53 % 的帶寬（24..35 ＝ 11 s、±18.64 %）。** 單一最寬的是 `test-spec-check-mutants`：中位數 22 s、16..26 ＝ **10 s ＝ 它自己的 45 %**；第二寬的是 `Run actions/checkout@v4`，中位數 4 s、3..8。 🔴 **而那一項在這張桌子上被重現了，絕對幅度比 CI 還大。** 同一顆 commit（`adbf9c9`）、`nproc=8`、loadavg **0.00**、同一道指令，連跑三次：**36.14 ／ 42.04 ／ 48.77 s**，全距 **12.63 s ＝ ±15.0 %**。**三個控制，前兩個否證了我先想到的機制：** ① 同一支 suite `--jobs 1`：**111.97 ／ 124.09 s**，全距 **12.12 s**，絕對抖動一樣 —— **不是平行度**；② 另一支也是 `--jobs 8` 的 `test-console-capture-mutants`（桌面 370.33 s、CI 361 s）在 CI 上是**全場最緊的**，5 s ＝ ±0.7 % —— 平行度第二次被否證；③ 一支長度相當的序列 suite `test-deskchan`：桌面 **60.61 ／ 59.84 ／ 61.96 ＝ ±1.75 %**，CI 57..60 ＝ ±2.6 %，**兩邊一致，所以這張桌子不是一個吵的儀器**。⚠️ **機制未定**，決定它的實驗：桌面重複要 n≥5 且打亂順序（兩條序列都單調上升，`36→42→49` 與 `112→124`，n＝3 與 n＝2，不算發現）；`/tmp` 的 `sc-mut-*` 殘留是 **0 個**，累積這個候選被排除。 🟢 **這一列以後引用 BIG3（±0.70 %）而不是 `suite_cost`（±1.41 %），而且不把 `test-spec-check-mutants` 當成本訊號** —— 上一段寫的「引用逐 step」現在有了「引用哪幾個 step」。 🔄 **2026-09-07（第四十段，同日第二段）：四個 run 入帳，而這一列上一段自己寫下的新規則，只活在散文裡 —— 這是同一個形狀的第三個實例，而且是在記著這個形狀的那一列裡產生的。** 🟢 **四個 run**：`citime check --last 40` 點名（37 個成功、51 列有紀錄、4 個缺），`34050588023`=**534**、`34050871155`=**539**、`34051113209`=**537**、`34052273693`=**529**。**四個全部用 `gh` ＋ WSL 真 `jq` 1.7 走一條完全不碰 `citime` 程式碼的路獨立重算，逐位數相同 —— 連 `exec`、`apt`、`lint apt` 三個中間項都相同**（`A3` 的語料庫宣稱同時第三次被獨立確認：7 個命中、6 個相異名字）。⚠️ **第四個在交接時還在跑**，所以開場的 `citime check` 只點得出三個 —— `check` 只看**成功**的 run，那是它對而不是它漏。🔴 **而 `539` 掉出帶外（523..538），這是這條帶第二次有點掉出去** —— **上一段新寫的規則第一次被用上，就是它回答了這件事**。🟢 **BIG3 n=35 → 39：497..504 ＝ 7 s、中點 500.5、半幅 3.5、±0.70 %、中位數 502 —— 一格都沒動**；同一批點讓 `suite_cost` n=35 → 39 從 523..538（15 s、±1.41 %）變成 **523..539（16 s、±1.51 %）**。**兩欄、同一組新資料、一個動一個不動**，而這是量出來的不是論證出來的。🔴 **所以規則第三次從散文變成程式**：`citime` 的 tsv 多一欄 `big3_s`，`A6`（**部分** BIG3 拒絕整列 —— 三支只加到兩支會小一截，而成本欄裡小一截讀起來是「suite 變快了」而不是「suite 沒跑」；**完全沒有**則寫 `-`，那是 `A4` 的地盤不是缺陷）、`A7`（每次 `stats` 都印 BIG3 佔 `suite_cost` 的比例並設下限，低於就 exit 1 —— **量：93.5..95.4 %，n=39，下限 85 % 是推**；併池比例 **94.34 %**）。自我測試 10 → **15/15**，含負控制 `P15`（把 BIG3 集合掏空，那一欄必須報不出數字）。🟢 **回填時把這個檔自己的宣稱測了第一次**：docstring 寫著這串序列是**導出的不是維護的**，所以整個 tsv 被**刪掉**再從 `gh` 重建 —— **55 列全部回來，`suite_cost` 逐位數相同，0 移動、0 遺失**，55 列全部拿得到 BIG3 值。🔴 **而重新導出時抓到上一段這一列自己一個數字**：「其餘 12 個 step 佔 **53 %** 的帶寬」重現不出來。12 個 step 的其他數字全部逐項重現（**24..35 ＝ 11 s、±18.64 %、佔成本 5.66 %**），但 53 % 是 `1 − 7/15` —— 它把帶寬當成可加的，而帶寬不可加：**7 ＋ 11 ＝ 18，對 `suite_cost` 的 16**。**撐得住的說法是三個各自的全距，不是三個佔比**，這一列以後不寫那個百分比。🟢 **這一列以後引用 `citime stats` 印的 BIG3 那一段，連 `suite_cost` 都不抄** —— 工具自己把 `suite_cost` 標成「coarse sieve，不要當帶引用」。⚠️ **沒有解決的兩件事一件都沒動**：`test-spec-check-mutants` 為什麼抖 12 秒仍然未定，🔴 **而這一段提了第四個機制，然後被自己的控制擋下來** —— 桌面跑套件時 repo 在 **9p**（`/mnt/c`），CI 不是。量：同一個 84,059 位元組的檔案讀 40 次，三輪，**9p 121／111／115 ms 對 ext4 43／43／40 ms ＝ 約 2.8 倍**。⚠️ **但相對抖動兩邊差不多**（±4.3 % 對 ±3.6 %），**所以它解釋得了桌面與 CI 的絕對差，解釋不了 ±15 % 的抖動**。旁證：sweep 跑 `test-rbcheck` 時 4 分 01 秒牆鐘只用掉 32 秒 CPU、狀態 `D+`，**87 % 在等 I/O**。**新的決定實驗**：把整棵樹複製到 ext4 上跑同一支套件、n≥5 且打亂順序，與原本那個實驗一次做完。`A5` 排除紅 run 有沒有讓帶偏仍然 n＝1 🔄 **2026-09-07（第四十一段，同日第三段）：三個 run 入帳，而這一批新點把上一段那個「一個動一個不動」變成第二次，而且更強。** 🟢 **點名的是 `citime check --last 40`**（37 個成功、55 列有紀錄、**3 個缺**）。交接時第三個還在跑，開場白預期「只點得出兩個」；它跑完轉綠之後三個都被點名，**那正是 `cmd_check` 只看 `conclusion == "success"` 的預期行為，不是它漏**。`34056712985` = BIG3 **503**／suite 541、`34056829568` = **500**／531、`34071562665` = **502**／541。 🟢 **三個全部走一條完全不碰 `citime` 程式碼的路重算，逐位數相同，連 `instr`／`apt` 兩個中間項都相同。** 而這一次的獨立性比前兩段深一層：**資料來源換成 `gh api` 的 REST 端點，它的欄位是 `started_at`／`completed_at`（蛇形），而 `citime` 吃的是 `gh run view --json` 的 `startedAt`／`completedAt`（駝峰）** —— 不同端點、不同欄位名、不同語言（WSL 的真 `jq` 1.7 對 Windows 的 Python）。 🔴 **而那條路自己也要控制，因為三個 payload 都剛好是 16,570 位元組：**`C0` 三個 sha256 相異（3 of 3），所以那個巧合是巧合；`C1` PowerShell 的 `>` 寫出 UTF-8 BOM（`ef bb bf`）而 jq 1.7 照樣解析得了 —— ⚠️ Python 的 `json.load` **不會**，下一個跨檢查若換語言要先處理；`C2` **正控制**：對一個非 BIG3 的 step 的 `completed_at` 加 1 秒，suite_cost 541 → **542**、BIG3 停在 **502** —— 所以那支 filter 是在讀時間戳而不是在印常數。 🟢 **`C3` 是新的，它把 `A6` 的理由從散文變成真實資料上的一個數字**：`A6` 的 docstring 寫「三支只加到兩支會小一截，讀起來像 suite 變快」，量：`test-console-capture-mutants` 361 ＋ `test-console-capture` 83 ＋ `test-deskchan` 58 = **502**，少掉 `test-deskchan` 是 **444** —— 少 58 秒 = **11.6 %**。 🟢 **BIG3 一格都沒動，三個新點全落在既有全距內**（引用 `citime stats --since changepoint` 印的那一段）：**n=39 → 42，497..504 = 7 s、中點 500.5、半幅 3.5、±0.70 %、中位數 502** —— 兩個端點一個都沒移。🔴 **同一批點讓 `suite_cost` 第二次變寬，而這次是兩個點掉在帶外**：523..539（16 s、±1.51 %）→ **523..541（18 s、±1.69 %）**，541 出現兩次。**兩欄、同一組新資料、一個動一個不動，第二次，而且比第一次明顯** —— 上一段是 15 → 16 s，這一段是 16 → 18 s。`A7` 佔比從 93.5..95.4 % 變成 **92.8..95.4 %**（下緣下移是 `suite_cost` 變大而 BIG3 沒變的算術後果，不是 BIG3 變差），下限 85 %（推）仍然守住；工具印的平均是 BIG3 **501.71** 對 suite_cost **532.26**。 🔴 **而這裡有一個我自己先寫錯、再由「驗證交給我的數字」抓回來的混淆，留著。** 開場交辦的「併池 94.34 %」我一度寫成是 `--pool` 旗標的輸出，並說「以後不把它當第二個獨立數字」。**兩件不同的事。** 量：● `--pool` 這一次確實不改變任何數字，因為 42 列的 job 集合只有一種，在這批資料上它是恆等運算；● 而「併池比例」是 `sum(BIG3) / sum(suite_cost)`，它**重現得出來而且真的動了**：n=39 是 19,567 / 20,742 ＝ **94.34 %**（逐位數符合交辦的數），n=42 是 21,072 / 22,355 ＝ **94.26 %**。⚠️ **它是導出來的不是工具印的**，所以引用它要說清楚是兩個合計的比；工具印的是 `A7` 那個**區間**（92.8..95.4 %），兩者不可互換。⚠️ **沒有解決的三件事一件都沒動**：`test-spec-check-mutants` 為什麼抖 12 秒（新實驗仍然是複製到 ext4 跑 n≥5 且打亂順序，這一段沒跑）；`A5` 排除紅 run 有沒有讓帶偏仍然 **n=1**；以及 `ci.yml` 59 對 61 未定。 🔄 **2026-09-07（第四十二段）：`n` 42 → 43，帶一格沒動，而那三件裡一件關掉、一件有了第一個讀數。** 量，`34121902909`（四個 job 全綠，含 `census`）：BIG3 **502**、`suite_cost` **536**；`stats --since changepoint` 現在是 **n=43，497..504 ＝ 7 s、±0.70 %、中位數 502**，`suite_cost` **n=43，523..541 ＝ 18 s、±1.69 %**，`A7` **92.8..95.4 %** —— **四個區間逐位數都沒動**，第 43 個點落在正中位數上。導出的併池比例 21,574 / 22,891 ＝ **94.25 %**（n=42 是 94.26 %），兩種算法一致（合計比＝平均比 501.72/532.35）。⚠️ 它仍然是導出的不是工具印的。 ✅ **第三件關了**：`ci.yml` 的 `run:` 步驟用 PyYAML 讀是 **61 ＝ 59 個行首形式 ＋ 2 個行內 `- run:`**，所以 **61 是對的、59 是選擇器的盲點**，那兩個是 `lint` 的（`#1` 是 `sudo apt-get`，`#2` 是 `shellcheck --severity=error tools/*.sh`，量 rc 0 綠）。做這件事的是 `tools/desk-sweep.py`，而它現在也是 `ci.yml` 的第 **62** 步。 🔴 **第一件有了第一個 ext4 讀數，而且它把問題換掉了**：那條「複製到 ext4 跑」的新實驗這一段跑了 n=3，**19.42 / 23.80 / 20.29 s**（全距 4.38 s、≈ ±10 %）—— 但**先前拿來比的 ext4 基線 1.52 / 1.47 s 不是一次快的執行，是一次拒絕的執行**：把 `.git` 拿掉之後 `git ls-files '*.md'` 從 108 變 **0**，suite 以 **rc 1** 印 `REFUSING: the unmutated self-test already fails (rc=2)`，量 **1.36 s**。所以「為什麼抖 12 秒」這個問題的前提要重新導一次;n≥5 且打亂順序仍然沒跑，這一列不假裝它跑了。 ⚠️ **第二件沒動而且今天又多兩個被排除的紅 run**（`5662438`、`2b95f8f`），所以 `A5` 的樣本變多、問題沒變。 🔴🔴 **2026-09-09(第五十段):這一列指定的引用命令今天會印一個沒有意義的數字,而原因是 `citime` 裡一個寫死的常數過期了一天。** `CHANGEPOINT = 2026-09-01T14:27:11Z` 是 2026-09-06 用置換檢定在 `suite_cost` 上定的;2026-09-08T20:43:13Z(`e79ff63`)發生了第二次階梯,`big3_s` **503 → 954**,而常數沒有動。量:那是整條序列**最大的相鄰跳躍 451 s**,第二大 **13 s**(比值 **34.7**),而 CP1 自己的相鄰跳躍只有 **4 s** —— 比日常抖動還小。後果:`stats --since changepoint` 併成 n=69、印 **± 31.778 %**;`A4` 看不見,因為 job 名字集合沒變、變的是 job **裡面**的步驟表。🔴 **而 CP1 定在 `suite_cost` 上、現在套在 `big3_s` 上**:在 `big3_s` 上它的均值差只有 **1.97 s**(組內 sd 3.49 / 1.80),CP2 是 **452 s** —— `big3_s` 這個欄位 2026-09-07 才加,邊界從沒對新的量重新導出過。🔴🔴 **階梯不是沒被看見**:`citime.py` `BIG3` 上方的註解把它記得清清楚楚(2.001×、用 run 自己的 wall clock 交叉確認到 6.9 %),**然後二十行下面的常數沒有動**,而後兩段改用**手打**的 `--since 2026-09-08T20:43:13Z` —— **慣例退回它取代的東西,而這是這支檔案裡同一形狀的第四次**。🟢 **`CHANGEPOINTS` 有序清單 ＋ `A8`(半寬>5.0 % 拒絕)＋ `A9`(分離>8.0 sd 拒絕並指名那一列)＋ `citime segments`(逐分區稽核,**整檔併池當正控制、必須發射**)。門檻兩側都量:`A8` 0.996 % / 31.778 %,`A9` 0.89 / 165.01。⚠️ **`A9` 找不到 CP1,而那是寫下來的限制不是調掉的**(CP1 在 `suite_cost_s` 上 1.65 sd、`big3_s` 上 0.87 sd,而乾淨分區可到 3.58 sd)。**帶引 `± 0.787 %(n = 11)`,945..960。** | **`R5` 的準備** 🔄 **2026-09-16（第八十段）：⊘，而理由是量出來的。** `BIG3` 這個數字沒有消費者：沒有任何 gate 的 DoD 說到它，也沒有任何檢查器讀它。把它再帶一次是把一個沒有人要的儀器算成債。**重新打開的條件**：下一次 CI 的 `BIG3` 走出目前帶引區間的上界，或者任何 gate 的 DoD 第一次引用它 |
