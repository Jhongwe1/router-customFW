# `PROGRESS.md` § Now, archived verbatim

A record, not a state document: it is not edited. On 2026-09-23 § Now became
current state only — a few sentences per row, rewritten each segment instead of
appended to — and the rows' accumulated text as of `f557873` was moved here
unchanged, with the paragraph under the table that pointed at the active step
list. `LOG.md` holds the same history as dated entries, and git holds every
earlier version.

## The table, as of `f557873`

| | |
|---|---|
| **Active gate** | 🔴 **`R3` —— 開於 2026-08-28。🆕 **2026-08-29 晚上，第一次上機，D1–D5 五列全部達成** —— 我建的 kernel 從 RAM 開到一個會回應打字的 shell，而且雙向 ping 得通（板子端 4/4，主機端自己的擷取同時有 request 與 reply）。**反-DoD 是正面成立而不是靠缺席**：三個原廠映像產不出來的鑑別字串同時出現 —— 映像自己標頭裡的 `start address: 0x80003600`（原廠 staged 的是 `0x80003440`）、只存在於我這棵樹的 `RLXFW-B00`、以及我的 build stamp `(key@K) … #1 Fri Aug 28 23:37:47 CST 2026`。🔴 **沒有發出任何 flash 寫入命令 —— 而那不等於「零 flash 位元組」，這一列不說第二句。** `RUNSHEET` §B3 的 `G8b` 自己寫著那句話「要一次完整 re-dump 對 `FLS-14` 雜湊」，而**這次上機一個 `FLR` 都沒跑**，所以 flash 這一側的證據比 `R0` 當時**還少**。量到的是：35 個 `--send` 全是 `DW` 讀、兩個 `J`、以及使用者空間的 `cat`／`ifconfig`／`ping`，沒有 `EW`／`EB`／`FLW`／burn；兩次上傳前 `AUTOBURN` 都讀 `00000000`；TFTP 目標是 `LOADADDR 80500000`，那是 RAM。**flash 位元組數：未量。** 🔄 **2026-08-30 seating 6：`R3-8b` 與 `R3-10` 都收掉了**，31 格全部命中、`FLR` 括號兩半合攏、`H601` 第一次進括號。**剩下 `R3-9` 的兩個儀器（`probe3` 保留位元圖、`w-assoc` 的 `M(T)` 表）與 `R3-11` 的寫上去。** 影片的原始素材已經拍到（電源循環 3，`V-3` → `V-7a`）。*(這一句原本寫「剩下 `R3-8b`、`R3-9`、`R3-10`、`R3-11`」。)* 🆕 **2026-08-30 桌面：`R3-8b` 的預測區塊寫好了，而它橫跨兩次電源循環** —— `bench/2026-08-30c/PREDICTIONS-B5-block2.md`，31 格，電源循環 3 是 `quietm` 加 `FLR` 括號的前半，電源循環 4 是後半（`FLR` 會寫 TFTP 長度全域，所以之後不能再 `put`；而 `J` 之後 loader 就沒了 —— 兩半不能共用一次上電）。**`R3-10` 因此重新定義成電源循環 4**，舊的寫「第二次上機拿 D4 與 D5」而那兩列上一次就拿到了。`R3-11` 的影片排在電源循環 3 拍。⚠️ **gate 還沒關**：D1–D5 達成不等於 `R3` 的所有步驟走完，而這一列不把兩件事混為一談。✅ **`R1h` 關了** —— `R1h-3` 的上機與 `R1h-4` 的寫上去都在 2026-08-29 收掉，ⓐ 有量測、ⓒ 正向收、ⓑ 記為第一次上機未定、ⓓ 後半有讀數。**`R2a/b/d` 結案 2026-08-28**，**`R1-gate` 2026-08-26**，**`R0` 2026-08-24**，`S0` 2026-08-23 🔄 **2026-08-31(seating 8,第十九段):`R3-9` 收掉了,而它是 `R3` 唯一還需要電源的步驟。**`probe3` 的兩個帶出來的儀器都落地(`M(T)` 階梯 `09 05 03 03`、保留位元圖 10 對 0 孤立),Group F 把 `SPEC.md` §17 的 `FW-34` 最後一列關掉(`R = 1.0000`),`FLR` 括號兩輪、八個視窗全部逐字相同,`MEM-17` 第一次兩端都有並量出衰減曲線。**`R3` 現在只剩 `R3-11` 的寫上去與影片,而它一次電都不用花。**⚠️ **gate 仍然沒關**:剩一步就是剩一步,這一列不把「最後一次上機做完」寫成「gate 關了」。 *(這一句之前寫「剩下 `R3-9` 的兩個儀器與 `R3-11` 的寫上去」。)* 🟢 **2026-08-31(第二十段,桌面,不通電):`R3` 關了。** 十二步走完十二步,D1–D5 五列**在同一次開機裡**全部成立且帶著鑑別字串,兩個決定的否證條件一條都沒開火,停損四條一條都沒到。逐條對照在 `notes/kernel-build.md` §21.4–§21.6,gate board 的證據欄指向它。🔴 **而 §21.7 列出 `R3` 沒有建立的東西,因為 gate 照樣關**:`G8b` 那句話仍然說不得(括號只到 0.0244 %,`H601` 6.3 %,沒有全片重讀);**還是沒有我自己的驅動**(D5 的 ping 走廠商的 `rtl819x`,那是 `R6` 的事);D3 自己寫的可觀測量 `MemTotal:` 這顆 kernel 根本不印,那一格是用替代品過的,記成 DoD 的缺陷而不是一次達成。⚠️ **Active gate 現在是空的,而下一個開哪一個是決定不是推導** —— gate board 上排在 `R3` 後面的是 `P4a`(可重現建置),而今天剛好量到它的第一筆輸入:`rep4` 與 `rep8` 的 `arch/rlx` object **逐位元組相同**、`.text` 相同、檔案大小相同,而 `vmlinux` 的 sha256 **不同** —— 非決定性在 `arch/rlx` 之外。**這一列不替擁有者開 gate。** 🟢 **2026-09-01(第二十一段,桌面,不通電):`P4a` 開了、量了、關了,而 `R3` 之後的 gate 是 `P4b-gate`。** `P4a` 的十二個字的 DoD 兩半都成立:同一棵樹建兩次 → 同一個 sha256(`p4a1`／`p4a2` 的 `c956c5b7…` 與 `p4a3`／`p4a4` 的 `4fc20ce4…`,兩對),正控制成立兩次(`PC-1` 改一個字串位元組、加一列宣告過的記號 `ID0`)。⚠️ **它關在 Level 1** —— 同一台機器 —— 而 Level 2(別人拿我發布的原始碼也重現得出來)是**擁有者裁決後記在殘留裡**的,`notes/reproducible-build.md` §6:七項裡五項被讀掉或量掉,剩下的一項是 `(key@K)`,因為這棵 2.6.30 沒有 `KBUILD_BUILD_USER`／`_HOST`。 🆕 **`P4b-gate` 開於 2026-09-01,而它是擁有者的決定不是這一列推導的** —— `REL-0` 問「要不要在 v0.2 之前開一個 release engineering 的 gate」,答案是開一個小的、只擁有四件事。**它不包含 tag 本身**:tag 是對外可見且實務上不可逆的動作,**等擁有者說,而且擁有者已經說了要等影片錄完**。  🟢 **2026-09-01（第二十二段）：`P4b-gate` 關了，四步全到，並且是循 DoD 逐列讀出來的而不是四個打勾。** 兩列 DoD 帶著被記下來的缺陷：`D2` 指名的是一個**路徑**而不是一個性質，而那個性質在那個路徑上達不到；`D3` 是「每一個**已發布**版本」成立而不是「每一個版本」，差別就是 `REL-2`。⚠️ **Active gate 現在是空的，而下一個開哪一個是決定不是推導。** 計畫說 `R4`（edit → result < 90 s）；`docs/GATE-RESULTS.md` 的操作條款第一次跑就指向 `CPU-45`。兩者的強弱與兩個削弱條件在 `GR-1`，**這一列不替擁有者開 gate**。 🆕 **2026-09-01，同一段的晚一點：擁有者開了 `R4`。** edit → result < 90 s 加上不碰電源的重置。**選它而不選 `CPU-45` 是排程不是規矩弱**：`CPU-45` 要花這個專案最貴的單位（一次電源循環），而 `R4` 修掉的是每一段都在付的週轉稅 —— 包括 `CPU-45` 自己那次上機的準備成本。`GR-1` 留著開，下次上機時與 `R3` 的殘留一起收。 🔴 **而開場的三分鐘就推翻了計畫對這個 gate 的兩個前提**：loader 根本沒在餵看門狗（`CLK-11`），而 NFS root 縮短的是 **userspace** 迴圈，`R5` —— gate board 說 **24** 段、計畫說 31，又一次那個 `Est.` 欄的毛病 —— 全是 **kernel** 驅動。兩個都寫進步驟表而不是靜靜修掉。 🟢 **`R4-0` 跑完了,2026-09-01(第二十三段),桌面,不通電,而它推翻了自己四個預測裡的四個。**機器管線 **50.4–71.1 s**(建置 38–58 ＋ 映像組裝 3.6–4.1 ＋ TFTP 1.5–1.6 ＋ 開機 7.260),所以 **`D4` 的 90 s 由機器那半達成,餘裕 19–40 s**,停損的第二條沒有觸發。🟢 **NFS root 離開這個 gate**:它拿掉的上傳是機器管線的 **2.2–3.3 %**,而對 kernel 改動是 **0 %**。🔴 **`--keep` 的張力兩頭都被量掉了**:它只買到 3–5 %(因為這棵樹的 `make` 什麼都沒動也重編 599 個物件,連續兩次),而且跟 `--marks` 一起用會直接被拒絕;它的可重現性代價量到是 **3,968,240 裡的 2 個位元組**,所以 `P4a` 的 `L2-6` 關了。**`R4-3` 接手的是另一個題目**:那兩個每次都被重寫、內容卻逐位元組相同的產生標頭。 🟢 **`R4-1` 的預測區塊凍結並提交了** —— `bench/2026-09-01/PREDICTIONS-B7-block5.md`,24 格,`check-predictions` 在桌面讀 **0 of 24**,兩個方向都寫了,`N = 20` 而且寫下了 20 次成功能證明什麼(失敗率 95 % 上界 13.9 %)與不能證明什麼。 🟢 **2026-09-02(第二十六段,seating 10):`R4` 關了。**五步走完五步,`D1`–`D4` 四列逐列讀出來,`docs/GATE-RESULTS.md` 收第七筆。**Actual 4 段**(第二十三～二十六),對 board 的 5 與計畫的 8。🔴 **而它關的時候帶著一句自己的否定**:沒有任何一次呼叫跑過 `S2`→`S7`,**73.88 s 是兩次執行相加不是一次量到的總計**,而 `--skip S2,S3` 在今天早上以前是**必要而不是選擇** —— `--cell-top` 的預設值是字面佔位字串,`S3` 對著它 exit 1,而 26 個控制沒有一個看得到。**`R4` 之後開哪一個 gate 是擁有者的決定**,board 上的候選是 `R5`(六支驅動)與 `CPU-45`／`GR-1`(要一次上機);`docs/GATE-RESULTS.md` 的 operating clause 在七筆上重跑,仍然只指 `CPU-45`,而且是從同一對指的 —— **兩筆新的條目沒有產生新的發射**,這一句被寫下來是因為相反的結果才可疑 🆕 **2026-09-02(第二十六段,同一段內):`R5` 開了,擁有者的決定。**六支驅動、每支十次開機無 oops、每支一份編得過但**標明未在硬體上 probe 過**的DT binding,加上 `docs/driver-diff.md` 與 `docs/interrupt-map.md`。🔴 **第一支是被逼出來的不是挑的**:`CPU-42` 量到這顆的 CP0 `Count` 沒有實作,所以 SoC timer 是前置。⚠️ **irqchip 刻意不在這個 gate 裡**,理由寫在步驟表上面。**Active step 是 `R5-0`,而它一次電都不用花** 🆕 **2026-09-03（第二十八段，桌面，不通電）：`R5-1` 收掉了，六步剩五步。** 第一支驅動是 clocksource，寫在 `drivers/clocksource/`，rating **0**（低於 `clocksource_jiffies` 的 1，所以它不靠 tie-break 就選不上），開機時零暫存器寫入。🔴 **這個 gate 的 `D3` 前提在同一天被自己的資料表讀窄了**：`CLK-06` 是 L1 不是 L2，帳本那一列已就地更正 —— **而 L2 沒有因此變空**，`notes/timer-driver.md` §3 列了十列，其中五列是第二個實作真的會做不一樣選擇的地方。**下一步是 `R5-2`，它要板子** 🆕 **2026-09-04（第三十一段，桌面，不通電）：`R5-10` 收掉了，六步剩四步（`R5-3`、`R5-4`…`R5-8`、`R5-9`、`R5-11`）。** 它原本排在最後，被 seating 11 的否定結果提前成 `R5-3` 的前置；今天做完之後，**`R5-3` 的第一格變成零風險**（只寫 `TCIR` bit 30、`GIMR` bit 9 保持清空、不裝 handler），而它的 `I4` 是 `request_irq(25, …)` 而不是自己寫 `GIMR`。🔴 **`R5-3` 會重建映像，所以它的桌面半段就是那一批**：`MARK-1`、`TMR-1` 的兩個驅動修法、`CFG-1` 的 `.config` 提交，三件都動 `RECIPE_ID`。~~下一步是 `R5-2`，它要板子~~ 🆕 **2026-09-04（第三十三段,seating 12,一次電源循環）:`R5-3a` 收掉了,而 `R5-3` 只走完一半。**六步仍剩四步 —— `R5-3` 因為拆成 `R5-3a`（中斷路徑,今天收）與 `R5-3b`（clockevent ＋ rating,未做）而**沒有**整格關掉,這一列不把半步寫成一步。🟢 **`docs/interrupt-map.md` §3.1 的七道閘現在全部是量的,而且有中斷走完它們**:119,818 次遞送、`irq_spurious` 0、`irq_stuck` 0,速率對 `hz_used/period_cycles/hz_kernel` 在兩個相差 16 倍的週期上都準到 **0.02 %**,量到的速率比 15.9873 對預測 16.0000。🔴 **`R5-3b` 的前置條件因此改變了一件**:`TCIR` 裡任何 pending 位元的壽命上限是一個 10 ms tick,因為廠商的 `bsp_timer_ack()` 在 W1C 暫存器上做 read-modify-write（`SPEC.md` `IRQ-09`）——**clockevent 的 oneshot 路徑若靠讀 `TC1IP` 判斷「是不是我這一發」,在這顆晶片上讀不到。** 🆕 **2026-09-06(第三十四段,桌面,不通電):`R5-3b` 拆成 `R5-3b-1` 與 `R5-3b-2`,前者的桌面半段收掉了。**六步變七步,剩四步。🔴 **而 `R5-3b` 這一列原本寫的救援路徑是錯的**:它寫「`The rescue path is R4's scripted reset`」,而 `J BFC00000` 是 loader 指令 —— Linux 卡死時沒有 loader 提示字元。**代價是使用者按電源**,那是這一段把 block 10 排成三次電源循環的理由。🔴 **上一列說 oneshot 讀不到 `TC1IP`,而今天的讀數讓那句話變成不相干**:量這顆映像的 `.config`,`CONFIG_TICK_ONESHOT` 根本不存在、`CONFIG_NO_HZ` 與 `CONFIG_HIGH_RES_TIMERS` 都是 n,所以 tick 核心**進不了 oneshot**。**那個限制沒有被推翻,它變成不可達** —— 而真正的限制在別的地方,是 `CLK-25`:TC1 一個 reload 暫存器不能同時滿足 clocksource 的 2 冪週期與 100 Hz clockevent 的 2,000 counts。 🔄 **2026-09-06(第三十五段,seating 13):`R5-3b-1` 收掉了,而 `CLK-25` 的兩個模式都在矽片上跑過。** 這一列講的互斥仍然成立且仍然是**讀**出來的;被量到的是模式的行為:`mode cs` 下 `period_cycles=256`／`period_jiffies=0`,`mode ce` 下 `2000`／`1`,`TC1DATA` 與 `TC0DATA` 逐位元組相同,三次電源循環來回切換,`tc0_undisturbed` 全程 1。~~**gate 仍然沒關**:`R5` 還有 `R5-3b-2`、`R5-4`…`R5-9`、`R5-11`~~。 🔴 **2026-09-07（第三十九段）：上面那句列舉在寫下之後三段都沒有人動，而 `R5-3b-2`（第三十六段）與 `R5-4`（第三十八段）都已經收掉。** 它把兩個已經關掉的步驟講成待辦，而這一列正是 CLAUDE.md 叫每一段先讀的檔案。🟢 **修法不是把列舉更新，是把列舉拿掉**：剩下哪幾步由 § Step list 的 ✅ 擁有，這一列只說 gate 沒關。同一張表的 `DAY-ZERO items 0–8` 那一列 2026-08-25 就記過同一件事 —— 在這裡重述 § Step list 的狀態，就是給那個狀態開第二個擁有者。**gate 仍然沒關。**  🟢🟢 **2026-09-11(第五十八段,桌面,不通電):`R5` 關了,~~十三步走完十三步~~ **十五步走完十五步**(`R5-0`…`R5-12`,`R5-3` 拆成 `R5-3a`／`R5-3b`,`R5-3b` 又拆一次) —— 🔴 **「十三」是寫下去就沒再重算的計數,而它被寫進 release 的那一節**;量 2026-09-11,在 release notes 逐個數字重新導出的時候抓到,而關它之前要先修的是這張表自己。** 六支驅動在**同一顆映像** `692A2801` 裡開了 **十七次**,每一支在每一次開機都印出自己的可觀測量;階梯 1,069 → 1,184 → 1,318 → 1,424 → **1,637** bytes 是單調的,而且那是掃 85 份帶記號的開機擷取導出來的、**不在任何一張卡片上**。🔴 **`D4` 沒有照字面達成**:`/proc/timer_list` 這顆核心裡**有**(`obj-y` ＋ `proc_create`)而從來沒有被讀過,量到的是比值 `2000 : 1` 不是對 `CLK-02` 的頻率 —— 這是**第三次**有 gate 的 DoD 指名了一個交不出那個性質的產物(條目 4、6、8),而 clause 因為它們不連續仍然看不到。🔴 **`R5` 花 32 段(第 27–58 段)對計畫的 31,1.03×**,停損 34 只剩 **2 段**;而步驟表帶進來的先驗 0.571×(n=2)會預測 17.7 段,**低估 1.81 倍**。校準寫在 gate board 底下,**而它的結果是一條帶不是一個倍率**(九個 gate 0.33×–1.38×,幅寬 4.15 倍)。🆕 **`R1-pub + R2c` 開了,擁有者的決定**,在 `R5` 關掉的同一段。計畫小計 **20**(12 桌面 ＋ 4 上機 ＋ 4 儀器),board 那一欄寫 14 —— 兩個擁有者的老毛病,而校準用的是計畫那一個。**它擋不住任何東西**,計畫寫它與 `R5` 平行、共用同一批 bench seating,所以它進來的時候沒有任何東西在等它。 🔄 **第六十四段(2026-09-13 14:04 開場,桌面,**不通電**):`R1-pub-2` 的產物全部落地,而這一段最大的發現是 repo 裡一個沒有任何文件注意到的衝撞。** 🔴🔴 **`plan:1038` 要求的 `R1b` load-delay 測試,逐字就是 `tools/hazlint` 自己對 violation 的定義** —— 而 `tools/rlxprobe/Makefile` 讓 `.bin` 在 hazlint 非 0 時建不出來、選項表裡沒有任何 waiver、`gate-check` 會在閘門被放寬時讓建置失敗(而且堅持退出碼必須是 1,所以「用錯的理由拒絕」也會被抓)、`cells.S:90 (a payload that built its victim instructions at)` 又預先封死了執行期產生指令。**建置閘門擋住了計畫書要求做的實驗。** 解法不是豁免、也不是位址視窗(視窗會安靜掉東西,而且會造出一個接縫,接縫兩邊各有 load 與消費者時兩邊都不檢查):`tools/hazdecl.py` 讓**未修改的** hazlint 掃**整顆**映像,再雙向裁決它的輸出,十二項檢查,**而對 probe5 來說 hazlint 退出 0 是建置失敗**。🟢 **閘門在第一次真跑就發火,而且發火在一件真的事情上**:`ds_d1` 被 survey 算進「a load sitting in a delay slot」,因為 load 的位置跟 padding 無關 —— padding 在分支目標與消費者之間。**錯的是宣告不是 payload**,而那一欄因此改成「手寫＋導出」兩個來源互相檢查;導出規則一共錯過兩次(另一次是 `main` 還需要生產者**是一個 load**,`mult`／`mtc0` 不是),所以 `H24b` 把它一對一對釘死。🟢 **C4 成立並且先記錄**:`qemu/2026-09-13/probe5.txt`,24 列全部 `LOCK`,事前登記 24 中 24,`aux.zero=0`／`trapped=0`／`addr0.bad=0`。🟢 **而讀那份擷取找到表裡一個真的洞**:`hilo` 族的 `open` 常數是預置的 LO,若 `mtlo` 沒落地那一族只會讀到 `LOCK` 或 `OTHER`,而「預置沒落地」會被誤讀成「沒有 hazard」;qemu 也證明不了它,因為 `mult` 會覆蓋兩個半邊。`hl_ctl` 為此而加 —— 預置 LO 與 HI、**完全不跑 `mult`** —— 量到 `cafe0000`／`beef0000`,所以 `mtlo`／`mthi` 都有作用。其他族不需要,而那是量的:`sb_m0_d0` 讀到 `LOCK` 值 `B10CB10C` **就是**「`$9` 真的拿到 `in_b`」的跨列控制。🟢 **`store` 那一列的形狀定義出來了,而普查那句話是不準而不是空的**:量九個 fixture、兩個控制都發火 —— hazlint 對 store 當**消費者**兩條運算元路徑都有規則,對 store 當**生產者**兩種形狀都沒有;而生產者那一側**是有理由的測不出來**(route ② 在母體規模上決定它,一個觀測到違規的 payload 觀測到的是自己壞了)。🔴 **兩件既有缺陷,都不是這一段造成的**:①**沒有任何東西在檢查產生出來的檔案有沒有過期** —— `isapay.py:835 (that check is a case in)` 與 `cells4.S:4 (that check is a case in)` 都聲稱 `emit --check` 是 `tools/test-isapay.py` 的一個案例而那個檔不存在,CI 只跑 `--self-test`,所以一顆過期或被手改的 `cells4.S` 會直接出貨(而那顆映像正要上機);這一段自己也踩到。②**`make show P=probe4` 是半盲的** —— probe4 不在那兩個 filter 裡,所以專為印出而產生的 `RB_WORDS_probe4 := 633` 從來沒印過、`*** NOT A DEVICE BUILD ***` 對它從來沒發火過。兩個都修了,而 `DW 80A03000 633` 是那個數字有史以來第一次被印出來。🔴 **我自己的解析器連續兩次說謊而兩次都印出一個數字**(一次抓到 hazlint 自己控制項的「Expected: 2 violations」,一次把 `VIOLATIONS` 錨在第 0 欄而那一行縮排兩格),兩次都是靠退出碼這個第二來源抓到 —— 這直接變成 `hazdecl` 的**第一項**檢查。🔴 **mutant runner 第一次跑,20 個全部「殺掉」而其實一個案例都沒執行**(突變體被複製到空的暫存目錄,資料檔不在那裡);抓到它的是 runner 記錄「是**哪一個**案例殺掉它」而不是只記有沒有。改成影子連結樹之後,同一支 runner 接著抓到一個**活著的**突變體(`hazdecl` 對 P9 的 survey 半邊沒有任何案例)以及我替它寫錯的案例名。🔴🔴 **而收工稽核抓到第三件既有缺陷,是三件裡最大的一件,而抓到它的是一個無法解釋的 sha 而不是檢查器**:`build/probe5/probe5.bin` 增量建置是 `ae5252d8…`,同一份原始碼從乾淨建是 `dc90e6d2…`。量:`touch probe5rows.h; make P=probe5 payload` **不會重編 `probe5.o`**,probe4 也一樣 —— `.c` 的規則沒有把**產生出來的** row 標頭列為相依,而 `P5_ROWS` 正是從驅動的 `.c` 裡面到達 `O_SEAL`、`RB_WORDS`、迴圈上界與兩個編譯期斷言的。**連出來的是一個 23 列的驅動壓在一張 24 列的表上,而這在 probe4 上是活的、而 probe4 正要上機**(`make show` 的 `DW` 字數來自新的 `.mk`,用它讀回來會越過或讀不到 seal)。🟢 沒有東西從那顆過期的映像出去:提交的 qemu 擷取來自 `qemu-run.sh` 自己從零的建置。修法是 `GENHDR_$(P)` 進相依 ＋ `dep-check` 當控制,🔴 **而 `dep-check` 的第一個負控制回綠**(recipe 的 `$(MAKE)` 沒帶 `-f`,子 make 讀到的是真的 Makefile),`THISMK` 為此而存在;修好後兩個方向都成立,`test-rlxprobe.sh` 的 `G4` 是它的八個案例,套件 **216 → 224**。🔴 **而 `dep-check` 一共修三次,後兩次都是「因為錯的理由是綠的」**:在全新的 `BUILD` 目錄裡 `.o` 與 `touch` 落在同一秒而 mtime 是一秒解析度(**一個 `touch` 測的是時鐘,跟測相依一樣多**),以及負控制那一臂的 `-f` 帶了路徑而 `-C` 會再解析一次、死在 *No rule to make target* —— **那是一個非零離開碼,而「它拒絕了」那一格把它算成通過**,抓到它的是旁邊要求 `FAIL` 訊息真的出現的那一格。**一個控制需要兩格,因為一格只看得到退出碼。****`test-rlxprobe.sh` 全綠**,`spec-check` rc=0,十二個閘門全綠。⚠️ **矽片上一列都還沒跑。** 🟢🟢 **2026-09-14（seating 21）:`R1-pub-3` 的 slot 1 收了 —— 兩顆 payload 在一次電源循環上跑完,ESC 視窗之後 37.823 秒,`check-predictions` 12 of 12。** `R1a` 的 75 列與 `R1b` 的 24 列都有讀數,四個強制控制(D2b 正、D2 負、C4 反向、三通道)全部成立。⚠️ ~~**gate 不縮**:`R1-pub-3` 的 slot 2 與 `R1-pub-4`／`-5`／`-6` 的矽片半還在~~ 🔄 **2026-09-15(第七十一段):`R1-pub-5` 與 `R1-pub-6` 的矽片列在第七十段做完了,而這一列到今晚都還寫著它們「還在」。剩下哪幾步這一列不再列舉 —— § Step list 與 `Next after this` 擁有那個狀態。🔴 這不是紀律問題:`tools/spec-check.py` 的 `C12` 把 `C12_ROW` 釘死在 `Next after this`,所以它讀不到這一列,而那支工具自己的註解(`C12` 上方)寫著這個列舉「已經被劃掉、不該存在」—— 它在 2026-09-12／13／14 被重新寫回來三次,而且沒有劃掉。** 帶維持 7–28 段、中位數 17。**零 flash 寫入命令、零 `FLR`。** 🟢🟢 **2026-09-16（第七十七段，桌面，不通電）：`R1-pub + R2c` 關了。** 十步走完十步，第 58 段到第 77 段共 **20 段**，**1.00× 計畫的 20**，在 7–28 的帶裡、高於中位數 17。`docs/GATE-RESULTS.md` 第九筆在檔裡，operating clause 在九筆上重跑且**開火** —— 共有的那件事是「兩個 kernel 計數器在同一瞬間讀到」，`R5` 的 `D4` 沒建立、`R1-pub` 的 `E5` 也沒。⚠️ **這一列不列舉剩餘步驟，那是 § Step list 的。** 🆕 **同一個段裡，`R1z` 開了 —— 擁有者的決定，一個償債 gate。** 而逆出它的是這個 repo 自己的控制：`spec-check` 的 `T23` 要求「接下來要做什麼」那一列在**活的**檔案上真的讀到至少一個開著的步驟，**否則 `C12` 是「什麼都沒讀就通過」** —— 所以一個關完的 gate 後面不允許沒有下一個。🔴 **`R1z` 的母體是從 § Carried forward 導出來的，不是手挑的**，而那是今天四支壞掉的檢查器都沒做、而 `tccensus` 做了的那件事。**零 flash 寫入命令、零 `FLR`、板子全程斷電。** 🟢🟢 **2026-09-16（第八十段）：`R1z` 關了，用掉 3 段對估的 3 —— **1.00×**，而它是第二個落在自己數字上的 gate。** 五步走完五步，`tools/cfcensus.py check` **29 → 0**，`--self-test` **64/64**，`ratchet` 在全零基線上 —— **它現在就是闘門**，而那是這個 gate 真正的到達點：一個新的孤兒**造不出來**，而不是造出來之後被普查發現。兩個否證條件一條都沒開火：`D2`（每一個檢查器改動都要更嚴）—— **這一段一支檢查器都沒改**，因為 `spec-check` 自己的說明已經寫好了反對那個改動的論證；`D1`（母體導出來不是挑的）。停損四條一條都沒到 —— 包含「任何一步需要板子就離開這個 gate」，而三段全部零電源循環。🔴 **而這一段最大的發現是一個盲點而不是一個成果**：這份普查**看不見一筆已經付掉的債** —— 它的母體是那張表，而那張表是一個宣稱；五筆早就付了而帳上還記著，一筆都不是重讀紀錄發現的。 🆕 **`P1` 開了（2026-09-16，擁有者的決定）：`mfgtest` 在一台好的裝置上通過，而每一項檢查都被弄失敗過一次。** 步驟表在上面，而它開場第一件事是一個**擁有者的裁決而不是工程的**：計畫寫的檢查清單裡有一項要求寫 flash，而 `CLAUDE.md` 寫著主線到 `R9` 為止零寫入。**零 flash 寫入、零 `FLR`、板子全程斷電。** 🟢🟢 **2026-09-17（第八十四段，桌面，零電源循環）：`P1` 關了。**六步走完六步，`D1`–`D4` 逐列讀過，兩條否證條件一條都沒開火（沒有任何檢查是「在這块硬體上不可否證」，而十一個檢查覆蓋的周邊多於六支驅動），三條停損一條都沒到（**4 段對帶寬 4–15**、**1 次電源循環對上限 3**、**每一個還原都是 量**）。🔴 **`D3` 的後半句是被一個它沒指名的儀器付的** —— 這個 gate 一個 `FLR` 都沒跑，造成的是 `MT-FLASH-3` 的 32 組 map（4,186,112 位元組，對兩次早八天的 seating 逐行相同）。`docs/GATE-RESULTS.md` 第十一筆 🆕 **2026-09-17（同一段）：`R6` 開了**，擁有者的決定，在 `P1` 關掉、`v0.4` 打上 tag 的同一段。🔴 **而它的步驟表是被「寫」出來的不是被找到的** —— `PROGRESS.md` 從來沒有过 `R6` 的步驟表，`R6-0` 只存在於計畫的散文里，而 `cfcensus` 的 `L2` 已經因為有人引用它而開过一次火。🔴 **`R6` 帶著一個沒有答案的前置，而它不是一個風險**：它自己的否證② 是描述符環的快取一致性，那是 `CPU-45`，而它兩個標記都是 `—`。**所以 `R6-0` 的第一件交付物是那一格的重新設計，不是驅動的骨架。** 預算 **41 段**（計畫的小計），而 gate board 的 `Est.` 格與 `plan/SESSIONS.md` 各自寫著 37 與 35 —— 三個數字，只有計畫的小計對得上 `Actual` 的定義 🔄 **2026-09-17(第八十六段,桌面,零電源):`R6-0` 關了,而關它的東西在第一次執行就產出一個發現。** 🔴🔴 **帶進來的第一件是識別碼衝突,本專案的第二次(第一次是 `NET-14`)**:`tools/regcensus.py` **早就存在** —— 已追蹤、`ci.yml:852`、`ci-expected.tsv:350`、21 個控制、綠的,加入於 `bebf51d` 2026-09-10 —— 而它是**不同的儀器**(讀反組譯與 `System.map`,為 `R5-7` 的 GPIO 寫入遮罩做的)。本列上一段說「`regcensus` 沒有以工具的形式提交到 `tools/`」,**照字面是假的**;而 `R6-0` 的 DoD 說它「對廠商乙太網路驅動」,已提交的那個**不讀任何 C 標頭**。這次比 `NET-14` 更會騙人,因為衝突的名字是一個在 CI 裡跑的已追蹤工具。**名字沒有重用**:欠的那個以 `tools/hdrcensus.py` 交付,18 個控制全過,`ci-census` 認得它(`ran 18/18 failed 0`),不宣告 skip 所以 `not-run-total` 不動。🔴🔴 **它第一次執行的發現是母體錯了**:`NET-21` 的交換器暫存器母體來自 loader 的 48 個 `lui` 站點,那是**某個行為者做了什麼**;量,標頭宣告 **546** 個位址而本 repo 只印過 **69** 個(**87.4 % 從沒印過**),ALE 區塊21 個裡 19 個沒印過,**而 `MSCR`／`VCR0`／`SWTCR1` 三個正是 `R6-2` 定義指名的東西**(`NET-35`)。🟢 **`R6-2` 的驅動寫好、建好、在映像裡**:`rtl819x-switch.c`,`MK9`,`recipe=f681f8e0`,`vmlinux` 4,175,548 bytes,`rlxfw-marks verify` 12 個 mark 各一次、`str:rtl819x-switch` 我的映像 3 次對廠商 0 次,`__initcall_rtl819x_sw_init4` = `subsys_initcall`。🟢 **而 `R6-2` 的 DoD 從「做不到」變成可量**:`SSIR 0xBB804204` 的 `SwitchFullRst`,廠商自己的 init 就在用(`NET-36`)。🔴 **一個稽核發現**:`MK7` 的 witness 量測建立在 `r0-vendor-kernel.bin` 上,那是 **LZMA 壓縮**的映像 —— 量,`Linux version`／`Realtek`／`eth`／`rtl819x` 對它全部回 0,所以那個 grep 不可能失敗。在解壓後的核心(sha256 `cf0d60a8…`)上重量、四個正控制讀 1/3/24/2,**結論活下來、證據沒有**,兩邊都寫進 `config/rlxfw-marks.tsv` 🔄 **2026-09-19(第八十七段,seating 27):`R6-2` 關了。** 交換器驅動第一次執行,`R6-2` 的 DoD 由量測滿足。詳見這一列下方的步驟表與 `bench/2026-09-19/CORRECTIONS-block27.md`。　🔄 **2026-09-22（第一百段，seating 38，一次電源按鍵）：`D4` 的「廠商沒有載運流量」從推論變成命令，而這一段最值錢的東西是一個被否證的預測。** 映像 `s100L`（`RECIPE_ID 82724c8f`、loud、`vmlinux` 4,573,867）。🟢 `ifconfig eth0 up` 回 `SIOCSIFFLAGS: No such device`，`/proc/net/dev` **仍然列出** `eth0`…`eth4`，`rlx0` 同一次開機開得起來、ping **4/4**（`NET-106`）；產物側在通電前就驗過：`objdump` 在 `re865x_open` 上只有四條指令、`li v0,-19`，對照組 `s99c` 是完整的 0x378 位元組。🔴🔴 **而 `phfollow` 預設生效了、吞吐量還是死了** —— `recov_mode` 開機是 **0**，所以我修掉一個動詞依賴、又送出了第二個（`NET-107`）。🔴🔴 **`NET-67` 的第八個候選死於單變因 A/B**：三個 TX 環基址持零不是原因，武裝 1 與武裝 4 的首次失聯是 1.138 s 對 1.124 s（`NET-108`）。🟢 引擎側的 `asicCounter` 第一次被讀，而讀得成是因為這顆映像刻意把廠商驅動留在裡面（`NET-109`）。零 flash 寫入、零 `FLR`、`n_writes 62`、`n_refused 0`。　🟢🟢 **2026-09-22（第一百零一段，桌面，零電源循環）：`R6` 關了。** **七步關掉、第八步記成不達成** —— `R6-6` 是 stretch、不是通過條件，而「八步走完八步」讀起來像八條都成立，**`D1`／`D2`／`D3`／`D5`／`D6` 成立，`D4` 部分成立且缺口具名並估價**。`D4` 剩下的那個合取項「廠商的沒有載入」由擁有者裁定為**下一個 gate 的頭條而不是本 gate 的超支**，理由是量出來的而不是估的：`CONFIG_RTL_819X_SWCORE=n` 在 vmlinux 連結留下**十個未定義符號、四個獨立位置**，VLAN 表在要消失的那個目錄裡是 `TACI` 協定而不是暫存器寫入，而同一個開關會一併刪掉 `/proc/rtl865x/` —— 那正是`NET-109 殘留` 下一步要讀的儀器。**先例是 `P4b-gate`**（2026-09-01 帶著未達成的 `D2` 關掉，理由記下、兩件事留在 gate 底下開著）。十二筆的 operating clause **開火了**，指名 `MT-PORT` 不說是哪一支驅動服務了那條鏈路 —— `P1` 當初點名交給 `R6-0`，而 `R6-0` 只兌現了量測那一半。詳見 `docs/GATE-RESULTS.md` 2026-09-22。 |
| **Active step** | 🟢 **2026-09-22（第一百零一段，桌面，零電源循環）：活的步驟是 `R6-7`，而它關了，`R6` 跟著關了。** 這一段沒有上電：先把上一段刻意沒有 push 的六個提交修回可以 push 的狀態（`citecheck` `C3` 69 → 0、`C7` 9 → 0，四十九個引用照「只改數字、新號碼讀回來導出」修好，兩個是**歷史引文**不准改、記在 baseline 與擁有者檔案裡），然後 `R6-4` 的裁決、`R6-6` 的較弱真話、`R6-7` 的寫上去與 `docs/GATE-RESULTS.md` 第十二筆。🔴 **最值錢的兩件都是收 gate 時撞到的**：`D5` 發表的「68 %」是拿**板子收**去比**廠商送**（`SPEC.md` `NET-102` 就地修正、`notes/nic-driver.md` § 16.3c），以及 `FW-109` —— 一個引用可以在寫下的當下就是錯的而 `citecheck` 報它 `STABLE`，一次桌面掃過撞到**五個**。 *(以下是上一段的全文：)* 🔄 **2026-09-21，第九十二段（seating 31，2026-09-20 22:29 開場，**四次冷開機**，00:4x 收工）：活的步驟仍然是 `R6-5`，第三次嘗試，而這一段把它的阻擋者換掉了。** 🟢 **映像 `s31b`（`RECIPE_ID f179cf21`）裡 `nic_do_arm()` 會重建兩個環並歸零兩個索引，板子因此撐過六次 `arm` 與五級叢發** —— seating 30 是第二次 `arm` 加流量就死掉的。🟢 **偵測器有三路正控制**（`dsynctest` 回 0／4／1，全部上電前預測）。🔴 **階梯推翻自己的假說**：門檻是環深 **8** 而不是 9，Δ 是 **1／2／3 而且瞬時**，seating 30 記的「恰好 4 並停在那裡」沒有重現，**而這代表本專案取過的每一個單次取樣 Δ 都是下界**。🔴🔴 **最大的結果不在卡片上：板子是「啞」不是「聾」** —— 四個 TX 描述子全部停在引擎手上、`tx_stopped 1`／`n_tx_wake 0`，而 `n_rx` 一路漲到 38。這指名推翻 `NET-54` 的入向 wedge、`NET-64` 的硬當、以及 **`NET-61` 是 `D5` 的阻擋者**（三次 wedge `n_dsync` 全 0）。🟢 **而新的 `arm` 把卡死的 TX 環拿得回來**，所以這個故障不再需要冷開機（`NET-68`），**但恢復不持久**。🔴 **`D5`／`D6` 仍然沒拿到**，理由第三次換人而且每一次都是量出來的。詳見 `SPEC.md` `NET-67`…`NET-69`、`notes/nic-driver.md` § 11、`bench/2026-09-21/CORRECTIONS-block33.md`。 *(以下是上一段的全文：)* 🔄 **2026-09-20，第九十一段（seating 30，18:35 開場）：這一列在此之前落後四段、三次上機。活的步驟是 `R6-5`，第二次嘗試。** 這一段把 `R6-5` 的阻塞從「機制未定」變成「機制已定」：`SPEC.md` `NET-61` 的鏈有六環各自的量測，`NET-59` 的四句話被它自己座位的擷取就地劃掉，而 `NET-62`／`NET-63`／`NET-64`／`NET-65`／`NET-66` 與 `MEM-18`／`FW-97`／`FW-98` 是這一段新增的。🔴 **`D5` 與 `D6` 仍然沒拿到，而現在是刻意不拿** —— 在一個會把 frame 帶著別人的長度遞交的狀態下量吞吐量，量到的是缺陷而不是路徑。🔴 **板子在段末是硬當的**（`NET-64`），需要一次 ≤ 5 秒的斷電才能做`bench/2026-09-20b/CORRECTIONS-block32.md` § 1 的事後剖驗。 🟢🟢 **第八十四段（2026-09-17 04:24 開場，**桌面**，零電源循環，板子全程斷電）：`P1-5` 關了，`P1` 跟著關了。** 🔴🔴 **最值錢的一件是一個普查：第八十三段的結語寫著「一個否證一行的量測通常也否證另外兩行，而這個 repo 裡沒有任何東西會去找另外兩行」—— 今天去找了，而它正好找到兩行。** `docs/mfgtest.md` § 2 的十一條合格判準逐列對上 `chk` 呼叫：**`MT-PORT` 宣告「輸出要指名廠商驅動」而它印的是埠名**、**`MT-MAC` 宣告「body checksum 0」而那個條件只在 `MT-RFCAL` 裡** —— 三列同一個方向，表比儀器強。🔴 **`MT-PORT` 少的那一項正是 `R6` 要的那一項**，所以這個專案擷取過的每一行 `MT-PORT` 都沒標明是誰服務的（`FW-93`）。🔴🔴 **第二件：發表的 map 摺要從已提交的擷取重算不出來**。`ae87ac03269985d6` 要 `tr -d '\r'` 才回得來，原樣是 `70484defc9714ecc…`；三處發表它的檔案都沒寫這一步。**根因和 2026-09-08 那四個假停止同一個，這是第四個消費者，前三個是閘門**（`FW-92`）。🔴 **第三件：類別 `S` 是五列不是四列**，而寫著四的那一句跟 `MT-ID` 那一列是同一個 commit（`e362ffa`）寫的 —— 從寫下來那天就是錯的，而它在「沒有證明什麼」那一節裡，是講得太強的方向。🔴 **第十筆 gate 被交代了一個事前登記的裁決，而它沒做** —— `git show 126f659` 量：它只改了標題、一句括號、一列表；裁決在這一筆補上，晚一筆，而晚了也寫進紀錄。🟢 `RECIPE_ID` 不拄任何紀錄，從 `config/` 的 30 個檔重算：**`bb684eb0`**。**閘門：`spec-check` rc=0、`flashwin scan` 對這台自己的 dump **4,738 檔 CLEAN** （113 個探針，正控制在 `upstream/` 上照樣開火）、`test-spec-check-mutants` **21/21**、`test-file-modes` 5/5。** 🔄 **2026-09-17(第八十六段):`R6-2` 的桌面半邊做完了,矽片那半沒有。** 驅動在映像裡、開機零寫入(`n_writes` 可證)、四個狀態(S0／S0′／S1／S3)與四個控制都寫在驅動自己的註解裡而不是在卡片上。**還缺的是一次上電**:`FULL_RST` 之後的讀數(S1)、dumb 設定之後的讀數(S3),以及 `S3 ≠ S1`。⚠️ **`R6-2` 不能在 `NET-37` 殘留之前把交換器設成 dumb** —— dumb 會把每個 `PVCR` 寫成 1,之後就分不開「原廠設的」與「我設的」　🔄 **2026-09-21，第九十三段（seating 32，00:54 開場，**三次電源循環**，03:2x 收工）：活的步驟仍然是 `R6-5`，第四次嘗試。🟢🟢 **`D6` 成立** —— 31.66 分鐘連續洪水，驅動計數器 `drop 0/0`、主控台在 `PRINTK=y` 的映像上 **0 位元組**（零 oops、零 wedge）、擷取全程不送指令；雙向 **7.067 GB**、**29.761 Mbit/s 合計**、主機端遺失 0.00518 %。🔴 **`D5` 仍然沒有**，而這一次的理由是機制而不是新嫌犯：`D5` 指名 `iperf3`，`iperf3` 一定開 TCP 控制連線，而**一條 TCP 連線是六個工作負載裡唯一與 wedge 共存、與不 wedge 互斥的因子**（三個否定控制：深度 4 的 flood ping、深度 34 的 27,017 frame/向、大訊框洪水疊併發小 ping，全部不 wedge）。`NET-71`–`NET-76`。　🔄 **2026-09-21，第九十四段（seating 33 與 34，12:13 開場，**兩次冷開機**）：活的步驟仍然是 `R6-5`，第五次與第六次嘗試，而這一段把 `D5` 的**敵人換掉了**。🟢🟢 **`NET-73` 唯一的 ⚠️ 關掉了，而答案否證了那一列的寬讀法**：一條 TCP 連線**不**足夠。四個階梯全部不 wedge —— 一條被拒的連線、五十條、**30.000103 秒內 35,062 條（1,168.73 條/秒）**、以及一次**完成**的三向交握（對象 `busybox telnetd`，行程表裡沒有 `iperf3`）—— 而 `iperf3` wedge。板子自己的 `/proc/net/snmp` 從另一側逐項對得上（`OutRsts` 35,114、`PassiveOpens` 1、`ActiveOpens` 2／`AttemptFails` 1、`InEchos`／`OutEchoReps` 4/4 → 28/28）。`NET-77`。🔴🔴 **而這個 wedge 不是 `NET-67`，是一個新的故障態**：驅動側 `tx_stopped 0`／`n_tx_stop 0`／`n_tx_full 0`／四個 `txd` OWN 全 CPU 擁有／`n_tx` 仍在前進，引擎側 `now_icr C4000000`（`STOPTX` 清著）與 `tpdcr0_pos` 前後不變，交換器 37 個暫存器跨故障**逐位元組相同**，`GDSR0 0012001E` 三個 runout／流控旗標全清 —— 而主機端 `tcpdump` 兩次都是**板子的任何訊框 0 個**，RX 一路健康。`NET-78`。🟢 **廠商的 TX 契約第一次讀全**（TX 128／RX 256、從不停佇列、環滿就地回收重試 128 次再丟）`NET-79`；🔴 而 `s32a` 把那個契約做成執行期開關跑了之後，它自己的 `n_tx_full 0` 說這個修法瞄錯了東西 `NET-80`。　🔄 **2026-09-21，第九十六段（seating 35，19:13 開場，**兩次電源按鍵**、八次開機，20:4x 收工）：活的步驟仍然是 `R6-5`，第七次嘗試，而這一段第一次有**參考實作的讀數**。** 🟢🟢 **廠商的驅動在這塊板子上跑完 `iperf3` 並且存活** —— 31.3 MBytes、25.4 Mbit/s、`Retr 0`，跑完之後雙向 ping 4 of 4 —— 所以 `D5` 的阻擋者在我的驅動裡而不在硬體路徑上；同一次開機裡的交接（`rlx0 down` → line 12 消失 → `eth4 up` 拿到 `12: 12 RLX LOPI eth4`）把 `docs/KNOWN-ISSUES.md` 的 `EBUSY` 否證掉。🟢🟢 **rlxfw 自己的驅動載運了 203 MBytes ／ 23.3 Mbit/s、`drop 0/0`**，是廠商的 0.87–0.92×，🔴 **而它不是 `D5`**：一次執行，而且控制連線在約 10 s 就停了。🔴🔴 **`NET-77` 的三個候選全部否證，`NET-73` 跟著倒**：板子當 server、沒有開任何連線也會卡死（`bytes_acked 36,238` 之後停住），而對一個**關閉的埠**灌 UDP、板子上**零使用者空間行程**，照樣卡死。最小重現是 **347 個訊框、8.01 秒、0.5 Mbit/s**，而且 `n_dsync 0` —— 環失步不在這個故障裡。🔴🔴 **最有價值的是一個單變數 A/B**：`tx_mode 1`（廠商的環滿契約）下佇列從不停、OWN 位元被讀 **1,548 次**、`tx_retry_max_seen 129`、**`n_tx_recovered 0`** —— **引擎的停頓是永久的，TX 側任何驅動策略都遮不住它**。`SPEC.md` `NET-84`…`NET-91`、`FW-106`／`FW-107`；`notes/nic-driver.md` § 14；`docs/nic-vendor-diff.md` § 11。 🔄 **2026-09-21（第九十八段，seating 36，**一次電源按鍵** —— 22:22 的冷開機，整晚只有那一次 —— 兩次 Linux 開機、四次蓄意弄壞、四次救回）：`R6-5` 的兩個問題都答完了，而答完它們的是一個**參考實作**而不是又一次猜測。** 🟢🟢 **`P0`：loader 在同一顆引擎、同樣四個 TX 描述子上撐過 16 倍的劑量。** 階梯 0.5／1／2／4／8 Mbit/s 各 8 秒再加 8 Mbit/s 連續 30 秒，**合計 31,475 個訊框**，每一級之後都回 ARP，灌的當下 ARP 負載 8/8 與 27/27 全回；`A3-CON`／`A5-CON` 兩個不送位元組的 console 擷取都 **0 bytes**（所以不是 `Assertion fail` 那條路），`A7-PROMPT` 與 `S0-catch` 對看門狗重置字串各 **0** 次而 `B1-rz` 是 **1** 次（那個 1 讓那兩個 0 成為讀數）。第一級是 **347 訊框／8.01 秒／0.501 Mbit/s**，也就是 `Y5` 的劑量本身。🟢🟢 **而「訊框真的到了引擎」是分開量的，那才是這一格值錢的一半**：loader 自己的三個全域，每一個都有程式碼側第二來源（`0x8040EAC4` 在 `rx_receive` 裡、`0x8040EAC8` 是 § 12.1 早就命名的 `tx_count`、`0x8040EAC0` 是 § 12.2 的 `tx_done_idx`），陰性對照是相隔 10 秒什麼都不送的兩次讀數**逐位元組相同**，跨一次 8 Mbit/s × 8 秒收訊框 **+5,544** 對送出 **5,534**、`tx_count` **+6**、`tx_done_idx` **3 → 1 =（3+6）mod 4**。`SPEC.md` `NET-95`…`NET-97`。🔴🔴 **`P1`：同一次上電、二十分鐘之內，rlxfw 在同樣 347 個訊框下不回了**，四張 TX 描述子 OWN 全立，而停住是**永久的**——`+16／+46／+120／+600 秒`四點，`n_tx` 四次都是 17、`n_tx_recovered` 四次都是 0，內部控制是 `n_rx` 與 `n_irq` **每格正好 +6**（每格之間只跑過一次 ping）。`NET-98`／`NET-99`。🟢🟢 **最大的收穫不在卡片上：恢復路徑量出來了。** `engine off` → `arm` → `engine on` 三個 `/proc` 寫入，在一次**全新開機的乾淨故障**上把四張 OWN 清掉並回到 **ping 4/4**，同一次開機再弄壞再救一次也成立；而佇列那一半是 `rtl819x-nic.c:1077` 既有的 level test 自己叫醒的（恢復後、ping 前讀 `tx_stopped 1`／`n_tx_wake 0`，ping 後讀 `0`／`1`）。`NET-100`／`NET-101`。⚠️ **`R6-6` 的可讀半邊第三次一致**（`VCR0`／`VCR1`／`PVCR0`–`PVCR4` 與 seating 33／34／35 逐位元組相同），兩個 DoD 條款仍然上電前就不可達。**零 flash 寫入、零 `FLR`、兩次上傳前 `AUTOBURN` 都讀 `00000000`、括號維持 0.0244 %。** |
| **Session history** | 🟢 **`POS-1` closed 2026-09-11 (the fifty-seventh segment), option ③ — owner's decision, delegated.** These four rows were `**Last session**` … `**but three**`, and 量 2026-09-10: they had not moved since `d453068` (2026-09-08 23:33) while the row above them was rewritten every segment, so `**Last session**` named the fifty-fourth and the three below it named the forty-fourth, forty-third and forty-second. **They were a second owner of what `LOG.md` already owns**, and this repository closed the identical shape at `P4b-1` (2026-09-01) by DELETING the second owner rather than synchronising it. 🟢 **The refutation condition was written first and did not fire**: 量 before the rows were touched — `LOG.md` holds **89** dated `## ` entries, one per segment, and all four of the segments those rows described (42nd, 43rd, 44th, 45th) are among them, at 1,540,554 bytes against this file's 784,918. Nothing was lost. ⚠️ **Option ① (rotate every segment) was rejected for a measured reason**: it needs a checker that does not exist — `spec-check`'s `C12` reads only `Next after this` — so it would be paying for a guard to protect a duplicate. **The owner of session history is `LOG.md`, one dated entry per segment. This file keeps no copy.** ⚠️ **`## Session ladder` is NOT touched**: it holds FROZEN copies of these same row labels, dated, and an archive of what this file said at the time is not a live second owner — it is the same shape as `## Corrections`. 🔴 **And it is the third owner, which is what the first two segments of `POS-1` did not see**: the four rows were supposed to rotate INTO that ladder, and they stopped. Collapsing them removes the live duplicate and leaves the archive standing.　🔄 **2026-09-22（第九十九段，seating 37，**一次電源按鍵** —— 00:45 的冷開機，一次 Linux 開機）：`D5` 拿到了，而拿到它的不是恢復機制。** 🟢🟢 **`NET-102`：`phfollow 1` 把吞吐量從 **0.04–0.07 Mbit/s**（五次控制臂，接收端每一次都是 **0.00 Bytes**）換成 **17.03 / 17.76 / 17.09 Mbit/s**（n=3，散布 4.2 %）—— **250 倍**，而對廠商同一顆 3.1.3 二進位的 25.4 Mbit/s 是 **68 %**。否證條件成立：`n_ph_used` **44,256** 對 0。 🟢🟢 **`NET-104`：驅動自己修好了自己的故障** —— `recover 1` 打在一個**已經存在**的 wedge 上，console 依序印出 `NET-101` 的三個 mark，沒有人打字；`recov_j_fire − recov_j_arm` = **100** = `recov_jiffies` 正好，兩次；整晚 **26 次停住、26 次恢復、0 次失敗、0 次誤觸**。 🔴 **`NET-105`：`NET-67 殘留` 的兩個最強候選同一晚死掉，而且沒有被取代** —— run-out 遮罩不是（loader 自己的 `0x000007F8` 上了、兩個來源一致，佇列還是停了兩次），修好 RX 配對也不是（`phfollow 1` 加 `recover 0`，`Y5` 的劑量照樣 wedge）。 🔴 **而我自己有一個推論在同一次上機裡被後面的格子推翻**：`F1` 那一格 `n_tx_stop` 沒動，我讀成「TX 停住是 RX 損壞的下游」，而 `H1`–`H3` 每一次都動了。活下來的窄句是：一次 30 秒、17 Mbit/s 的傳輸**可以**不停住。 🟢 **`R6-5` 的 DoD 兩條都成立了。** 🔴 **而這一句收工時寫錯過**：曾經寫「30 分鐘洪水那條一次都沒跑」，而它 2026-09-21 seating 32 就跑了 —— `NET-76`，`ping -f -w 1800`，**1,899.593 秒 = 31.66 分鐘**，驅動計數器 `drop 0/0`、`n_tx_stop 0`、零 oops、log 完整擷取。**錯誤的根因值得記下來**：我讀 DoD 時問的是「這一次上機做了嗎」，而 DoD 是**整個 gate 累積**的、不是每次上機重新起算的。 `notes/nic-driver.md` § 16、`docs/nic-vendor-diff.md` § 15。**零 flash 寫入、零 `FLR`、`AUTOBURN` 上傳前讀 `00000000`、括號維持 0.0244 %。** |
| **Next after this** | 🔄 **2026-09-21（第九十二段收工，seating 31，四次冷開機）：下一段是 `R6-6` ＋ `R6-7`，由擁有者在段中決定，而 `R6-5` 的 `D5`／`D6` 不在其中。** **帶進去的，按順序**：① 🔴 **`R6-6` 的設計已經寫好而且它的 DoD 有兩個子句上電前就確定達不成** —— 兩個埠需要兩個同時在線的端點（一條線量到只有一個埠 `LinkUp`，十三次讀數無例外），而 **rlxfw 根本不能寫 VLAN 表**（讀 `rtl819x-switch.c:88-92`，表走 TACI 間接路徑）。能成立的說法比 DoD 弱而且要寫進 `GATE-RESULTS.md`。② 🔴 **VLAN 的寫入路徑是廠商的 `/proc/rtl865x/memory`，而它沒有任何界限檢查**（讀 `rtl865x_proc_debug.c:4115-4163`），所以每一格 `echo write` 前面要有同一個位址的 `echo read`，而且 payload 要遠低於 64 字元。**只動 `PVCR0`–`PVCR3`**（loader 每次開機重寫，會自癒）；**不准跑 `reset full`**（會讓寫入變成no-op）；**不准用 `restore 0`**（slot 0 是 `S0′`，從來不是可用組態）。③ ⚠️ **操作者已同意中途換埠**,而換埠是有時序的實體動作:要等他說開始、視窗開長、讓儀器自己記時序。④ 🔴 **`R6-7` 要把 `D5`／`D6` 寫成沒達成並指名新的擁有者** —— repo 裡**沒有**任何一條允許帶著未達成的 DoD 列關掉 gate 的成文規則（`PROGRESS.md:1812`：*a gate whose DoD has an unmet row is not closed*），最接近的是 `R1-gate` 的先例,而它的第三個條件（*a gate held open for a seating that is not booked*）今天為假。⑤ 🔴 **`SPEC.md` 自相矛盾**：`NET-40` 說 `NET-10 殘留` 關了，而 `NET-10 殘留` 還開著問同一個問題 —— 已記進 §17，要在下一段就地和解。⑥ ⚠️ **完整 `desk-sweep` 這一段沒有跑**（只跑了目標式閘門），`CLAUDE.md` 明寫目標式收工不是它的替代品。⑦ 零 flash 寫入、零 `FLR`、四次上傳前 `AUTOBURN` 都讀 `00000000`。 *(以下是上一段的全文：)* 🔄 **2026-09-20（第九十一段收工，seating 30，一次電源循環，兩次開機）：下一步是 `R6-5` 的第三次上機，而它的第一件事是一次 ≤ 5 秒的斷電。** **帶進去的，按順序**：① 🔴 **先分辨板子與序列鏈路** —— `usbipd detach`、讀 Windows 側 `COM3` 回不回來、再 attach。`CLAUDE.md` 2026-09-17 說 attach 狀態下 `getportnames()` 與 `Get-PnpDevice` **不是**判別器，而這一段的 `NET-64` 就是用那兩個讀數排除轉接器的，所以它還沒資格說是板子。② 🔴 **≤ 5 秒斷電 ＋ `bench/2026-09-20b/CORRECTIONS-block32.md` § 1 的五格事後剖驗**（ESC 視窗要先開並驗證握住 port）。③ 🔴 **`NET-61 殘留` 的階梯還欠七級**，而重跑它需要一顆 `nic_do_arm()` 會重設 `nic_rx_idx` 的映像 —— 否則每一級的 `arm` 都會殺死板子（`NET-64`）。④ ⚠️ **三份 0 位元組的擷取還在 scratchpad**，`NET-64` 最關鍵的陰性結果沒有可引用的產出物。⑤ ⚠️ **主機端的格子改用 `ping -I 10.1.1.2`**（按來源位址綁定），這一段的十八份主機 `.log` 因為用了介面名稱而讓 `audit-bench-log` 亮了 36 個 hit，已用白名單處理而不是改擷取。⑥ ⚠️ **`D5` 是刻意不做而不是做不到** —— 在會把 frame 帶著別人的長度遞交的狀態下量吞吐量，量到的是缺陷。 🔄 **2026-09-20（第八十九段收工，桌面，零電源循環，跨午夜）：六個紅的 CI suite 全部綠了，`R6-5` 的映像側準備好了，下一步是 `R6-5` 的上機。** **做完的**：`if: !cancelled()` 讓一次 push 顯示所有紅（事先登記、run `35453791429` 命中——`text` job 跑 6m28s 而不是 23s 中止，報六個而不是一個）；三個會腐朽的語料常數改成性質斷言；`bootbytes` 的選擇改成 `RLXFW-B10` 內容閘門（`"boot"` 是 `"reboot"` 的子字串）；`reply-size` 的兩份 parse 併成一份加六個控制項；`audit-bench-log` 兩筆帶理由的 allowlist；六個引用修好加一列 baseline 刪除。**`R6-5` 的映像側**：`R6-4a`（`netif_wake_queue` + `ndo_tx_timeout` + 五個 `/proc` 欄位 + `txstall` 正控制）已編譯，`CONFIG_NET_PKTGEN=y`（`(NEW)` 預測 0，成立），映像 `r6nic5` `RECIPE_ID ffe4b5cf`、`vmlinux` 4,212,358 位元組；🔴 **而那個 id 已經不是這棵樹的 id 了**——收工稽核把 `config/rlxfw-user/iperf3/` 四個檔案加進 `config/`，量，樹現在是 **`f2aa2fdd`**。`r6nic5` 是一顆真實存在、`ffe4b5cf` 也是它真實的 id，但**下一次建置會拿到別的**，而一張預測 `RLXFW-ID0=FFE4B5CF` 的卡片會把一台裝著正確映像的良品判紅。**`RECIPE_ID` 只能從產出物讀，不能從這一列抄。**`iperf 3.1.3` 交叉編譯成功 252,644 位元組、零原始碼修改、`hazlint` 0 違規。🔴 **下一段的第一件事**：把 `iperf3` 加進 `config/rlxfw-initramfs.tsv`（會動 `RECIPE_ID`，要重建），然後凍卡片——`R6-5` 的計畫列明寫 **card 要**。🔴 **通電前必做**：`SIRR` 的 `TRXRDY`（`unlock i-mean-it` 然後 `echo start > /proc/rtl819x-switch`），否則 ping 100 % loss 而且看起來像驅動壞掉。⚠️ **`txstall` 的格子要兩次 `ping`**：這顆映像的 `ping` 永遠送四個而環剛好四個。 *(這一格原本是 2026-09-17 第八十四段的 `R6-0` 交辦。)* 🆕 **2026-09-17（第八十四段收工）：`P1` 關了、`v0.4` tag 了、`R6` 開了，下一步是 `R6-0`。** `R6-0` 是**桌面**、**零電源循環**、**不用卡片**，而它產出的是 `R6-1` 那次冷格 seating 的**卡片設計** —— 兩者不能塔在一起。**三件，而計畫自己把第三件提到前面兩次**：① 既有材料普查（母體從**兩個不共用程式碼的來源**導出：`SPEC.md` 經由 `spec-check` 自己的剖析器，以及 `regcensus` 對廠商乙太網路驅動的**程式碼**侧）；② DoD 與否證條件（已經寫在步驟表裡）；③ **`CPU-45` 那一格的重新設計**。🔴 **普查的事前登記，寫在步驟表的 DoD 格裡**：`docs/mfgtest.md` § 5 宣稱*「這個 repository 裡每一列 `NET-*` 都是 loader-state」*，若成立，**`R6-1` 那次冷格 seating 背著整個量測地基**，而普查的價值是告訴那一次 seating 什麼不能漏。⚠️ **帶進來的兩件**：`MT-PORT` 要印出哪一支驅動在服務（`FW-93`；改它會動 `RECIPE_ID`），以及 `CAPD-1` 第二次移交到 `R6`（`R6` 第一張凍結的卡片必須自帶一個宣告的日期，否則那一列要記成 `⊘`）  🔄 **2026-09-17(第八十五段,seating 26 收工):`R6-0` 與 `R6-1` 都走完了,一次電源循環,11:02 → 12:06,三張卡片。下一步是 `R6-2`。** 🟢🟢 **`R6` 的 D1 —— 也就是這個 gate 的前提條件 `CPU-45` —— 答完了,而答案是「不一致」**:用交換器 CPU 埠的 RX DMA 當真的 bus master,四個緩衝區裡兩個、兩次獨立執行,快取在引擎覆寫 DRAM 之後回舊值,而「被逐出」那條退路被資料自己關掉(`V2 ≠ V0` 證明 DMA 寫了,一次 miss 會取回 `V2`)。**所以 `R6-3` 的環與付載緩衝區放未快取視窗,是量出來的要求而不是預防措施** —— 這個 gate 停損那一列事先寫的「保守成本」現在有讀數撐著。🟢 **`R6-1` 的 DoD 三列全部成立**:普查標為 Linux 下未定的暫存器**全部至少讀過一次**(十一個,loader 側 block 24、Linux 側 block 26);`CPU-45` 回的是判決而不是「分不開」;廠商驅動對照在**同一次上電**裡取得。🟢 **`R6-2` 現在做得到了,而在今天早上它做不到**:它的 DoD 要「一個與 reset 不同的讀回值」,而那十一個暫存器在今天以前**兩種狀態下都沒有值可以不同**。🔴 **而 `R6-0` 只走完一半,說清楚而不是算它過**:普查的**內容**交付了(兩個不共用程式碼的來源都到位,程式碼側那個有兩個獨立實作互相對照、606 = 606、負控制發火),但 **`regcensus` 沒有以工具的形式提交到 `tools/`**,所以它不是一個下一段跑得起來的儀器。**那是帶進 `R6-2` 的第一件。** ⚠️ **`CAPD-1` 的字面條件滿足了而機制沒有**:量,`cardcheck.py` 裡 `started_wallclock`／`strptime`／`datetime` 出現**零次**,`capdate.py` 從不開啟 `PREDICTIONS-*.md` —— **沒有任何檢查器讀得到卡片宣告的日期**,寫下來不等於被檢查。三張卡片都帶了 `declared-date` 這個 `cardnum` 列,那是機器看得見的最小一步,而它跟任何東西都還沒有比對過 🔄 **2026-09-17(第八十六段收工)**:`R6-0` 關了。下一步是 **`R6-2`** 的矽片半邊(一張卡片、一次電源循環),它的前置是 `NET-37` 殘留與 `NET-31` 殘留 —— 兩個都是桌面、零電源、而且**必須在 dumb 寫入之前**做完。之後是 **`R6-3`**,它的階梯比計畫多一階:`CPUICR` bit 20 `SWINTSET` 可以在沒有環、沒有描述符、沒有 PHY、沒有網路線的情況下升起中斷,把「loopback 不動」與「我的中斷沒來」拆成兩個觀察 🔄 **2026-09-17 收工,下個段的目標(擁有者指定):走完 `R6-4`,並寫出 `R6-5` 的上機卡片。** `R6-2` 的桌面半邊已落地;`R6-3` 的階梯比計畫多一階(`CPUICR` bit 20 `SWINTSET`,零環零描述符零 PHY),而**它的第一格是輪詢而不是中斷處理常式** —— 廠商的 NIC 驅動擁有 IRQ 12,不能不共享地 `request_irq`。⚠️ **`R6-3` 的前幾階不需要解掉 `NET-38` 殘留 ①**:最小階梯不碰任何位元欄位。⚠️ **`R6-2` 不能在 `NET-37` 殘留與 `NET-31` 殘留之前把交換器設成 dumb** —— 兩個都是桌面、零電源、而且 dumb 寫入會把每個 `PVCR` 寫成 1,之後就分不開「原廠設的」與「我設的」  🔄 **2026-09-19 收工(第八十七段,seating 27,一次電源循環 + 一次因我的失誤而多花的電源按鍵)。`R6-2` 關了;下一步是 `R6-3`。** 兩個前提在上機前就在桌面關掉了(`NET-37 殘留` 九分之九零殘差、`NET-31 殘留`／`NET-10 殘留` 由 `PSRP8` 的第一個讀數關掉)。🔴 **`R6-3` 的第 0 階照設計不成立**:`SWINTSET` 在引擎關、遮罩關時不留痕跡也不升起任何 `CPUIISR` 位元,而一個 off-card 控制(同一條 `echo write` 路徑寫 mbuf 大小欄**留住了**)證明那是晶粒不是儀器 —— `SPEC.md` `NET-47`。所以 `R6-3` 的第一格要改成「引擎開或遮罩開之後再寫 `SWINTSET`」,或者放棄那一階直接進 loopback。🆕 **下一顆映像該帶的東西已經量到**:廠商的 `rxRing`／`txRing`／`mbufRing`／`nic_mbuf` 這四個 `/proc` 檔被一個純 `#define` `RTL_DEBUG_NIC_SKB_BUFFER` 閘住,加一個 `-D` 就有 —— 那是廠商自己對描述子環的看法,正是上一段被否證的描述子論證所缺的第二來源(`SPEC.md` `NET-42`)  🔄 **2026-09-19 收工（第八十八段，seating 28，一次電源循環）。`R6-3` 與 `R6-4` 都走完了；下一步是 `R6-5`。** 🟢🟢 **`R6-3` 的四階梯全走，而描述子佈局是在任何驅動執行之前就在 loader 提示符下關掉的** —— 冷讀 loader 自己的活環、推一次 1 MiB TFTP、再讀同兩個視窗；`CPURPDCR0` 與 `CPURMDCR0` 各 +8 且同步、`CPUTPDCR0` +4、OWN 位元 1→0。`SPEC.md` `NET-48`–`NET-54`。🟢🟢 **`ping` 雙向 4 of 4 走在 `rlx0` 上**，`n_napi_poll` = `n_napi_complete`，`nd_stats rx 5/452 tx 5/452` 對 4×98+60。🔴🔴 **而這一段最有價值的是一個對我自己診斷的否證**：一個看起來很完整的 run-out 診斷，被我設計來證明它的單變數 A/B 打掉 —— 未修正的映像在同一台冷開機過的硬體上跑出 0.0531 %，對修正版的 0.0536 %。卡死的是交換器／PHY 入向狀態，而廠商自己的驅動是那個控制。⚠️ **這次上機沒有凍結卡片** —— 預測寫在每一個 runner 腳本裡、執行之前，但 `check-predictions` 無法評分，也沒有 mtime 證據。**那是一個用可審核性換迫近度的交換，寫下來而不是略過** 🔄 **2026-09-20 收工（第九十段，seating 29，四次電源事件）：`R6-5` 跑了，`D5` 與 `D6` 都沒拿到，而沒拿到的理由每一條都是量出來的。****兩張卡片都凍了**：block 28（`B29`，**8 of 45**）與 block 29（`B30`）。🟢 `iperf3` 第一次進映像（`config/rlxfw-initramfs.tsv` 38→39 列，`RECIPE_ID` `edc94765`，並且**重建逐位元重現已稽核的產出物** sha256 `3144db60…`）；開機擷取 **1,874 = 預測 1,874，三次獨立命中**（`bootbytes` 從 mark 表導出，不是抄的）。🟢🟢 **`§ 7.7` 的驗收集合全部成立**（`n_tx_stop 1`、`n_tx_wake 1`、`tx_stopped 0`、`now_icr C4000000→44000000→C4000000`、ping 恢復），而 block 28 那個看起來像否證的 `n_tx_wake 0` **是失控引擎的後果不是 wake 位置錯**。🔴🔴 **四個新發現**：`NET-56` 接觸不良造成單向失效（交換器 port 3 計數器是唯一能分開它與軟體原因的讀數，而那正是 `NET-54 殘留` 點名缺的那一個）；`NET-57` 廠商把 `ether_setup` 改成 `tx_queue_len = 0` → noqueue → `dev_watchdog_up()` 從未呼叫，且 `NETDEV_TX_BUSY` 是丟包不是重排隊；`NET-58` re-open 不重新武裝描述子基底，引擎走到環外對任意 DRAM 做 DMA；`NET-59` **一個真正的 TCP 負載把整台板子掃住而機制未定**（兩次重現、console 100 秒 0 bytes、`reboot -f` 無效、**不是 panic**）。🔴 **`NET-60` 是對我自己桌面結果的修正**：iperf 3.1.3↔ 3.16 互通在 loopback 上四案全過、在真實 1500 徑上失敗，**loopback 測不了一個失敗模式是短讀的協定**；解法是主機也跑 3.1.3（qemu，量：能綁真實介面）。⚠️ **最貴的過程錯誤是我的**：block 29 把十四個已凍結的 cell 打進一個已經死掉的 shell，因為 cell 之間沒有任何檢查；`check-predictions` 只看存在與 mtime，所以 39 bytes 的空擷取跟真的一樣得分。加了兩行存活探測之後，下一次失敗只花掉 **一個** cell。🔴 **下一段的第一件事是 `NET-59`**：有界傳輸 `-n 64K` 往上二分，每一步之間存活探測，每一步讀 `n_irq`。**零 flash 寫入、零 `FLR`、每次開機靜止時 `n_writes 0`**；⚠️ 廠商韌體因為一次 ESC 視窗沒先開而無人觀察地跑了約一分鐘，這一段因此**不能說「只有我的映像執行過」**　🔄 **2026-09-21（第九十三段收工，seating 32）：下一段的第一格不需要電源也不需要新映像 —— 主機端用 `socat` 開一條純 TCP 連線、完全不跑 `iperf3`，把「一條 TCP 連線」與「`iperf3` 這支程式」分開。** 那是 `NET-73` 唯一的 ⚠️。之後是 `R6-6` ＋ `R6-7`（`R6-6` 三個 DoD 條款有兩個上電前就確定不可達，已寫在 `bench/2026-09-21/PREDICTIONS-B35-block33.md:356-377`），而映像側的提案是 `s32a`：N−1 在途上限、TX/RX 環深度 4/8 → 32/64、廠商式的同步回收重試（環滿丟包而不停佇列）、以及 `nic_xmit` 的呼叫脈絡計數 —— **四個都掛在執行期開關上，預設等於現在的行為，好讓同一顆映像一次開機就跑得出「壞→好」的對照**。　🔄 **2026-09-21（第九十四段收工，seating 33＋34）：下一段的第一格是「把 `iperf3` 與『在連線上真的流動的資料』分開」，而那是 `NET-77` 剩下的兩個候選。** 做法不用新映像：主機端 `socat` 對板子上 `iperf3 -s` 開的連線灌資料（板子端只有 listener，沒有 `iperf3` 的送出路徑），或反過來。之後是 `NET-78 殘留` —— 它指名的下一批讀數是**埠出口與 PHY 的 MII 側**、**交換器的位址表與轉送決定**、以及 **`ph_portlist`（驅動寫 `0x3F`）在故障態下解到哪些埠**，三者都**不在**現在這支驅動的 dump 裡，所以要一顆新映像或走廠商的 `/proc/rtl865x/memory` 讀。🔴 **`R6-6` 沒有跑，而它能讀的那一半已經有了**：`VCR0`／`VCR1`／`PVCR0`–`PVCR4` 在 block 37 的三份 dump 裡，七個值與 seating 33 一致；DoD 的兩個條款仍然是上電前就量到不可達，`R6-7` 要照實寫。🔴 **`R6-7` 沒有寫**，而它現在要寫的東西比開場時多：`D5` 未達成的**理由變了**（不再是 `NET-67`，是 `NET-78`）。⚠️ **完整 `desk-sweep` 這一段沒有跑**（約 31 分鐘、99 個宣告步驟）；跑過的是 `spec-check`／`citecheck`／`ledgerscan check`／`capdate`／`cardcheck`／`check-predictions`／`bootbytes`／`test-reply-size`。 　🔄 **2026-09-21（第九十五段收工，桌面，**零電源循環**，板子全程斷電）：`D3` 自己寫下的否證條件第一次被當成交付物執行 —— `docs/nic-vendor-diff.md`。下一段的第一格仍然不用電、不用新映像。** **帶進去的，按順序**：① 🟢 **廠商驅動的同一次開機對照是拿得到的，而 repo 記載的「拿不到」理由不成立** —— `docs/KNOWN-ISSUES.md:870`／`:941` 說 `ifconfig eth4 up` 回 EBUSY，而 量，那一格（`bench/2026-09-20/X9-eth4.log`）**沒有先 `ifconfig rlx0 down`**；seating 28 先 down 過，交接成功（`notes/nic-driver.md:518-519`）。讀 `rtl819x-nic.c:1633-1646` 的 `free_irq` 與 `rtl_nic.c:4192-4210` 的 `rtl865x_init_hw()` 兩頭都釘死。**三個指令，搭任何一次開機，零額外電源。** ② 🔴 **`NET-81`**：廠商在這塊板子送普通單播走 ASIC 硬體查表（`ph_portlist = 0x07`、`ph_flags` 帶 `HWLOOKUP`｜`BRIDGING`），rlxfw 走 direct 配 `0x3F` —— **泛流到六個 MAC**。決定它的讀數是跨故障的**每埠 MIB 計數器**，走廠商 `/proc/rtl865x/asicCounter`，不用新映像。③ 🟢 **`NET-82`**：`NET-61` 在廠商原始碼裡有結構性的解 —— 廠商跟 `ph_mbuf` 指標走、從不索引 mbuf 環；rlxfw 早就把那個指標寫在 RX pkthdr word 0 而沒讀過。⚠️ 改它會讓 `NET-70` 的 `n_dsync` 從故障指示變成正常讀數，代價要先寫下來。④ 🔴 **`FW-105`**：`citecheck` 的 `CITE_RX` 在逗號處停住 —— 11 個逗號形式引用、32 個行號、**21 個看不見**。修它要先有正控制，是下一段的工作。⑤ ⚠️ **`A3`／`A4` 沒做**：TX pkthdr 的 word 2/3/4 仍不在 `/proc` dump 裡（所以一個 ASIC 回寫跨故障看不見），`-DRTL_DEBUG_NIC_SKB_BUFFER` 仍不在板子模板裡。兩個都動 `RECIPE_ID`，同一顆映像。⑥ 🔴 **整個對照是單來源的**：`SOURCES.json:115-118` 宣告 `utessel-edimax` 是「第二份獨立實作」且 `needed_by: R6`，量，**從來沒 clone 過**；`openwrt-rtk` 與 `vankel-rtl819x-sdk` 也是。⑦ ⚠️ 這一段**沒有任何東西在矽片上跑過**；零 flash 寫入、零 `FLR`、括號維持 0.0244 %。　🔄 **2026-09-21（第九十六段收工，seating 35）：下一段的第一格不用電也不用新映像 —— `bench/2026-09-21e/y5-rateladder.py` 的階梯往**下**走，把門檻從「≤ 0.5 Mbit/s」變成一個數字。** **帶進去的，按順序**：① 🔴 **`NET-67 殘留` 現在是唯一的問題**：引擎為什麼停。`NET-88` 把 TX 側的驅動策略整片排除掉了，所以下一批讀數在引擎那一側；`NET-72` 留下的唯一候選是 `arm` 的 OWN-clear，**而它要一顆映像**（沒有動詞能只清一格）。② 🔴 **`NET-87` 的矛盾要先解**：`W2` 的 1,023 frame/s 撐 72.91 秒不死，而 `Y5` 的 43 frame/s 死 —— 分開它們的不是訊框率，固定訊框大小的速率二分是桌面寫得出、一次上機跑得完的。③ ⚠️ **卡片九格沒跑**（`W3`×2、`W4`×3、四個 `W5-*` 後態），理由逐格寫在 `notes/nic-driver.md` § 14.8，**而 `check-predictions` 記的 43 of 52 不改**。④ 🔴 **`FW-107` 的註解要修**（`rtl819x-nic.c:2322-2331`），它動 `RECIPE_ID`，所以跟 `NET-82` 的 `ph_mbuf` 修法與 `txd ph2/ph3/ph4` 的 dump 擴充搭同一顆映像。⑤ ⚠️ **完整 `desk-sweep` 這一段沒有跑**（約 31 分鐘、99 個宣告步驟）；跑過的是 `spec-check`／`cardcheck`／`check-predictions`／`flashwin scan`。⑥ 零 flash 寫入、零 `FLR`、🔴 **八**次上傳前 `AUTOBURN` 都讀 `00000000`（收工稿先寫成**五**，那是段中量一次之後拿舊數字引用而沒重算；量，`*-ab2.log` 是 8 個）、**廠商韌體全程一次都沒有執行**。　🔄 **2026-09-21（第九十七段收工，桌面，零電源）：下一段上機，而它的第一格是新的、不用映像也不用開機——**在 loader 提示字元下對板子灌 UDP，再問它還回不回 ARP**。** 理由是讀 `stage2.bin` 量到的（`NET-93`、`NET-94`）：loader 跑的是**同樣四個 TX 描述子**、同樣的 `TXFD` 門鈴、同樣的 `0x3F` 泛流常數，而它在極度稀疏的送出下跑 1 MiB TFTP 不死；而 `looprun` 的 `S5c` 每一次上機都證明 loader 在提示字元下**會回 ARP**。**所以它是一個已經在裝置上的測試平台。** 兩個結果都值錢：**活** → 引擎在這種流量下沒問題，故障在 rlxfw 的軟體，所有驅動側候選都還活著；**死** → 故障在兩個實作**之下**，rlxfw 被洗清，而 `R6-5` 整個框架要重寫（包括 `D5` 在這顆硬體上到底可不可達）。🔴 **它排在原本的 ①（+5／+30／+120／+600 s 的 OWN 位元讀數）之前**，因為它可能讓 ① 變得不相干。🔴 **而「把環加深」這個修法的理由被削弱了**：`NET-93` 否證了「四格太淺」是**差別**，它仍然可能是**邊際**，但不再是解釋。 🔄 **2026-09-21（第九十八段收工，seating 36）：下一段是 `P2` 的映像，而它要建什麼現在是量出來的而不是提案。** **帶進去的，按順序**：① 🔴 **`P2` 的偵測那一半是唯一要寫的新程式**——恢復那三步在今晚的矽片上成立兩次（`NET-101`），而佇列的喚醒 `rtl819x-nic.c:1012` 已經在做。**不准用 `watchdog_timeo`**（`NET-55`／`NET-57` 量過是死碼）、**不准用 `ndo_stop`／`ndo_open` 重開**（`X16` 把一個剛救回來的介面又弄壞了，`NET-58` 重現）、**`engine off` 必須排在 `arm` 前面**（`NET-64`）。② 🔴 **§ 12.4 的 run-out 遮罩分歧要跟同一顆映像搭**——它**不可能**不用映像測：讀 `rtl819x-nic.c:1409-1412`，NAPI complete 每次都把那些位元 OR 回去，所以經 `/proc` 寫 `CPUIIMR` 會被下一個進來的封包還原，**而讀回格會給假綠**（`echo read` 不產生線上流量）。那一格在通電前被丟掉，理由寫在 `docs/nic-vendor-diff.md` § 14.4。③ 🔴 **`NET-67 殘留` 仍然開著**：引擎為什麼停，今晚一個字都沒回答；有的是一個會動的修法，不是機制。④ ⚠️ **`NET-82` 的 `ph_mbuf` follow 與 `FW-107` 的註解修正**同一顆映像，維持不變。⑤ ⚠️ **`R6-6` ＋ `R6-7`**：可讀半邊已經第三次一致，`R6-7` 要把 `D5` 寫成未達成並指名新的擁有者——而 `docs/GATE-RESULTS.md` 的契約是*一個關掉的 gate 一筆*，`PROGRESS.md:1812` 又說 DoD 有未達成列的 gate 不算關，**這個衝突要由擁有者裁決**，第九十八段沒有替他裁。⑥ 🔴 **這一段自己的三個缺陷**：卡片的 `B1-PING` 沒有任何一格把 `rlx0` 帶起來（補成宣告的 off-card 格）、一個 133 字元的 `--send` 超過 127 上限而整格沒跑、以及我為那一格寫的否證條件 `n_arm_flush` **不是有效的判別器**（`arm` 沒跑與 `arm` 跑成功都讀 0）。⑦ ⚠️ **完整 `desk-sweep` 這一段有跑**（見收工）。⑧ 零 flash 寫入、零 `FLR`、`AUTOBURN` 兩次都 `00000000`。　🔄 **2026-09-22（第一百段收工，seating 38）：**~~下一段是 `D2` —— `R6-4` ①的裁決、`R6-6` 寫較弱的真話、`R6-7` 與 `docs/GATE-RESULTS.md` 第十二筆，由擁有者在段中決定。~~ ✅ **三件都在第一百零一段執行完畢，所以這一句是歷史而不是指向。** 帶進去的四件，每一件都有讀數：① 🔴 **`recover` 要做成編譯預設**，那是一個初始化值，而 `NET-107` 是它的理由；② 🔴 **`asicCounter` 要一個健康基線** —— 這一晚的讀數全在故障之後取，所以「故障造成的」與「一直就這樣」分不開（`NET-109 殘留`）；③ ⚠️ **ethtool 的三個 ops 在映像裡而不可執行** —— 這顆映像沒有 `ethtool`，下一步是一個靜態 MIPS `linkprobe`；④ 🔴 **`NET-110`**：`NET-78` 那個狀態這一晚是瞬態的、約四分鐘自己好了，而沒有任何一次上機等過那麼久。　🔴🔴 **而下一段的第一件事不是 `D2`，是修 69 個腐掉的引用。** 量，在乾淨的樹上、提交之後（所以 `0 suspended`、讀數是真的）：`citecheck` `C3` **69 個不在基線上的腐引用**、`C7` **9 個新的指到空行**。原因不是本段加的 `SPEC.md` 列（那是較小的一半），而是 `rtl819x-nic.c` 從 **2,912 行變成 3,108 行**，所以整個 repo 裡每一個 `rtl819x-nic.c:NNNN` 引用都移位了。🔴 **因此這一段的提交沒有 push** —— CI 跑 `citecheck`，推上去會紅。不今晚修的理由寫在 `docs/KNOWN-ISSUES.md`：`CLAUDE.md` 已經記過自動修復器會去改一個同一行的人類句子明文禁止改的號碼。　🔄 **2026-09-22（第一百零一段收工，桌面）：`R6` 關了，下一個 gate 由擁有者開。** 帶進去的，按順序：① 🔴 **`D4` 剩下的那個合取項是下一個 gate 的頭條** ——拿掉廠商樹，十個未定義符號、四個位置、VLAN 表走 `TACI`、而且會一併刪掉 `/proc/rtl865x/`（所以 `NET-109 殘留` 的健康基線要**先**取）；② 🔴 **`recover` 做成編譯預設**，一個初始化值，`NET-107` 是理由，要搭一顆映像；③ 🔴 **十二筆 clause 開火的那件事**：`MT-PORT` 要說出是哪一支驅動，而 `config/mfgtest.sh:57` 讀的是廠商的 `/proc/rtl865x/port_status`，那個檔案答不出來 —— 要換來源不是換一行；④ 🔴 **`FW-109` 的 enforcer 買得到而沒買**：四個正控制已經有了，缺的是一段；⑤ ⚠️ **`D5` 缺的那個量**：廠商方向相符的**接收**數字，三個命令、零電源；⑥ ⚠️ `phylib` 的正確形狀是 `mii_bus` 服務 port 0–4，`phy_device` 掛 `rlx0` 是架構性的錯；⑦ ⚠️ `linkprobe`（靜態 MIPS）才能執行那三個 `ethtool` op。　🔄🔄 **2026-09-23：擁有者裁定下一個 gate 是 `P2`（開機時間分段 ＋ 吞吐，兩套韌體、同一支腳本），而開它本身是下一段的第一件工作。** 🔴 **開一個 gate 有一個量出來的代價，而這個 repo 從來沒有寫下來過**：步驟表插在本檔第 66 行，量 `caac06e`（`R6` 那一次）把本檔從 **2,308 行變成 2,426 行**，`:1701` 的內容因此換掉了 —— 所以**插入點以下每一個指向 `PROGRESS.md` 的引用都會移位**，其中 **12 個在凍結的卡片裡**（`2026-09-14b` ×2 的 `:122`／`:1701`／`:1707`、`2026-09-14c` 的 `:1707`、`2026-09-20b` 的 `:2340`、`2026-09-21` 的 `:111`／`:129`、`2026-09-21b` ×2 的 `:110`／`:128`／`:130`），**不准修**。⚠️ **而 `citecheck` 今天對這十二個一個 finding 都沒報，而我原本要寫的解釋（`FW-109` 的洗白面）被我自己的量測推翻了一半**：量，兩張卡片的引用行 `git blame` —— `2026-09-14b` 的是 `f9fffa4e`（**2026-09-14，早於 `caac06e`**，所以那一個**不是**洗白），`2026-09-21b` 的是 `b4d36007`（2026-09-21，晚於）。**所以這十二個不是同一個故事**，而每一個落在 `STABLE`／`CHANGED`／洗白哪一格**未定**。**決定它的實驗**：對那十二個逐一問 `citecheck` 的 verdict（`CHANGED 23` 那一格很可能裝著它們），再決定插入之後要修哪些、把哪些記進 baseline。一個命令、零電源。**先量再插。**　⑨ 🔴 **`D4` 剩下的那個合取項現在是無主的** —— 它本來被寫成「下一個 gate 的頭條」，而 `P2` 要的是廠商映像**在場**而不是廠商樹被刪掉，所以那句話被就地劃掉了（`docs/GATE-RESULTS.md` 2026-09-23）。　⑧ 🔴🔴 **下一個 gate 的第一個動作是處理那十七列** —— `cfcensus` `L1` 在 `R6` 關掉的那一刻從 0 變成 17，因為 carried-forward 表的擁有者欄只能指名 gate，而十七列指名的都是 `R6`。**沒有任何一列改過**。基線已經提到 17 並附上第一個非零的摘要 `cefc023c`，而降回去要發生在「逐列認養或婉拒」的那個提交裡。**零 flash 寫入、零 `FLR`、括號維持 0.0244 %。**　⚠️ **這一列現在不指向任何步驟，而那是真的**：`R6` 關了，下一個 gate 由擁有者開，上面七件是給它的輸入而不是一張步驟表。 <!-- C12: between gates, no step id --> |
| **Blocked on** | **Nothing at the desk.** 🔄 **2026-08-27 改寫，而改寫的內容是這一列自己上一版的更正。** 上一版說可重現建置容器「擋的是 `R2c`，`R2a/b/d` 完全不需要它」。**不對**：`plan/router-rebuild-plan.md:1084` 那個容器的標題就是「**前置 —— 可重現的建置環境（1 個工作段，`R2a` 之前）**」，因為 `R2a` 要**用 T-vendor 重建 BusyBox 1.13.4**、`R2b` 要**分別建三個 drop 的 `boa`**，兩件事都要 32-bit 的 `rsdk` 跑得起來。當初那句 narrowing 把 gate board 的「none of it needs `R1`」讀成了「不需要容器」。🔴 **但結論的方向沒變，只是理由換了**：真正不需要容器的是**六棵樹的相似度矩陣**（材料全在 `$FWRE_WORK/extracted/`，量 2026-08-27 六個 `boa` 的位元組數與計畫表逐一吻合）與 **`R2d` 的兩條 grep**，那是這個 gate 的大半價值，**所以 `R2a/b/d` 照樣現在就開得起來**，而容器變成它自己的步驟 `R2a/b/d-3`。三條路線仍然未決，逐字保留：Docker Desktop 的 WSL 整合／在 distro 裡 `apt install docker.io`／`debootstrap` ＋ 釘住 `snapshot.debian.org` 的 chroot 且沒有 daemon —— **最後一條可以說比 Docker 的 layer cache 更可重現**，而那正是 `P4a` 要的性質。它擋 `R2c`、擋 `R2a`／`R2b` 的重建半段、擋 `P4a`；它從來沒擋過 `R1`。🔄 **而這一列等的那次電源循環早就花掉了** —— `R1g-4a`，2026-08-25。`R1h` 的 bench 半段等的是 `R3` 尾部那一次，**而那不是「被擋住」，是排程** 🆕 **2026-09-01:`P4b-gate` 有一件擋在擁有者身上,而那不是缺陷是設計** —— **60 秒影片要擁有者自己錄**(`study/20260901-video-runbook.md` 有全部指令、圍堵檢查與字級的算術),~~而 **tag `v0.2` 等影片錄完、等擁有者說**。~~ 🔄 **2026-09-11:這一句從 2026-09-01 起就是假的,而推翻它的是同一個檔案。** 量:`git tag -l` 有 `v0.0` 與 `v0.2`,remote 也有,而 § Release clock 寫著 **`v0.2` SHIPPED 2026-09-01**,tag `6cb9bf3`,GitHub 上有 release。**同一個檔案的兩列矛盾了十天**,而抓到它的是擁有者問「我沒看到 tag release」。🔴 **而它帶出一件真的到期的事**:`README.md` § *Which gates make which version* 說 **`v0.3` = `R4` ＋ `R5`**,`R4` 關於 2026-09-02、`R5` 關於 2026-09-11 —— **所以 `v0.3` 在今天變成到期的**,而 § Release clock 自己寫著 *規則從 `v0.3` 起遵守*,意思是它要 tag **也要** release。**tag 與 release 都是對外可見且實務上不可逆的動作,規矩是等擁有者說,所以這一段不做**;`REL-3` 是它的擁有者。桌面這邊沒有任何東西擋著:`P4b-gate` 的四件事都是桌面工作。  🆕 **2026-09-01 晚：`R4` 開了，而它的前三步一步都不擋電源。** `R4-0`（量現在的迴圈）、`R4-1`（寫預測區塊）、`R4-3`（工具）都是桌面。**只有 `R4-2` 要電，而它是搭便車的，不是自己一次電源循環**。🔴 **而這一句今天寫錯過一次，已撤回**：它曾經寫「`CLK-08` 從 2026-08-24 就是空格、自動化不了」。量：`CLK-08` **量到了** —— 1118.133 ms 與 557.583 ms。誤讀的來源是 `SPEC.md` §17 同一個 id 的殘留列，它的散文還寫著「還是空的」。真正沒量的窄得多：**最低 `OVSEL` 那一檔**（`WDTCNR = 0`，也就是 `J BFC00000` 觸發的那一檔），推 2.184 ms。　🔄 **2026-09-22（第九十九段收工，seating 37）：下一段的第一件是一顆把 `phfollow` 編譯進去當預設的映像，而它不用電源。** **帶進去的，按順序**：① 🔴 **`NET-102` 的 17 Mbit/s 是用一個 `/proc` 動詞量到的**，而一支「要先打一個動詞才正確」的驅動不算能用。把 `NIC_RXBUF_PHMBUF` 做成預設，然後一次開機就要能在不打任何動詞的情況下拿到那個數字。② 🔴 **`R6-5` 可以關了，而擋住 `R6` 的不是它**：兩條 DoD 都成立（`D6` = `NET-76`，seating 32；`D5` = `NET-102`，seating 37）。真正開著的是 **`R6-0`（半）、`R6-3`／`R6-4` 在步驟表上沒有標記，而 `R6-4` 的 `phylib` 那一半量到是 0（驅動裡 `phylib`／`phy_connect`／`mdiobus` 各 0 次）**，以及 `R6-6`（stretch）與 `R6-7`。③ 🔴 **`NET-67 殘留` 還是唯一的問題**，而且比開場時窄：兩個候選死了。下一批讀數在引擎那一側 —— `tpdcr0_pos` 跨 wedge、`/proc/rtl865x/asicCounter` 的 CPU 埠 `Snd`，兩個從 seating 31 就被點名而沒有一次做過。④ 🔴 **`NET-103 殘留`**：訊框率的二分，把「43 與 1,500 之間」變成一個數字；固定訊框大小，桌面寫得出來。⑤ ⚠️ **`R6-7` 的 `GATE-RESULTS` 第十二筆沒寫，而這是一個決定**：該檔案的契約是「一個關掉的 gate 一筆」，`PROGRESS.md:1812` 說 DoD 有未達成列的 gate 不算關，而 `R6-5` 確實有一列未達成 —— **兩條規則在這裡一致，所以根本沒有衝突要裁**。寫一筆說「這個 gate 沒關」會讓`GATE-RESULTS.md` 停止意指「關掉的 gate」，並且污染 operating clause 的輸入集。**寫上去的那一半已經存在**，在 `notes/nic-driver.md` § 16 與 `docs/nic-vendor-diff.md` § 15。⑥ ⚠️ **`R6-6` 的寫入半邊是被決定跳過的不是忘了** —— 兩條 DoD 上電前就不可達，而廠商 `/proc` 寫入路徑 `NET-100` 已經證過，所以成功不關任何東西、硬當要花一次電源。可讀半邊第五次一致（37 個暫存器逐位元組相同）。⑦ 🔴 **四個缺陷是我的，三個是重踏記錄裡的坑**（`FW-108`）。⑧ 零 flash 寫入、零 `FLR`、括號維持 0.0244 %。 |

## The step-list pointer, as of `f557873`

**Step list for the active gate**: 🔄 **2026-09-22: there is no active gate.** `R6` closed and the next one is the owner's to open, so this pointer names nothing until it does — which `cfcensus` `L5` is the checker for, and it fired on the commit that marked the board and left the header behind. ~~**§ Step list below** — 🆕 `R6`, written~~
2026-09-17 on entering it, the day `P1` closed and `v0.4` was tagged. ~~🆕 `R3`, written
2026-08-28 on entering it, the day `R2a/b/d` closed.~~ 🔴 **That sentence named `R3` for seventeen
days after `R3` closed** — a pointer that also names a gate is a second owner of
which gate is active, and nothing here noticed. **The lists below it are kept,
open ones first: `R1h` (open), then `R2a/b/d` and `R1-gate` (closed)** — the column
*"where it is most likely to be wrong"* is only worth having if it can be read
afterwards against what actually went wrong. 🔄 **It used to read
`plan/DAY-ZERO.md` items 0–8, and that was a second owner of this state** —
`DAY-ZERO` owns the instruments that had to exist before any gate could run, a
gate owns its own steps. ⚠️ **`R1h`'s list is still live, not history**: 🔄 **`R1h-2` closed 2026-08-29
with `R3-7` — they are the same step — so what is live is `R1h-3`, which shares
`R3-8`'s seating, and `R1h-4`, which follows it.** *(This sentence paired a
closed step with a closed step until 2026-08-29.)*
