# Findings

**A map, not a summary.** This repository holds about 400 KB of dense technical
prose across `PROGRESS.md`, `SPEC.md`, `RUNSHEET.md` and `LOG.md`, and without a
layer saying *which part is worth reading* that density reads as noise. Every row
below is one finding, ordered by **the decision it changed**, and links to the
file that owns it. Nothing here is the owner of anything.

Each row is marked: **量** measured on the device · **讀** read out of code, a
dump or a document · **推** inferred, pending a measurement.

---

## It changed what a payload of mine may execute

| | | |
|---|---|---|
| 🔴 讀 | **This unit's vendor kernel emulates `ll`/`sc` and `sync`, and does not emulate the FPU at all.** `do_ri` calls `simulate_llsc` and `simulate_sync` under `#ifndef`s the board config leaves unset; `simulate_rdhwr`'s two call sites are `#if 0`'d out where mainline calls them unconditionally; **there is no `math-emu` under `arch/rlx`** and `do_cpu` gives `SIGILL` for any coprocessor but 0. The binary agrees: zero `ll`, `sc`, `sync`, `lwc1`, `swc1` and `sdc1` in 2.85 MB of text, and zero `fpu_emulator`/`cp1emu` strings — ⚠️ not *zero FPU opcodes*: that span also holds one `COP1` and two `ldc1`, all three inside a 1 KiB non-code island and excluded by adjudication rather than by a bound | **`CLAUDE.md`'s rule *never measure the ISA under Linux* gave two reasons and one of them is false.** The rule stands — `simulate_llsc` alone is enough — but a rule whose stated reason is wrong is a rule that will be misapplied at the next edge case. It also means an FPU instruction from a payload running under the vendor kernel gets `SIGILL` rather than a silent emulation · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 🔴 讀 | **This unit's kernel contains MIPS16 code and calls it with `jalx`.** 180 in-image `jalx` targets, of which **179 in two clusters at and after `__iram = 0x802B8000`** — the 180th, `0x8000231C`, is a data word sitting in zero padding 2.8 MB below `__iram`, which `hazlint` 1.3 excludes and `opcount` does not (both are right; they answer different questions) — against a 1.26 % base rate for random data and `jal`/`j` controls at 99.9 % / 99.6 %. One target disassembles under the vendor's own objdump as a complete function whose literal pool holds `0xB8010028`, a KSEG1 register address; 48 random bytes through the same disassembler do not cohere | **A 4-byte linear scan is not a superset of the instructions there**, and two instruments in this repository said it was. `opcount.py` gained `--mips16` as an explicit precondition; `hazlint` 1.3 refuses such a range outright rather than reporting a conservative-looking number for it. The twelve vendor userland ELF binaries were re-checked by two independent tests; `stage2.bin`, a raw image with no ELF header, by the `jalx` test alone with its control fired (99.4 % of 499 `jal` in range). All clean, so no count already here moves — **the claim was wrong even though its conclusions survived** · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 🔴 讀 | **This unit's own kernel executes the `cache` instruction — 37 of them, D side only.** (🔄 this row read *the MIPS-II `cache` instruction* until 2026-08-28; the vendor's own assembler rejects it for `mips1` **and** `mips2` and accepts it from `rlx4181` on, so it is MIPS-III/32 carried as an extension — and `Config.M = 0` still says this is not a MIPS32 core.) One span, `0x8000CA40`–`0x8000CD4C`; op fields only `{0x11 DInval, 0x15 DWBInval, 0x19 DWB}`; base registers only `{v0, a0}`; offsets only the eight at stride `0x10`. The other 15 hits in the image are data and differ from those on **all three** properties. The instrument's control: the same scanner over `stage2.bin` reproduces the five known CCTL sites *and* the one known data false positive | **A refutation condition this project wrote for itself is met** — *"refuted by finding a `cache` instruction anywhere in vendor code that executes"* — and the scan that returned zero had only ever covered the 56 KB loader. It does **not** make this a MIPS32 core (`Config.M = 0` is 量); it means **there may be a working D-cache invalidate on this part**, which is what the one unanswered gate decision needs · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔄 量 | **"The D-cache is write-through" does not follow from what was measured.** Both cells that established it stored to a word that had been *executed and never loaded* — a D-cache **miss** — and under a miss, write-through and write-back-without-write-allocate give the identical reading. The owner file carried the disjunction from day one; four downstream files dropped the parenthesis | **A descriptor ring is a write *hit*** — load the status word, store the ownership bit — so the CPU→memory direction is **not** covered for the pattern the Ethernet driver will actually use. Both GPL drops' board configs for this SoC say `CONFIG_ARCH_CACHE_WBC=y`. **A load-bearing alternative does not belong in a parenthesis** · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔴 量 | **`Status.BEV` is 0, and the core fetches from `0x80000080`.** The second is the part that needed a payload: `trap_init` copying 128 bytes there proves the copy, not the dispatch. What proves the dispatch is that `break` trapped into a handler this project installed and **came back** — `break.count=1`, `cause=00000024` (ExcCode 9 = `Bp`), `break.epc=80500270`, the exact address of the `break` in the emitted image | **`R1a`'s exception handler can live at `0x80000080`** — one of the four decisions `R1-gate` exists to unblock, and it is now a reading rather than an argument · [`SPEC.md` `CPU-27`](../SPEC.md) |
| 🔴 量 | **`mfc0` always writes its destination register on this core** — `nowrite = 0` across all 256 census rows, where the census reads every register twice with two different primes so that a non-writing `mfc0` returns its own prime and gets its own state | **This is the row the other rows stand on.** Without it, every `reads zero` in the census is indistinguishable from `the destination was never touched` — including `Count`'s, which decides whether a timer driver is a prerequisite · [`SPEC.md` `CPU-38`](../SPEC.md) |
| 🔴 量 | **`Count` (CP0 rd 9) is not implemented** — reads zero, and a 100,000-iteration loop moves it by nothing. Cross-checked three ways: the census row, the raw before/after pair (both primed with *different* values, so an instrument failure cannot wear the result's clothes), and a trap counter bracketing the call | **`R5-0`'s SoC timer driver is a PREREQUISITE, not a bonus**, and `R1c` loses its first timing route. A decision, not a gap · [`SPEC.md` `CPU-42`](../SPEC.md) |
| 量 | **CP0 register 20 reads zero for real, and its write side has an effect** — so it is a **write-only command register**. `probe1` read the same zero and could not tell that from *the destination was never written*; the double-prime census can | The flush recipe `R5b`'s MTD driver writes is a store to a register that will never read back what you put in it. That is the kind of thing a driver author discovers at 2 a.m. otherwise · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔄 量 | **The cache geometry is not in CP0 on this part.** `Config` reads zero — with `nowrite = 0` proving the destination *was* written — so `Config.M = 0` and there is no `Config1`. And the R3000 sizing walk cannot answer either: its algorithm needs cache isolation, which this core does not implement, so it can only return its own *no answer* value | **`CPU-25` is now *both routes were measured shut*, not *not tried*.** What is left is an eviction walk that needs no isolation — which is a payload, not a register read · [`SPEC.md` `CPU-25`](../SPEC.md) |
| 🔴 量 | **The loader re-stages the vendor kernel at `0x80500000` on a watchdog reset, not only on a cold power-on** — measured by a second `J 80500000` printing `decompressing kernel:` after a payload's own reset | **A payload cannot be re-run on one power cycle without re-uploading it.** Any runsheet cell that says *run it again for a repeatability control* is wrong as written · [`docs/loader-command-semantics.md`](loader-command-semantics.md) |
| 讀 | **The general exception vector on this core is `0x80000080`, not `0x80000180`.** R3000 layout: `0x80000000` is the UTLB refill vector and the loader installs nothing there. Three sources — this unit's `trap_init`, the vendor bootcode's own comment, and `arch/rlx/kernel/traps.c` — and none says `0x180`. Corroborated by the loader returning from exceptions with `rfe`, while the `eret` encoding occurs zero times in the same file *(and that zero has a positive control: the same grep finds both `rfe`s)* | **The address `0x80000180` had reached four files.** A handler written there lands in RAM nothing reads, the payload looks installed, and the fault still hangs · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **A fault the loader does not handle hangs the board forever.** `do_reserved` at `0x80400BE8` prints twice and executes `j 0x80400C18` — a branch to itself. The exception entry has already cleared `IEc`, and the watchdog is not armed. The vendor source says so in as many words: `prom_printf("Undefined Exception happen."); for(;;); /*Just hang here.*/` | **One fault = one power cycle, and there is no spare unit.** Every payload is ordered by increasing risk and writes each result before taking the next · `PROGRESS.md` `R1-gate` |
| 讀 | **`do_reserved` dereferences the faulting code's own `$a0`.** It is the raw vector target with no `SAVE_ALL`, and its third instruction is `lw a3,148(v0)` with `v0` = the faulting `$a0`. `rlx_cctl(0x002)` would have handed it the integer 2, so `0x96` — kuseg, TLB-mapped, and a miss goes to `0x80000000`, which the loader never populated | **Undetermined, and the sharper version was withdrawn the same day.** The escalation *"it could branch into the loader's flash-write path"* was refuted: `0x5A5AA5A5` decodes as `BLEZL` with target `0x7FFE9B58`, not a jump. `SAFE_A0` stays because it is two instructions and removes an undetermined case · [`tools/rlxprobe/cache.S`](../tools/rlxprobe/cache.S) |
| 讀 | 🔴 **A TFTP upload named `boot.img` sets the loader's write pointer to `0x80000000`** — the name compare at `0x8040A6A8` reaches `0x80401250`, which stores `0x80000000` into both `LOADADDR` and the running write pointer at `0x8040DD10`, and `0x80401A10`'s memcpy walks up from there. It writes over the UTLB refill vector **and** over the general exception vector, destroying the loader's own exception handling mid-transfer | **`boot.img` and `nfjrom` go on the do-not-type list.** Found by the adversarial pass on the exception-path read, which had claimed nothing ever writes low RAM — a computed base its own scans admitted they could not see · `RUNSHEET.md` |
| 讀 | **This loader was not built from either GPL drop this project holds.** The vendor's `do_reserved` is `prom_printf("Undefined Exception happen."); for(;;);` — one call, no CP0 reads. The shipped `0x80400BE8` has two `prom_printf`s, `mfc0 c0_cause`, `mfc0 c0_epc` and a `pt_regs` load; and `do_watch`, whose string is in the binary, appears in neither tree | Every *"the vendor source says why"* argument has to state which half it is leaning on. The string match is evidence of provenance; it is not evidence that the C on screen is the C that was compiled · `docs/loader-command-semantics.md` |
| 讀 | **This core uses the R3000 cache model** — `Status.IsC`/`SwC` — **plus a Lexra CP0 register 20** carrying the invalidate and writeback commands. Two sources agree on `0x002` and `0x200`; two further commands the bootcode issues at reset have one source and no name | Decides where the handler goes, and what `R5b` and `R6` must do. The two unnamed commands are **not written** by any payload · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **The load delay slot is architecturally exposed.** Upstream's `P9-12` filled it — with the wrong instruction — and the device emitted exactly 16 bytes per iteration, one 16550 FIFO, 272 times over ten minutes | `-march=mips32` is banned; `hazlint` is a **build gate**, not a lint, and no payload exists unless it exited 0 · [`tools/hazlint`](../tools/hazlint) |
| 讀 | **`-march=mips1` does not compile on gcc 12.4.0 without `-msoft-float`** — *"`-march=mips1` requires `-mfp32`"* | The plan's build line was unbuildable as written. `-msoft-float` rather than `-mfp32`, because this core has no FPU · `SPEC.md` `TC-06` |
| 🔴 量 | **CP3 is reachable on this die: all eight `mfc3` stubs executed and none trapped.** `m.traps=00000000`, `m.cause` still `deadc0de` (never written). No reading equals its own prime and `v1 == v2` for all eight, so the destination was written and the value is stable. `CU3` sticks (`1000fc00` -> `9000fc00`). 量 2026-08-29, `bench/2026-08-30/QJ.log` | **The emulator says the opposite and says it confidently** -- on qemu all eight trap with `m.cause=1000042C`. A payload may issue `mfc3` on this part. ⚠️ Registers 0 and 4 read `20000000` and both tops read zero, so this is a base and **not a window**: `w-imem` stays 未定 |
| 🔴 量 | **This core retires the `cache` instruction** -- ops `0x11`, `0x10`, `0x15`, `0x19`, all `n=00000000`, **while `x ri` traps in the same run under the same handler** (`cause=00000028`, ExcCode 10). 量 2026-08-29 | `CPU-44` closes positive, and the 37 D-side `cache` ops in this unit's shipped kernel are instructions its silicon will execute. ⚠️ **Retires is not invalidates**: `x.c10.treated` and `x.c10.twin` are both 1, so the untreated victim moved too |

## It changed where a payload of mine may write

| | | |
|---|---|---|
| 量 | **`0x81000000` is not scratch.** A live 32-byte-periodic descriptor table sits at `0x81000400` with `next = prev = &next`, and uninitialised DRAM cannot produce its own address | The runsheet's §C had argued from *"far above the loader's image"*, which excludes two known things and nothing allocated at run time. The upload address moved to `0x80A00000`, chosen by a probe rather than by an argument · `RUNSHEET.md` §C |
| 量 | **`0x81000000` word 1 is rewritten to `0x00000144` on every boot** — three times reproduced, and word 2 survives, so it is not decay | A canary belongs at `0x80A00000`, never at `0x81000000` · `SPEC.md` `MEM-14` |
| 量 | **This DRAM keeps its *contents* across a short power-off, not just its bias.** `0x80A00000` read back sixteen words written by hand in the previous power cycle, byte for byte. After 16 hours: zero retention | **Any "it survived a power cycle" claim must state how long the power was off.** Every result block is poisoned before it is written · `SPEC.md` `MEM-15` |
| 量 | **`EW` rounds an unaligned address up; `EB` does not.** One loader, two write primitives, opposite handling | Every `EW` written from here on has to assume it · `SPEC.md` `LDR-08` |
| 讀 | **`AUTOBURN`'s initialiser in the loader image is `1`.** So the loader boots with auto-burn on, and a TFTP upload that completes without `AUTOBURN 0` having been typed is written to flash | Found while precomputing bench expectations, before the first upload. `R0`'s whole zero-write claim rests on it, and it is measured at the burn path's own instruction *during* every transfer · `SPEC.md` `LDR-23` |
| 🔴 量 | **The device node this project was about to declare cannot exist, and the obvious substitute is the dangerous one.** `R3-9` and two notes asked for `nod /dev/mtd0` and `wc -c < /dev/mtd0`, a second path to flash through my own driver. 量, two routes with a positive control on each: both built `.config`s carry `# CONFIG_MTD_CHAR is not set` (control: nine other `^CONFIG_MTD` lines in the same file), and both `System.map`s hold **zero** mtdchar symbols against **six** mtdblock/mtdcore ones. Major 90 has no chrdev, so the node opens `ENODEV` | **`/dev/mtdblock0` is the substitution to refuse.** `mtd0` is `0x000000`–`0x130000`, which contains **both** regions this project forbids writing — the loader, and the per-unit region a factory reset does not restore — `mtdblock` has a write path, and **mode `0400` is not a control**: root ignores DAC, so on an interactive shell one typo is an erase-block write at offset 0 on a device with no spare. `/dev/mtdblock1` runs the identical read path and buys the identical reading with **zero** writable nodes over either region. **The control is the absence of a node, not the mode bits** · [`notes/kernel-build.md`](../notes/kernel-build.md) |
| 🔴 量 | **A bench card's pre-flight expected `13 passed` from a tool that had been rewritten to print `19` in the same session that wrote the card.** `flashwin` went from 13 controls to 19 after an adversarial pass found 24 of 45 mutants alive, three of which printed this unit's MAC. Nothing re-read the card afterwards | **The first line of a pre-flight is the one place a mismatch reads as *wrong version, stop*** — and it is a line an operator runs before power, with the board in their hand. Both numbers are now measured on the card: 19 with this unit's flash dump present, 16 plus one skip covering 3 without it. The same stale pair (*"10 ok … closes against 13"*) was still in the CI table's prose while the gate column beside it already said 19 · [`bench/2026-08-30c/PREDICTIONS-B5-block2.md`](../bench/2026-08-30c/PREDICTIONS-B5-block2.md) |

## It changed what the bench procedure must do

| | | |
|---|---|---|
| 🔴 量 | **The loader's `DW` sends its reply at 3,594–3,726 B/s, not at the 38400 line rate of 3,840.** Measured from first read to last read of one contiguous reply in the committed `.timing` files: `H2g` 9,660 B / 2.688 s, `H1c` 1,667 B / 0.447 s. n=2, and the **longer** reply is the slower one. 🔄 **2026-08-30: the explanation that stood here was backwards and it is corrected rather than dropped.** *(It read: “so it is the first line's fixed cost amortised rather than a per-byte overhead.”)* A fixed startup cost `T = F + N/r` gives a rate that **rises** toward `r` as `N` grows; it cannot produce one that falls with size. `Q-3` added a third point at 23,523 B / 6.697 s = **3,512 B/s**, and fitting `T = a + b·N` over the three gives a **negative** intercept — the signature of the clock starting at the first *read* rather than at the first byte, because `console-capture.py` drains on a 50 ms grid and a short reply is already partly buffered when timing begins. 🔴 **The figure to size a window against is the marginal rate, 3,458–3,497 B/s**, not 3,594. `B7c` was excluded and the exclusion is written down: its 1,272 bytes are an ESC window's echoes around a 71-byte reply, so a rate computed over them is a rate of nothing | **Every `--seconds` on every runsheet card had been sized against a number nobody had measured.** `reply-size.py` says how many bytes come back and says nothing about how long they take, and `console-capture.py` has no early stop without `--idle` — so a window set too small truncates, and **a truncated reply and a refused command are not the same shape**: a refusal is `Unknown command !` at once with a prompt, a truncation stops mid-line with none. First used to size `bench/2026-08-30/`'s card at 2.29× and 7.10× · [`docs/loader-command-semantics.md`](loader-command-semantics.md) §f, [`SPEC.md` `LDR-40`](../SPEC.md) |
| 🔴 量 | **`§B5-c1`'s *the head does not discriminate* is a statement about `nfjrom` files, and a flat payload's head does.** Every `nfjrom` in this family shares `rtkload`'s `start.o`, which is why sixteen identical bytes cannot say whether an upload landed. An `rlxprobe` payload is linked by `rlxprobe.lds` and its first word is `_start`'s first instruction: 量 `probe1` `3C1D8051`, `probe2` `3C1D8050`, `probe3` `3C1D8051` against the staged image's `00000000`. **The same mechanism runs underneath both**: the word at `0x8050000C` carries `_bss_start` = `0x80500000 + size`, exactly as `0x8050001C` carries `__vmlinux_end` — one linker constant, two linkers | **Quoting the refutation outside its corpus would have deleted `probe3`'s only upload check.** `probe3` is 29,088 bytes and ends 958,050 bytes *below* the staged vendor image, so block 1's before/after tail pair — the trick that works because `loudm` sticks out 66,542 bytes *above* it — has nothing to stand on here. The head is the only discriminator for power cycle 1 and the tail is the only one for power cycle 2; **neither method transfers** · [`RUNSHEET.md`](../RUNSHEET.md) §B5-c1, `bench/2026-08-30/PREDICTIONS-B5-block0.md` §9 |
| 🔴 量 | **The one cell that checked whether the upload landed could not fail.** `RUNSHEET` `K2` read `DW 80500000 1` and rested on its own sentence *"my image and the staged one differ in their first 16 bytes"*. That measurement was finally taken and it says the opposite: the first **24** bytes are byte-identical — three device captures across two power cycles (`bench/2026-08-23/B.log:16`, `2026-08-24c/G1a.log`, `2026-08-24d/G5-rb1.log`) against all five `nfjrom` files this project can build, because every one of them is linked from the same `rtkload` `start.o` | **The image identifies itself eight words in, not four.** The `lui`/`addiu` pair at `0x80500018` carries `__vmlinux_end`, so one `DW 80500000 8` reads back the uploaded image's own length: `26101400` is `loudm`, `2610AC00` is the `quiet` pair, and `26101000` is **either** `loud` **or** the staged vendor image — only the `lui` separates those two, which is why both words are read. And the tail, 66,542 bytes above the staged image's end, is the one part of the upload that can be watched **changing** without poisoning the fallback · [`notes/kernel-build.md` §12](../notes/kernel-build.md) |
| 🔴 量 | **`uname` is not an applet in this unit's `busybox`, and the seating asked for it three times.** 量, the device's own `busybox` under `qemu-mips-static`: `busybox uname -a` → **`uname: applet not found`**. The binary lists **50** applets and `uname` is the only one of the fourteen this seating needs that is missing; the controls are in the same run — a non-applet gives `sh: … not found`, and `cat` reaches the filesystem | **It is not a missing symlink, so the fix that had already been written for it would not have worked either.** `notes/kernel-build.md` §9 records a `/bin/uname` symlink added for `K5` and then removed — three passes and none asked whether the applet exists. `K5` becomes `cat /proc/version`, which prints `linux_banner` verbatim and therefore carries `(key@K)` and the gcc version that `uname -a` drops — two thirds of the discriminator · [`notes/kernel-build.md` §12.7](../notes/kernel-build.md) |
| 🔴 讀 | **`SPEC.md` held two rows for one loader mechanism and they disagreed, and the wrong one is the one three files quoted.** `LDR-26` said `nfjrom` and `boot.img` both force the load address to `0x80000000`; `LDR-37`, eleven rows below it, and this file's own do-not-type row both say `boot.img` writes the address. 讀 this unit's `stage2.bin` at `0x80401208`: a `nfjrom` match sets `0x8040D390 = 1` and **branches past the `boot.img` test entirely** | **The correction runs toward the danger.** With `nfjrom` as the TFTP filename the loader jumps to the load address that is already set — `0x80500000`, the right one — so the accident **looks like a successful boot** and silently costs the `J` line, the `AUTOBURN` timing and `K2`. And the guard is on `loader-tftp.py`'s `--filename`, not on the local path, so *rename the file* is hygiene and *pass `--filename`* is the control · [`notes/kernel-build.md` §12.6](../notes/kernel-build.md) |
| 🔴 讀 | **The rung-2 and rung-3 cells gave the board the workstation's own address, and then pinged the loader's.** `K6` said `ifconfig eth0 10.1.1.2 up` and `K7` said `ping -c 4 10.1.1.1`, while this same runsheet's §G3 records *"`IPCONFIG 10.1.1.1`, workstation at `10.1.1.2/24`"* | **Both halves fail, and the capture reads exactly like `K7`'s own definition of a driver that transmits and does not receive.** Board `10.1.1.10` → host `10.1.1.2`, `ip neigh flush` first (量: a stale loader entry broke a ping outright once), and `arp` added to the `tcpdump` filter — one word that separates *ARP never resolved* from *the driver does not transmit* · `RUNSHEET.md` §B5-c4 |
| 🔴 讀 | **What gets uploaded is `nfjrom`, about 1.03 MB — and the runsheet had been quoting the `vmlinux` ELF, four times larger.** Three sizes are three different things: the ELF the build produces (3,968,113–4,042,388), the decompressed image the desk channel ingests (3,472,384 / 3,546,112), and the `nfjrom` the loader is handed (**1,027,072 / 1,053,696**). ⚠️ And that artefact is *literally named* `nfjrom`, one of the two filenames the loader tests by name. 🔄 **2026-08-29: `LDR-26` said both force `0x80000000` and only `boot.img` does** — `nfjrom` arms the auto-execute at whatever address is already set, which is why that accident *looks like* a boot; the string `Jump to 0x` separates it, and a correct `J` never prints it | **The TFTP transfer size and `K2`'s `image_end − 16` readback were both wrong**, and the file has to be renamed before it is uploaded — a warning that used to be about a filename nobody would type by accident and is now about the one the build produces. 🔄 **The rename is hygiene; the control is `--filename`**, which is what the loader sees — `--image` is a local path it never receives · [`notes/kernel-build.md` §13.3](../notes/kernel-build.md) |
| 🔴 讀 | **This board prints NOTHING between kernel entry and userspace, and `CONFIG_EARLY_PRINTK=y` is a trap.** 量 on the built `vmlinux`: `printk` is three 20-byte `move v0,zero / jr ra` stubs, and **`early_printk` is a WEAK EMPTY 16-byte body** at `0x80013bec` that nothing under `arch/rlx` overrides. `panic_printk` is real but reaches the wire only after a console registers. Second source, on this device: `bench/2026-08-24c/G6.log` goes from `start address:` straight to the WLAN driver. **So `R3`'s D3 had no observable at all**, and the first seating would have spent a power cycle to learn nothing. The eleven boot marks go through `prom_putchar`, which needs no console and cannot hang. | `notes/kernel-build.md` §11.1, `TC-29` |
| 🔴 讀 | **The most likely way this board's first boot dies is silent by construction.** `bsp_setup()` ends `ret = bsp_swcore_init(version); if (ret) bsp_machine_halt();`, and `bsp_machine_halt()` is a bare `while(1)` with no message anywhere. `B07` prints that return value — the one failure nobody could otherwise have told apart from any other hang. | `notes/kernel-build.md` §11.2, `TC-30` |
| 量 | **The console line buffer is 128 bytes, and `readline` writes its NUL only on the CR path.** A line of exactly 128 characters is unterminated and the tokeniser scans past it into the saved registers | `C7` was about to send 173 characters, which `readline` cuts at 128 with `EW` as the command. **No command line may ever be 128** · `SPEC.md` `LDR-06b` |
| 量 | **`DW <addr> N` prints `4 × ceil(N/4)` words and rounds *up*** | A readback shorter than the structure comes back looking complete — which would have faked exactly the failure the cell was hunting · `PROGRESS.md` Corrections |
| 量 | **A stale neighbour entry costs the first transfer three retransmits**, and after a kernel run a stale *loader* entry broke ping outright. The loader synthesises its MAC from the IP it was given; the vendor kernel uses the real one | The host must flush the neighbour entry at **every** loader↔kernel transition. It is an `R4` requirement for unattended `bench-ci` · `RUNSHEET.md` Results |
| 量 | **An ESC window of 45 s was too short**: the boot began 19 s after the stream stopped. The cost is wildly asymmetric — an extra ESC second is free, a missed window costs a power cycle | Standing change: `--esc 180 --seconds 200` · `PROGRESS.md` Corrections |
| 量 | **The reset button is a GPIO on `PABCD` bit 5, active low — it is not `RESET#`.** Bit 5 goes 1→0 under the held button with `CNR` and `DIR` unchanged and no `Booting...` | Retired a planned cell before it was spent, and refuted *"this unit's loader polls only the UART"* · `SPEC.md` `BRD-05` |
| ✅ 🔴 量 | **`console-capture.py capture` with neither `--seconds` nor `--idle` never returns** — **and as of 2026-08-30 it refuses instead, and records both terminators in its metadata.** Both default to `0.0` and the read loop breaks on neither; `timeout -s TERM 8` gives `rc=124`. A kill loses `.meta.json`; the `.log` and `.timing` survive, flushed per chunk. 量 2026-08-29 at the desk, before power | **14 of `RUNSHEET` §B5's 15 capture rows omit it, and block 0 omits it on `Q-A`** -- while all ten committed `A-catch` captures passed one. ⚠️ That census is an inference from `stop_reason`, because the metadata records neither `seconds` nor `idle` -- which is the larger defect. A card nobody has run literally is a card that has not been checked. 🔄 **The severity was inverted and is corrected**: 量 on a pty, `SIGINT` writes a complete `.meta.json` and only `SIGTERM` loses it, so a bench operator pressing Ctrl-C loses nothing and `--force` is never the correct recovery — the overwrite refusal is the safety feature, not the obstacle. ✅ **Closed 2026-08-30**: the guard sits after `_check_send` and before the port is opened (量: of this suite's four terminator-less invocations only `P4` reaches it), `seconds` and `idle` are in the metadata, `test-console-capture.sh` is 29 → **40**, and §B5's card carries a terminator on all fifteen capture rows · `tools/console-capture.py`, `RUNSHEET` §B5-c14 |
| 🔴 量 | **The `G8a`/`G8b` flash bracket has never sampled `H601`.** It reads 256 of the loader region's 24,576 bytes and 256 bytes of the `cr6c` header — a region no rule forbids writing — and **0 of `H601`'s 8,192**. `H601` is the one region `CLAUDE.md` calls *not restored by reset*, and `bench/2026-08-30b/L3.log` is why it stopped being theoretical: this project's own kernel instantiated an MTD partition `0x000000`–`0x130000`, which contains it | **Six days of write-ups said “the two regions that would change” and neither of them was the one that cannot change back.** The bracket gains a third region, its expectation is computed from the 2026-08-16 dump rather than from a capture that can never exist, and the reach is stated every time it is quoted: **768 bytes of 4,194,304 = 0.018 %** · [`bench/2026-08-30c/PREDICTIONS-B5-block2.md`](../bench/2026-08-30c/PREDICTIONS-B5-block2.md) §8, [`tools/flashwin.py`](../tools/flashwin.py) |
| 🔴 量 | **This unit's own flash dump renders two captures taken off the device eight days later, byte for byte.** The 2026-08-16 dump (`FLS-14`, 4,194,304 B, 6,300.1 s) at `0x000000` and `0x060000`, rendered in the loader's own `DW` format, reproduces `bench/2026-08-24d/G8a-rd0.log` and `G8a-rd6.log` exactly — 777 bytes each, through a **different read path** (`console-dump.py`'s chunked `FLR`+`DB` against a hand-typed `FLR`+`DW`). And the two committed dumps are byte-identical over all 4,194,304 bytes | **It is what lets a region with no committable capture still have a falsifiable expectation.** The control is public even when the reading is not: a reader who cannot see the `H601` rendering can see that the renderer which produced it reproduces two readings they can · [`SPEC.md` `FLS-14`](../SPEC.md), [`tools/flashwin.py`](../tools/flashwin.py) |
| 🔴 量 | **This unit's busybox has no applet that can digest a stream**, so the flash cannot be cross-read from its own userspace. Fifty applets, and `dd`, `md5sum`, `od`, `hexdump`, `cmp`, `cksum`, `sum` and `sha1sum` are none of them; `config/rlxfw-initramfs.tsv` declares three device nodes and no `/dev/mtd*` | **The full re-dump `G8b` says “zero flash bytes” needs costs 105 minutes on the loader's wire and would cost seconds as `dd if=/dev/mtd0 \| md5sum` from a shell.** What blocks it is the applet table, not the device node — and putting a foreign binary in the image breaks Decision B's *this unit's own binaries, unmodified*. `wc` **is** on the table, so a node alone still buys a readability reading · [`SPEC.md` `FW-26`](../SPEC.md), [`notes/kernel-build.md`](../notes/kernel-build.md) §17.7 |

## It changed what an instrument may claim

🆕 **2026-08-28 — a census that only ran `--version` was not a read-only
operation, and nothing warned.** `rsdk-linux-config` is a statically linked i386
ELF that runs `make` in the tree it lives in. It deleted 2,580 tracked files
from a pinned vendor clone — **not** the symlink farm the first write-up named;
that directory holds 93 tracked symlinks, so the characterisation was generalised
from the first forty lines of the `git status` — and it also wrote a file into this repository's root
— a place no vendor-tree check watches. What made it recoverable was that the
tree is a clone at a known sha and that every mtime fell inside one 16-second
window. `tools/vendor-tripwire.sh` exists because of it, and it detects; it
cannot prevent.

🆕 **2026-08-28 — two instruments of this project got their first ground truth,
and they passed.** `opcount --mips16` and `hazlint`'s MIPS16 refusal have only
ever been run on a stripped image, where the symbol table that would confirm them
does not exist. A `vmlinux` built here from the vendor's own source has one: the
counter says *MIPS16 reached, 25 distinct targets*, and `readelf -s` says *39
symbols marked `[MIPS16]`*. **That is consistent and it is not agreement** — 25
of 39, and the missing 14 are the blind spot both tools already document, a
MIPS16 routine entered through `jr`/`jalr` on an odd address rather than through
`jalx`. So the ground truth confirms the detector and puts a number on its
undercount at the same time. Before this, both instruments rested on one
adjudicated disassembly and a self-consistency argument.

🆕 **2026-08-28 — a claim was refuted by doing the thing it was a claim about.**
*"None of the five shipped configs enables MIPS16, and this binary has it"* was
carried as evidence that the drops in hand did not build this image. Building one
of those five configs produces MIPS16. The discriminator is gone; a stronger one
(each drop's own `.config` names the rsdk it selects) replaced it.


| | | |
|---|---|---|
| 🔴 量 | **`hazlint`'s MIPS16 excision is printed and does not happen on a relocatable object — and the error is the safe direction.** On an `ET_REL` `.o` the holes come from `st_value`, which is section-relative, while the spans carry a synthetic base, so they never intersect. 量 across four trees: **26 objects (27 marked) print `EXCISED BY NAME` and not one scanned fewer bytes than its `SHF_EXECINSTR` sections hold** | Those bytes are **scanned as 32-bit code rather than skipped**, so the defect can manufacture a violation and cannot hide one. `RUNSHEET` `P2`'s 0 therefore stands without waiting on the fix, which is `TC-m` and is still carried · [`notes/kernel-build.md` §14.4](../notes/kernel-build.md) |
| 🔄 讀 | **The desk channel's four instruction counts were listed program counters, not instructions.** `-d in_asm` logs at translation time and qemu re-translates a block entered at a different offset. Re-derived from the same logs: **828 / 843 / 908 / 938 distinct** against the 880 / 880 / 968 / 1,003 printed — and the control's 880 appears in **none** of the six logs that run left behind | The stop addresses reproduce exactly, so the conclusion they carried is unaffected; what is not recoverable is *why* the counts differ, **because the qemu invocation was never recorded**. Every run now prints its own command line · [`notes/kernel-build.md` §15.1](../notes/kernel-build.md) |
| ⚠️ 量 | **Two of this repository's own qemu captures disagree about malta's ISA COM1.** A payload writing `0xB80003F8` under `-kernel` is heard (5,893 bytes, committed 2026-08-26); a `-bios`-only stub writing the same address is not, and a poll of `0xB80003FD` reads 0 forever. One variable differs — the entry mechanism. That the GT64120's decoders are what is missing is **推** | The ISA addresses are in `qemu/README.md`'s own table, and taking them into a `-bios` run gets silence a reader would score as *the code never got there*. Carried as `TC-n` rather than smoothed over · [`qemu/README.md`](../qemu/README.md) |
| 🔴 讀 | **A control that asserts a property of the command line has to go THROUGH the command line.** `hazlint` 1.4's `K11`–`K16` called `_scan_elf`, a private copy of `main()`'s pipeline, while their names claimed properties of the CLI: three mutations recreating `TC-f` verbatim passed all twenty controls. They now spawn the real program — **not** an in-process `main()`, which recurses and whose anti-recursion guard would be the same defect one level down. Moving them found **three states the copy accepted that the real program refuses**. `_scan_elf` is deleted rather than left unused. | `PROGRESS.md` carried-forward, `tools/hazlint` |
| 🔴 讀 | **`grep -r` over `arch/rlx` has never seen the BSP** — 13 files, 91,549 bytes — and fifteen zero-claims re-run under `-R` are **all unrefuted**, with two positive controls firing. **But the prescribed fix is worse than the defect where it does not apply**: at a drop root `-R` reports 79,857 paths for 66,977 real files (+19.2 %) and follows three `romfs/tmp -> /var/tmp` links out of the tree, where four of this project's own analysis logs live and would be reported as vendor content. A canary was planted to measure it. | `notes/kernel-build.md` §10, `TC-27` |
| 🔴 讀 | **`arch/rlx/bsp` is a dangling symlink in two of the three GPL drops**, because `target` is a tracked symlink only in `rtl819x-toolchain`. A BSP question re-run across the three returns 13/0/0 and reads as *only one drop has it* — which is false. The BSP is in all three at `boards/rtl8196e/bsp/`, 12 files, byte-identical but for one `#if` on the reboot path. **Every BSP reading must go through that path.** | `notes/kernel-build.md` §10.4, `TC-28` |
| 讀 | **`arch/rlx` defines no `sched_clock`**, so `printk` timestamps come from the weak jiffies generic at `kernel/sched_clock.c:39` and not from CP0 `Count`, which this die does not implement (`F50b`) and which would have printed `0.000000` on every line. Real at 10 ms — and the `0.000000` → moving transition marks where the timer interrupt started. | `notes/kernel-build.md` §11.6, `TC-31` |
| 讀 | **A row of `SPEC.md` had been outside two of its own checker's checks since the day it was written, and the checker reported it as *skipped*.** One unescaped `\|` inside a backticked span gave the row eight cells in a seven-column table. Every check reads cells **by index**, so the shift did not fail them — they read a different cell and passed on it, and C4/C5 read the *source* column as the owner, found no path, and filed the row under *owner is a gate rather than a file* | **The one row that session existed to upgrade was the one row two checks could not see.** `C8` added — a row's cell count must equal its header's — with a ninth mutation that is not invented: it re-creates that exact escape. Pointing the same scan at the rest of the repository found **two more**, both mis-rendering their tables since they were written · [`tools/spec-check.py`](../tools/spec-check.py) |
| 量 | **A 0.26 % deviation reported as a property of the board was a property of the stopwatch.** The interval had been hand-timed over 61.842 s; a human reading a clock is good to about ±0.15 s, and ±0.15 s in 61.8 s *is* ±0.25 %. Re-measured over a 2,080-second baseline: **200.0049 MHz ± 7 ppm** | Two published numbers superseded, and the compiled-in `0x0BEBC200` turns out to be right to 24 ppm · `SPEC.md` `CLK-02` |
| 量 | **The ESC heartbeat's achieved period is not the requested one** — 20.35 ms for a requested 20.00, and 2.32 ms for a requested 2.00. The difference is a fixed ~0.3 ms of host overhead, and the floor of the loop is 29 µs | The grid every interval is quantised to is now **measured per capture and recorded in its metadata**, with a case that proves the field is not repeating the argument back · `tools/console-capture.py` |
| 讀 | **`hazlint`'s population control built its words with a private helper**, so it exercised a code path no scanned file ever took. It could not have caught any defect in span construction and it looked exactly like it could | Found by an adversarial review that confirmed 17 of 24 claimed properties. The suite went 42→56 with a mutation per fix · `SPEC.md` `TC-07` |
| 讀 | **`readelf --dyn-syms` reports nothing on this unit's binaries** — they have no section headers — which is indistinguishable from clean | An acceptance condition whose check could not have failed. Replaced by a `PT_DYNAMIC`/`DT_SYMTAB` walk and `nm --undefined-only` before strip, which are different **in kind** · `notes/rootfs-census.md` |
| 量 | **`NET-13` was withdrawn twice in one day — the finding and my correction of it.** The recorded jack order rested on labels assigned after the fact; the linear map that replaced it was checked by a cell whose expected value had been derived from the map it was supposed to test | The jack map is **undetermined**, and the deciding experiment writes the jack into the capture's filename · `SPEC.md` `NET-13` |
| 量 | **A published interval had no anchor, and four defensible anchors differ by up to 1.7 ms.** `CLK-15` recorded 344.7–356.9 ms for the silence after `Booting...` without saying which byte it started from. Only *the NUL after `Booting...\r\n`* reproduces that range on its own nine captures; the other three do not | The anchor is part of the definition now, and the check requires the other three to **disagree** — otherwise "this is the one it used" would be a guess. **Two adjacent silences of the same length is how a measurement ends up wearing another one's name**, and this repository had already recorded that happening once · [`tools/boot-timeline.py`](../tools/boot-timeline.py) |
| 量 | **`CLK-15` said cold and warm resets give the same silence. They do not, and the difference survives inside one power cycle.** Split by the loader's own warm-boot line rather than by the artifact byte — which would have made the conclusion true by construction — cold is 348.0–356.9 ms (n=7) and warm 338.2–347.6 ms (n=7), non-overlapping. Within `2026-08-24c`: +4.5 ms. Within `2026-08-25`: +14.5 ms | Part of that row's recorded 3.5 % *unexplained* spread was two populations read as one. **The mechanism is undetermined and the experiment that decides it is two `DW`s** — the SPI controller's registers after a cold boot against after a warm reset · `SPEC.md` `CLK-15` |
| 讀 | **A negative control I had just written was counting coincidences.** `probe2`'s `restore.stillhandler` read 20 on a run whose restore was perfect: ten of the handler's 22 words are `nop` and the emulator's vector page starts as zeros, so every one of them matched by accident | It counts only positions the install demonstrably changed now. **Found by the first harness run of the fixed payload**, which is the argument for running the harness before the bench rather than after · [`tools/rlxprobe/probe2.c`](../tools/rlxprobe/probe2.c) |
| 讀 | **A suite that cannot reach a state cannot test the code that produces it.** `probe2`'s census tells *the register answered* from *nothing wrote the destination* by priming `$v0` before the call — and the emulator's `mfc0` always writes `rt`, so that state never occurs under it | One of the four mutations builds a payload with a single census stub emitting `nop` and requires that row to report *not written*. Without it the whole mechanism would have shipped tested by nothing · [`tools/test-rlxprobe.sh`](../tools/test-rlxprobe.sh) |
| 🔴 讀 | **The kernel's `icache: 16kB/16B, dcache: 8kB/16B` line is a printed build constant, not a probe.** Every value is a `#define` in `arch/rlx/bsp/bspcpu.h:12-22`; `cache-rlx.c:378` only prints them, and the same file uses them in `#if` preprocessor conditionals at `:99`, `:438`, `:649` | **This is the trap `R1h-4`'s DoD names by name** -- a geometry number wearing a measurement's clothes. `probe3`'s walk is the only 量; they agree, and that is corroboration rather than one number. The same line carries `dcache: 8kB`, for which **no measurement exists at all** |
| ⚠️ 算 | **`G0`'s pointer-shape refutation fires on noise.** *"Any one pointer-shaped word and the address is re-picked"*, applied to 64 words of uniform random data, fires with probability 1-(63/64)^64 = **63.6 %**; even the *aligned* reading fires at **22.2 %**. 2026-08-29 was its first run: two of the 64 arena words carried a listed prefix and neither was aligned | A refutation condition that fires on two thirds of random samples is not one. It needs a rate, not a presence test. The shape on that list with real power is *any two windows byte-identical* -- a stuck read path, which nothing else there catches, because bias garbage is supposed to look like structure |
| 🔴 量 | **Forty green cases, and ten mutants of the guard they had just been written for were alive.** `console-capture.py` gained a refusal for a capture with no terminator and its suite went 29 → 40, all green. A mutation pass over **25** edits of that guard killed 15. The four classes the forty could not see: a **waiver** (`if args.esc > 0: return` in front of the guard — and `--esc 25` is `A-catch`'s own shape; 量 on a pty that mutant gives `rc=124` with the `.meta.json` lost, which is the failure the guard was added for), the **contract** (not one case asserted an exit code, so `SystemExit(0)` was green; none looked at stdout, so a refusal printed there was green), the **message** (one substring, so a refusal naming a flag that does not exist passed), and the **position** (the guard below the overwrite refusal passed) | **A suite's own green is a claim about the suite, not about the tool.** Six cases close the four classes, `N28` on the refusal's FIRST line because a substring test over a fourteen-line message is nearly unfalsifiable, and `N29` because `N21` only appeared to pin the guard's upper edge — it sends 127 characters, a length `_check_send` accepts, so it passes either way. **The 25 mutants are now a committed suite that runs the whole file per mutant and refuses if the unmutated baseline is not green** · [`tools/test-console-capture-mutants.py`](../tools/test-console-capture-mutants.py) |
| 🔴 量 | **Nine rows of this page were rendering as paragraphs of pipes, and a checker found them because it was generalised.** `spec-check`'s `C8` checked one file's table rows against their header. Pointed at all 71 tracked `.md` it found **eight** ragged rows where the census behind it had said six — that census had looked at three files. Generalising it exposed three shapes it could not see at all: a row split over more than one physical line (a literal newline inside a code span, which ends the row there and hid every row below it in the same table from every checker here); a `\|`-line belonging to **no** table, which `C8` cannot see by construction because `C8` walks tables — **nine rows of this file, including its three newest findings, stranded by one blank line**; and a code span whose whole content is whitespace — **four of those**, and the fourth is the carried-forward row that describes the defect, which only turned up when the finished checker was run over the original tree at the end of the session; the count assembled one file at a time, in the order the defects surfaced, had said three | **One of them made a *reading* wrong, not a rendering.** `docs/loader-command-semantics.md` annotated **both** exits of `readline`'s three-way branch with the same empty character, so *"only one writes a terminator"* named neither. Settled by disassembling this unit's own loader: `li v0,10; beq` is the LF exit and returns with no NUL, and the NUL at `0x804070fc` sits in the delay slot of the CR path's jump. The same blank-line defect was recorded against `SPEC.md` in August and found then by an adversarial pass; this time a checker found it · [`tools/spec-check.py`](../tools/spec-check.py) |

## It changed the shape of the plan

| | | |
|---|---|---|
| 🔴 讀 | **The threshold `R2b` was going to decide on is below the score of a program that shares no source with it.** `plan/router-rebuild-plan.md:1128` sets `FLOOR = binsim(unit-2018, v3.4.0)` on `boa` = **0.0650**, and two *different programs* out of one tree reach **0.1581**. So "above `FLOOR`" was satisfied by a pair with nothing in common, and the plan's warn band covered the whole no-evidence region | **The rule the gate's last step will apply had to be rebuilt before it was used once.** The metric prints `REFUTED` and exits 1 rather than substituting a better number · [`notes/binsim.md`](../notes/binsim.md) |
| 🔴 讀 | **A similarity threshold is not a number, it is a number *at a denominator* — and the first replacement floor ignored that.** `binsim` divides by the smaller feature set. The cell chosen first cleared the no-shared-source level by 0.65 pp while being read on a set **1.46×** larger than the one the rule divides by; at a matched denominator the ordering reverses, and a pair sharing its **whole upstream source** across a compilation-model change scores **0.1212**, *below* the **0.1551–0.1581** a pair sharing **none** reaches | **The three-band rule gains a gate instead of a band**: a comparison across a compilation-model change is **VOID**, not a fail, and the container fingerprint is checked before the score is read. The floor is now the no-shared-source level itself, asserted as the top of a three-program population at one denominator · [`notes/which-drop.md`](../notes/which-drop.md) |
| 🔴 讀 | **"Two different programs" is not "two programs that share no source", and the corpus is emphatic.** Inside this unit's own rootfs, **422 of the 630 cross-program cells** over its 36 largest programs sit above 0.1646 — topped by `sysconf`/`timelycheck` at **0.9967**, two vendor tools that share their source | Any floor read off "a different program" is reading whatever the vendor happened to build out of shared code. The population a threshold is taken over has to be stated, and restricted by size · [`notes/which-drop.md`](../notes/which-drop.md) |
| 🔴 讀 | **Product line is crossed with the clustering, not confounded with it** — the opposite of what this project had been saying about its own corpus. From `/etc/version`: N150RT appears in all three clusters, the lowest within-cluster cell (0.9740) is above the highest between-cluster one (0.8951), and the vendor's own version number is refuted the same way — two images both stamped V2.1.6 land in different clusters | **The clusters can be read as build generations.** What stays collinear is date and SDK generation, which is a tautology rather than a confound · [`notes/which-drop.md`](../notes/which-drop.md) |
| 讀 | **One upstream program held constant across six trees separates "the toolchain moved" from "the source moved" on one edge.** BusyBox 1.13.4 is in all six, so its cells move only when the code generator's output does: across 2016→2018 it holds at 0.9995–1.0000 while `boa` drops 8.7–11.0 points. Across the compilation-model change both collapse and nothing separates them | 🔄 **2026-08-28: the first half stands and the second is refuted by the rebuilds.** `R2a` is a toolchain test; **`R2b` is not a drop test on this channel**, because a `-march` change costs 0.3360 and changing the program costs 0.9359 · [`notes/which-drop.md`](../notes/which-drop.md), [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 🔴 讀 | **One compiler flag costs more of the similarity score than changing the program does.** Six rebuilds of one program, every factor held but one: `-march` alone (4181 vs 5281, one source, one `.config`, one gcc, one binutils) is **0.3360**; the toolchain generation alone is **0.2132**; swapping Realtek's 8196E `boa` for Actiontec's superset fork of it — 27 files modified, 28 added — is **0.9359**. Jaccard agrees (0.2009 against 0.6371). ⚠️ Not pre-registered: it was built to interpret a number that could not be read on its own | **The instrument `R2b` was to decide on measures the code generator, so identifying a GPL drop through it was the wrong axis**, and `TC-02` stays 推 for a harder reason than a weak score. It also closes the option of reconstructing a config from the shipped binary: the vendor's own five configs for this SoC span 0.9347–0.9976, at most 0.065 against a 0.156 shortfall · [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 🔴 讀 | **Twelve shipped userspace binaries across six firmware trees report zero load-delay violations**, at 19.71–28.71 % nop-after-load, read on the same `[DT_INIT, DT_FINI)` window by the same tool. The positive control for that zero is in the same table: the `-march=5281` rebuilds of the same source return **5,224–10,494** | **This unit's userspace, like its kernel, was built for a core that exposes the load delay slot** — so a toolchain that does not pad it would be the first in this corpus. ⚠️ The channel fixes the *side*, not the generation: `rsdk-1.3.6-4181` with `-fuse-uls` is indistinguishable here and is killed by three other channels · [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 推 | **A matrix over six *shipped images* cannot name a GPL drop, whatever it scores.** The gate's own refutation condition was satisfied — the fifteen scores span 92.8 pp against a 5 pp bar — and the answer is still undetermined, which means that condition was never the binding one | **`TC-02` stays 推, as a conclusion rather than a deferral.** What the matrix does instead is exclude the 2019/2020 generation and set the bar the rebuild must clear · [`SPEC.md` `TC-02a`](../SPEC.md) |

| | | |
|---|---|---|
| 讀 | **A gate dependency inherited from the first version was false.** Traced component by component, `R3` needs nothing from `R1`: it builds on the vendor toolchain, which is an existence proof, and the codegen safety net is independent | `R1` split into `R1-gate` and `R1-pub`. **`R3` moves from cumulative segment 61 to ≈41 with no work removed** — what moves is when you find out whether the kernel boots · `PROGRESS.md` Corrections |
| 讀 | **Two public Linux trees exist for this SoC, one carrying a device tree for a TOTOLINK board on this exact part**, and neither was in `SOURCES.json` as a source | The response is not to cut `R5`: it gains a `driver-diff` — write blind, then diff register by register, and **send the disagreements back to the silicon to decide** · `SOURCES.json` |
| 量 | **31 of 55 ELFs on this unit's own rootfs carry `system` or `popen`.** busybox carries neither; `dnsmasq` carries `popen` and the vendor's `udhcpd` carries `system` | Two components `R7` had named had to be replaced by ~350 lines of our own · [`notes/rootfs-census.md`](../notes/rootfs-census.md) |
| 讀 | **A key-derivation choice had never been costed in memory.** scrypt at meaningful parameters wants 16 MiB of the 26 MiB this kernel gets, from one unauthenticated request | The rate limiter moves in front of the KDF, and the parameters are set by an anti-DoS budget · plan D8 |

## It answered a question that had been open

| | | |
|---|---|---|
| 🔴 量 | **`TC-21` is asserted rather than assumed: the same six sources give 0 and 11.** `hazlint` over every `.o` under `arch/rlx` on four trees reads **0 violations in 1,607–1,685 loads**; the same sources re-assembled from the build's **own recorded `.o.cmd` line** with `-Wa,-march=5281` appended and nothing else changed read **11** — `entry.o` 5, `genex.o` 1, `strlen_user.o` 2, `strnlen_user.o` 2, `strncpy_user.o` 1, `scall32-o32.o` 0 | Eleven load-use hazards in hand-written kernel assembly are prevented by the assembler's default `-march` and not by the author, and that is now a paired measurement instead of an inference from a whole-image table. ⚠️ The enumeration nearly missed it: `find` reaches 57/58 objects and `find -L` 63/64, and the six it cannot see are the BSP · [`notes/kernel-build.md` §14](../notes/kernel-build.md) |
| 🔴 量 | **The boot ladder printed before the device was plugged in, and `RLXFW-B07` has both of its values.** Through a MIPS32 emulator with `prom_putchar` redirected: `RLXFW-B00` … **`RLXFW-B07=FFFFFFFF`**, eight marks in order, then `bsp_machine_halt`. **`B02` prints `00018000`** — the emulator's `PRId` — and neither `00018000` nor `0000CD01` occurs as a literal in the image | `B07=00000000` is the pass and `FFFFFFFF` is the switch core not answering, **read off a wire instead of inferred from the absence of everything after it**; and `B02` is demonstrated to be a run-time read before the power cycle that depends on it. The control is an unmarked image printing 0 bytes through the same channel · [`notes/kernel-build.md` §15.4](../notes/kernel-build.md) |
| 🔴 量 | **Realtek's `rtkload` pipeline reproduces the drop's own shipped `nfjrom` byte for byte** — 854,016, sha256 `5cc8d61d4b4e8914`. Of five artefacts, four reproduce exactly and `memload-full` differs by 492 bytes that are entirely `DW_AT_comp_dir` (101 characters against 58, ten translation units, +43 each) | **`TC-d`'s image half closes**, and the wrapper stops being a thing this project runs hopefully. That `memload-full` differs is what proves the identity is not a file compared with itself · [`notes/kernel-build.md` §13](../notes/kernel-build.md) |
| 🔴 讀 | **The core is a Lexra RLX4181, named from a `PRId` assignment table in the vendor's own kernel source.** `PRID_IMP_RLX4181 = 0xcd00` in `arch/rlx/include/asm/cpu.h`, against `PRId = 0x0000CD01` measured on this die; `RLX5281` is `0xdc01` and is excluded. Corroborated by the board config, by third-party ports printing the same name for this SoC, and — differently in kind — by this unit's kernel **behaving** like the RLX4181 column: zero `ll`/`sc`/`sync` in 2.85 MB of text, where RLX5281 boards build with both | **`CLAUDE.md` had banned writing either model number and named its own lifting condition; the condition was met by a file already in the tree.** What the ban was protecting against is still real and travels with the answer: one source in three byte-identical copies, a table no code in the port reads, and an encoding that breaks for two of its six entries · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 讀 | **Four of the seven CP0-20 (`CCTL`) commands now have a name from a source that states it, and one command had no row here at all.** `arch/rlx/mm/cache-rlx.c` — Realtek, 2008 — gives `0x1 DInval`, `0x2 IInval`, `0x100 DWB`, `0x200 DWB_Inval`. The earlier conclusion *"there is no Lexra-specific cache file"* was reached by listing `arch/mips/mm/`; this SoC builds `arch/rlx/`, a directory this repository already cited elsewhere for the exception vector | 🔴 **RETRACTED 2026-08-26 — both are named, and neither is a cache command**: `0x010 = IMEM0FILL`, `0x020 = IMEM0OFF`, the lifecycle controls of a 16 KiB local instruction scratchpad. Two independent sources — the Lexra LX4189 datasheet § 5.2, and `arch/rlx/include/asm/rlxregs.h` **in the drops this project already held** — corroborated by this unit's own kernel programming a 16 KiB window into `CP3 $0`/`$1` and then issuing `0x010`. **The same search failure as `arch/rlx/` in the left column, one file over.** *(as written:)* `0x010` and `0x020` are **still** unnamed in every source — and are now known to be issued by **two independent implementations on this one unit**, both at reset. The route that would name them from the silicon is writing them, which is declined on a one-device budget; the route left is a document · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **The cache geometry has its first source cut from this unit**: three `sltiu … 0x4000` thresholds in the kernel's own D-cache routines are `cpu_dcache_size * 2`, so that build declares **8 KiB**, and eight `cache` ops per 128 bytes declares a **16-byte line** | **Three numbers, three different strengths, and they must not be quoted as one.** The line size is readable from the binary alone; the 8 KiB needs the source to interpret the constant; **the I-cache size is not readable by this route at all**. And a build constant is what the vendor *believed* about its silicon — it is a prediction with a refutation condition, not a measurement · [`SPEC.md` `CPU-25`](../SPEC.md) |
| 讀 | **What copies flash `0x060010` into RAM at `0x80500000`**: `check_image()` itself, at `0x80407E44`, with the destination read *out of the flash header* — there is no `lui X,0x8050` anywhere in stage 2. **And the refutation that had been recorded against that attribution was itself wrong**: `check_image` reads `gCHKKEY_HIT` at its 17th instruction, not its first two. The block counter *is* at `0x8040DBA8`; it reads 0 because a later rootfs scan sets it to exactly zero at `0x80407FF4` and then polls ESC | It also explains something nobody had explained: `---Escape booting by user` is absent from every ESC-streamed capture because `doBooting` received `a0 = 0` and took the silent rescue path · `PROGRESS.md` `C-16` |
| 讀 | **The 350 ms of silence after `Booting...` is stage 1 copying 20,924 bytes out of memory-mapped SPI NOR, uncached, a word at a time** — and `Booting...` is printed by **stage 1**, which means the experiment this project had written down for it pointed at the wrong binary. Decompression is eliminated **by measurement**: the same LZMA parameters took 1.013 s for the 976 KB kernel, which scales to 17–18 ms here by two independent routes | The DRAM training sweep, which the previous guess named, is ≤32 register writes · `SPEC.md` `CLK-15` |
| 量 | **`C-8` closed on the boot text rather than on the register it was waiting for.** The loader prints `Reboot Result from Watchdog Timeout!` where a cold boot prints a single space — while `WatchDogIND` itself reads *clear* at the prompt, refuting the predicted value | A warm reset is distinguishable from a cold one with no register read at all · `SPEC.md` `CLK-13` |
| 量 | **`0xB8000000` reads `0x8196E001`** — the SoC naming itself, while its own loader prints `chipName: UNKNOWN` | Free and unasked, during a cell about something else · `SPEC.md` `CPU-32` |
| 🔴 量 | **The I-cache geometry, by experiment: 16 KiB, 16-byte lines, 2-way, 512 sets.** Both of 否證 ⓐ's controls fired -- every victim STALE at 1/2/4/8 KiB, every victim FRESH at 32/64 KiB -- and the 16 KiB point reproduced inside the seating. 量 2026-08-29 | `CPU-25` and its §17 residual both close. The three numbers are one structure rather than three readings: `T = 8192` is exactly a two-way 16 KiB cache's way size. ⚠️ Size still cannot separate the I-cache from the 16 KiB scratchpad; **associativity can**, because a scratchpad has no sets |
| 🔴 量 | **The Ethernet cable is on `eth4`**, and the host's own capture says so rather than the board: one interface up at a time, `eth0`-`eth3` silent with **nothing at all in the host capture**, `eth4` 4/4 at 0 % loss with ARP and ICMP both directions. 量 2026-08-29 | The carried-forward *the socket* item is answered for the jack now in use. 🔴 **And the recorded port masks were wrong**: `eth0=0x1`, `eth2=0x2`, `eth3=0x4`, `eth4=0x8`, with `0x10` belonging to `eth1`, the WAN. The old row counted the WAN's mask among the four LAN ports and omitted `0x1` |
