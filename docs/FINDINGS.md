# Findings

**A map, not a summary.** This repository holds about 400 KB of dense technical
prose across `PROGRESS.md`, `SPEC.md`, `RUNSHEET.md` and `LOG.md`, and without a
layer saying *which part is worth reading* that density reads as noise. Every row
below is one finding, ordered by **the decision it changed**, and links to the
file that owns it. Nothing here is the owner of anything.

Each row is marked: **量** measured on the device · **讀** read out of code, a
dump or a document · **推** inferred, pending a measurement.

---

## It changed how this repository checks itself

| | | |
|---|---|---|
| 🔴 量 | **A checker written to make one claim machine-checked was itself read as a command by a second checker, and the shape has a name in this repository already.** A bench card's `--send` strings may not exceed 128 bytes; the card claimed *"the longest is 104"* in prose and two of them were 175 and 165. The repair was a `cardnum` row — `count <card> --send '[^']{128,}'` must be 0 — and `cardcheck commands` finds cells with the regex `--send\s+'([^']*)'`, so it read the guard's own text as a cell typing `[^`. Rewritten `-{2}send`: identical to `re`, invisible to a scanner reading the file as text | **`c-apt-ure` one layer up.** `CLAUDE.md` records a CI census counting a suite whose NAME contains `apt` as an `apt` step; this is the same error with the pattern and the population swapped. Both were found because something downstream went red, not because the pattern was read carefully · `bench/2026-09-06/PREDICTIONS-B11-block10.md` § 4 |
| 🔴 讀 | **A ledger that reports an unrecognised depth SKIPS the row rather than failing it, so a typo in the one machine-readable cell passes.** 量 2026-09-06: three new rows declared depth `full`; `DEPTHS = ("none", "name", "line")` (`tools/ledgerscan.py:364`) and the checker reported them as *naming none of* the vocabulary — then said **ok**. A row reading `ful1` would be skipped identically. 🔴 **And the vocabulary has no word for *the whole file***: `arch/rlx/kernel/rlx-cevt.c` went from two string literals to all 244 lines in this segment, and the cell reads `line` before and `line` after | **`LEDGER-2`'s fix added a depth comparison and left the depth's own alphabet unchecked.** The obvious repair is wrong for the same reason `LEDGER-2`'s was: `scan` derives its depth from whether a citation carries a line number, and *whole file* cannot be derived from a citation at all · `docs/blind-write-ledger.md` § 4.8.1, `PROGRESS.md` `LEDGER-3` |
| 🔴 量 | **A pending bit that reads 0 after sixteen full timer periods is not a hardware result — the vendor's own tick handler erases it a hundred times a second.** `bsp_timer_ack()` is `REG32(BSP_TCIR) \|= BSP_TC0IP;`, a read-modify-write on a register whose `IP` bits are write-1-to-clear, so it writes back every bit it read as a 1 and clears all of them — including one belonging to a driver it has never heard of. The flag's duty cycle is **0.19 %** at the card's period and **87.20 %** at 2⁸, and both numbers were written before the cells ran. | **The reading was already in this repository and its consequence was not.** `docs/blind-write-ledger.md` recorded that function as a write one segment earlier; nothing anywhere said what the write does to a foreign bit. **A ledger row that records a write without its effect is a reading banked and not spent** — and the card, written by the same person, assigned that outcome to *"the timer block needs something not identified"* · `SPEC.md` `IRQ-09`, `docs/interrupt-map.md` § 3.6, `bench/2026-09-04/CORRECTIONS-block9.md` § 1 |
| 🟢 量 | **An interrupt was delivered 119,818 times at the rate the driver programmed, and the SECOND period is what makes that a slope rather than a coincidence.** `Δirq_count / Δjiffies` against `hz_used / period_cycles / hz_kernel`: 0.0199 % at 2⁸, 0.0994 % at 2¹², and 0.0206 % at 2¹² with the NIC up and four pings in flight. **Measured ratio between the two periods 15.9873 against a predicted 16.0000.** | **A single rate can be produced by any source that happens to run at it; a rate that shrinks 16× when this timer's period grows 16× cannot.** The card's own § 7.4 says it does not measure a rate, and the two cells that do were added beside the frozen card rather than into it. Ownership of the line is established the same way: `/proc/interrupts` had no IRQ 25 before `request_irq` and none after `free_irq` · `SPEC.md` `IRQ-08`, `notes/timer-driver.md` § 11.4 |
| 🔴 量 | **Fixing a number that was only ever DISPLAYED wrong would have shipped an image that was wrong where it counts, and reading the code would not have shown it.** `rtl819x-timer` compiled in 14,286,057 Hz where the Linux-side rate is **200,000** — 71.43× — so the obvious repair is to derive the rate at init. 量 by putting the numbers through `clocksource_hz2mult()`'s own integer arithmetic: it returns **u32** by a cast, and at 200,000 Hz the compiled `shift` of 24 gives an exact mult of 83,886,080,000 which **truncates to 2,281,701,376** — 136 ns per count against a true 5,000, a factor of 36.76, with no diagnostic. 🔴 **2.6.30 has no `clocks_calc_mult_shift()`**, so the shift has to be searched for by the driver, and it is searched against the LARGEST mask the driver can be set to — a smaller mask only relaxes the second bound, so a shift chosen there survives every period | **A repair that moves one variable in an expression nobody re-evaluated.** The consumer of `hz` is `mult`, and `mult` has a type. Found by writing a twenty-line script rather than by reading the header and nodding · `SPEC.md` `CLK-24`, `notes/timer-driver.md` § 10.1 |
| 🔴 量 | **A number that had been copied into nine committed files was 132 away from what it claimed to be, and every one of the nine said it confidently.** *`tc1_ext_gap_max` reads 6,475,672, exactly 140,693,532 mod 2²⁷* — 量 2026-09-04, recomputed from the two captures: `140,693,532 mod 2²⁷` is **6,475,804**, which is both the wrapped TC1 delta and what `tc1_ext` accumulated. `gap_max` reads 6,475,672 because `tc1_ext_reads` goes 1 → 3 across that arm, so it is the **largest single** inter-read gap of two, not the span. 🟢 **The mechanism got sharper rather than weaker**: the alias is exact and what was lost is precisely one period | **A modulus attached to the wrong field, then propagated by quotation.** Found only by the audit pass that recomputes every written figure from the artefacts instead of re-reading the sentence — the three passes before it chase numbers that MOVED, files that CHANGED and rows that describe state, and none of them recomputes · `LOG.md` § 12 |
| 🔴 讀 | **This kernel's idea of how long a jiffy is comes from an IBM PC's timer chip, and it is right here only because 1,193,182 happens to divide cleanly at `HZ=100`.** `arch/rlx/include/asm/timex.h:21` sets `CLOCK_TICK_RATE = 1193182` — the i8253 PIT frequency — and `include/linux/jiffies.h` derives `LATCH` → `ACTHZ` → `NSEC_PER_JIFFY` from it rather than from `CONFIG_HZ`. 量, replicating `SH_DIV` in integer arithmetic: at `HZ=100` it lands on exactly 100.000000 Hz and 10,000,000 ns, **0 ppm**; at `HZ=250` on 249.984375 Hz, **+62.5 ppm**; at `HZ=1000`, **−153 ppm** | **A ±50 ppm acceptance criterion can be missed by the kernel's own constant before any hardware is involved.** `R5-2` subtracts two numbers 18 ppm apart and one of them comes from here, so the assumption was checked rather than carried · [`SPEC.md`](../SPEC.md) `CLK-20` |
| 🔴 讀 | **A document called one of its rows *the most valuable thing in it*, and the sentence that settles that row had been quoted in this repository for ten days — in a different file, for a different question.** `docs/blind-write-ledger.md` § 4.3 named `CLK-06` — whether `CDBR`'s `0x000E` means divide-by-14 or divide-by-15 — as *the diff's best row*, on the ground that it was an unresolved **decision**. D § 8.2.8 Table 26 states it: *"Assume DivFactor=N, Base clock = System_clock/N"*, and *"Both values 0x0000 and 0x0001 disable the clock"* excludes the `N−1` reading. `SPEC.md` `CLK-02` has cited that sentence since 2026-08-26 and used it only to decide which end of the divider the words *base clock* name | **A fact with one owner that never travelled to the second file depending on it** — the `CPU-27` class, and this time it inflated a claim rather than leaving a gap. The row is L1 fact now, the ledger is corrected in place, and `D3`'s protected layer keeps the five decisions that survive · [`docs/blind-write-ledger.md`](blind-write-ledger.md) § 4.3 |
| 🔴 讀 | **Two sources agreed on a bit's name, the name was right, and it was one bit too coarse — which was invisible until something needed the bit next door.** `GIMR` bit 8 is *`TCIE`, Timer/Counter interrupt enable* in this repository, from **B** and **D** agreeing (`docs/loader-phy-and-switch.md` § 2). 讀 2026-09-03, D Table 14/15 in full: bit 8 is `TC0_IE` and bit **9** is `TC1_IE`, with `TC0_IP`/`TC1_IP` matching in `GISR`. The measured `GIMR = 0x00008100` therefore says *TC0's interrupt is on and **TC1's is off*** | **The two-source rule checks whether sources agree, not whether the thing they agree on is specific enough to answer the next question.** Here it decided a hazard: a TC1 timeout cannot reach the vendor's tick handler through bit 8, which is what let `R5-1` program TC1 at all · [`SPEC.md`](../SPEC.md) `REG-01`, `REG-02` |
| ⚠️ 讀 | **A section table reported a driver growing `__param` by 3,424 bytes, and the driver declares no module parameter.** 量 2026-09-03 over two `vmlinux` builds differing only by one driver: `size -A` says `__param` went 1,076 → 4,500, while `__start___param … __stop___param` is **700 bytes in both**. The section's `sh_size` runs to the next 4 KiB boundary, and `.rodata`'s growth pushed those 700 bytes across it | **A per-section diff of two links is not a per-section diff of two programs.** In the same pair `.text` grew 2,944 bytes while the ELF file grew 797, because `.text` sits in ~73 KiB of alignment slack. Quote the loadable content, or quote the file, and say which · [`notes/timer-driver.md`](../notes/timer-driver.md) § 6.1 |
| 🔴 量 | **A hand-typed grep reported `0`, the sentence written from it was false — and the first explanation of why was false as well.** `grep -ril rlx-time` returns **0 files** here, and 量 (directory listing, nothing opened) `arch/rlx/kernel/` carries **both** `rlx-time.c` and `rlx-cevt.c`: the needle named a real file and **the 0 was correct**. What was wrong was the inference. This repository cites `rlx-cevt.c` **to the line** (`notes/vendor-kernel-isa.md:33`). **Zero citations is not zero reading, and a zero on one spelling says nothing about another.** It is why `R5-0`'s ledger is computed by `tools/ledgerscan.py` rather than written, and why that tool's positive control **is this citation** | **A sweep with no positive control.** The needle was a guess at the file's name and the file is named for what it does, not what it is. Found by reading the one file in this repository that is an entire record of greps · `docs/blind-write-ledger.md` § 1 |
| 🔴 量 | **Nothing in this repository could notice a tool that has a `--self-test` and no CI step.** `tools/rlxfw-marks.py` — one of the two tools that edit Realtek's source (`rlxfw-kbuild.sh` applies `config/host-compat/`), and the one whose every inserted line is a declared row with a reason, 32 controls — was in neither `ci.yml` nor `ci-expected.tsv` and had never run on a runner. **`ci-census` cannot see that class by construction**: its population *is* that table, so a suite never declared is not a missing suite. Different from the `verify-backup-copy` case, which was declared, did run, and was lost by a sweep's own selection | **A checker whose population cannot contain the thing it would have to find.** Different from the `verify-backup-copy` case, which was declared, did run, and was lost by a sweep's own line-based selection — that one the census caught · `PROGRESS.md` `CI-3` |
| 🔴 量 | **One control's expected value was a literal copy of a number two other files also carried, and two of the three were stale.** `tools/test-config-gates.sh` `S3` asserted `18 passed, 0 failed` for `rlxfw-marks`; `README.md` said *eighteen controls*; the tool reports **32**. The fix is not the new literal — it is that `tools/ci-expected.tsv` owns that number because `ci-census` re-derives it from the run, and `S3` now reads it there, with a third case requiring a missing table row to be reported as such rather than as a failing tool | **The same shape this repository has recorded three times** (`looprun`'s 23-against-26, `notes/leak-surface.md`'s three file counts). Found by the closing superlative sweep, not by a checker · `tools/ci-expected.tsv` |
| ⚠️ 量 | **A citation COUNT is not a stable quantity and a path count is.** Between this ledger's draft and its commit, `spi_mtd` went 45 → 66 citations and `bsp` 40 → 55 with **no new reading at all** — the increase is the ledger, `LOG.md` and `CHANGELOG.md` discussing paths already declared. The per-domain citation counts are dropped from the ledger's tables and the path counts stay | **A number that moves when you write about it cannot be quoted as a finding.** Same judgement `notes/leak-surface.md` reached about its three file counts, one tool over · `docs/blind-write-ledger.md` § 4 |
| ⚠️ 量 | **A scanner cannot tell a citation from an example, and the correct fix is not to edit the prose.** `ledgerscan`'s first control claiming *gpio/wdt/led/keys are empty on the real tree* went red within the hour, because this repository's own census row — describing the classifier's bug — names `drivers/input/keyboard/gpio_keys.c`. Rewriting that sentence would make a checker green by editing prose; the ledger declares the path with `origin: none` instead, and the control was narrowed to what a control can assert | **A control asserting something only the document it guards can assert.** The cheap fix — rewrite the prose so no path appears in it — makes a checker green by editing prose. The path is declared `origin: none` instead, with a constraint on the exemption · `docs/blind-write-ledger.md` § 4.1 |

---

## It changed what an iteration of `R5` costs

| | | |
|---|---|---|
| 🔴 量 | **One real edit on a reused tree rebuilds the whole tree, and the cause is this project's own identity string.** Editing one file under `config/rlxfw-src/` gives **592 `CC` / 32.58 s**; the *same edit* where `RECIPE_ID` does not move gives **3 `CC` / 11.75 s**. `RECIPE_ID` is a sha256 over `config/` and reaches every C object's command line, so it moves 592 of them — and `R5`'s drivers live under `config/`. **`--keep` buys nothing for an `R5` iteration** | **Single-variable**: the same edit, made where `RECIPE_ID` does not move, is 3 `CC`. Two things had to be built first — `rlxfw-marks --if-needed`'s three states, and a copy that does not rewrite an identical file, because kbuild triggers on mtime and an unconditional copy is a floor under the measurement · `notes/incremental-build.md` § 7, `SPEC.md` `TC-45` |
| 🟢 量 | **Confining that define to the one object that uses it costs nothing and buys 20 s.** The staged tree has exactly one consumer (`init/main.c:576`). `--id-scope main` gives **4 `CC` / 12.53 s** for the same edit, and two **fresh stages** at one recipe produce a **byte-identical** `vmlinux` | **Not turned on**: `global` remains the default because it is what every reading so far was taken under, and `looprun`'s `S2` re-stages, so the win is entirely on the desk side · `SPEC.md` `TC-46`, `PROGRESS.md` `INC-2` |
| 🔴 量 | **An incremental build does not reproduce its own product, whatever else is held fixed.** `linux-2.6.30/.version` is a build counter incremented at each link and reaching the image through `UTS_VERSION`'s `#N`: `11 → 12 → 13` across two 2-`CC` no-ops, image differing each time. Every reproducibility claim in this repository is a **fresh-stage** claim and stays one | **Found by an experiment failing for the wrong reason.** The comparison that exposed it was asking whether a build flag moved the product; it did not, and the measuring stick did. A measurement that cannot reproduce itself cannot compare two treatments · `notes/reproducible-build.md` § 8 |
| ⚠️ 量 | **The bench seam's cost was counted in seconds where this project's scarce unit is power cycles.** `S2`/`S3` are desk stages running before `S4`'s reset, and `loop_once` raises on the first non-zero `rc` — so a failed build never touches the board. A no-skip `--mode bench` needs **no additional power cycle** | **The unit was wrong, not the number.** `CLAUDE.md`: one power cycle is the most expensive unit here. Read out of `looprun.py` with no power spent · `notes/dev-loop.md` § 10.6 |

---

## It changed what a payload of mine may execute

| | | |
|---|---|---|
| 🔴 讀 | **One timer channel cannot be both the clocksource and the clockevent, and the standard escape is closed by this part's own vendor code.** A clocksource needs a **power-of-two** period — every core computes `(now - last) & mask`, exact only when the hardware period is `mask + 1` — and a 100 Hz clockevent needs `hz_used / HZ` = **2,000** counts. `2000 = 2⁴ × 125`, and no power of two at or above the driver's `2^8` floor divides it. The textbook fix, a software-extended clocksource, needs to ask the hardware *is a wrap pending?* — and `IRQ-09` measured that any pending bit in `TCIR` lives at most one 10 ms tick, because the vendor's tick ack is a read-modify-write on a write-1-to-clear register. **So it would be right most of the time**, and a clocksource that is right most of the time hands the core a backwards step its unsigned arithmetic turns into a jump of nearly the whole mask | **The driver became two exclusive MODES rather than one more feature**, and the escape is recorded as refuted rather than skipped — the difference being that a skipped idea gets tried again. Found by trying to write the clockevent, not by reading the datasheet · `SPEC.md` `CLK-25`, `notes/timer-driver.md` § 12.1–§ 12.2 |
| 🔴 讀 | **The obvious evidence for *the system tick is mine* is a tautology on this board, and writing the bench card is what exposed it.** My clockevent runs at 100 Hz; the vendor's TC0 interrupt keeps arriving at 100 Hz after the handover (讀 `tick_setup_device()`: the old device's handler becomes `clockevents_handle_noop`, the hardware is untouched). So `Δ(line 25) ≈ Δjiffies` **and** `Δ(line 13) ≈ Δjiffies`, and neither says whose interrupt drives the clock | **A new driver verb.** `cereload` changes the live period *after* the handover: double it and the kernel gets 50 interrupts a second while still believing `HZ = 100`, so its clock runs at half speed and nothing inside it can notice. Two references neither of which is the kernel's clock — `Δce_cycles / hz_used` on the board, and the host's capture timestamps off it. **Correlation became causation for the price of one `/proc` write** · `notes/timer-driver.md` § 12.6 |
| 🔴 讀 | **This unit's vendor kernel emulates `ll`/`sc` and `sync`, and does not emulate the FPU at all.** `do_ri` calls `simulate_llsc` and `simulate_sync` under `#ifndef`s the board config leaves unset; `simulate_rdhwr`'s two call sites are `#if 0`'d out where mainline calls them unconditionally; **there is no `math-emu` under `arch/rlx`** and `do_cpu` gives `SIGILL` for any coprocessor but 0. The binary agrees: zero `ll`, `sc`, `sync`, `lwc1`, `swc1` and `sdc1` in 2.85 MB of text, and zero `fpu_emulator`/`cp1emu` strings — ⚠️ not *zero FPU opcodes*: that span also holds one `COP1` and two `ldc1`, all three inside a 1 KiB non-code island and excluded by adjudication rather than by a bound | **`CLAUDE.md`'s rule *never measure the ISA under Linux* gave two reasons and one of them is false.** The rule stands — `simulate_llsc` alone is enough — but a rule whose stated reason is wrong is a rule that will be misapplied at the next edge case. It also means an FPU instruction from a payload running under the vendor kernel gets `SIGILL` rather than a silent emulation · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 🔴 讀 | **This unit's kernel contains MIPS16 code and calls it with `jalx`.** 180 in-image `jalx` targets, of which **179 in two clusters at and after `__iram = 0x802B8000`** — the 180th, `0x8000231C`, is a data word sitting in zero padding 2.8 MB below `__iram`, which `hazlint` 1.3 excludes and `opcount` does not (both are right; they answer different questions) — against a 1.26 % base rate for random data and `jal`/`j` controls at 99.9 % / 99.6 %. One target disassembles under the vendor's own objdump as a complete function whose literal pool holds `0xB8010028`, a KSEG1 register address; 48 random bytes through the same disassembler do not cohere | **A 4-byte linear scan is not a superset of the instructions there**, and two instruments in this repository said it was. `opcount.py` gained `--mips16` as an explicit precondition; `hazlint` 1.3 refuses such a range outright rather than reporting a conservative-looking number for it. The twelve vendor userland ELF binaries were re-checked by two independent tests; `stage2.bin`, a raw image with no ELF header, by the `jalx` test alone with its control fired (99.4 % of 499 `jal` in range). All clean, so no count already here moves — **the claim was wrong even though its conclusions survived** · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 🔴 讀 | **This unit's own kernel executes the `cache` instruction — 37 of them, D side only.** (🔄 this row read *the MIPS-II `cache` instruction* until 2026-08-28; the vendor's own assembler rejects it for `mips1` **and** `mips2` and accepts it from `rlx4181` on, so it is MIPS-III/32 carried as an extension — and `Config.M = 0` still says this is not a MIPS32 core.) One span, `0x8000CA40`–`0x8000CD4C`; op fields only `{0x11 DInval, 0x15 DWBInval, 0x19 DWB}`; base registers only `{v0, a0}`; offsets only the eight at stride `0x10`. The other 15 hits in the image are data and differ from those on **all three** properties. The instrument's control: the same scanner over `stage2.bin` reproduces the five known CCTL sites *and* the one known data false positive | **A refutation condition this project wrote for itself is met** — *"refuted by finding a `cache` instruction anywhere in vendor code that executes"* — and the scan that returned zero had only ever covered the 56 KB loader. It does **not** make this a MIPS32 core (`Config.M = 0` is 量); it means **there may be a working D-cache invalidate on this part**, which is what the one unanswered gate decision needs · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔄 量 | **"The D-cache is write-through" does not follow from what was measured.** Both cells that established it stored to a word that had been *executed and never loaded* — a D-cache **miss** — and under a miss, write-through and write-back-without-write-allocate give the identical reading. The owner file carried the disjunction from day one; four downstream files dropped the parenthesis | **A descriptor ring is a write *hit*** — load the status word, store the ownership bit — so the CPU→memory direction is **not** covered for the pattern the Ethernet driver will actually use. Both GPL drops' board configs for this SoC say `CONFIG_ARCH_CACHE_WBC=y`. **A load-bearing alternative does not belong in a parenthesis** · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔴 量 | **`Status.BEV` is 0, and the core fetches from `0x80000080`.** The second is the part that needed a payload: `trap_init` copying 128 bytes there proves the copy, not the dispatch. What proves the dispatch is that `break` trapped into a handler this project installed and **came back** — `break.count=1`, `cause=00000024` (ExcCode 9 = `Bp`), `break.epc=80500270`, the exact address of the `break` in the emitted image | **`R1a`'s exception handler can live at `0x80000080`** — one of the four decisions `R1-gate` exists to unblock, and it is now a reading rather than an argument · [`SPEC.md` `CPU-27`](../SPEC.md) |
| 🔴 量 | **`mfc0` always writes its destination register on this core** — `nowrite = 0` across all 256 census rows, where the census reads every register twice with two different primes so that a non-writing `mfc0` returns its own prime and gets its own state | **This is the row the other rows stand on.** Without it, every `reads zero` in the census is indistinguishable from `the destination was never touched` — including `Count`'s, which decides whether a timer driver is a prerequisite · [`SPEC.md` `CPU-38`](../SPEC.md) |
| 🔴 量 | **`Count` (CP0 rd 9) is not implemented** — reads zero, and a 100,000-iteration loop moves it by nothing. Cross-checked three ways: the census row, the raw before/after pair (both primed with *different* values, so an instrument failure cannot wear the result's clothes), and a trap counter bracketing the call | **`R5-0`'s SoC timer driver is a PREREQUISITE, not a bonus**, and `R1c` loses its first timing route. A decision, not a gap · [`SPEC.md` `CPU-42`](../SPEC.md) |
| 量 | **CP0 register 20 reads zero for real, and its write side has an effect** — so it is a **write-only command register**. `probe1` read the same zero and could not tell that from *the destination was never written*; the double-prime census can | The flush recipe `R5b`'s MTD driver writes is a store to a register that will never read back what you put in it. That is the kind of thing a driver author discovers at 2 a.m. otherwise · [`notes/cache-model.md`](../notes/cache-model.md) |
| 🔄 量 | **The cache geometry is not in CP0 on this part.** `Config` reads zero — with `nowrite = 0` proving the destination *was* written — so `Config.M = 0` and there is no `Config1`. And the R3000 sizing walk cannot answer either: its algorithm needs cache isolation, which this core does not implement, so it can only return its own *no answer* value | **`CPU-25` is now *both routes were measured shut*, not *not tried*.** What is left is an eviction walk that needs no isolation — which is a payload, not a register read · [`SPEC.md` `CPU-25`](../SPEC.md) |
| 🔴 量 | **The loader re-stages the vendor kernel at `0x80500000` on a watchdog reset, not only on a cold power-on** — measured by a second `J 80500000` printing `decompressing kernel:` after a payload's own reset | **A payload cannot be re-run on one power cycle without re-uploading it.** Any runsheet cell that says *run it again for a repeatability control* is wrong as written · [`docs/loader-command-semantics.md`](loader-command-semantics.md) |
| 讀 | **The general exception vector on this core is `0x80000080`, not `0x80000180`.** R3000 layout: `0x80000000` is the UTLB refill vector and the loader installs nothing there. Three sources — this unit's `trap_init`, the vendor bootcode's own comment, and `arch/rlx/kernel/traps.c` — and none says `0x180`. Corroborated by the loader returning from exceptions with `rfe`, while the `eret` encoding occurs zero times in the same file *(and that zero has a positive control: the same grep finds both `rfe`s)* | **The address `0x80000180` had reached four files.** A handler written there lands in RAM nothing reads, the payload looks installed, and the fault still hangs · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **A fault the loader does not handle hangs the board forever.** `do_reserved` at `0x80400BE8` prints twice and executes `j 0x80400C18` — a branch to itself. The exception entry has already cleared `IEc`, and the watchdog is not armed. The vendor source says so in as many words: `prom_printf("Undefined Exception happen."); for(;;); /*Just hang here.*/` | **One fault = one power cycle, and there is no spare unit.** Every payload is ordered by increasing risk and writes each result before taking the next · `PROGRESS.md` `R1-gate` |
| 讀 | **`do_reserved` dereferences the faulting code's own `$a0`.** It is the raw vector target with no `SAVE_ALL`, and its third instruction is `lw a3,148(v0)` with `v0` = the faulting `$a0`. `rlx_cctl(0x002)` would have handed it the integer 2, so `0x96` — kuseg, TLB-mapped, and a miss goes to `0x80000000`, which the loader never populated | **Undetermined, and the sharper version was withdrawn the same day.** The escalation *"it could branch into the loader's flash-write path"* was refuted: `0x5A5AA5A5` decodes as `BLEZL` with target `0x7FFE9B58`, not a jump. `SAFE_A0` stays because it is two instructions and removes an undetermined case · [`tools/rlxprobe/cache.S`](../tools/rlxprobe/cache.S) |
| 讀 | 🔴 **A TFTP upload named `boot.img` sets the loader's write pointer to `0x80000000`** — the name compare at `0x8040A6A8` reaches `0x80401250`, which stores `0x80000000` into both `LOADADDR` and the running write pointer at `0x8040DD10`, and `0x80401A10`'s memcpy walks up from there. It writes over the UTLB refill vector **and** over the general exception vector, destroying the loader's own exception handling mid-transfer | **`boot.img` and `nfjrom` go on the do-not-type list.** Found by the adversarial pass on the exception-path read, which had claimed nothing ever writes low RAM — a computed base its own scans admitted they could not see · `RUNSHEET.md` |
| 🔴 讀 | **The loader's `FLR` reads flash by programmed I/O through `SFDR`, and never through the memory-mapped window.** The chain is `0x80409A44` → `0x80404F38` → a `.bss` function pointer at `0x8040FC10` (`+0x3C` of a 72-byte chip table whose **three** registration sites all pass the same `0x804065DC`) → the `Fast Read` opcode `0x0B000000` → the engine at `0x80405F70`, whose data loop at `0x80406008` is fifteen instructions per four bytes: one `lw` from `0xB800120C` and four `sb`. 讀, a census of every `lui` immediate: the window base `0xbd00` occurs **exactly once** in stage 2, as a `printf` argument inside `FLW` | **It withdrew a cell that was about to be spent at the bench.** `SPEC.md` §17 scheduled `FLR 80C00000 100000 100000` to decide whether the memory-mapped window prefetches; a number measured on the `SFDR` port says nothing about the window, so no band it returned could have answered the question. The precondition that killed it was written into `§19.7.5 ③` by an adversarial pass the day before · [`notes/kernel-build.md`](../notes/kernel-build.md) §20, `SPEC.md` `LDR-42` |
| 🔴 讀 | **`probe3`'s associativity ladder is one discriminator reported four ways, not four pieces of evidence.** For `C` = 16 KiB with 16-byte lines, a two-way cache (512 sets) and a direct-mapped one (1,024 sets) predict the **same** smallest evicting M at strides `C/8`, `C/4` and `C/2` — 9, 5, 3 — because at those strides both wrap onto a handful of sets. They part only at `T = C`, where every victim maps to one set: two-way needs 3, direct-mapped needs 2 | **It stops the `M(T)` table being written up as four confirmations.** The 2026-08-29 reading already excluded direct-mapped, because the search keeps the strictly smaller M and a direct-mapped part would have reported `(16384, 2)`; what the ladder adds is that a reader can check that instead of trusting an argument about tie-breaking · [`docs/probe3-cells.md`](probe3-cells.md) §6.2a |
| 🔴 量 | **A mutation harness that kills everything and one that works are indistinguishable from a list of kills.** `test-replay-capture-mutants` reported 14 of 14 killed on a temp tree missing `tools/ci-expected.tsv`, which control `R14` reads — so the unmutated tool was already red there and every kill was invalid. The only row that could see it was the one declared EQUIVALENT and therefore **required to survive** | **It is the `flashwin` defect appearing inside the file written to prevent it**, one tool later, and it changes what a mutation suite must contain: an in-tree baseline (the unmutated tool must pass in the same temp tree) is now in both new harnesses, and an equivalent-with-a-proof row earns its place twice over · [`tools/test-replay-capture-mutants.py`](../tools/test-replay-capture-mutants.py) |
| 🔴 量 | **`wc` is one of this busybox's fifty applets and was not in the image, and nothing compared the two populations.** `FW-26` answers *what can this binary do*; a bench cell needs *what can this image invoke*, and `config/rlxfw-initramfs.tsv` declares eleven busybox symlinks. Two cells were lost at the bench to that gap | **A card is the one artefact here written by hand and executed by a machine that cannot ask questions**, so the check is now a tool whose `A11` runs the whole committed card and requires exactly the two cells that failed. Its corpus sweep also found a loader verb missing from the tool's own table, and that `/proc` belongs to the kernel rather than the initramfs · [`tools/cardcheck.py`](../tools/cardcheck.py) |
| 讀 | **This loader was not built from either GPL drop this project holds.** The vendor's `do_reserved` is `prom_printf("Undefined Exception happen."); for(;;);` — one call, no CP0 reads. The shipped `0x80400BE8` has two `prom_printf`s, `mfc0 c0_cause`, `mfc0 c0_epc` and a `pt_regs` load; and `do_watch`, whose string is in the binary, appears in neither tree | Every *"the vendor source says why"* argument has to state which half it is leaning on. The string match is evidence of provenance; it is not evidence that the C on screen is the C that was compiled · `docs/loader-command-semantics.md` |
| 讀 | **This core uses the R3000 cache model** — `Status.IsC`/`SwC` — **plus a Lexra CP0 register 20** carrying the invalidate and writeback commands. Two sources agree on `0x002` and `0x200`; two further commands the bootcode issues at reset have one source and no name | Decides where the handler goes, and what `R5b` and `R6` must do. The two unnamed commands are **not written** by any payload · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **The load delay slot is architecturally exposed.** Upstream's `P9-12` filled it — with the wrong instruction — and the device emitted exactly 16 bytes per iteration, one 16550 FIFO, 272 times over ten minutes | `-march=mips32` is banned; `hazlint` is a **build gate**, not a lint, and no payload exists unless it exited 0 · [`tools/hazlint`](../tools/hazlint) |
| 讀 | **`-march=mips1` does not compile on gcc 12.4.0 without `-msoft-float`** — *"`-march=mips1` requires `-mfp32`"* | The plan's build line was unbuildable as written. `-msoft-float` rather than `-mfp32`, because this core has no FPU · `SPEC.md` `TC-06` |
| 🔴 量 | **CP3 is reachable on this die: all eight `mfc3` stubs executed and none trapped.** `m.traps=00000000`, `m.cause` still `deadc0de` (never written). No reading equals its own prime and `v1 == v2` for all eight, so the destination was written and the value is stable. `CU3` sticks (`1000fc00` -> `9000fc00`). 量 2026-08-29, `bench/2026-08-30/QJ.log` | **The emulator says the opposite and says it confidently** -- on qemu all eight trap with `m.cause=1000042C`. A payload may issue `mfc3` on this part. ⚠️ Registers 0 and 4 read `20000000` and both tops read zero, so this is a base and **not a window**: `w-imem` stays 未定 |
| 🔴 量 | **This core retires the `cache` instruction** -- ops `0x11`, `0x10`, `0x15`, `0x19`, all `n=00000000`, **while `x ri` traps in the same run under the same handler** (`cause=00000028`, ExcCode 10). 量 2026-08-29 | `CPU-44` closes positive, and the 37 D-side `cache` ops in this unit's shipped kernel are instructions its silicon will execute. ⚠️ **Retires is not invalidates**: `x.c10.treated` and `x.c10.twin` are both 1, so the untreated victim moved too |

## It changed where a payload of mine may write

| | | |
|---|---|---|
| 量 | **`0x81000000` is not scratch.** A live 32-byte-periodic descriptor table sits at `0x81000400` with `next = prev = &next`, and uninitialised DRAM cannot produce its own address | The runsheet's §C had argued from *"far above the loader's image"*, which excludes two known things and nothing allocated at run time. The upload address moved to `0x80A00000`, chosen by a probe rather than by an argument · `RUNSHEET.md` §C |
| 量 | **`0x81000000` word 1 is rewritten to `0x00000144` on every boot** — three times reproduced, and word 2 survives, so it is not decay | A canary belongs at `0x80A00000`, never at `0x81000000` · `SPEC.md` `MEM-14` |
| 量 | **This DRAM keeps its *contents* across a short power-off, not just its bias.** `0x80A00000` read back sixteen words written by hand in the previous power cycle, byte for byte. After 16 hours: zero retention | **Any "it survived a power cycle" claim must state how long the power was off.** Every result block is poisoned before it is written · `SPEC.md` `MEM-15` |
| 量 | **`EW` rounds an unaligned address up; `EB` does not.** One loader, two write primitives, opposite handling | Every `EW` written from here on has to assume it · `SPEC.md` `LDR-08` |
| 讀 | **`AUTOBURN`'s initialiser in the loader image is `1`.** So the loader boots with auto-burn on, and a TFTP upload that completes without `AUTOBURN 0` having been typed is written to flash | Found while precomputing bench expectations, before the first upload. `R0`'s whole zero-write claim rests on it, and it is measured at the burn path's own instruction *during* every transfer · `SPEC.md` `LDR-23` |
| 🔴 量 | **The device node this project was about to declare cannot exist, and the obvious substitute is the dangerous one.** `R3-9` and two notes asked for `nod /dev/mtd0` and `wc -c < /dev/mtd0`, a second path to flash through my own driver. 量, two routes with a positive control on each: both built `.config`s carry `# CONFIG_MTD_CHAR is not set` (control: nine other `^CONFIG_MTD` lines in the same file), and both `System.map`s hold **zero** mtdchar symbols against **six** mtdblock/mtdcore ones. Major 90 has no chrdev, so the node opens `ENODEV` | **`/dev/mtdblock0` is the substitution to refuse.** `mtd0` is `0x000000`–`0x130000`, which contains **both** regions this project forbids writing — the loader, and the per-unit region a factory reset does not restore — `mtdblock` has a write path, and **mode `0400` is not a control**: root ignores DAC, so on an interactive shell one typo is an erase-block write at offset 0 on a device with no spare. 🔄 **2026-08-30, the rebuild: `CONFIG_MTD_CHAR` is ON now, and the node set went further than this row's answer.** The image declares **only odd MTD minors** — `/dev/mtd0ro` `c 90 1` and `/dev/mtd1ro` `c 90 3`, which 讀 `mtd_open` cannot be opened for writing **by the kernel** — and `/dev/mtdblock1` is withdrawn, so there is **no** node in the image anything can write flash through, not merely none over the forbidden regions. *(As written: `/dev/mtdblock1` runs the identical read path and buys the identical reading with zero writable nodes over either region.)* **The control is the absence of a node, not the mode bits** — and it is complete now: 量, `mknod` is not among this `busybox`'s fifty applets, and the rule is enforced by `mkinitramfs` `A24`/`A25`/`A26` rather than argued in a comment · [`notes/kernel-build.md`](../notes/kernel-build.md) |
| 🔴 量 | **A bench card's pre-flight expected `13 passed` from a tool that had been rewritten to print `19` in the same session that wrote the card.** `flashwin` went from 13 controls to 19 after an adversarial pass found 24 of 45 mutants alive, three of which printed this unit's MAC. Nothing re-read the card afterwards | **The first line of a pre-flight is the one place a mismatch reads as *wrong version, stop*** — and it is a line an operator runs before power, with the board in their hand. Both numbers are now measured on the card: 19 with this unit's flash dump present, 16 plus one skip covering 3 without it. The same stale pair (*"10 ok … closes against 13"*) was still in the CI table's prose while the gate column beside it already said 19 · [`bench/2026-08-30c/PREDICTIONS-B5-block2.md`](../bench/2026-08-30c/PREDICTIONS-B5-block2.md) |

## It changed what the bench procedure must do

| | | |
|---|---|---|
| 🔴 讀＋量 | **An unattended loop uploaded to the board with the loader's ECHO as its only evidence that the burn flag was off.** `looprun --mode bench` went `S5` rescue → `S6` upload. `loader-tftp.py` does parse `AutoBurning=0` out of the rescue report and refuses without it — but `C-6` (量 2026-08-24) is why an echo and the word at `0x8040D4A0` are two sources, and `RUNSHEET` `G2`/`H1a` both make the read-back mandatory **before** a `put`. 🟢 **The seating then reproduced `C-6` in its own rescue transcript**: `AUTOBURN: 0` → `Unknown command !`, `AUTOBURN 0` → `AutoBurning=0`, for all three commands. And it sharpened what the tool's check is — `"AutoBurning=0" in replies`, a **substring test over a transcript that contains the failure as well as the success** | **`S5b` reads the word back and aborts on anything but `00000000`; `--skip S5b` is REFUSED, because a guard a flag can switch off is not a guard; and *no read-back line at all* fails, because silence is not a zero.** `S6b` is the second guard — what is at `0x80500000` must be the file `S6` sent, **derived from that file and never typed** — and it is skippable, because it protects the seating and not the device. Both were written an hour before power and both fired on the board · `tools/looprun.py`, `bench/2026-09-02/CORRECTIONS-block7.md` §5 |
| 🔴 量 | **A card can inherit an expectation without the step that produces it, and the abort will fire on a healthy board.** `Y-ab` predicted `AUTOBURN` = `00000000` and made it the first abort condition; the board said `00000001`, which is the documented power-on default. Every `00000000` in this repository is read after a rescue has sent `AUTOBURN 0`, and the block has no rescue step because it uploads nothing. **The card written the same evening repeated it.** | `CARD-1`, `bench/2026-09-01/CORRECTIONS-block6.md` §2 |
| 🔴 量 | **A capture opened 1.093 s late reads as healthy by the rule meant to catch it.** `CLK-18` excludes captures whose first byte arrives within 0.02 s of the capture opening. `Y0-A`'s first byte is at 1.093 s and passes — while being demonstrably mid-boot, missing the `ramSize: 32M` line the whole cold/warm discriminator lives on. What caught it was the *value* (0.064 s to the prompt against a 2.176–2.636 s band), not the rule. | `bench/2026-09-01/CORRECTIONS-block6.md` §1 |
| 🔴 量 | **The loader's `DW` sends its reply at 3,594–3,726 B/s, not at the 38400 line rate of 3,840.** Measured from first read to last read of one contiguous reply in the committed `.timing` files: `H2g` 9,660 B / 2.688 s, `H1c` 1,667 B / 0.447 s. n=2, and the **longer** reply is the slower one. 🔄 **2026-08-30: the explanation that stood here was backwards and it is corrected rather than dropped.** *(It read: “so it is the first line's fixed cost amortised rather than a per-byte overhead.”)* A fixed startup cost `T = F + N/r` gives a rate that **rises** toward `r` as `N` grows; it cannot produce one that falls with size. `Q-3` added a third point at 23,523 B / 6.697 s = **3,512 B/s**, and fitting `T = a + b·N` over the three gives a **negative** intercept — the signature of the clock starting at the first *read* rather than at the first byte, because `console-capture.py` drains on a 50 ms grid and a short reply is already partly buffered when timing begins. 🔴 **The figure to size a window against is the marginal rate, 3,458–3,497 B/s**, not 3,594. `B7c` was excluded and the exclusion is written down: its 1,272 bytes are an ESC window's echoes around a 71-byte reply, so a rate computed over them is a rate of nothing | **Every `--seconds` on every runsheet card had been sized against a number nobody had measured.** `reply-size.py` says how many bytes come back and says nothing about how long they take, and `console-capture.py` has no early stop without `--idle` — so a window set too small truncates, and **a truncated reply and a refused command are not the same shape**: a refusal is `Unknown command !` at once with a prompt, a truncation stops mid-line with none. First used to size `bench/2026-08-30/`'s card at 2.29× and 7.10× · [`docs/loader-command-semantics.md`](loader-command-semantics.md) §f, [`SPEC.md` `LDR-40`](../SPEC.md) |
| 🔴 量 | **`§B5-c1`'s *the head does not discriminate* is a statement about `nfjrom` files, and a flat payload's head does.** Every `nfjrom` in this family shares `rtkload`'s `start.o`, which is why sixteen identical bytes cannot say whether an upload landed. An `rlxprobe` payload is linked by `rlxprobe.lds` and its first word is `_start`'s first instruction: 量 `probe1` `3C1D8051`, `probe2` `3C1D8050`, `probe3` `3C1D8051` against the staged image's `00000000`. **The same mechanism runs underneath both**: the word at `0x8050000C` carries `_bss_start` = `0x80500000 + size`, exactly as `0x8050001C` carries `__vmlinux_end` — one linker constant, two linkers | **Quoting the refutation outside its corpus would have deleted `probe3`'s only upload check.** `probe3` is 31,536 bytes and ends **955,602** bytes *below* the staged vendor image 🔄 *(29,088 and 958,050 until 2026-08-31, when the retained bitmap region and the `M(T)` ladder moved it to 29,680 and 957,458; 2026-08-31's Group F moved it again. The gap is what matters and it has not closed in three moves — the payload would have to grow by a factor of thirty)*, so block 1's before/after tail pair — the trick that works because `loudm` sticks out 66,542 bytes *above* it — has nothing to stand on here. The head is the only discriminator for power cycle 1 and the tail is the only one for power cycle 2; **neither method transfers** 🟢 **量 2026-08-31 (seating 8), and the pair closed on the device**: `K-2b` read `8050000C: 25087B30`, so `_bss_start = 0x80507B30` and `0x80507B30 − 0x80500000 = 31,536` — **the same number `K-1` reported the loader accepting**, arrived at by the linker's symbol table on one side and the TFTP byte counter on the other. ⚠️ Neither catches a truncated transfer ALONE: the head states the size the image *should* be, not the number of bytes that landed; it is the PAIR that closes it · [`RUNSHEET.md`](../RUNSHEET.md) §B5-c1, `bench/2026-08-30/PREDICTIONS-B5-block0.md` §9, `bench/2026-08-31c/PREDICTIONS-B6-block4.md` §4.2 |
| 🔴 量 | **The one cell that checked whether the upload landed could not fail.** `RUNSHEET` `K2` read `DW 80500000 1` and rested on its own sentence *"my image and the staged one differ in their first 16 bytes"*. That measurement was finally taken and it says the opposite: the first **24** bytes are byte-identical — three device captures across two power cycles (`bench/2026-08-23/B.log:16`, `2026-08-24c/G1a.log`, `2026-08-24d/G5-rb1.log`) against all five `nfjrom` files this project can build, because every one of them is linked from the same `rtkload` `start.o` | **The image identifies itself eight words in, not four.** The `lui`/`addiu` pair at `0x80500018` carries `__vmlinux_end`, so one `DW 80500000 8` reads back the uploaded image's own length: `26101400` is `loudm`, `2610AC00` is the `quiet` pair, and `26101000` is **either** `loud` **or** the staged vendor image — only the `lui` separates those two, which is why both words are read. And the tail, 66,542 bytes above the staged image's end, is the one part of the upload that can be watched **changing** without poisoning the fallback · [`notes/kernel-build.md` §12](../notes/kernel-build.md) |
| 🔴 量 | **`uname` is not an applet in this unit's `busybox`, and the seating asked for it three times.** 量, the device's own `busybox` under `qemu-mips-static`: `busybox uname -a` → **`uname: applet not found`**. The binary lists **50** applets and `uname` is the only one of the fourteen this seating needs that is missing; the controls are in the same run — a non-applet gives `sh: … not found`, and `cat` reaches the filesystem | **It is not a missing symlink, so the fix that had already been written for it would not have worked either.** `notes/kernel-build.md` §9 records a `/bin/uname` symlink added for `K5` and then removed — three passes and none asked whether the applet exists. `K5` becomes `cat /proc/version`, which prints `linux_banner` verbatim and therefore carries `(key@K)` and the gcc version that `uname -a` drops — two thirds of the discriminator · [`notes/kernel-build.md` §12.7](../notes/kernel-build.md) |
| 🔴 讀 | **`SPEC.md` held two rows for one loader mechanism and they disagreed, and the wrong one is the one three files quoted.** `LDR-26` said `nfjrom` and `boot.img` both force the load address to `0x80000000`; `LDR-37`, eleven rows below it, and this file's own do-not-type row both say `boot.img` writes the address. 讀 this unit's `stage2.bin` at `0x80401208`: a `nfjrom` match sets `0x8040D390 = 1` and **branches past the `boot.img` test entirely** | **The correction runs toward the danger.** With `nfjrom` as the TFTP filename the loader jumps to the load address that is already set — `0x80500000`, the right one — so the accident **looks like a successful boot** and silently costs the `J` line, the `AUTOBURN` timing and `K2`. And the guard is on `loader-tftp.py`'s `--filename`, not on the local path, so *rename the file* is hygiene and *pass `--filename`* is the control · [`notes/kernel-build.md` §12.6](../notes/kernel-build.md) |
| 🔴 讀 | **The rung-2 and rung-3 cells gave the board the workstation's own address, and then pinged the loader's.** `K6` said `ifconfig eth0 10.1.1.2 up` and `K7` said `ping -c 4 10.1.1.1`, while this same runsheet's §G3 records *"`IPCONFIG 10.1.1.1`, workstation at `10.1.1.2/24`"* | **Both halves fail, and the capture reads exactly like `K7`'s own definition of a driver that transmits and does not receive.** Board `10.1.1.10` → host `10.1.1.2`, `ip neigh flush` first (量: a stale loader entry broke a ping outright once), and `arp` added to the `tcpdump` filter — one word that separates *ARP never resolved* from *the driver does not transmit* · `RUNSHEET.md` §B5-c4 |
| 🔴 讀 | **What gets uploaded is `nfjrom`, about 1.03 MB — and the runsheet had been quoting the `vmlinux` ELF, four times larger.** Three sizes are three different things: the ELF the build produces (3,968,113–4,042,388), the decompressed image the desk channel ingests (3,472,384 / 3,546,112), and the `nfjrom` the loader is handed (**1,027,072 / 1,053,696**). ⚠️ And that artefact is *literally named* `nfjrom`, one of the two filenames the loader tests by name. 🔄 **2026-08-29: `LDR-26` said both force `0x80000000` and only `boot.img` does** — `nfjrom` arms the auto-execute at whatever address is already set, which is why that accident *looks like* a boot; the string `Jump to 0x` separates it, and a correct `J` never prints it | **The TFTP transfer size and `K2`'s `image_end − 16` readback were both wrong**, and the file has to be renamed before it is uploaded — a warning that used to be about a filename nobody would type by accident and is now about the one the build produces. 🔄 **The rename is hygiene; the control is `--filename`**, which is what the loader sees — `--image` is a local path it never receives · [`notes/kernel-build.md` §13.3](../notes/kernel-build.md) |
| 🔴 讀 | **This board prints NOTHING between kernel entry and userspace, and `CONFIG_EARLY_PRINTK=y` is a trap.** 量 on the built `vmlinux`: `printk` is three 20-byte `move v0,zero / jr ra` stubs, and **`early_printk` is a WEAK EMPTY 16-byte body** at `0x80013bec` that nothing under `arch/rlx` overrides. `panic_printk` is real but reaches the wire only after a console registers. Second source, on this device: `bench/2026-08-24c/G6.log` goes from `start address:` straight to the WLAN driver. **So `R3`'s D3 had no observable at all**, and the first seating would have spent a power cycle to learn nothing. The eleven boot marks go through `prom_putchar`, which needs no console and cannot hang. | `notes/kernel-build.md` §11.1, `TC-29` |
| 🔴 讀 | **The most likely way this board's first boot dies is silent by construction.** `bsp_setup()` ends `ret = bsp_swcore_init(version); if (ret) bsp_machine_halt();`, and `bsp_machine_halt()` is a bare `while(1)` with no message anywhere. `B07` prints that return value — the one failure nobody could otherwise have told apart from any other hang. | `notes/kernel-build.md` §11.2, `TC-30` |
| 量 | **The console line buffer is 128 bytes, and `readline` writes its NUL only on the CR path.** A line of exactly 128 characters is unterminated and the tokeniser scans past it into the saved registers | `C7` was about to send 173 characters, which `readline` cuts at 128 with `EW` as the command. **No command line may ever be 128** · `SPEC.md` `LDR-06b` |
| 量 | **`DW <addr> N` prints `4 × ceil(N/4)` words and rounds *up*** | A readback shorter than the structure comes back looking complete — which would have faked exactly the failure the cell was hunting · `PROGRESS.md` Corrections |
| 量 | **A stale neighbour entry costs the first transfer three retransmits**, and after a kernel run a stale *loader* entry broke ping outright. The loader synthesises its MAC from the IP it was given; the vendor kernel uses the real one | The host must flush the neighbour entry at **every** loader↔kernel transition. It is an `R4` requirement for unattended `bench-ci` · `RUNSHEET.md` Results |
| 量 | **An ESC window of 45 s was too short**: the boot began 19 s after the stream stopped. The cost is wildly asymmetric — an extra ESC second is free, a missed window costs a power cycle | Standing change: `--esc 180 --seconds 200` · `PROGRESS.md` Corrections |
| 量 | **The reset button is a GPIO on `PABCD` bit 5, active low — it is not `RESET#`.** Bit 5 goes 1→0 under the held button with `CNR` and `DIR` unchanged and no `Booting...` | Retired a planned cell before it was spent, and refuted *"this unit's loader polls only the UART"* · `SPEC.md` `BRD-05` |
| ✅ 🔴 量 | **`console-capture.py capture` with neither `--seconds` nor `--idle` never returns** — **and as of 2026-08-30 it refuses instead, and records both terminators in its metadata.** Both default to `0.0` and the read loop breaks on neither; `timeout -s TERM 8` gives `rc=124`. A kill loses `.meta.json`; the `.log` and `.timing` survive, flushed per chunk. 量 2026-08-29 at the desk, before power | **14 of `RUNSHEET` §B5's 15 capture rows omit it, and block 0 omits it on `Q-A`** -- while all ten committed `A-catch` captures passed one. ⚠️ That census is an inference from `stop_reason`, because the metadata records neither `seconds` nor `idle` -- which is the larger defect. A card nobody has run literally is a card that has not been checked. 🔄 **The severity was inverted and is corrected**: 量 on a pty, `SIGINT` writes a complete `.meta.json` and only `SIGTERM` loses it, so a bench operator pressing Ctrl-C loses nothing and `--force` is never the correct recovery — the overwrite refusal is the safety feature, not the obstacle. ✅ **Closed 2026-08-30**: the guard sits after `_check_send` and before the port is opened (量: of this suite's four terminator-less invocations only `P4` reaches it), `seconds` and `idle` are in the metadata, `test-console-capture.sh` is 29 → **40**, and §B5's card carries a terminator on all fifteen capture rows · `tools/console-capture.py`, `RUNSHEET` §B5-c14 |
| 🔴 量 | 🟢 **2026-08-30: it has now — `V-rdh` ≡ `Z-rdh` ≡ the desk expectation, byte for byte, on both power cycles. 256 of 8,192 bytes, so the region is 3.1 % read and the other 96.9 % is still not.** 🔄 **2026-08-31: 512 of 8,192 = 6.3 %, and the fourth window is the canary page `0x006400` — the only span `FLS-21` ever saw move. Four readings, two power cycles, all byte-identical to the dump. And the bracket now has a NEGATIVE CONTROL: every destination is read before its `FLR`, so *the RAM already held these bytes* is excluded rather than assumed.** *(As written that morning:)* **The `G8a`/`G8b` flash bracket has never sampled `H601`.** It reads 256 of the loader region's 24,576 bytes and 256 bytes of the `cr6c` header — a region no rule forbids writing — and **0 of `H601`'s 8,192**. `H601` is the one region `CLAUDE.md` calls *not restored by reset*, and `bench/2026-08-30b/L3.log` is why it stopped being theoretical: this project's own kernel instantiated an MTD partition `0x000000`–`0x130000`, which contains it | **Six days of write-ups said “the two regions that would change” and neither of them was the one that cannot change back.** The bracket gains a third region, its expectation is computed from the 2026-08-16 dump rather than from a capture that can never exist, and the reach is stated every time it is quoted: **768 bytes of 4,194,304 = 0.0183 %** · [`bench/2026-08-30c/PREDICTIONS-B5-block2.md`](../bench/2026-08-30c/PREDICTIONS-B5-block2.md) §8, [`tools/flashwin.py`](../tools/flashwin.py) |
| 🔴 量 | **This unit's own flash dump renders two captures taken off the device eight days later, byte for byte.** The 2026-08-16 dump (`FLS-14`, 4,194,304 B, 6,300.1 s) at `0x000000` and `0x060000`, rendered in the loader's own `DW` format, reproduces `bench/2026-08-24d/G8a-rd0.log` and `G8a-rd6.log` exactly — 777 bytes each, through a **different read path** (`console-dump.py`'s chunked `FLR`+`DB` against a hand-typed `FLR`+`DW`). And the two committed dumps are byte-identical over all 4,194,304 bytes | **It is what lets a region with no committable capture still have a falsifiable expectation.** The control is public even when the reading is not: a reader who cannot see the `H601` rendering can see that the renderer which produced it reproduces two readings they can · [`SPEC.md` `FLS-14`](../SPEC.md), [`tools/flashwin.py`](../tools/flashwin.py) |
| 🔴 量 | **This unit's busybox has no applet that can digest a stream**, so the flash cannot be cross-read from its own userspace. 🔄 **2026-08-31: true about digests, and the conclusion drawn from it was too wide.** `wc -l` counts a content-derived quantity and needs no digest: 量 on the silicon, `busybox wc -lc` over both partitions returned the newline counts computed at the desk from the 2026-08-16 dump, on two power cycles — **4,194,304 bytes, the whole part**. That separates *this path returns this flash* from *it returns a quarter of it and reports success*, which is a live alternative (`FW-34`). ⚠️ It is an aggregate: it places no byte and `FLS-20` still belongs to the `FLR` bracket. 🔴 **And `wc` itself was not in the image** — an applet on the list is not a symlink in the initramfs. Fifty applets, and `dd`, `md5sum`, `od`, `hexdump`, `cmp`, `cksum`, `sum` and `sha1sum` are none of them; `config/rlxfw-initramfs.tsv` declares three device nodes and no `/dev/mtd*` | **The full re-dump `G8b` says “zero flash bytes” needs costs 105 minutes on the loader's wire and would cost seconds as `dd if=/dev/mtd0 \| md5sum` from a shell.** What blocks it is the applet table, not the device node — and putting a foreign binary in the image breaks Decision B's *this unit's own binaries, unmodified*. `wc` **is** on the table, so a node alone still buys a readability reading · [`SPEC.md` `FW-26`](../SPEC.md), [`notes/kernel-build.md`](../notes/kernel-build.md) §17.7 |

## It changed what an instrument may claim

🆕 **2026-09-03 — a trust flag that reports trusted on data it cannot
vouch for, and it is mine.** `rtl819x-timer`'s `tc1_ext_trusted` is
`gap < (MASK >> 1)`, which is correct **inside its domain** — sampling gaps
under half a counter period — and nothing enforces that domain. 量: past one
period the masked difference **aliases back into the trusted band**, so a 30 s
gap loses 3 wraps and a 60 s gap loses 6, and the flag reads `1` for both.
**A flag that is silent outside its domain is useless; a flag that says
*trusted* outside it is worse than none**, because a reader who checked it has
done the right thing and still been told the wrong answer. What changed: `R5-2`'s
card does not quote `tc1_ext` at all, and it **predicts the false `1`** so the
defect is confirmed on the silicon instead of staying an argument. `SPEC.md`
`CLK-22`; ~~the driver fix is `R5-3`'s~~ 🟢 **the fix shipped in 2.0 and ran
on the silicon in seating 12** — `tc1_ext_trusted` is decided in **jiffies**
now, which this driver never reduces, and 量 it reports **0** at period 8
(`gap_max_j = 1` against `period_jiffies = 0`, i.e. a 255-count mask sampled
every 10 ms cannot be trusted **and the flag says so**) and **1** at periods
12 and 20. `PROGRESS.md` `TMR-1` closed 2026-09-06.

🆕 **2026-09-03 — and a resolution floor makes a criterion unmeasurable
without making it look unmeasurable.** `R5-2`'s `D4` asks for ±50 ppm and the
cell that was going to produce it compared two 60-second samples of the
kernel's clock. 量: with `clocksource_jiffies` selected that clock advances on
a **10 ms grid**, which is **166.7 ppm** over 60 s — nine times the 17.99 ppm
the cell was predicting. **Nothing about the cell looked wrong**: it named a
tolerance, an interval and a prediction, and printed nine decimal places. What
changed: every comparison now goes against `jiffies × (tc0data >> 4) +
(tc0cnt >> 4)`, a continuous reference the driver was already printing, and the
grid stops being the limit.

🔴 **2026-09-03, seating 11 — and the sentence above used to name the constant
instead of the field, which is the same defect one level down.** *(It read
"every comparison now goes against `jiffies × 142858 + (tc0cnt >> 4)`, a
continuous 14.29 MHz reference at 0.0012 ppm per count … and the 18 ppm becomes
arithmetic over two measured constants".)* 量, `TM-1`, the first reading of
these registers under a kernel: `CDBR` is `03E80000` (divisor **1000**) and
`TC0DATA` is `00007D00` (reload **2,000**), not the loader's 14 and 142,858 —
the vendor's `rlx-time.c` reprograms the divider at boot and both give 100 Hz. 🔴 **CORRECTED 2026-09-04: `arch/rlx/bsp/timer.c` does, not `rlx-time.c`, which is a shim.**
So the reference is **200.005 kHz at 0.083 ppm per count**, and the `+17.99 ppm`
figure was arithmetic over **two constants that belonged to the loader**. The
method survives untouched — 0.083 ppm is still 2,000× finer than the ±50 ppm
`D4` asks for — and only the numbers move. **The lesson is the one this entry
was already about**: a criterion can be unmeasurable without looking it, and so
can a constant be stale without looking it. Reading the reload out of each dump
is what makes it neither. `notes/timer-driver.md` § 8.1,
`bench/2026-09-03/CORRECTIONS-block8.md` § 1.

🆕 **2026-09-03 — a checker that is green over the wrong axis.**
`ledgerscan check` joins the set of cited paths against the set of declared
paths and **never reads the depth column**. 量: it reported *ok* over 32 paths
while `kernel/time/jiffies.c` was declared `name` and the scan read it `line`.
Depth is what separates *saw the interface* from *read the code* — the
distinction the ledger exists to make — so the gate was green over everything
except the thing that matters. ⚠️ **The obvious repair is wrong**: `scan`'s
depth is the maximum observed anywhere in the repository and the ledger's is
what a row says it took, so a mismatch has to be **reported**, not **decided**.
`PROGRESS.md` `LEDGER-2`.

🆕 **2026-09-03 — and twice in one afternoon a scan could not have failed.**
The clocksource census counted `jal` only and its positive control
(`init_jiffies_clocksource`, which provably calls `clocksource_register`) did
not fire — because `return f(x);` is a **tail call** and gcc emits `j`. The
indirect-reachability scan then reported 0 with **all three** of its positive
controls also at 0, because `panic`, `do_timer` and `clocksource_get_next`
simply never have their addresses taken. **The first was caught by having two
other controls that did fire; the second by noticing that all three read the
same.** What changed: a control is now chosen for *having the property the scan
looks for*, not for being an important symbol — the replacements are three of
my own driver's symbols that a `proc_dir_entry` store guarantees will appear.

🆕 **2026-08-31 — a third containment shape, and it found something on its first sweep.** `flashwin render` governs what may be *printed*;
`flrbracket run` governs where a *bracket's* read-back may land; both act
when a file is produced. **`flashwin scan` asks whether a file this
repository has ALREADY COMMITTED holds forbidden content** — by the bytes,
not by the text shapes an address takes, with the probe set cut from the
reference dump at **every** offset and filtered on the REFERENCE side.
量: that costs **113 needles**, fewer than an aligned scheme, because
`H601` is **98.22 %** one repeated byte value and holds **40** distinct
values in 8,192 — a measurement that removed a caveat from the guarantee
rather than documenting it. 🟢 rlxfw's own tree is CLEAN over **1,381**
files, `K-P3` included. 🔴 The default sweep, which does **not** exclude
`upstream/`, reads 1,683 and reports **1 HIT**: `BENCH-LOG.md:2557`,
sixteen bytes of `H601` as an `xxd` line, in a public repository.
⚠️ **Neither existing tool identifies it as forbidden CONTENT** —
`leakscan` does not name that line at all (it names **22 hit rows over 18 distinct lines** of that file (`:215`, `:216`, `:248`, `:1848`, `:1931`, `:2042`–`:2044`, `:2057`, `:2656`, `:3309`, `:3312`, `:5181`, `:5186`, `:5196`–`:5197`, `:5329`–`:5330`)), and `audit-bench-log` names it only on the topic keyword `H601` (the flash bytes at that offset ARE the
ASCII `H`,`6`,`0`,`1`), among 183 hits, exiting 0. It does **not** move
`FLS-22`'s owner decision. 🔴 **And it changed the tool's default**: the
first walk excluded `upstream/`, which hid the only finding the tool has
ever made. 量 · owner `notes/leak-surface.md` § 7

🆕 **2026-08-31 — a mutation harness counted five kills that turned no case
red, and the file that taught this repository what an invalid kill looks like
had no suite of its own.** 量: `test-replay-capture-mutants` scored a kill on
`rc != 0` alone, and adding `W0` (*a kill counts only if the case the row NAMES
went red*) showed **five of its fourteen rows** turning **no** case red — each
replaced a `Refuse` with an `IndexError` or a `FileNotFoundError`, the controls
caught only `Refuse`, and the whole self-test died on a traceback before
printing anything. 🔴 **The repair is the controls, not the labels**: every
control now reports rather than raising, keeping the message assertion that
separates a refusal (which names its reason) from a crash (which does not).
The same day `tools/test-flashwin-mutants.py` arrived — the suite `flashwin`
never had, though three other files cite its 2026-08-30 invalid 8-of-8 pass as
the reason they carry a baseline — and it went red on its first run in three
places, all three real: a matcher stopping at the first occurrence of each
needle (no control had the same window twice, which is `K-P3`'s exact shape), a
`DW` address-column rule no case could see (`S5` asserted only THAT a hit
happened), and a row naming `C9` where the measurement says `C10`. 量 · owner
`tools/test-replay-capture-mutants.py`, `tools/test-flashwin-mutants.py`

🆕 **2026-08-31 — *the fifth tree* was three claims and each was narrower than
the last.** `RUNSHEET` `P2` sweeps `hazlint` over every `arch/rlx` object a
build produced. `PROGRESS.md` recorded a residual as *one* uncovered tree
needing a 480 MB re-stage first. 量: **eight trees are on disk with their
objects** (no rebuild), **four** were uncovered rather than one, and then —
hashing every `arch/rlx` object per tree — **two of the four are byte-identical
to trees `P2` had already swept**, so exactly **one** new object set arrived and
`-j4`/`-j8` is deterministic over `arch/rlx`. 🟢 The chain also reaches the
silicon with no vendor binary executed: the swept tree's `vmlinux` `cmp`s equal
to the image build's input, and that build's `nfjrom` `cmp`s equal to the image
seating 7 uploaded — the link `Q0` never covered. 量 · owner `SPEC.md` `TC-21`

🆕 **2026-08-31 — a gate that only runs before a push had been red for two
sessions, and reverting a file's content does not revert its mtime.**
`check-predictions.py --sweep bench` returned **13 OUT OF ORDER**, all from one
frozen prediction block whose content is git-clean at `68b0bec` (2026-08-29
18:58) and whose mtime was **2026-08-31 14:17:46** — the eighteenth session's
line-number rewrite touching it and then `git checkout --`-ing it back. The
sweep cannot be in CI (**git stores no mtimes**; a fresh clone reads 128 of 156
cells as out of order), so it is a pre-push gate on one machine, and two
sessions closed without running it. The instance was repaired from
`git log --format=%aI` — a stronger record than the mtime it replaced, not a
forged one — and the class is carried forward. 量 · owner `PROGRESS.md`
carried-forward


🆕 **2026-08-30 — `CONFIG_PRINTK=n` does not make this kernel quiet, and the
prediction that said it would was refuted on the wire.** `quietm` was built with
`# CONFIG_PRINTK is not set` and printed **849** bytes where the block predicted
**401**. All five predicted terms were exactly right; the sixth was asserted to
be zero and is **448 bytes** — fifteen lines of Realtek driver output. They are
`rtlglue_printf`, which `include/net/rtl/rtl_types.h:366` `#define`s to
`panic_printk` with **no Kconfig symbol in the way**, and `panic_printk` is
declared `asmlinkage` in the `#else` branch of `CONFIG_PRINTK` on purpose. 量
`kernel/Makefile:5`: this board builds `printk_log.o` **instead of** `printk.o`,
so the file the prediction reasoned from — `kernel/printk.c` — **is not compiled
here at all**. 🔴 **The quotable number is 97, and 274 was published for about an hour before the same evening's adversarial pass caught it** — `drivers/net/rtl819x/**/*.o` matches 28 objects of which **seven are `built-in.o`**, so 量 all `*.o` = 274, excluding `built-in.o` = **97**, and the top-level `built-in.o` alone = 97. 97 is the call-site count, identical in both builds, and what makes that identity mechanism is that **22 of the 28 objects are byte-different** while `printk` goes 998 → 0 over the same material. ⚠️ Thirteen of the fifteen lines are `rtlglue_printf`; two are direct `panic_printk` from outside that directory. The
whole-tree figure moves 719 → 1,594 between them because the tree redefines both
names in both directions in at least eight files, and a count of a moving
population is not a reading. `SPEC.md` `FW-31`, `notes/kernel-build.md` §17.8.

🆕 **2026-08-30 — a code span left open by an unmatched backtick had been
committed for a day, and no check here could see it.** `C8b` walks table rows,
`C9` walks code spans and only fires on whitespace-only content — and `C9` pairs
backtick runs across the **whole file**, so an unmatched run is invisible until
later text happens to supply a partner. 量: three instances. In
`notes/kernel-build.md` a whole sentence rendered as code while `0x2B0000` — the
number the sentence is about — rendered as prose; in `RUNSHEET.md` a span closed
with a run of **two**, which leaves the backtick **count** even so a parity test
misses it. `C10` is the check, the paragraph is its unit because a blank line
ends a span, and it pairs by run **length**. 🔴 **Its first version passed all
fifteen controls and three of twelve mutants survived** —
`tools/test-spec-check-mutants.py` — so `T16`, `T17` and a fixture fence — ⚠️ *(this named `T14` too; `T14` is the doubled-closer case and it kills `N4`, not one of the three survivors)* —
exist because a mutation pass demanded them, not because they were foreseen.
⚠️ The third instance is in a **frozen prediction block** and is deliberately
**not repaired**: `check-predictions.py` reads its mtime, so editing it would
make its 2026-08-25 captures read as older than the prediction naming them. The
exemption carries its reason and a control (`T13`) asserting it is still
load-bearing.


🆕 **2026-08-28 — a census that only ran `--version` was not a read-only
operation, and nothing warned.** `rsdk-linux-config` is a statically linked i386
ELF that runs `make` in the tree it lives in. It deleted 2,580 tracked files
from a pinned vendor clone — **not** the symlink farm the first write-up named;
that directory holds 93 tracked symlinks, so the characterisation was generalised
from the first forty lines of the `git status` — and it also wrote a file into this repository's root
— a place no vendor-tree check watches. What made it recoverable was that the
tree is a clone at a known sha and that every mtime fell inside one 16-second
window. `tools/vendor-tripwire.sh` exists because of it, and it detects; it
cannot prevent.

🆕 **2026-08-28 — two instruments of this project got their first ground truth,
and they passed.** `opcount --mips16` and `hazlint`'s MIPS16 refusal have only
ever been run on a stripped image, where the symbol table that would confirm them
does not exist. A `vmlinux` built here from the vendor's own source has one: the
counter says *MIPS16 reached, 25 distinct targets*, and `readelf -s` says *39
symbols marked `[MIPS16]`*. **That is consistent and it is not agreement** — 25
of 39, and the missing 14 are the blind spot both tools already document, a
MIPS16 routine entered through `jr`/`jalr` on an odd address rather than through
`jalx`. So the ground truth confirms the detector and puts a number on its
undercount at the same time. Before this, both instruments rested on one
adjudicated disassembly and a self-consistency argument.

🆕 **2026-08-28 — a claim was refuted by doing the thing it was a claim about.**
*"None of the five shipped configs enables MIPS16, and this binary has it"* was
carried as evidence that the drops in hand did not build this image. Building one
of those five configs produces MIPS16. The discriminator is gone; a stronger one
(each drop's own `.config` names the rsdk it selects) replaced it.


| | | |
|---|---|---|
| 🔴 量 | **`--esc-period X` does not produce a cadence of X, and the overhead is ADDITIVE rather than multiplicative.** n = 4 over two requested periods, out of `console-capture`'s own `achieved_period_s`: requested 0.002 gave 0.002140 and 0.002156; requested 0.0005 gave **0.000666 and 0.000664**. Achieved minus requested is **140 / 156 / 166 / 164 µs**. A multiplicative model fitted to the coarse pair (1.07×) predicts 35 µs at the fine one; the reading is 165. ⚠️ The two groups still differ by ~15 µs and n = 4 cannot explain that | **Any cell that writes `--esc-period X` and then reasons about X is reasoning about a number the instrument does not produce** — including `SPEC.md` `CLK-14`'s own sentence that 0.0005 would give a ~0.5 ms cadence, which this refutes. Read `achieved_period_s` out of the metadata, which exists for exactly this · `SPEC.md` `CLK-14`, `bench/2026-09-02/CORRECTIONS-block7.md` §3 |
| 🟢 量 | **A quantity whose floor is the instrument's own read period was separated from it by changing the ruler and watching the number not move.** `boot-timeline`'s `entry` is the largest gap between two reads that returned data, so it cannot be smaller than one read period. At a 2.1 ms cadence it read 2.4 ms — **1.1 read periods**, i.e. at the floor, which is why `ESC-1` was opened. At a **3.2× finer** cadence it reads **2.3 / 2.4 ms**, now 3.5 read periods, and a coarse-grid reset **in the same power cycle** reads 2.3 | **The finer ruler is the experiment; the same-cycle coarse control is what makes it one rather than a comparison across days and board states.** So 2.3–2.4 ms is a silence on the board. ⚠️ It bounds the watchdog timeout **above** — it spans the timeout plus the reset plus the time to the first boot byte — so the derived 2.184 ms is consistent with it and not confirmed by it · `ESC-1`, `bench/2026-09-02/CORRECTIONS-block7.md` §4 |
| 🔴 量 | **A new parser's negative controls all passed while it returned nothing, and only the positive control saw it.** `looprun`'s `DW` line regex ended `)+$` under `re.M`; the console sends CRLF, so `$` sits behind a `\r` and every match failed. `C7` (a different image), `C8` (a capture that lost a line) and `C10` (right words, wrong address) were all green — they were failing for the wrong reason — and `C6`, the one case that requires a **match**, was the only red | **A harness that kills everything and a harness that tests nothing print the same thing**, and this is the third recorded instance of that shape here. It is also why the fix was validated a second way, outside the fixtures: against the **17** committed captures that read `0x8040D4A0`, of which 12 read zero after a rescue and 5 read the power-on `00000001` after a reset — **both outcomes from the device rather than from a fixture** · `tools/looprun.py` |
| 🔴 量 | **Every `make` in this tree rebuilt all 599 objects, and the cause is one unescaped `#`.** kbuild's own `V=2` reason macro says `597 - due to command line change`. `arg-check` compares the command saved in `.<target>.o.cmd` with the one make computes; on `init/main.o` those strings are **byte-identical, 1,132 characters, zero differing words**. What differs is what make LOADS: the saved line is 1,131 bytes with one `#` and the loaded value is **1,023 bytes with none**, truncated at `-D"KBUILD_STR(s)=`, because a bare `#` starts a comment. `scripts/Makefile.lib` and `scripts/Kbuild.include` are byte-identical to mainline v2.6.30 -- the control is the top-level `Makefile`, which the same comparison reports as 159 changed lines -- so this is a 2009 kernel meeting GNU Make 4.3, where 2.6.30's `$(subst \#,\\\#,…)` is measurably a no-op | **A no-op `make vmlinux` goes 599 CC / 28.0-32.1 s to 2 CC / 6.9-10.3 s**, and a full build 599 to 594, because the broken comparison was also compiling five `init/` objects twice inside every build. 🔴 **And it buys exactly zero on this project's default path**, which re-stages the tree for every build -- so the finding is a number the owner can decide against, not a faster loop arrived at quietly · [`notes/incremental-build.md`](../notes/incremental-build.md) |
| 🔴 量 | **The diagnosis this was found under was wrong, and the patch written for it refuted its own premise.** `LOOP-1` said two generated headers rewritten with a fresh mtime force the rebuild. Both facts held -- the headers are rewritten, `bounds.h` is named in 566 of 731 `.*.o.cmd` -- and with a `filechk` patch applied both mtimes froze while the rebuild stayed at **599** | **The header churn was downstream of the same cause**: once `arg-check` works, `kernel/bounds.s` is no longer regenerated and the rule never runs. The patch was nearly dropped and one measurement kept it -- `touch kernel/bounds.c` costs **3** compilations with it and **573** without · [`notes/incremental-build.md`](../notes/incremental-build.md) §5.7 |
| 🟢 量 | **Two committed instruments read one artefact and nothing compared them; now three identities do.** `looptime` and `boot-timeline` both parse a capture's `.timing`, both map a byte offset to a time, and both publish an interval anchored two bytes apart. The third identity is exact rather than tolerant: `lead - artifact` must equal the read gap between the CR of `\r\nBooting` and the `B`. 量 over the corpus: **390 artefacts, 3 identities, 0 disagreements**, and `X3`'s population of 6 is independently the same six captures §6.5 of the loop file found opening on a byte the board did not send | **The 1 ms that looked like a disagreement is fully accounted for**: five of those six have their anchors in different reads. ⚠️ It does not retroactively catch `CLK-18` -- the quantity did not exist before that fix -- it makes a revert of the fix red. The controls document three real divergences the corpus cannot reach, including that one parser sorts and the other does not while its lookup breaks early · [`tools/xcheck.py`](../tools/xcheck.py) |
| 🔴 量 | **A boot capture's `to-prompt` time depends on which byte you call the start, and two instruments here disagreed by a third of a second.** Six of fifteen cold captures open on a power-on line-transition byte — `0x00`, `0xFC` or `0xFF` — that precedes `Booting` by **0.321–0.350 s**. `looptime.to_prompt` started its clock there; `boot-timeline`'s `artifact` column has measured exactly that interval since 2026-08-25. `SPEC.md` `CLK-18` cited the first, and its **unexplained 165 ms two-group split was that byte** — from `Booting` the range is 0.220 s with no gap, and 21 warm boots in one power cycle agree (largest gap 0.053 s). | `SPEC.md` `CLK-18`, `TOOL-1` |
| 🔴 量 | **A tool that drops a capture reports `0 unknown` and is telling the truth about the wrong population.** `boot-timeline` classified 51 captures and printed `0 unknown` while `bench/2026-09-01/Y0-A` — a `.log` and a `.timing` in a directory it was pointed at — never became a row at all. `unknown` counts rows whose *class* is undecided; a capture that never became a row is invisible to it. Fixed, and the fix's first detector **missed its own motivating case** (it tested for `chipName`/banner; `Y0-A` opens after both) — it named two other partial boots instead, which is how the miss was found. | `tools/boot-timeline.py` `B3b` |
| 🔴 讀 | **`started_wallclock` is written by `strftime` and truncated to the second, while `duration_s` in the same file keeps microseconds.** `console-capture.py:430-431` takes both from one `time.monotonic()`, so the recorded start is `floor(true_start)` and every interval derived from the pair carries ±1 s. 量 over every seating in `bench/`: more than a hundred consecutive pairs have a gap of about **−0.09 s** and **not one is below −1** — they are the truncation and not overlapping captures | **A tool that called those overlaps would be reporting on its own arithmetic**, so `looptime.py`'s bound is derived from the instrument's source rather than chosen to make the data green. It also bounds `check-predictions.py`: that field is the clone-stable ordering signal its own docstring proposes, and two captures taken in the same second cannot be ordered by it · [`notes/dev-loop.md`](../notes/dev-loop.md) §9 |
| 🔴 量 | **`hazlint`'s MIPS16 excision is printed and does not happen on a relocatable object — and the error is the safe direction.** On an `ET_REL` `.o` the holes come from `st_value`, which is section-relative, while the spans carry a synthetic base, so they never intersect. 量 across four trees: **26 objects (27 marked) print `EXCISED BY NAME` and not one scanned fewer bytes than its `SHF_EXECINSTR` sections hold** | Those bytes are **scanned as 32-bit code rather than skipped**, so the defect can manufacture a violation and cannot hide one. `RUNSHEET` `P2`'s 0 therefore stands without waiting on the fix, which is `TC-m` and is still carried · [`notes/kernel-build.md` §14.4](../notes/kernel-build.md) |
| 🔄 讀 | **The desk channel's four instruction counts were listed program counters, not instructions.** `-d in_asm` logs at translation time and qemu re-translates a block entered at a different offset. Re-derived from the same logs: **828 / 843 / 908 / 938 distinct** against the 880 / 880 / 968 / 1,003 printed — and the control's 880 appears in **none** of the six logs that run left behind | The stop addresses reproduce exactly, so the conclusion they carried is unaffected; what is not recoverable is *why* the counts differ, **because the qemu invocation was never recorded**. Every run now prints its own command line · [`notes/kernel-build.md` §15.1](../notes/kernel-build.md) |
| ⚠️ 量 | **Two of this repository's own qemu captures disagree about malta's ISA COM1.** A payload writing `0xB80003F8` under `-kernel` is heard (5,893 bytes, committed 2026-08-26); a `-bios`-only stub writing the same address is not, and a poll of `0xB80003FD` reads 0 forever. One variable differs — the entry mechanism. That the GT64120's decoders are what is missing is **推** | The ISA addresses are in `qemu/README.md`'s own table, and taking them into a `-bios` run gets silence a reader would score as *the code never got there*. Carried as `TC-n` rather than smoothed over · [`qemu/README.md`](../qemu/README.md) |
| 🔴 讀 | **A control that asserts a property of the command line has to go THROUGH the command line.** `hazlint` 1.4's `K11`–`K16` called `_scan_elf`, a private copy of `main()`'s pipeline, while their names claimed properties of the CLI: three mutations recreating `TC-f` verbatim passed all twenty controls. They now spawn the real program — **not** an in-process `main()`, which recurses and whose anti-recursion guard would be the same defect one level down. Moving them found **three states the copy accepted that the real program refuses**. `_scan_elf` is deleted rather than left unused. | `PROGRESS.md` carried-forward, `tools/hazlint` |
| 🔴 讀 | **`grep -r` over `arch/rlx` has never seen the BSP** — 13 files, 91,549 bytes — and fifteen zero-claims re-run under `-R` are **all unrefuted**, with two positive controls firing. **But the prescribed fix is worse than the defect where it does not apply**: at a drop root `-R` reports 79,857 paths for 66,977 real files (+19.2 %) and follows three `romfs/tmp -> /var/tmp` links out of the tree, where four of this project's own analysis logs live and would be reported as vendor content. A canary was planted to measure it. | `notes/kernel-build.md` §10, `TC-27` |
| 🔴 讀 | **`arch/rlx/bsp` is a dangling symlink in two of the three GPL drops**, because `target` is a tracked symlink only in `rtl819x-toolchain`. A BSP question re-run across the three returns 13/0/0 and reads as *only one drop has it* — which is false. The BSP is in all three at `boards/rtl8196e/bsp/`, 12 files, byte-identical but for one `#if` on the reboot path. **Every BSP reading must go through that path.** | `notes/kernel-build.md` §10.4, `TC-28` |
| 讀 | **`arch/rlx` defines no `sched_clock`**, so `printk` timestamps come from the weak jiffies generic at `kernel/sched_clock.c:39` and not from CP0 `Count`, which this die does not implement (`F50b`) and which would have printed `0.000000` on every line. Real at 10 ms — and the `0.000000` → moving transition marks where the timer interrupt started. | `notes/kernel-build.md` §11.6, `TC-31` |
| 讀 | **A row of `SPEC.md` had been outside two of its own checker's checks since the day it was written, and the checker reported it as *skipped*.** One unescaped `\|` inside a backticked span gave the row eight cells in a seven-column table. Every check reads cells **by index**, so the shift did not fail them — they read a different cell and passed on it, and C4/C5 read the *source* column as the owner, found no path, and filed the row under *owner is a gate rather than a file* | **The one row that session existed to upgrade was the one row two checks could not see.** `C8` added — a row's cell count must equal its header's — with a ninth mutation that is not invented: it re-creates that exact escape. Pointing the same scan at the rest of the repository found **two more**, both mis-rendering their tables since they were written · [`tools/spec-check.py`](../tools/spec-check.py) |
| 量 | **A 0.26 % deviation reported as a property of the board was a property of the stopwatch.** The interval had been hand-timed over 61.842 s; a human reading a clock is good to about ±0.15 s, and ±0.15 s in 61.8 s *is* ±0.25 %. Re-measured over a 2,080-second baseline: **200.0049 MHz ± 7 ppm** | Two published numbers superseded, and the compiled-in `0x0BEBC200` turns out to be right to 24 ppm · `SPEC.md` `CLK-02` |
| 量 | **The ESC heartbeat's achieved period is not the requested one** — 20.35 ms for a requested 20.00, and 2.32 ms for a requested 2.00. The difference is a fixed ~0.3 ms of host overhead, and the floor of the loop is 29 µs | The grid every interval is quantised to is now **measured per capture and recorded in its metadata**, with a case that proves the field is not repeating the argument back · `tools/console-capture.py` |
| 讀 | **`hazlint`'s population control built its words with a private helper**, so it exercised a code path no scanned file ever took. It could not have caught any defect in span construction and it looked exactly like it could | Found by an adversarial review that confirmed 17 of 24 claimed properties. The suite went 42→56 with a mutation per fix · `SPEC.md` `TC-07` |
| 讀 | **`readelf --dyn-syms` reports nothing on this unit's binaries** — they have no section headers — which is indistinguishable from clean | An acceptance condition whose check could not have failed. Replaced by a `PT_DYNAMIC`/`DT_SYMTAB` walk and `nm --undefined-only` before strip, which are different **in kind** · `notes/rootfs-census.md` |
| 量 | **`NET-13` was withdrawn twice in one day — the finding and my correction of it.** The recorded jack order rested on labels assigned after the fact; the linear map that replaced it was checked by a cell whose expected value had been derived from the map it was supposed to test | The jack map is **undetermined**, and the deciding experiment writes the jack into the capture's filename · `SPEC.md` `NET-13` |
| 量 | **A published interval had no anchor, and four defensible anchors differ by up to 1.7 ms.** `CLK-15` recorded 344.7–356.9 ms for the silence after `Booting...` without saying which byte it started from. Only *the NUL after `Booting...\r\n`* reproduces that range on its own nine captures; the other three do not | The anchor is part of the definition now, and the check requires the other three to **disagree** — otherwise "this is the one it used" would be a guess. **Two adjacent silences of the same length is how a measurement ends up wearing another one's name**, and this repository had already recorded that happening once · [`tools/boot-timeline.py`](../tools/boot-timeline.py) |
| 量 | **`CLK-15` said cold and warm resets give the same silence. They do not, and the difference survives inside one power cycle.** Split by the loader's own warm-boot line rather than by the artifact byte — which would have made the conclusion true by construction — cold is 348.0–356.9 ms (n=7) and warm 338.2–347.6 ms (n=7), non-overlapping. Within `2026-08-24c`: +4.5 ms. Within `2026-08-25`: +14.5 ms | Part of that row's recorded 3.5 % *unexplained* spread was two populations read as one. **The mechanism is undetermined and the experiment that decides it is two `DW`s** — the SPI controller's registers after a cold boot against after a warm reset · `SPEC.md` `CLK-15` |
| 讀 | **A negative control I had just written was counting coincidences.** `probe2`'s `restore.stillhandler` read 20 on a run whose restore was perfect: ten of the handler's 22 words are `nop` and the emulator's vector page starts as zeros, so every one of them matched by accident | It counts only positions the install demonstrably changed now. **Found by the first harness run of the fixed payload**, which is the argument for running the harness before the bench rather than after · [`tools/rlxprobe/probe2.c`](../tools/rlxprobe/probe2.c) |
| 讀 | **A suite that cannot reach a state cannot test the code that produces it.** `probe2`'s census tells *the register answered* from *nothing wrote the destination* by priming `$v0` before the call — and the emulator's `mfc0` always writes `rt`, so that state never occurs under it | One of the four mutations builds a payload with a single census stub emitting `nop` and requires that row to report *not written*. Without it the whole mechanism would have shipped tested by nothing · [`tools/test-rlxprobe.sh`](../tools/test-rlxprobe.sh) |
| 🔴 讀 | **The kernel's `icache: 16kB/16B, dcache: 8kB/16B` line is a printed build constant, not a probe.** Every value is a `#define` in `arch/rlx/bsp/bspcpu.h:12-22`; `cache-rlx.c:378` only prints them, and the same file uses them in `#if` preprocessor conditionals at `:99`, `:438`, `:649` | **This is the trap `R1h-4`'s DoD names by name** -- a geometry number wearing a measurement's clothes. `probe3`'s walk is the only 量; they agree, and that is corroboration rather than one number. The same line carries `dcache: 8kB`, for which **no measurement exists at all** |
| ⚠️ 算 | **`G0`'s pointer-shape refutation fires on noise.** *"Any one pointer-shaped word and the address is re-picked"*, applied to 64 words of uniform random data, fires with probability 1-(63/64)^64 = **63.6 %**; even the *aligned* reading fires at **22.2 %**. 2026-08-29 was its first run: two of the 64 arena words carried a listed prefix and neither was aligned | A refutation condition that fires on two thirds of random samples is not one. It needs a rate, not a presence test. The shape on that list with real power is *any two windows byte-identical* -- a stuck read path, which nothing else there catches, because bias garbage is supposed to look like structure |
| 🔴 量 | **Forty green cases, and ten mutants of the guard they had just been written for were alive.** `console-capture.py` gained a refusal for a capture with no terminator and its suite went 29 → 40, all green. A mutation pass over **25** edits of that guard killed 15. The four classes the forty could not see: a **waiver** (`if args.esc > 0: return` in front of the guard — and `--esc 25` is `A-catch`'s own shape; 量 on a pty that mutant gives `rc=124` with the `.meta.json` lost, which is the failure the guard was added for), the **contract** (not one case asserted an exit code, so `SystemExit(0)` was green; none looked at stdout, so a refusal printed there was green), the **message** (one substring, so a refusal naming a flag that does not exist passed), and the **position** (the guard below the overwrite refusal passed) | **A suite's own green is a claim about the suite, not about the tool.** Six cases close the four classes, `N28` on the refusal's FIRST line because a substring test over a fourteen-line message is nearly unfalsifiable, and `N29` because `N21` only appeared to pin the guard's upper edge — it sends 127 characters, a length `_check_send` accepts, so it passes either way. **The 25 mutants are now a committed suite that runs the whole file per mutant and refuses if the unmutated baseline is not green** · [`tools/test-console-capture-mutants.py`](../tools/test-console-capture-mutants.py) |
| 🔴 量 | **Nine rows of this page were rendering as paragraphs of pipes, and a checker found them because it was generalised.** `spec-check`'s `C8` checked one file's table rows against their header. Pointed at all 71 tracked `.md` it found **eight** ragged rows where the census behind it had said six — that census had looked at three files. Generalising it exposed three shapes it could not see at all: a row split over more than one physical line (a literal newline inside a code span, which ends the row there and hid every row below it in the same table from every checker here); a `\|`-line belonging to **no** table, which `C8` cannot see by construction because `C8` walks tables — **nine rows of this file, including its three newest findings, stranded by one blank line**; and a code span whose whole content is whitespace — **four of those**, and the fourth is the carried-forward row that describes the defect, which only turned up when the finished checker was run over the original tree at the end of the session; the count assembled one file at a time, in the order the defects surfaced, had said three | **One of them made a *reading* wrong, not a rendering.** `docs/loader-command-semantics.md` annotated **both** exits of `readline`'s three-way branch with the same empty character, so *"only one writes a terminator"* named neither. Settled by disassembling this unit's own loader: `li v0,10; beq` is the LF exit and returns with no NUL, and the NUL at `0x804070fc` sits in the delay slot of the CR path's jump. The same blank-line defect was recorded against `SPEC.md` in August and found then by an adversarial pass; this time a checker found it · [`tools/spec-check.py`](../tools/spec-check.py) |
| 🔴 量 | **The kernel that booted on this silicon could not be rebuilt from its own recorded configuration, and the whole difference was one compiler flag no committed file carried.** `.text` 2,444,228 against 2,427,448 from the same `.config-installed`, byte for byte, on the same host. Everything that could explain it was measured and did not: pinned drop at `HEAD 5c9be5d9`, clean worktree, **0** ignored files; **0** differing `.config-built` lines; **0** differing compiled-file list entries (599 each); **0** differing marks; identical symbol sets both ways; and `-j4` against `-j8` gives the same `.text` to the byte, so this tree's header-rewriting Makefile is excluded rather than assumed. The difference is `-fno-if-conversion` — 量 over **all 746** `.cmd` files kbuild writes per object: **588 differ and there is exactly ONE difference shape** | **A rebuild that followed every committed file produced a kernel with SEVEN load-use violations in it, and every gate here stayed green** (量: `hazlint` on that build reports 7, each a `movz` reading the register a `lw` two instructions earlier writes). The flag is `TC-25`, which takes `hazlint` 7 → 0. Three files declare 36 kconfig symbols, 31 image entries and 15 source insertions with a reason each; this one was in none of them. `config/rlxfw-cflags` declares it now and `rlxfw-kbuild.sh` refuses without it · [`notes/kernel-build.md`](../notes/kernel-build.md) §18.5 |
| 🔴 量 | **The leak gate reads 240 of the 899 paths `git ls-files` returns, and `upstream/`'s 302 are invisible to it by construction.** A submodule is one gitlink, so every sweep built on `git ls-files` has read **zero** of them. The 658 unread tracked files plus those 302 hold **100** hits on the four patterns that can identify a unit — **28 inside `bench/` itself**, including the two host-side `tcpdump` captures, which are `.txt` where the gate globs `*.log`. 量, counts only and no value printed: exactly **one** MAC on this model's vendor OUI exists in the corpus, in **seven** files, **four of them in a public repository** | ⚠️ **The gap itself was already recorded** — `bench/README.md`, the same day, names the gitlink and says `audit-bench-log.py` *"has never looked there"*, and that `upstream/BENCH-LOG.md` prints actual `H601` byte values. What this row adds is the scan, the split that makes a count mean something, the 658 unread tracked files, and an instrument. **The gate scans what an instrument wrote and not what a person typed**, and a person is the one who can mistype an address into prose. ⚠️ Whether that address is this unit's is **undetermined**: `FW-17`'s default-SSID correlation needs a `N150RT_xxxxxx` string and 量 there are **zero** in either repository. `tools/leakscan.py` never prints what matched — the question is whether an address is in a published file, and printing it answers that the worst possible way · [`notes/kernel-build.md`](../notes/kernel-build.md) §18.7 |
| 🔴 量 | **The instrument that made that finding had the value, the vendor and two published counts all wrong, and the fix was to change the kind of evidence.** `FC:19:28` is **Actions Microelectronics**, not TOTOLINK; the value is the **workstation's own USB GbE adapter** — byte-identical to the `enx<12hex>` interface name in nine tracked files, the source of the echo **replies** 4/4 in both host captures, and **0** occurrences in this unit's 4 MiB dump. The attribution recorded as *undetermined* was determinable the whole time: `FW-17`'s SSID route was unavailable and it was never the only one, and **the arbiter had been on the same disk for two weeks**. `leakscan --attribute` replaces prefix recognition with a lookup in the dump | The instrument's own author was the unreliable component · [`notes/leak-surface.md`](../notes/leak-surface.md) §1 |
| 🔴 量 | **The pattern written to catch this unit's MAC in bare form cannot fire on this unit, and it looked like it was working because it fired on something else.** `MAC, bare 12 hex` is OUI-restricted to `00e04c` and `fc1928`, and neither is this device's. It is not fixed by adding the right OUI — that publishes half the address in a committed file — but by asking the dump. A ninth pattern, `MAC, enx interface`, closes a second blind spot: `\b` does not fall between `enx` and the first hex digit, and 量 **0** files under `bench/**/*.log` carry that form, so the gate went red on nothing | A check that can only ever return 0 · [`notes/leak-surface.md`](../notes/leak-surface.md) §2 |
| 🔴 量 | **This unit's own MAC is in a public file, and it is not the value that was flagged.** One `UNIT` classification in 161 identity hits: `upstream/BENCH-LOG.md:216`, in the dump twice, both inside `H601`, labelled 「（裝置）」 — with the flagged workstation adapter on the line above it. **45 of `H601`'s 146 non-zero bytes are recoverable from the public repository**; `HW_WLAN0_WSC_PIN` and the region checksum are not. Left in place, as a recorded decision: the device is end of life | Left in place as a recorded decision · [`SPEC.md`](../SPEC.md) `FLS-22` |
| 🔴 量 | **A coverage measurement whose own positive control passed by covering 8,042 bytes with an eight-byte slice.** The first pass reported 98.9 % of `H601` recoverable from public text; `H601` is 8,046 zero bytes out of 8,192 and an all-zero candidate matches everywhere. **A control whose reading is absurd is still a reading**, and it is the only thing that caught it | A control whose reading is absurd is still a reading · [`notes/leak-surface.md`](../notes/leak-surface.md) §4 |
| 🔴 量 | **A mutation pass reported eight of eight killed and every kill was invalid.** All eight were "killed by C7b,C7c" — the same two controls — and the unmutated tool through that same symlinked temp root was already **22 of 24**: those two resolve the repository root with `realpath`, which a symlink farm sends back to the real tree. **A kill that happens for a reason unrelated to the mutation proves nothing**, and the missing control is the one `test-spec-check-mutants.py` already has: run the UNMUTATED tool through the harness first. Re-run in the real tree: **5 killed, 3 alive**, and the three named real gaps in `flashwin normalise` — a garbage data line under a valid echo, a reply with no data lines, and a normaliser that sorts the words | The all-same-killer pattern is the tell · [`tools/ci-expected.tsv`](../tools/ci-expected.tsv) `flashwin` |
| 🔴 讀 | **Three header words appended to a payload's block all landed on ones already in use, and a collision is silent.** `H_BMP_KEPT`/`H_KSEG0`, `H_LAYOUT_BMPK`/`H_G_TIMER` and `H_BMP_FRESH`/`H_T_SEP_A` shared words 48, 49 and 50 of `probe3`'s header for one session -- the seventeenth and the eighteenth are two segments of 2026-08-31, so the window is hours and not days. Both defines compile, both `rb_put` calls run, and **the later stage wins** — so `rbcheck` would have read the retained region's offset as `g_timer` and recounted the header, and Group W would have taken `H_KSEG0` and half of the separated pair. `H_T_SEP_B` at 51 does **not** collide, so `t.sep` would have returned one number of two and looked like a reading | **The comment that placed them reasoned *"44..47 are occupied"* and the census stopped at 47 — the reasoning was right and the census was short.** So the repair is a census and not a better comment: `tools/test-rlxprobe.sh` `Y2c` refuses any two `#define` in a payload that name the same word, with a population control and a mutation that puts the 2026-08-31 collision back · [`tools/rlxprobe/probe3.c`](../tools/rlxprobe/probe3.c) |
| 🔴 讀 | **A value marked *measured* cited the device printing a compile-time constant of its own.** `SPEC.md`'s `FLS-11` and `MAP-12` both gave `0xBD000000` the 量 mark on the strength of the loader emitting `offset 0x003f0000<0xbd3f0000>`. A `lui` immediate census over the whole loader finds `0xbd00` **exactly once**, and it is that `printf`'s argument | **It is `CPU-25`'s `icache: 16kB/16B` one file over — a build constant wearing a measurement's clothes — and it was load-bearing**: trusting it would have meant writing a timing group with no liveness control for a window nothing has read outside Linux. `f-alias` and `f-live` are that assumption turned into two cells that can fail, and neither puts a flash byte in the block · [`SPEC.md`](../SPEC.md) |
| 🔴 量 | **A positive control passed while the check it controls never ran.** `rbcheck`'s `C17` asserted *a consistent retained region produces no finding*, and two mutations that push the tool into its pre-2026-08-31 one-region branch — which reports and never fails — left it **green**. It was found by requiring each mutation to turn the case it NAMES red, not merely to make something red | **A control whose job is to be green cannot be trusted to notice a checker that declined to check.** `C17` now asserts the branch was ENTERED, and `W0` — a kill is only valid if the named case went red — is in three of this repository's mutation harnesses · [`tools/rbcheck.py`](../tools/rbcheck.py) |
| 🔴 量 | **A source line number is a citation that rots silently, and the fix is a format rather than a checker.** Editing `probe3.c` on 2026-08-31 invalidated fourteen `probe3.c:NNN` references at once and an owner audit, not a check, is what found them: the line still exists, so *does this line exist* passes | **A reference now carries the line AND a token from it, matched within three lines**, and `spec-check` `C11` went red twice on the day it was written — on the format's own exemplar from the day before, and on the sixteen references the next payload edit moved. The second time it named the new line for every one, so the re-derivation was machine-directed and no frozen record was touched · [`tools/spec-check.py`](../tools/spec-check.py) |

## It changed what this repository may claim about itself

**These are not about the device.** *(This opened "These four" until 2026-09-02, when three more arrived and made it five, then six, then seven. The number is deleted rather than corrected — it carries no weight in the sentence, which is exactly why nobody ever re-derived it; the same call `docs/KNOWN-ISSUES.md`'s row count got.)* They are about the difference between
what this repository's files say and what its artefacts and its own history
actually contain — and every one of them was found by re-deriving a number
rather than by a checker.

| | | |
|---|---|---|
| 🟢 量 | **An image that has executed on the silicon was rebuilt byte for byte, from its own recorded inputs, on another day.** `rlxfw-r51-20260903.bin` → `39abf11c2d6fd0ce…`, 1,030,144 bytes, `cmp` rc=0, `vmlinux` identical underneath; **n = 3** across two cell names and two days, from a fresh 480 MB stage, with `--config`/`--initramfs` recovered from `<cell>.config-installed` and `<cell>.initramfs.spec` rather than retyped. 🔴 **It is still a property of one workstation's disk**: those inputs are in `$FWRE_WORK`, which no clone has and which the next build of the same cell name overwrites (`CFG-1`) | `P4a` measured two builds minutes apart; this is the one that matters · `notes/reproducible-build.md` § 4.1, `notes/dev-loop.md` § 11.2 |
| 🔴 量 | **A flag documented as a convenience was the only thing making the tool runnable, and nothing knew.** Block 7's card explains `--skip S2,S3` as *the image is staged and rebuilding at the bench would spend 40 s of the scarcest resource here* — true, and not why it was needed. `S3` assembles from the tree `S2` stages, and **nothing carried the path between them**: `--cell-top` defaulted to a literal placeholder string and `rtkimage build` exits 1 against it. Proved by running `--mode desk --skip S2` and watching it stop, not by reading | **`--mode bench` without that flag had never been runnable, and 26 controls could not see it because every one of them either skipped both stages or ran in `--mode replay`.** A defect that only the *unskipped* path can show is invisible to a suite that never takes it. The path is now read out of the driver's own `make -C` line, and a driver run without that line raises at `S2` rather than letting `S3` assemble from somewhere else · `SEAM-1`, `notes/dev-loop.md` §10.3 |
| 🔴 量 | **The gate that created the gate ledger did not put itself in it.** `P4b-2` wrote `docs/GATE-RESULTS.md` with five entries on 2026-09-01 while `P4b-gate` was closing the same day, and that file's own `D2` is *one entry per closed gate*. The only exclusions it documents are `S0` and `R0`, on a hindsight-contamination argument that does not reach a gate which closed one day earlier with no gate closing in between | **A completeness property stated in a DoD with no checker behind it.** Found by reading, when `R4-4`'s own step row — *gains its sixth entry* — turned out to name the wrong ordinal. The entry was written and placed in close order, so the operating clause reads the sequence it would have read on the day · `docs/GATE-RESULTS.md` |
| 🔴 量 | **A tool's control count was quoted from a partial view one minute after the count changed.** `2a379ed` took `looprun` from 23 to 26 controls; the prose written after it — `PROGRESS.md`'s Active step, the same file's Last session, `LOG.md`, and the bench card's own §4 — all said 23. `tools/ci-expected.tsv` said 26 throughout, because `ci-census` re-derives that number from the run | **Nothing re-derives a control count that appears in PROSE**, and `ci-census` compares the table against the run and never against the write-up. Four places, one commit apart, and the machine-checked copy was the correct one · `PROGRESS.md`, `bench/2026-09-02/PREDICTIONS-B8-block7.md` |
| 🔴 量 | **Two builds of one tree, run back to back, differed in 84 of 3,935,472 bytes — and 78 of them were the initramfs packer's clock, not the kernel's.** `usr/gen_init_cpio` stamps every directory, symlink and device node with `time(NULL)` (three sites) and every regular file with the source file's `st_mtime`; the declaration is 13 + 8 + 5 + 5, and 13+8+5 = **26 entries × 3 bytes = 78**. The kernel's own `UTS_VERSION` accounts for the other 6, in two copies (`linux_banner` and `init_uts_ns`). **Zero** differing bytes in `.text` | **The standard fix everyone reaches for — freeze `KBUILD_BUILD_TIMESTAMP` — is 7.1 % of this problem**, and it is the only part that costs anything: the kernel's stamp is one of three strings that prove the running image is not the vendor's, and the packer's clock is not. So the expensive-looking trade-off was one fourteenth of the work and the cheap 92.9 % was free · [`notes/reproducible-build.md`](../notes/reproducible-build.md) |
| 🔴 量 | **A comment byte is a source byte and it does not change the image, so this gate's own positive control is false as written.** `P4a`'s DoD says *changing one source byte changes it*. One character of a string literal moves the sha256; one character inside a comment in the same file leaves it byte-identical. The second was PREDICTED before it ran, because a control that can only come out one way is not a control | **A DoD is refutable too.** The wording needs *that reaches the image*, and the defect is recorded rather than quietly repaired — the gate closes with its own sentence marked wrong · [`notes/reproducible-build.md`](../notes/reproducible-build.md) |
| 🔴 讀 | **A gate asserted *eleven marks* through a pattern a twelfth mark did not match, and stayed green while the file declared twelve.** `test-config-gates` counted `^  B[0-9]`; `ID0` is not a `B`. The build driver counted the same way and would have printed 15 for 16 applied rows | **A checker reading a pattern narrower than the claim it makes is invisible exactly when it matters** — when something new arrives. Both now read the tool's own total, and the ladder count is pinned separately from it · [`PROGRESS.md`](../PROGRESS.md) |
| 🔴 量 | **17 of this repository's 59 CI runs are red, and nothing had ever counted them.** `gh run list --limit 200`, 2026-09-01: 41 success, 17 failure, 1 in flight, the failures spanning 2026-08-28 to 2026-09-01. Six are diagnosed here; **eleven have never been looked at** | **Three consecutive sessions stated this number and all three were wrong** — *three*, then *four*, then *five* — each read off `gh run list --limit N` for an N chosen while typing. `--limit N` is a window and was three times mistaken for the population. The number does not carry any of the sentences it appeared in, which is why nobody counted it · [`docs/KNOWN-ISSUES.md`](KNOWN-ISSUES.md) |

---


## It changed the shape of the plan

| | | |
|---|---|---|
| 🔴 量 | **An incidental reading from 2026-08-24 decided a question asked fourteen days later, and it reversed the answer.** Seating 11 concluded `TC1IP` does not latch while `GIMR`'s `TC1IE` is clear. **Two enables were clear at once** — `TCIR` bit 30, the timer block's own, and `GIMR` bit 9, the controller's — and the reading cannot separate them. `RUNSHEET` `C5` (量 2026-08-24, an unpredicted half of a cell about `phy_read`) shows `GISR` moving `88000004` → `88000104` → `88000004` **with `GIMR` bit 8 cleared by hand**: on this die a `GIMR` mask stops *delivery*, not *latching*. So the cause is `TCIR.TC1IE = 0`, a bit `rtl819x_tc1_arm()` never writes. 🟢 **`R5-3`'s masked-observation strategy is therefore not refuted and it gets a zero-risk cell back** | The best argument here for writing down a result nobody asked for · `notes/timer-driver.md` § 9, `docs/interrupt-map.md` § 3.2 |
| 🔴 量 | **`R4`'s headline was NFS root, and the loop it shortens is not the loop this gate exists to serve.** The TFTP upload NFS root removes is **1.545–1.639 s**, which is **2.2–3.3 %** of a machine pipeline of 50.4–71.1 s — and it removes **none** of it for a change to a kernel source file, because a kernel still has to be built and uploaded whatever the root filesystem is. `R5`, the gate `R4` exists to make fast, is six **kernel** drivers. 讀 the same day: `config/rlxfw-kernel.delta` carries no `CONFIG_NFS_FS`, no `CONFIG_ROOT_NFS` and no `CONFIG_IP_PNP`, so it was never a setting to flip either | **NFS root leaves `R4`.** The step list had written that decision down in advance so it would be visible if it went this way, and it went this way · [`notes/dev-loop.md`](../notes/dev-loop.md) §9 |
| 🔴 量 | **The gate's stop-loss was written to protect a trade that does not exist.** `--keep` was expected to buy a large speedup at a large cost to `P4a`'s reproducibility claim. It buys **3–5 %**, because `make vmlinux` recompiles all 599 objects with nothing touched — twice in a row, with no `.config` re-install — and it **refuses to run with `--marks` at all**. Its cost is **2 bytes of 3,968,240**, both the `#1`/`#3` of `.version` | **`R4-3` inherits a different question, and a bigger one**: `bounds.h` and `asm-offsets.h` are rewritten with byte-identical content and a fresh mtime on every make, and **580 of 731** `.cmd` files depend on one of them. Nothing to do with `--keep`, nothing to do with `P4a` · [`notes/dev-loop.md`](../notes/dev-loop.md) §5 |
| 🔴 讀 | **The threshold `R2b` was going to decide on is below the score of a program that shares no source with it.** `plan/router-rebuild-plan.md:1128` sets `FLOOR = binsim(unit-2018, v3.4.0)` on `boa` = **0.0650**, and two *different programs* out of one tree reach **0.1581**. So "above `FLOOR`" was satisfied by a pair with nothing in common, and the plan's warn band covered the whole no-evidence region | **The rule the gate's last step will apply had to be rebuilt before it was used once.** The metric prints `REFUTED` and exits 1 rather than substituting a better number · [`notes/binsim.md`](../notes/binsim.md) |
| 🔴 讀 | **A similarity threshold is not a number, it is a number *at a denominator* — and the first replacement floor ignored that.** `binsim` divides by the smaller feature set. The cell chosen first cleared the no-shared-source level by 0.65 pp while being read on a set **1.46×** larger than the one the rule divides by; at a matched denominator the ordering reverses, and a pair sharing its **whole upstream source** across a compilation-model change scores **0.1212**, *below* the **0.1551–0.1581** a pair sharing **none** reaches | **The three-band rule gains a gate instead of a band**: a comparison across a compilation-model change is **VOID**, not a fail, and the container fingerprint is checked before the score is read. The floor is now the no-shared-source level itself, asserted as the top of a three-program population at one denominator · [`notes/which-drop.md`](../notes/which-drop.md) |
| 🔴 讀 | **"Two different programs" is not "two programs that share no source", and the corpus is emphatic.** Inside this unit's own rootfs, **422 of the 630 cross-program cells** over its 36 largest programs sit above 0.1646 — topped by `sysconf`/`timelycheck` at **0.9967**, two vendor tools that share their source | Any floor read off "a different program" is reading whatever the vendor happened to build out of shared code. The population a threshold is taken over has to be stated, and restricted by size · [`notes/which-drop.md`](../notes/which-drop.md) |
| 🔴 讀 | **Product line is crossed with the clustering, not confounded with it** — the opposite of what this project had been saying about its own corpus. From `/etc/version`: N150RT appears in all three clusters, the lowest within-cluster cell (0.9740) is above the highest between-cluster one (0.8951), and the vendor's own version number is refuted the same way — two images both stamped V2.1.6 land in different clusters | **The clusters can be read as build generations.** What stays collinear is date and SDK generation, which is a tautology rather than a confound · [`notes/which-drop.md`](../notes/which-drop.md) |
| 讀 | **One upstream program held constant across six trees separates "the toolchain moved" from "the source moved" on one edge.** BusyBox 1.13.4 is in all six, so its cells move only when the code generator's output does: across 2016→2018 it holds at 0.9995–1.0000 while `boa` drops 8.7–11.0 points. Across the compilation-model change both collapse and nothing separates them | 🔄 **2026-08-28: the first half stands and the second is refuted by the rebuilds.** `R2a` is a toolchain test; **`R2b` is not a drop test on this channel**, because a `-march` change costs 0.3360 and changing the program costs 0.9359 · [`notes/which-drop.md`](../notes/which-drop.md), [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 🔴 讀 | **One compiler flag costs more of the similarity score than changing the program does.** Six rebuilds of one program, every factor held but one: `-march` alone (4181 vs 5281, one source, one `.config`, one gcc, one binutils) is **0.3360**; the toolchain generation alone is **0.2132**; swapping Realtek's 8196E `boa` for Actiontec's superset fork of it — 27 files modified, 28 added — is **0.9359**. Jaccard agrees (0.2009 against 0.6371). ⚠️ Not pre-registered: it was built to interpret a number that could not be read on its own | **The instrument `R2b` was to decide on measures the code generator, so identifying a GPL drop through it was the wrong axis**, and `TC-02` stays 推 for a harder reason than a weak score. It also closes the option of reconstructing a config from the shipped binary: the vendor's own five configs for this SoC span 0.9347–0.9976, at most 0.065 against a 0.156 shortfall · [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 🔴 讀 | **Twelve shipped userspace binaries across six firmware trees report zero load-delay violations**, at 19.71–28.71 % nop-after-load, read on the same `[DT_INIT, DT_FINI)` window by the same tool. The positive control for that zero is in the same table: the `-march=5281` rebuilds of the same source return **5,224–10,494** | **This unit's userspace, like its kernel, was built for a core that exposes the load delay slot** — so a toolchain that does not pad it would be the first in this corpus. ⚠️ The channel fixes the *side*, not the generation: `rsdk-1.3.6-4181` with `-fuse-uls` is indistinguishable here and is killed by three other channels · [`notes/rebuild-vs-shipped.md`](../notes/rebuild-vs-shipped.md) |
| 推 | **A matrix over six *shipped images* cannot name a GPL drop, whatever it scores.** The gate's own refutation condition was satisfied — the fifteen scores span 92.8 pp against a 5 pp bar — and the answer is still undetermined, which means that condition was never the binding one | **`TC-02` stays 推, as a conclusion rather than a deferral.** What the matrix does instead is exclude the 2019/2020 generation and set the bar the rebuild must clear · [`SPEC.md` `TC-02a`](../SPEC.md) |

| | | |
|---|---|---|
| 讀 | **A gate dependency inherited from the first version was false.** Traced component by component, `R3` needs nothing from `R1`: it builds on the vendor toolchain, which is an existence proof, and the codegen safety net is independent | `R1` split into `R1-gate` and `R1-pub`. **`R3` moves from cumulative segment 61 to ≈41 with no work removed** — what moves is when you find out whether the kernel boots · `PROGRESS.md` Corrections |
| 讀 | **Two public Linux trees exist for this SoC, one carrying a device tree for a TOTOLINK board on this exact part**, and neither was in `SOURCES.json` as a source | The response is not to cut `R5`: it gains a `driver-diff` — write blind, then diff register by register, and **send the disagreements back to the silicon to decide** · `SOURCES.json` |
| 量 | **31 of 55 ELFs on this unit's own rootfs carry `system` or `popen`.** busybox carries neither; `dnsmasq` carries `popen` and the vendor's `udhcpd` carries `system` | Two components `R7` had named had to be replaced by ~350 lines of our own · [`notes/rootfs-census.md`](../notes/rootfs-census.md) |
| 讀 | **A key-derivation choice had never been costed in memory.** scrypt at meaningful parameters wants 16 MiB of the 26 MiB this kernel gets, from one unauthenticated request | The rate limiter moves in front of the KDF, and the parameters are set by an anti-DoS budget · plan D8 |

## It answered a question that had been open

| | | |
|---|---|---|
| 🟢 量 | **`J BFC00000` is a reset, on this silicon, twenty-one times out of twenty-one.** Read out of four sources since 2026-08-24 and never issued. Seating 9 issued it 21 times across two power cycles: the command echo, then `Booting...`, then `Reboot Result from Watchdog Timeout!` where a cold boot prints a single space — with that cold boot taken in **the same seating**, as the negative control — then a reachable `<RealTek>`. Zero failures in 20 consecutive is a 95 % upper bound of **13.9 %** on the per-reset failure rate, which refutes a reset that fails often and **does not** make the loop reliable. | `SPEC.md` `LDR-33` |
| 🟢 量 | **A watchdog reset clears the TFTP length global an `FLR` writes, so a flash read and an upload fit in one power cycle.** `docs/loader-command-semantics.md` records that no `put` may follow an `FLR`. Seating 9 ran four `FLR`s, then `J BFC00000`, then a 1,029,120-byte upload accepted in 1.54 s that booted to a shell byte-identically to three earlier boots — **with the power switch never touched**. | `SPEC.md` `FW-32` |
| 🟢 量 | **Twenty watchdog resets change nothing in the four flash windows this project samples.** 1,024 bytes at `0x000000`, `0x060000`, `0x006000` and `0x006400`, all byte-identical to the 2026-08-16 dump, with all four pre-reads differing so *the `FLR` wrote* is measured rather than assumed. 🔴 **0.0244 % of the part** — it cannot see a write outside the windows, or two writes that cancel. | `SPEC.md` `FLS-20` |
| 🔴 讀 | **The kernel's flash path is ~16× the loader's stage 1, and the one candidate that was worth a factor of 4 is refuted: both read a 32-bit word at a time.** What replaces it was not on the list. **≤9×** — stage 1's copy loop executes at `0xBFC001D0`, KSEG1, uncached by architecture, so its eight instruction fetches per iteration are themselves reads of the SPI device it is copying from: 9 uncached word reads per 4 bytes, against the kernel loop's 1 from I-cache-resident text. **4×** — stage 1 writes `SFCR` zero times in 4,848 bytes while stage 2 writes it twice, so stage 1 runs at the reset default `SPI_CLK_DIV = 111B` (DIV 16) and everything after runs at the `001B` (DIV 4) `REG-13` measured | **An `undetermined` becomes 1 × 4 × ≤9, with the product over-predicting the measurement by 2.1–2.3× — and that gap is a term the model does not have, because the measured figure is `busybox wc`'s end-to-end rate and the model is of the SPI bus alone.** 🔴 **An adversarial pass the same day cut what this is entitled to say**: the 2.1–2.3× is a **lower bound** (351.9 ms is `CLK-15`, which owns the silence and not the loop, and anything in it that is not SPI makes the over-prediction larger), and the free cell that would test it reads out the **product** of the two factors rather than either one. 🟢 It also closed a gap: the stage stage 1 copies to `0x80100000` writes `SFCR` zero times too, so the reset default holds across the whole path. 🟢 The same disassembly re-derives **20,924** from two immediates, which is the first time this repository can show where that number comes from. ⚠️ The reset default has one kind of source — the datasheets — and nothing here has ever read `SFCR` before stage 2 runs. One unknown is left, the window's prefetch behaviour, and it has a cell that costs no power cycle · [`notes/kernel-build.md` §19.7](../notes/kernel-build.md) |
| 🔴 量 | **`TC-21` is asserted rather than assumed: the same six sources give 0 and 11.** `hazlint` over every `.o` under `arch/rlx` on four trees reads **0 violations in 1,607–1,685 loads**; the same sources re-assembled from the build's **own recorded `.o.cmd` line** with `-Wa,-march=5281` appended and nothing else changed read **11** — `entry.o` 5, `genex.o` 1, `strlen_user.o` 2, `strnlen_user.o` 2, `strncpy_user.o` 1, `scall32-o32.o` 0 | Eleven load-use hazards in hand-written kernel assembly are prevented by the assembler's default `-march` and not by the author, and that is now a paired measurement instead of an inference from a whole-image table. ⚠️ The enumeration nearly missed it: `find` reaches 57/58 objects and `find -L` 63/64, and the six it cannot see are the BSP · [`notes/kernel-build.md` §14](../notes/kernel-build.md) |
| 🔴 量 | **The boot ladder printed before the device was plugged in, and `RLXFW-B07` has both of its values.** Through a MIPS32 emulator with `prom_putchar` redirected: `RLXFW-B00` … **`RLXFW-B07=FFFFFFFF`**, eight marks in order, then `bsp_machine_halt`. **`B02` prints `00018000`** — the emulator's `PRId` — and neither `00018000` nor `0000CD01` occurs as a literal in the image | `B07=00000000` is the pass and `FFFFFFFF` is the switch core not answering, **read off a wire instead of inferred from the absence of everything after it**; and `B02` is demonstrated to be a run-time read before the power cycle that depends on it. The control is an unmarked image printing 0 bytes through the same channel · [`notes/kernel-build.md` §15.4](../notes/kernel-build.md) |
| 🔴 量 | **Realtek's `rtkload` pipeline reproduces the drop's own shipped `nfjrom` byte for byte** — 854,016, sha256 `5cc8d61d4b4e8914`. Of five artefacts, four reproduce exactly and `memload-full` differs by 492 bytes that are entirely `DW_AT_comp_dir` (101 characters against 58, ten translation units, +43 each) | **`TC-d`'s image half closes**, and the wrapper stops being a thing this project runs hopefully. That `memload-full` differs is what proves the identity is not a file compared with itself · [`notes/kernel-build.md` §13](../notes/kernel-build.md) |
| 🔴 讀 | **The core is a Lexra RLX4181, named from a `PRId` assignment table in the vendor's own kernel source.** `PRID_IMP_RLX4181 = 0xcd00` in `arch/rlx/include/asm/cpu.h`, against `PRId = 0x0000CD01` measured on this die; `RLX5281` is `0xdc01` and is excluded. Corroborated by the board config, by third-party ports printing the same name for this SoC, and — differently in kind — by this unit's kernel **behaving** like the RLX4181 column: zero `ll`/`sc`/`sync` in 2.85 MB of text, where RLX5281 boards build with both | **`CLAUDE.md` had banned writing either model number and named its own lifting condition; the condition was met by a file already in the tree.** What the ban was protecting against is still real and travels with the answer: one source in three byte-identical copies, a table no code in the port reads, and an encoding that breaks for two of its six entries · [`notes/vendor-kernel-isa.md`](../notes/vendor-kernel-isa.md) |
| 讀 | **Four of the seven CP0-20 (`CCTL`) commands now have a name from a source that states it, and one command had no row here at all.** `arch/rlx/mm/cache-rlx.c` — Realtek, 2008 — gives `0x1 DInval`, `0x2 IInval`, `0x100 DWB`, `0x200 DWB_Inval`. The earlier conclusion *"there is no Lexra-specific cache file"* was reached by listing `arch/mips/mm/`; this SoC builds `arch/rlx/`, a directory this repository already cited elsewhere for the exception vector | 🔴 **RETRACTED 2026-08-26 — both are named, and neither is a cache command**: `0x010 = IMEM0FILL`, `0x020 = IMEM0OFF`, the lifecycle controls of a 16 KiB local instruction scratchpad. Two independent sources — the Lexra LX4189 datasheet § 5.2, and `arch/rlx/include/asm/rlxregs.h` **in the drops this project already held** — corroborated by this unit's own kernel programming a 16 KiB window into `CP3 $0`/`$1` and then issuing `0x010`. **The same search failure as `arch/rlx/` in the left column, one file over.** *(as written:)* `0x010` and `0x020` are **still** unnamed in every source — and are now known to be issued by **two independent implementations on this one unit**, both at reset. The route that would name them from the silicon is writing them, which is declined on a one-device budget; the route left is a document · [`notes/cache-model.md`](../notes/cache-model.md) |
| 讀 | **The cache geometry has its first source cut from this unit**: three `sltiu … 0x4000` thresholds in the kernel's own D-cache routines are `cpu_dcache_size * 2`, so that build declares **8 KiB**, and eight `cache` ops per 128 bytes declares a **16-byte line** | **Three numbers, three different strengths, and they must not be quoted as one.** The line size is readable from the binary alone; the 8 KiB needs the source to interpret the constant; **the I-cache size is not readable by this route at all**. And a build constant is what the vendor *believed* about its silicon — it is a prediction with a refutation condition, not a measurement · [`SPEC.md` `CPU-25`](../SPEC.md) |
| 讀 | **What copies flash `0x060010` into RAM at `0x80500000`**: `check_image()` itself, at `0x80407E44`, with the destination read *out of the flash header* — there is no `lui X,0x8050` anywhere in stage 2. **And the refutation that had been recorded against that attribution was itself wrong**: `check_image` reads `gCHKKEY_HIT` at its 17th instruction, not its first two. The block counter *is* at `0x8040DBA8`; it reads 0 because a later rootfs scan sets it to exactly zero at `0x80407FF4` and then polls ESC | It also explains something nobody had explained: `---Escape booting by user` is absent from every ESC-streamed capture because `doBooting` received `a0 = 0` and took the silent rescue path · `PROGRESS.md` `C-16` |
| 讀 | **The 350 ms of silence after `Booting...` is stage 1 copying 20,924 bytes out of memory-mapped SPI NOR, uncached, a word at a time** 🔄 **(2026-08-31: both halves confirmed from the disassembly, and the 20,924 is now re-derived from two immediates at `0xBFC001C0` and `0xBFC001C8` rather than quoted — `notes/kernel-build.md` §19.7.1)** — and `Booting...` is printed by **stage 1**, which means the experiment this project had written down for it pointed at the wrong binary. Decompression is eliminated **by measurement**: the same LZMA parameters took 1.013 s for the 976 KB kernel, which scales to 17–18 ms here by two independent routes | The DRAM training sweep, which the previous guess named, is ≤32 register writes · `SPEC.md` `CLK-15` |
| 量 | **`C-8` closed on the boot text rather than on the register it was waiting for.** The loader prints `Reboot Result from Watchdog Timeout!` where a cold boot prints a single space — while `WatchDogIND` itself reads *clear* at the prompt, refuting the predicted value | A warm reset is distinguishable from a cold one with no register read at all · `SPEC.md` `CLK-13` |
| 量 | **`0xB8000000` reads `0x8196E001`** — the SoC naming itself, while its own loader prints `chipName: UNKNOWN` | Free and unasked, during a cell about something else · `SPEC.md` `CPU-32` |
| 🔴 量 | **The I-cache geometry, by experiment: 16 KiB, 16-byte lines, 2-way, 512 sets.** Both of 否證 ⓐ's controls fired -- every victim STALE at 1/2/4/8 KiB, every victim FRESH at 32/64 KiB -- and the 16 KiB point reproduced inside the seating. 量 2026-08-29 | `CPU-25` and its §17 residual both close. The three numbers are one structure rather than three readings: `T = 8192` is exactly a two-way 16 KiB cache's way size. ⚠️ Size still cannot separate the I-cache from the 16 KiB scratchpad; **associativity can**, because a scratchpad has no sets |
| 🔴 量 | **The Ethernet cable is on `eth4`**, and the host's own capture says so rather than the board: one interface up at a time, `eth0`-`eth3` silent with **nothing at all in the host capture**, `eth4` 4/4 at 0 % loss with ARP and ICMP both directions. 量 2026-08-29 | The carried-forward *the socket* item is answered for the jack now in use. 🔴 **And the recorded port masks were wrong**: `eth0=0x1`, `eth2=0x2`, `eth3=0x4`, `eth4=0x8`, with `0x10` belonging to `eth1`, the WAN. The old row counted the WAN's mask among the four LAN ports and omitted `0x1` |
