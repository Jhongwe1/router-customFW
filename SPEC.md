# SPEC —— 這個專案握有的每一個數字

**這是一份數值索引，不是任何結論的擁有者。** 每一列帶一個穩定 id、數值、
兩個證據標記、背後有幾個獨立來源，以及**擁有這個發現的檔案**的連結。
語意、推導過程與否證條件都留在擁有者檔案裡；這張表存在的目的，
是讓一個設計決定不必先去把它們翻出來。

房規第一條 —— 一份狀態只有一個擁有者 —— 在這裡沒有被暫停。
**若這份檔案與擁有者檔案不一致，擁有者檔案贏，而這一列是過期的。**
數值變了就在同一個 commit 裡改這裡；一列若說不出擁有者，那列本身就是 bug。

**範圍：一台實體裝置。** TOTOLINK N150RT，硬體 V2.0，二手購入，從未部署。
凡是屬於「這個型號」而非「這台」的值都會註明。凡是只屬於這台的
—— MAC、序號、射頻校正、WPS PIN —— **完全不在這裡**，理由見 §18。

---

## 0. 怎麼讀一列

兩個標記，因為這個專案已經被「數值對、名字錯」咬過兩次
（`RUNSHEET.md` 的 `B3`、`B4`）。

| 欄 | 標的是什麼 |
|---|---|
| **V** | **數值**的來源 —— 這個數字是怎麼來的 |
| **N** | **名稱與語意**的來源 —— 誰說這個欄位叫這個名字、是這個意思 |

| 標記 | 意思 |
|:---:|---|
| **量** | **在這台裝置上量到的** —— 一次讀數，並指名是哪一份 capture。封裝絲印記為「量」而來源記 `P`：油墨是對這台的觀察，不是對它行為的觀察 |
| **讀** | **從程式碼或 dump 讀出來的** —— 靜態，對指名的那份 artefact 為真 |
| **推** | **推測的，待量測** |
| **文** | **讀自文件的** —— datasheet 或廠商標頭說的，既不是這台的量測、也不是它自己的碼。單獨一個 `文` 永遠不夠讓一個值進到程式碼裡 |
| **—** | **尚未確立。** 這一格是刻意留白的，§17 會說什麼能填它 |

> **`V` 是 `—` 的列，值欄一定要帶一個明說的記號**：`留白`／`未定`／`未讀`（開著的問題，§17 欠它一個實驗），或 `§18`／`選定`（根本不是一個待量的值）。**沒有來源標記卻擺著一個值，讀起來就像已經確立。**`tools/spec-check.py` 的 C3 檢查這一條。

來源，而它們的可信度並不相等：

| | 是什麼 |
|:---:|---|
| **A** | 這台自己的 flash dump 與從中切出來的 binary —— `stage2.bin`、rootfs。對「**這台**做了什麼」是決定性的 |
| **B** | 廠商 GPL 原始碼 —— bootcode、Linux 2.6.30、`apmib`。**是不同世代的 SDK。** 它解釋一個常數為什麼是這個值，永遠不能代替 A |
| **C** | `upstream/`，pin 在 `4d3ff26` —— 它在**同一台實體裝置**上取得的量測與報告 |
| **D** | `refs/RTL8196E-VEx-CG_Datasheet_1.1.pdf` —— Draft Rev D1.1，2013-07-17，`-VE1/2/3` 內嵌 DRAM 版。**這台不是那顆**；周邊暫存器地圖共用是一個假設。標 `CONFIDENTIAL`，引用、不提交 |
| **E** | 廠商 Linux 標頭檔 —— 暫存器名稱與位元欄位 |
| **P** | 這台的封裝絲印或照片 |
| **S** | 本 repo 自己的 bench 逐字紀錄 —— `bench/<日期>/*.log` |
| **X** | 第三方公開規格。列出來是為了被否證，永遠不當證據 |

**兩個獨立來源，否則就是未定。** 來源欄只有一項的列，就是還沒達到
`CLAUDE.md` 那條門檻的列 —— 而把這件事寫在表上，正是這一欄存在的理由。

### 這張表怎麼被檢查

`python3 tools/spec-check.py`。八項檢查：

| | 檢查什麼 |
|:---:|---|
| C1 | 每一列都有格式正確且唯一的 id |
| C2 | `V` 與 `N` 欄帶合法標記 |
| C3 | 值標了 `留白`／`未定`／`未讀` ⇒ §17 一定列著它。`V` 是 `—` ⇒ 值一定帶一個明說的記號 |
| C4 | 擁有者欄指到的檔案真的存在 |
| C5 | 這一列的字面值（十六進位字、`0x` 形式、帶千分位的數）**仍然出現在它的擁有者檔案裡** |
| C6 | §18 那條規則：把 `tools/audit-bench-log.py` 的樣式跑一遍，每個命中都必須在一張帶理由的白名單上 |
| C7 | 帶著值的列至少寫出一個來源 |
| C8 🆕 | **每一列的格數等於它自己表頭的欄數。** 2026-08-26 補，而它要抓的缺陷從 `CPU-19` 寫下來那天就在檔案裡：`` `Status.IsC\|SwC` `` 裡那個 `\|` 沒跳脫，於是那一列在七欄的表裡有八格。**其他每一項檢查都是「按索引拿格子」**，所以斷點之後每一欄都位移一格，而它們不會報錯 —— 它們讀到另一格然後通過。C4／C5 把**來源欄**當成擁有者欄，找不到路徑，就把這一列算進「owner 是 gate 而不是檔案，跳過」。**一列被兩項檢查悄悄豁免，而摘要把它印成「跳過」而不是「壞掉」** |

**九個突變，每一個都必須產生一個這個檔案原本沒有的 finding**，而且必須由指名的那一項檢查、用指名的那句話抓到 —— 被別的檢查抓到是一個假的控制，而重現一個本來就有的 finding 不算數。**控制在每次執行時都跑**（跟 `audit-bench-log.py` 同一個理由），任何一個沒立，工具就拒絕對檔案發表意見。

**它抓不到的，寫在這裡免得乾淨的結果被讀成比它更多的東西**：C5 比對的是字面值不是意思，所以散文被改寫而數字沒動會過，數字正確但擁有者檔案本身錯了也會過；它完全不知道一個值對不對 —— 那只有 bench 知道。`plan/` 是 gitignored 的，在 clone 裡驗不了。**沒有任何東西在檢查「兩個來源互相同意」**，那條房規靠讀。🆕 **C8 只數格子**：一個跳脫正確、但被放進錯誤欄位的值，它一個字都看不見 —— 它能保證的只有「其他六項檢查讀到的是它們以為的那一格」。

---

## 1. 身分

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `IDN-01` | 產品 | TOTOLINK N150RT | 量 | — | P、C | `upstream/notes/hardware-inspection.md` |
| `IDN-02` | 硬體版本 | `V2.0` | 量 | — | P（機殼標籤） | `upstream/notes/anatomy-n150rt.md` |
| `IDN-03` | 序號 | *(屬於這台 —— §18)* | — | — | — | — |
| `IDN-04` | 板上絲印 | `0422C`，在 SDRAM 旁 | 量 | — | P | `upstream/notes/hardware-inspection.md` §7.1 |
| `IDN-05` | PCB 廠標／防燃等級 | `JL-2` · UL `94V-0` | 量 | — | P | 同上 |
| `IDN-06` | PCB 日期標 | `18.15` —— 2018 年第 15 週 | 量 | 推 | P | 同上 §6 |
| `IDN-07` | 組裝時間下界 | **不早於 2018-09** | 推 | — | 五個彼此一致的日期碼 | 同上 §6 |
| `IDN-08` | 組裝方式 | 單面貼裝；背面是裸銅、焊點與兩張標籤 | 量 | — | P | 同上 §7.1 |

## 2. 矽片 —— SoC 與 CPU 核心

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `CPU-01` | SoC | **Realtek RTL8196E** | 量 | 量 | **×3** —— P、開機 banner（C、S）、開機碼自己對 ID 的比較（A） | `upstream/notes/uart-findings.md` §2 |
| `CPU-02` | SoC 封裝絲印 | `RTL8196E · I510VG1 · GF23 TAIWAN` | 量 | — | P | `upstream/notes/hardware-inspection.md` |
| `CPU-03` | 晶片 ID 比較常數 | `0x8196E000`，比對的是 reset 時對 `0xB8000000` 的一次讀取 | 讀 | 讀 | A | `upstream/notes/uart-findings.md` §2 |
| `CPU-32` 🆕 | 晶片識別暫存器的實際讀數 | `0xB8000000` 讀到 **`0x8196E001`** —— 前五位 `8196E` 就是料號，末三位 `001` 最可能是版本（*推*：沒有第二個來源這樣切這個欄位）。同一次讀到的 w2 `00000002`、w3 `00100200` 無名。🔴 **而同一次開機的 log 印 `chipName: UNKNOWN`** —— loader 在一個它讀得到的固定位址上認不出自己。這一列直接頂到 `NET-23`（廠商 kernel 的網卡驅動把這顆叫 `8196C`）與 `CPU-03`（比較常數 `0x8196E000`，末位不帶版本） | 量 | 讀（`8196E`）· 推（版本欄怎麼切） | S `B7b`（`DW B8000000 1`，2026-08-24）+ A（`CPU-03` 的比較常數） | `RUNSHEET.md` `B7b` |
| `CPU-04` | **CPU 核心** | 🔴 **`PRId = 0x0000CD01`，量 2026-08-25b**（`bench/2026-08-25b/H2a.log` census row `0x78`，兩個 prime 都回同一個值，`S_VALUE`；`H2g.log` 區塊字 400–402 逐字同意）。**事前寫在 `PREDICTIONS-b4-block1.md` 裡的預測就是這個數字，原封命中。** ⚠️ **值變成量，名字沒有** —— 這個 repo 裡沒有任何來源把 `0xCD01` 對應到 `RLX4181` 而不是 `RLX5281`。那個 `52481` 是這台自己的 kernel 印的十進位，也就是同一個數字換個底，不是一次識別。**所以 `CLAUDE.md` 的禁令不變：不要寫 `RLX5281`**，而現在缺的是一份把 `PRId` 值對到 Lexra 型號的來源，不是一次量測。*(以下為值仍未定時寫的：)* 🔄 **2026-08-25：名字那一側的票數變了，而值那一側一格都沒動。** 指向 **RLX4181** 的有四票：**D**、**一份公開（非洩漏、可再散布）的 `RTL8196E-CG` datasheet**、以及兩棵這顆 SoC 的第三方 Linux port（`ggbruno/openwrt`、`shibajee/linux-rtl8196e`，兩者都用 `CPU_RLX4181` / `-march=rlx4181`）。指向 **RLX5281** 的最強證據是 **`rsdk-1.5.5-5281-EB-…` 這個目錄名 —— 那是一個 toolchain 套件的名字，不是矽片的宣稱**。🔴 **裝置本身仍然兩個都不說，所以這一格仍然是未定，而 `CLAUDE.md` 的禁令不變**  🔴 **2026-08-27：名字有來源了，而 `CLAUDE.md` 指名的就是這一種來源。**廠商自己的 `arch/rlx/include/asm/cpu.h` 是一張 **PRId 指派表**，標頭寫明「Values of the PRId register used to match up various MIPS cpu types」並畫出欄位（bits 15:8 = Processor ID）：`PRID_IMP_RLX4180 0xc100`、**`PRID_IMP_RLX4181 0xcd00`**、`PRID_IMP_RLX5181 0xcf00`、`PRID_IMP_RLX5280 0xc600`、`PRID_IMP_RLX5281 0xdc01`、`PRID_IMP_RLX4281 0xdc02`。量到的 `0x0000CD01` 的 bits 15:8 是 `0xCD` → **`RLX4181`，revision 1**，而 **`RLX5281` 是 `0xDC`，被正面排除而不只是未證實**。🔄 **三個旁證，加上一個並非旁證的一致性檢查 —— 而這一行的第一版寫「四個旁證，其中兩個不是 Realtek 下游」，那是錯的：**① `boards/rtl8196e/config.in` 的 `ARCH_CPU_RLX4181=y`；② ⚠️ **這是一致性檢查，不是獨立證據。** 2.85 MB 程式區裡 `ll`／`sc`／`sync` 各 0 條，對得上 4181 那一欄 —— 但 binary 裡沒有那些指令**正是因為** `boards/rtl8196e/config.in` 這樣設，所以它是旁證①在下游被看到一次，同一件事數兩遍。而且方向上與 `TC-13` 相衝（廠商組譯器對 `-march=rlx4181` **接受** `ll`／`sc`），也與 `arch/rlx/include/asm/atomic.h` 相衝（它在 `#if defined(CONFIG_CPU_RLX4181)` 下發 `ll`）。**推**，而且講的是這次 build 不是這顆矽片；③ 第三方 Linux port 在 RTL8196E 上印 `0000cd01 (Lexra LX4380 / RLX4181)` —— ⚠️ 那些 port 是同一份 Realtek SDK 的 fork，帶著同一張表，所以是單一來源看兩次；④ LKML 的 Lexra 系列定義 `PRID_IMP_LX5280 = 0xC600`，與同一張表的 5280 那一格吻合。**四個裡只有這一個不是 Realtek 下游，而它驗的是 `0xC6` 那一格、不是 `0xCD`。**⚠️ **三個弱點一起記**：三棵樹在這個標頭上逐位元組相同（`md5 c99116184b0e81fb987b7a7f4b4bdbba`，4,422 bytes；🔄 **這裡到 2026-08-28 為止公布的 `ceb6bf89…` 不是這個檔案的任何一種 digest** —— 那是被拿去跨樹比對的那 21 行切片的 md5。宣稱是對的，被當成它稽核軌跡的那個數字不是），是一個來源的三份副本；**這張表沒有任何程式碼在讀**（`cpu_probe()` 設 `PRID_IMP_UNKNOWN` 之後下一行就用 `read_c0_prid()` 蓋掉）；而表自己的編碼不一致 —— `0xdc01`／`0xdc02` 低位元組非零，破壞了前四格的 bits-15:8 紀律 | — | 讀 | 上述四票全部只影響「名字」欄；**「值」欄要等 `PRId`** ＋ 🆕 B `arch/rlx/include/asm/cpu.h` 的 PRId 指派表與 `boards/rtl8196e/config.in` | `refs/README.md`、gate R1 |
| `CPU-05` | `/proc/cpuinfo` `system type` | `RTL819xD` —— 第三種不同的叫法，除了矽片 ID 的 `8196E` 與網卡驅動的 `8196C` | 量 | — | C | `upstream/test-ledger.md` `P5-5` |
| `CPU-06` | `/proc/cpuinfo` `cpu model` | `52481` —— 一個十進位數字，**不是核心名稱** | 量 | — | C | 同上 |
| `CPU-07` | BogoMIPS | `398.95` | 量 | — | C | 同上 |
| `CPU-08` | TLB entries | `32`。🆕 **2026-08-25b 用一條完全獨立的路線佐證**：census row `0x08` 是 `mfc0 v0, c0_random`，八列回十六個不同值（`S_MOVES`），index 欄位（bits 13:8）落在 **5 … 29，每一個都在 0…31 之內**。傳統 64 entry 的 R3000 會讓 `Random` 在 8…63 之間繞。**這條路線裡沒有任何 TLB 探測**，量的是核心自己的替換計數器 | 量 ×2 | 量 | C；🆕 量 `bench/2026-08-25b/H2a.log` row `0x08` | 同上 |
| `CPU-09` | MIPS16 | `mips16 implemented : yes`（kernel 自己的說法）  🔴 **2026-08-27：不只是 kernel 這樣說，它裡面真的有 MIPS16 程式碼在跑。**`arch/rlx/include/asm/cpu-features.h:22` 是 `#define cpu_has_mips16 1`、`arch/rlx/kernel/proc.c:35` 硬寫 `mips16 implemented\t: yes`（⚠️ **是宣告不是探測**）；而這台解壓後的 kernel 有 **180 個 `jalx` 目標落在映像內、27 個相異、分三群**，其中 `0x802B8118` 用廠商自己的 `rsdk-1.3.6-4181` objdump `-m mips:16` 反組譯出一個完整函式（`bteqz` 跳到那個 `jr ra`、PC 相對載入正好指到延遲槽後的那個字、literal pool 裡有 `0xB8010028` 這個 KSEG1 暫存器位址）。**十二支使用者空間 binary 與 `stage2.bin` 全部乾淨**（ELF `e_flags` 沒有 `0x04000000`，`jalx` 落在檔內的目標 0 個）。細節見 `CPU-48` | 量 | — | C、D ＋ 🆕 A、B | 同上 |
| `CPU-10` | 硬體 watchpoint | `no` | 量 | — | C | 同上 |
| `CPU-11` | 時脈 | **400 MHz** | 量 | — | 開機 banner（C、S）、D | `bench/2026-08-23/A-catch.log` |
| `CPU-12` | 位元組序 | **big-endian** | 讀 | 讀 | A —— 每一支 ELF 都是 `ELFDATA2MSB` | `upstream/notes/anatomy-n150rt.md` |
| `CPU-13` | 手冊宣稱的 ISA | `Supports MIPS-1 ISA, MIPS16 ISA` | 文 | 讀 | **只有 D** | `SOURCES.json` `ds-rtl8196e-vex` |
| `CPU-14` | **load delay slot** | **架構外露 —— 沒有 interlock** | 量 | 讀 | **×2** —— C 對 `stage2` 的指令普查（1,474 個 load、646 個後面接 `nop`、後面接「讀取剛載入暫存器」的 0 個），以及矽片上一次單一變數實驗。**2026-08-24 在本 repo 用 `tools/hazlint` 獨立重算，三個數字全中**，負控制是上游那份真的在這台上失敗過的 payload | `tools/hazlint` |
| `CPU-15` | 矽片有沒有 `lwl`/`lwr`/`swl`/`swr` | *(未定)*  🔄 **2026-08-27：桌面證據換方向了，而它仍然不是量到的。****支持這顆有**：三份 drop 的每一塊 RLX 板子都設 `ARCH_CPU_ULS=y`；`arch/rlx/kernel/unaligned.c` 與 `lib/memcpy.S` 在沒有 ULS 時**有一條逐位元組的 `#else` 路徑**，所以那個旗標是有意義的；而**這台自己的 kernel 的 `memcpy` 就在用它們**（`0x80002464` 起 `lwl t0,0(a1)`／`lwr t0,3(a1)` … `sw t0,0(a0)`，展開四次）。**推**：若這顆沒有這四條，`memcpy` 第一次非對齊複製就會吃 Reserved Instruction，而 `do_ri` 裡沒有任何 ULS 模擬，會撞上 `die_if_kernel("Reserved instruction in kernel code")`；這台開得起來。⚠️ **反方**：公開的 Lexra 說法是這族核心拿掉了這四條，而 LKML 的 LX5280 patch 明說 LX5280 沒有。⚠️ **`tools/isa-probe.sh` 對這一格什麼都沒說**：八個欄位（含 `lx4180` 與 `mips1`）全部接受 `lwl`，那張表只會**從 MIPS-I 往下扣**，從來沒扣過 ULS。**這一格仍由 `R1a` 一條 `lwl` 決定** | — | — | 裸機 RI 處理器下執行一條 `lwl` 就結案 | `notes/lwl-mystery.md`、gate R1a |
| `CPU-16` | ……這台的 binary 裡有幾個 | `stage2` **0** · `busybox` **0** · `boa` **144**  🆕 **加上這台自己的 kernel：`.text`（`0x80000000`–`0x802B8000`）裡 101 組成對點位**（`lwl`／`lwr` 82、`swl`／`swr` 19，**全部大端定向，BE 101 LE 0**），而 `.text` 以上 0 組、未成對的半條 511 個。`tools/opcount.py --pairs`（2026-08-27 新增）比對的是「同一個 `rt`、同一個 base、位移正好差 3、四個字以內」——四個欄位同時吻合。**控制**：`boa` 70 組（70×2＋4 未成對 = 144，與本列既有的數字逐一對上）、`busybox` 0、`stage2` 0、**3,374,772 位元組的 `/dev/urandom` 0 組**（53,085 個半條，一組都沒有）。🔴 **而 2019 那次歸零現在有一個機制候選，量在廠商工具鏈上**：同一份 C、同樣 `-O2`、同樣走 `-S`：`rsdk-1.3.6`（gcc 3.4.6，4181 與 5281 兩份都試）發 **0** 條、改用四個 `lbu` 加位移；`rsdk-1.5.5`（gcc 4.4.5）發 **4** 條。**而且與 `-march` 無關** —— `lx4180`／`rlx4181`／`rlx5281`／`mips1` 掃過去，1.3.6 全 0、1.5.5 全 4。這台 kernel 的 banner 是 `4.4.5-1.5.5p2`。⚠️ 手上唯一的 1.5.5 是 **5281／p4**，這台是 4181／p2，沒有量到 | 讀 | 讀 | A、`tools/opcount.py`，控制在 `tools/test-opcount.sh` ＋ 🆕 A（kernel）、`tools/opcount.py --pairs`、廠商工具鏈 | `notes/lwl-mystery.md` |
| `CPU-17` | `movz` / `movn` | **loader 程式區裡 18 個**，其中兩個在 `check_image()` 裡，每次開機都會跑；18 份 capture 裡沒有任何例外訊息  🆕 **2026-08-27：廠商自己的 opcode 表說 `rlx4181` 有這兩條。**`tools/isa-probe.sh` 逐條組譯：`movz`／`movn` 在 `rlx4181`／`rlx5181`／`lx5280`／`rlx5281`／`rlx4281` **接受**，在 `lx4180`、`mips1`、`mips2` **拒絕**。正負控制都成立（`addu` 八欄全過、`daddu` 八欄全擋）。⚠️ **而「這一格原本沒有任何來源」是錯的，這句話的第一版這樣寫過**：`SOURCES.json` 早就記著 gcc-4.8.4 的 Lexra patch「以 `INSN_RLXB` 管條件搬移，那正是讓 `CPU-17` 那 18 條 `movz`／`movn` 排除掉 baseline LX4180 的東西」。新的是**這次是在手上的工具鏈上量的**，而不是讀一份沒人下載過的 gist 的描述 —— 而兩者一致，那正是房規要的第二個來源 | 讀（數量）· 量（那個「沒有」） | 讀 | A、C ＋ 🆕 `tools/isa-probe.sh` | `docs/loader-command-semantics.md` §9、`C-12` |
| `CPU-18` | loader 程式區裡的 `ll` `sc` `cache` `pref` `sync`、branch-likely、SPECIAL2/3、FPU、`jalx` | **全部為零** | 讀 | 讀 | A，**三支**解碼器（第三支是 `tools/hazlint --isa`，寬鬆與嚴格兩種判準都得 18），每一支都在 rodata 裡示範過會誤報，那就是控制 | 同上 |
| `CPU-29` | `mult`/`div` 後面緊接 `mfhi`/`mflo` 的位址數 | **16 個**，最早在 `0x80403C14` | 讀 | 讀 | A，`tools/hazlint --survey` | `docs/loader-command-semantics.md` §9 |
| `CPU-30` | `mtc0` 後面緊接 `mfc0` 的位址數 | **3 個** —— `0x80400408`、`0x8040041C`、`0x80400660`，第一個是同一顆 `Status` 寫完立刻讀 | 讀 | 讀 | A，同上 | 同上 |
| `CPU-31` | 非 load 的 hazard 規則（`HI`/`LO`、CP0） | *(未定)* —— `CPU-29`／`CPU-30` 說原廠編譯器**沒有**在中間補 `nop`，這與「有互鎖」相容，也與「有互鎖但這兩處是 bug」相容。數得出來不等於判得出來 | 讀（計數）· — （規則） | — | A | `C-9`、gate R1b |
| `CPU-19` | 快取管理模型 | **R3000 級的 CP0，外加一顆 Lexra 的 CP0 20（`CCTL`）—— 而「R3000 級」這件事本身在 2026-08-25b 之後是量到的，不是從程式碼讀的**：`Config` 讀 `00000000` → `Config.M = 0`，不是 MIPS32 級 CP0；`moves = 8` 且那八列全是 rd 1 的八個 select → CP0 忽略 select 欄位；`rfe` 之後 `status_end == status == 0x1000FC00` 逐位元相同 → R3000 三層 KU/IE 堆疊語意成立；`Random` 的 index 欄落在 5…29、全在 0…31 之內 → 佐證 32 個 TLB entry。**四條獨立路線，裡面沒有任何 TLB 探測。** 機制側量於 2026-08-25（`probe1`，`bench/2026-08-25/H1b.log`、`H1c.log`，兩條通道 104/104 逐字一致）：① **I-cache 確實會拿到陳舊位元組** —— cell 1「不施加任何處置、走 cached window」兩個 victim 都 `01` STALE，**這是負控制，它成立整張表才有意義**，而它與 qemu 相反；② **`CCTL 0x002`（單獨 invalidate I）就足夠** —— cells 2／3／6 六個 victim 全 `02` FRESH，且 2026-08-25b 由 `probe2` 在**不同位址範圍、不同 store 路徑**上再量一次（`install.bad = 0`，`break` 陷進我們裝的 handler 並返回）；③ 🔴 **`Status.IsC\|SwC` 在這顆核心上不隔離，隔離期間的 store 會寫進 DRAM** —— cell 4 兩個 victim 都 `07` CORRUPT，`240222b2 → 000222b2`、guard `03e00008 → 00e00008`，每字最高位元組被清零、stride 4。🔄 **2026-08-26 收窄一句，而收窄的理由是量測本身撐不住它**：原本這裡寫「**D-cache 是 write-through**」，但 cell 1 與 cell 5 的 store 兩格都打在 **D-cache 沒有持有的 line** 上，所以 write-through 與「write-back 但不做 write-allocate」會給出同一個讀數；而兩份 GPL drop 的 `boards/rtl8196e` 都寫 `CONFIG_ARCH_CACHE_WBC=y`（write-back —— **同源，一票不是兩票**）。撐得住的說法是：**一個打在 D-cache 沒持有的 line 上的 cached store，不必刷就會到記憶體**。🆕 **而這台自己的 kernel 會用 MIPS-II 的 `cache` 指令刷 D**（37 條，`0x8000CA40`–`0x8000CD4C`，op 只有 `0x11`/`0x15`/`0x19`，stride `0x10`），**I 側一條都沒有**；`notes/cache-model.md` 的否證條件字面上成立，而 `Config.M = 0` 仍然排除 MIPS32  🆕 **2026-08-27：快取管理模型從原始碼讀到了，而它既不是 r3k 也不是 r4k。**`arch/rlx/mm/cache.c:166` 的 `cpu_cache_init()` **無條件**呼叫 `rlx_cache_init()`，沒有探測也沒有分支（對照組：同一棵樹的 `arch/mips/mm/cache.c:161–173` 確實有 r3k／r4k 那個分支，所以掃描器不是瞎的）。🔴 **而它是寫回式的**：`boards/rtl8196e/config.in` 的 `ARCH_CACHE_WBC=y`（五份出貨 `.config` 也都有 `CONFIG_CPU_HAS_WBC=y`），於是 `cache-rlx.c` 取 `CACHE_DCACHE_FLUSH = 0x15 DWBInval`、`CACHE_DCACHE_WBACK = 0x19 DWB`、CCTL 取 `0x100 DWB`／`0x200 DWB_Inval` —— 若不是 WBC，這四個常數會全部塌成 `0x11`／`0x1`。**這給 `CPU-19` 殘留的『寫通 vs 寫回』一個來源，但那是讀不是量** | 🔄 **量** | 🔄 **讀** —— `CCTL` 這個名字 2026-08-26 起有來源（`arch/rlx/mm/cache-rlx.c` 明寫），不再是推 | A + B + 量 `bench/2026-08-25/H1b.log`、`H1c.log`、`bench/2026-08-25b/H2a.log` ＋ 🆕 B `arch/rlx/mm/cache.c`、`cache-rlx.c`、`boards/rtl8196e/config.in` | `notes/cache-model.md`、`docs/rlx-cache-and-cp0.md` |
| `CPU-20` | CP0 r20 —— `IInval`（作廢 I-cache） | `0x002` | 讀 | 🔄 **讀** —— 名字出自 `arch/rlx/mm/cache-rlx.c` 的編碼表，2026-08-26；在那之前這一格是「從用法推出來的意思」 | 🔄 **×3** —— A loader `0x804066e8`（發出 `mtc0` 的是 `0x80406704`）、🆕 A 這台的 kernel `0x8000CB34`／`0x8000CB5C`／`0x8000CCA8`、B `c-r3k.c` ＋ `cache-rlx.c`。**它的效果本身是量的**，見 `CPU-19` | 同上 |
| `CPU-21` | CP0 r20 —— `DWB_Inval`（寫回**並**作廢 D-cache） | `0x200` | 讀 | 🔄 **讀** —— 同一份來源。⚠️ 舊名字寫「flush D-cache」，那句話沒說它作不作廢；**它會作廢**，而這正是 `CPU-45` 需要的那個動作 | 🔄 **×3** —— A loader `0x804066cc`、🆕 A 這台的 kernel `0x8000CB50`／`0x8000CBF4`／`0x8000CCE8`、B | 同上 |
| `CPU-22` | CP0 r20 —— `DInval`（作廢 D-cache，不寫回） | `0x001` | 讀 | 🔄 **讀** —— 2026-08-26 起有名字；在那之前**任何來源都沒有名字** | 🔄 **×2** —— B `c-r3k.c` 的 `CONFIG_RTL865XB` 路徑、🆕 A 這台的 kernel `0x8000CA24`（`mfc0`／`ori`／`xori` 那個唯讀改寫 idiom）。**這台的 loader 從不發它** | 同上 |
| `CPU-23` | CP0 r20 —— `DWB_Inval \| IInval` | `0x202` | 讀 | 🔄 **讀** —— 兩個位元現在各自有名字，這一列不再是從兩個用法推出來的 | 🔄 **×2** —— A loader `0x804004f8`、🆕 A 這台的 kernel `0x8000225C` | 同上 |
| `CPU-24` 🔄 | CP0 r20 —— `0x010`、`0x020` | 🔴 **有名字了，2026-08-26，而且它們根本不是快取命令。`0x010 = IMEM0FILL`、`0x020 = IMEM0OFF`** —— 一塊 **16 KiB 本機指令 scratchpad（I-MEM）** 的生命週期控制：`0x020` 清掉它的 valid bit，讓 IMEM 區域的 fetch 落回 I-cache；`0x010` 從 `CP3 $0`／`$1` 把它填滿，**填的期間核心整個停住**。🔴 **而且 CCTL 是 0→1 邊緣觸發的** —— 寫一個已經是 1 的位元不做任何事，所以「寫一次就期待有效果」的探針是一支不會失敗的工具；`rlx_cctl` 本來就是 clear／write／clear，加上 `CPU-39` 量到讀側恆為 0，RLX4181 那個 read-modify-write 形式在這顆上退化成普通寫入。⚠️ **bit 6／7 的來源互相矛盾**（LX4189 說 `IROMOn`／`IROMOff`，`rlxregs.h` bit 6 說 `IMEM0ON` 而 bit 7 空著），`0x400`／`0x800`（`DMEM0ON`／`DMEM0OFF`）只有單一來源 —— 這三格仍是**未定**。*(這一格從寫下來到 2026-08-25 都寫「任何來源都沒有名字」，而**擋在前面的只是搜尋方向**：命名檔就在已經在手的 GPL drop 裡，和 `cache-rlx.c` 同一棵樹。)* | 讀 | 🔴 **讀** | 🔴 **×4，其中兩個彼此獨立** —— ① B **`arch/rlx/include/asm/rlxregs.h:630-638`**，三份 drop 皆有但**逐位元組相同**（md5 `623d85d7…`），**一票不是三票**；② **Lexra LX4189 Data Sheet Rel 1.9 §5.2**，位元圖＋語意散文，**核心廠商的文件，與①獨立**；③ **D §1／§2／方塊圖**「16Kbyte I-MEM, 8Kbyte D-MEM」，**而 `SOURCES.json:195` 早就逐字引了這句**；④ 🔴 **A 這台自己的 kernel `0x80002210`–`0x80002300`** —— 先 `Status \|= CU3`、`CCTL 0x020`、`CCTL 0x202`、把 `0x002B8000`＋`0x3FFF`（**16 KiB**）寫進 `CP3 $0`／`$1`、再發 `CCTL 0x010`，然後 `0x002C0000`＋`0x1FFF`（**8 KiB**）進 `CP3 $4`／`$5`。**名字與行為互相解釋**，而兩個尺寸與 D 的 I-MEM／D-MEM 尺寸逐一吻合。⚠️ 這台的 **loader 程式區一條 COP3 都沒有**（掃 primary opcode `0x13`：97 個命中全部 ≥ `0x8040A5B8` 且全是 ASCII），所以 loader 是對著 **reset 預設**的 BASE/TOP 發這兩個命令 | `notes/cache-model.md` |
| `CPU-46` 🆕 | **本機 scratchpad —— I-MEM 16 KiB、D-MEM 8 KiB** | **存在，而且這個 repo 到 2026-08-26 為止從來沒有任何一列記過它。**🔴 **對 `CPU-25` 是第一級威脅**：LX4189 §5.2 說「IMEM 無效時，IMEM 區域的可快取 fetch 才由 I-cache 服務」—— 反過來讀就是**有效時不由 I-cache 服務**。而 **I-MEM 是 16 KiB，預測的 I-cache 也是 16 KiB**，所以任何「大小」量測都分不開這兩個結構。窗口位置由 `CP3 $0`／`$1`（IMEM）與 `$4`／`$5`（DMEM）決定，讀它們需要先設 `Status.CU3`  🔴 **2026-08-27：這台自己的 kernel 在開機時就把兩塊都設起來了，所以這一格不再只有資料表。**讀 `0x80002230`–`0x800022E8`（`arch/rlx/mm/imem-dmem.S` 的 `_imem_dmem_init`）：先 `mtc0 zero,$20` 再 `li 0x20`／`mtc0 $20`（**IMEM0OFF**），再 `0x202`（`DWB_Inval` 加 `IInval`），然後 `__iram = 0x802B8000` 遮 `0x0FFFC000` 寫 **`mtc3 $0`（IW base）**、加 `0x3FFF` 寫 **`mtc3 $1`（IW top）** —— **16 KiB**，接著 `0x10`／`mtc0 $20`（**IMEM0FILL**）；再拿 `__dram = 0x802C0000` 遮 `0x0FFFE000` 寫 **`mtc3 $4`**、加 `0x1FFF` 寫 **`mtc3 $5`** —— **8 KiB**。**這就是這個映像裡僅有的四條 `mtc3`。** CP3 `$0`／`$1`／`$4`／`$5` 的指派、兩塊的大小、以及 `CPU-24` 的 `0x010`／`0x020` 名字與 0→1 邊緣，四件事同時由這台自己的 binary 佐證。🔴 **而它與 drop 相衝兩處**：`bspcpu.h` 寫 `cpu_imem_size 0`；`imem-dmem.S` 在非 `RTL_819XD` 下 `IMEM0_SIZE` 是 **4096**（`addiu 0xFFF`），這台的 binary 是 `0x3FFF`。兩處都記成不一致，不做調和 | 讀 | 讀 | **×2** —— D §1／§2／方塊圖（尺寸，⚠️ 版本限制詞見 `CPU-25`）＋ 🔴 **A 這台的 kernel**（把 `+0x3FFF`／`+0x1FFF` 寫進那四個 CP3 暫存器）—— **後者切自這台，不受版本限制詞影響，而且它與前者吻合**。CP3 暫存器**名字**只有第三方 `lxregs.h` 一票。🆕 🔴 **2026-08-26：拿到 LX4189 全文之後，這一票變得更孤單而不是更有伴。** §5.4 說 LX4189 的 IMEM base／top 是**硬接的組態接腳**（`CFG_IWBASE[31:10]`、`CFG_IWTOP[17:10]`），**根本沒有 CP3 暫存器**。所以這顆 SoC 的核心暴露了一組核心廠商文件裡不存在的東西：CP3 編號＝第三方 header 一票＋這台 kernel 的行為，**核心廠商的文件不背書**。⚠️ **prompt 當下的窗口位置未定** —— loader 不設它 ＋ 🆕 A 這台 kernel `0x80002230`–`0x800022E8` | `notes/cache-model.md`、`docs/probe3-cells.md` |
| `CPU-47` 🆕 | **這台的 kernel 模擬了哪些指令** | 🔴 **`ll`／`sc` 模擬、`sync` 模擬成 no-op、FPU 不模擬、`rdhwr` 不模擬、`lwl` 一族不需要模擬。** `arch/rlx/kernel/traps.c:546` 的 `do_ri()` 在 `#ifndef CONFIG_CPU_HAS_LLSC` 下呼叫 `simulate_llsc`、在 `#ifndef CONFIG_CPU_HAS_SYNC` 下呼叫 `simulate_sync`，而 `simulate_rdhwr` 兩個呼叫點都被廠商 `#if 0` 掉（mainline 的 `arch/mips` 是無條件呼叫，所以那是廠商改的）。**`arch/rlx` 底下根本沒有 `math-emu` 目錄**（`arch/mips/math-emu` 與 `arch/x86/math-emu` 都在，那是 `ls` 找對地方的控制），`do_cpu` 只處理 `cpid == 0`，其餘一律 `SIGILL`。二進位側同意：`.text` 裡 `ll` 0、`sc` 0、`sync` 0、`lwc1`／`swc1`／`sdc1` 0（⚠️ **不是「零個 FPU opcode」** —— 同一段裡有 **1** 個 `COP1`（`0x80188938`）與 **2** 個 `ldc1`（`0x801888C8`／`0x80188A1C`），三個都落在 `0x80188800` 那塊 1 KiB 非程式碼的島裡，**是逐條裁決掉的、不是被界擋掉的**），字串 `fpu_emulator`／`cp1emu`／`FPU emulator` 各 0 次。非對齊**位址**（AdEL/AdES）**有**模擬 —— `do_ade` → `emulate_load_store_insn`，它那三行 `die_if_kernel` 的字串在這台 binary 裡都在。🔴 **這否證了 `CLAUDE.md` 那條上機守則的一半**：「kernel 模擬 `ll`／`sc` 與 FPU」—— `ll`／`sc` 對，**FPU 錯**。結論（裸機才量得到 ISA）不動，因為單是 `simulate_llsc` 就足夠 | 讀 | 讀 | **×2** —— A 這台的 kernel（`cf0d60a8…`）＋ B `arch/rlx/kernel/traps.c`、`boards/rtl8196e/config.in` | `notes/vendor-kernel-isa.md` |
| `CPU-48` 🆕 | **這台的 kernel 裡有 MIPS16 程式碼，用 `jalx` 進去** | 🔴 **180 個 `jalx` 目標落在映像內、27 個相異、三群**（`opcount --mips16`；⚠️ `hazlint` 對同一份檔案報 **179／26**，差的那一個字是 `0x80002310`，它坐在函式之間的零填充裡而且延遲槽與返回點都是 `nop` —— `hazlint` 要擋 build 所以多一條規則把它剔掉，`opcount` 只出報告所以不剔。**兩個數字都不是錯的，而只看到其中一個的讀者會以為有一個是**），主群在 `0x802B8118`–`0x802BC9F8`（`.iram` 與其後）。對照：同一支掃描器對 `jal` 得 31,082/31,110 落在映像內、`j` 得 20,163/20,234，而 26 位元的 J 欄位跨 256 MB，隨機資料只會有 **1.26 %** 落在這個 3.37 MB 映像裡。`0x802B8118` 用廠商自己的 objdump `-m mips:16` 反組譯出一個完整函式，四個內部自洽點（`bteqz` 目標正好是那個 `jr ra`；PC 相對載入指到延遲槽後那個字；那個字是 `0x802FB544`；下一個 literal pool 有 `0x804CA900`／`0x804CA8F4`（`.bss`）與 `0xB8010028`（KSEG1 暫存器））。**負控制**：同一支反組譯器對 48 個隨機位元組給出 `ld`／`daddiu` 這些 MIPS64 形式、一個落在映像外的 `jal`、以及跳出區段的分支。🔴 **代價落在本專案自己的工具上**：`opcount.py` 的 docstring 說線性掃描「不會漏掉指令」，**在 MIPS16 區段裡那是錯的**，而 `notes/lwl-mystery.md` 引用著它。**六棵樹十二支 binary 與 `stage2.bin` 逐一驗過都乾淨**（ELF `e_flags` 無 `0x04000000`；`jalx` 落在檔內的目標 0 個），所以 `CPU-16` 的數字不受影響。`hazlint` 1.3 從此對含 MIPS16 的範圍**拒絕報告**（K10 五案控制） | 讀 | 讀 | **×2** —— A 這台的 kernel ＋ 廠商 `rsdk-1.3.6-4181` 的反組譯器；B `cpu-features.h`、`proc.c` | `notes/vendor-kernel-isa.md` |
| `CPU-25` 🔄 | 快取大小／line 大小／關聯度 | 🔴 **2026-08-27：大小與 line 有值了，關聯度沒有，所以這一列不再是留白但也還沒滿。** **I-cache 16 KiB、D-cache 8 KiB、兩者 line 16 byte、無 L2** —— 見本格末尾。**關聯度仍然沒有任何來源，它移到 §17 的 `CPU-25` 殘留。** *(以下是這一列在 2026-08-27 之前的全文，留著因為它記的是兩條路怎麼被量掉的：)* *(留白 —— 而 2026-08-25b 之後它是「路被量掉了」不是「還沒試」)* 🔴 **CP0 這條路關了**：census row `0x80`（`Config`，rd 16）讀 `00000000` 且 `S_ZERO`，而同一次 `nowrite = 0`（256 列全部）證明 `mfc0` 在這顆核心上一定寫 `rt`，所以那個 0 是真的 0 → **`Config.M = 0`，沒有 `Config1`**，也就沒有 `IS/IL/IA`、`DS/DL/DA` 可讀。🔴 **`GEOM=1` 也關了，而理由是量測不是謹慎**：`rlx_r3k_size` 的演算法靠隔離 —— 它先在 base 寫 marker 再讀回（不隔離的核心也會過，因為 store 與 load 都進 DRAM，守門為錯的理由通過），再把 `base+k*4`（k=32..0x40000）逐一清零、在 base 寫 −1、然後往上找第一個非零。**核心不隔離時那些字剛被清成 0 且保持 0，迴圈走到天花板，回傳 `0` —— 而 `0` 正是它自己「核心不回答」的值。** `CPU-35` 已量到這顆不隔離，所以這個實驗只能產出 0。⚠️ 順帶更正表上與 `cache.S` 的措辭：`GEOM=1` **不是**「寫 1 MiB 真記憶體」，loop 2 每圈一個 `sw`、14 圈，加 base 兩次，每次呼叫約 16 個字 —— **1 MiB 是外框不是體積**。🔴 **還活著的路是 eviction walk（`probe3`），而且是兩支不是一支** —— `H1` cell 1／cell 5 的機制量的是 **I 側**，而這一列自己那個 8 KiB／16 B 的預測是 **D 側**；D 側那支由 cell `c-A` 在執行期閘控。格子表 `docs/probe3-cells.md` §1.1。一支不需要隔離的 eviction walk（`probe3`），用 `H1` cell 1 已經證明的機制（寫進指令流的 store 看不到）—— 以 stride S 排 N 個 victim、執行、改寫、再執行，讀 STALE/FRESH 的邊界；掃 N 給大小、掃 S 給 line 大小、樣式給關聯度。桌面工作  🔴 **2026-08-27：留白填上了，而填它的是廠商自己的板級標頭，不是任何一次量測。**`boards/rtl8196e/bsp/bspcpu.h`（三棵樹逐位元組相同）在 `CONFIG_RTL_8196E` 這一支下：**I-cache 16 KiB、D-cache 8 KiB、兩者 line 都是 16 byte、無 L2（`cpu_scache_size 0`）、TLB 32 entry**。**16 byte 這一項有第二個來源，而且是這台自己的 binary**：`cache-rlx.c` 只有在 `cpu_dcache_line != 32` 時才選 `CACHE16_UNROLL8`，而 `CPU-44` 從這台 kernel 讀到的正是八條、stride `0x10`、蓋 128 byte。TLB 32 也與 `CPU-08` 在裝置上量到的 `Random` 範圍一致。⚠️ **標頭自己不一致**：`cpu_dcache_line_mask` 兩支都硬寫 `0xF`，所以走 32 byte 那一支的板子會拿到 16 byte 的遮罩 —— 不是這台的問題，但那是廠商標頭裡一個真的缺陷。⚠️ **關聯度仍然沒有來源**，而 `CPU-46` 那個混淆（I-MEM 16 KiB 與 I-cache 16 KiB 同大小）不因這一列消失 | 讀 | 讀 | 🔴 **2026-08-26 更正這一格的來源盤點，而更正的方向是「來源比以為的多」。** 這一列從 2026-08-25 起寫「第三方一票」，**而 `refs/RTL8196E-VEx-CG_Datasheet_1.1.pdf` 自己第一頁就寫了兩個尺寸**：§1「a 16Kbyte I-Cache, 8Kbyte D-Cache, 16Kbyte I-MEM, and 8Kbyte D-MEM are provided」、§2 Features 同樣的字、方塊圖「I-Cache=16kB／D-Cache=8kB」—— **而 `SOURCES.json:195` 從來源索引寫下來那天就逐字引著這句話**。和 `arch/rlx/` 同一種失誤：**事實在專案裡，搜尋去了別的地方**。所以：**大小 讀×3**（D、這台 kernel 的 `sltiu … 0x4000`、第三方 dtsi）；**line 大小 讀×2**（這台 kernel 的八條 stride `0x10`，只講 D 側；dtsi 講兩側）—— ⚠️ **D 給尺寸但不給 line 大小也不給關聯度**（對 11,467 行做 `cache line`／`line size`／`associat` 的 grep 只命中交換器 MAC 表）；🔴 **所以「任何來源都沒有」的那一格是關聯度，不是 I-cache 大小**。🔴 ⚠️ **而這裡有一個必須跟著走的限制詞**：`SOURCES.json` 自己對 `ds-rtl8196e-vex` 的 caveat 寫得很白 —— 那份是 **`-VE1/2/3` 版本，DRAM 是 MCM 內嵌的，而這台是外接 SDRAM（Winbond W9825G6KH，`MEM-01`，量），所以這台不是那顆零件**；`CPU-02` 量到的封裝絲印是 `RTL8196E · I510VG1 · GF23 TAIWAN`，**完全沒有版本後綴**。撐得住的說法是：**同樣兩句話也出現在公開的 `RTL8196E-CG` 資料表**（`SOURCES.json` `reference_only` 早就為了這件事收了它），所以這是**兩份 Realtek 文件、講同一個家族的兩個版本**，而**這台屬於這個家族是量的**（`CPU-01`，×3）。兩份比一份強，而且仍然不是對這顆晶粒的讀數。🔴 **而且 D 同一頁還講了 `CPU-46`：16 KiB I-MEM 與預測的 16 KiB I-cache 同大，eviction walk 用大小分不開它們**🆕 B `boards/rtl8196e/bsp/bspcpu.h` ＋ A 這台 kernel 的 unroll 形狀 | gate R1h |
| `CPU-26` | loader 自己的例外處理 | 🔴 **2026-08-25 改寫，因為原本這一列認錯了表。** 有例外處理，而且**它是永久 hang**：`do_reserved` 在 `0x80400BE8` 印兩行（`cp0_cause=%X, cp0_epc=%X, ra=%X` 與 `Undefined Exception happen.`，兩條字串都不以 `\n` 結尾）然後執行 `80400c18: 08100306 j 0x80400c18` —— **跳到自己**。例外進入已把 `IEc` 推成 0，看門狗沒上膛。真正的分派表是 BSS 裡的 `exception_handlers[32] @ 0x8040EB40`，由 `trap_init @ 0x8040D07C` 用 `set_except_vector @ 0x80400BD0` 填 32 次，之後 `[0] = 0x80400580`（IRQ，走 `rfe` 會回來）、`[23] = 0x804007C0`（Watch，會回來）、其餘 30 筆 = `0x80400BE8`。**`0x8040A5C0` 不是例外表**：它是 `BootStateEvent[3][8]`，24 筆，TFTP／ARP 開機狀態機（`eth_tftpd.c:174-208`，24/24 格與原始碼的兩處刻意不對稱吻合）。🔄 **2026-08-25 在裝置上量到，整張表逐字對上**（`H0b`，`DW 8040EB40 32`）：`[0]=80400580`、`[23]=804007C0`、**其餘三十筆 `80400BE8`**。🔴 **其中 `exception_handlers[9] == 0x80400BE8` 是 `docs/rlxprobe-audit-2026-08-25.md` § Must-fix 1 底下唯一還沒被驗證的承重連結**，現在是量的：`rlx_do_break` 少了 `SAFE_A0` 這件事維持滿級嚴重度，`H2` 延後到 `R1g-4b` 是對的。**而 `0x80000080` 的向量本身也在同一次上機量到** —— `H0a` 32/32 字與 06:09 事先登錄的清單相同，`H0a2`（`DW 8040054C 32`）證明 `trap_init` 的 128 byte 複製完整落地，`H0a3`（`DW A0000080 32`）證明讀到的不是陳舊的 D-cache line。⚠️ **核心實際上從哪個 base 取指令仍然是 `Status.BEV`**，沒有任何 loader 命令讀得到 CP0 | 🔄 **量**（表的內容與向量頁）· 讀（語意） | 讀 | A + 廠商 bootcode `irq.c` + 🆕 量 `bench/2026-08-25/H0a.log`、`H0a2.log`、`H0a3.log`、`H0b.log` | `notes/cache-model.md` 更正段、`RUNSHEET.md` §Results B4 |
| `CPU-33` 🆕 | **一般例外向量位址** | 🔴 **`0x80000080`（R3000 佈局），而 `0x80000000` 是 UTLB refill 向量、loader 從未填它。** 三個獨立來源一致、零個說 `0x80000180`：這台自己的 `trap_init` 建 `lui v0,0x8000` + `ori t0,v0,0x80` 並從 `0x8040054C` 複製 128 bytes、廠商 bootcode 註解 *"vector base is 80000000, offset 0x80"*、`linux-2.6.30/arch/rlx/kernel/traps.c:691` 的 `RLX_TRAP_VEC_BASE 0x80000080`。旁證帶正控制：這個 loader 用 `rfe`（`0x42000010`，兩處）返回，而 `eret` 的編碼 `0x42000018` 在同一份反組譯裡 **0 次** —— 同一個 grep 找得到那兩個 `rfe`。**`0x80000180` 是 MIPS32 的位址，而它一度出現在七個 committed 位置**。🔄 **2026-08-25 補**：那 128 bytes 的**全部 32 個字**現在記在 owner 檔裡（前 11 個是派送碼，word 13 起是 loader IRQ handler 前 19 條指令被 128-byte memcpy 順手拖進來的死拷貝）。在此之前，`RUNSHEET.md` 的 `H0a` 引用「`notes/cache-model.md` 列的那 32 個字」而**那個檔案一個字都沒列**，其中 11 個字全 repo 只存在於 runsheet 那一行、另外 21 個沒有任何檔案預測過 | 讀 ×3 | 讀 | A + 廠商 bootcode + 廠商 kernel | `notes/cache-model.md` |
| `CPU-34` 🆕 | 這台的 loader 是不是從手上這兩份 GPL drop 建的 | 🔴 **不是。** 廠商的 `do_reserved` 是 `prom_printf("Undefined Exception happen."); for(;;);` —— 一個呼叫、零個 CP0 讀；出貨的 `0x80400BE8` 有兩個 `prom_printf`、`mfc0 c0_cause`、`mfc0 c0_epc` 與一個 `pt_regs` 載入。`do_watch` 的字串在 binary 裡，兩棵樹裡都沒有這個名字。**所以每一句「廠商原始碼說⋯」都要講清楚靠的是哪一半**：字串吻合是出處的證據，不是「螢幕上的 C 就是被編譯的 C」的證據 | 讀 | 讀 | A + 兩棵 GPL 樹 | `docs/loader-command-semantics.md` |
| `CPU-27` | `<RealTek>` 提示字元時的 `Status.BEV` | 🔴 **`0`，量 2026-08-25b。`Status = 0x1000FC00`**（`bench/2026-08-25b/H2a.log` 的 `status=`，`H2g.log` 區塊字 6，census row `0x60` 三處一致），**bit 22 = 0**。所以向量在 RAM 的 `0x80000080`，而核心真的從那裡取指 —— `break` 陷進我們裝的 handler 並回來（`break.count=1`、`cause=0x24`、`epc=80500270`）是那個「取指」的直接證據，不是推論。🆕 **被撤回的 `1000FC01` 是對的，而且現在有機制**：量到的字與它**只差 bit 0 `IEc`**，而 `IEc=0` 正是「`J <addr>` 清掉 `IE`」對 payload 的預測。當初撤回是因為它只有一個出處，不是因為它錯。順帶 `CU0`（bit 28）為 1、`IM[7:2]` 未遮罩（`0xFC00`）也一併變成量。⚠️ **量到的是 payload 執行時的 `Status`，不是提示字元下的 `Status`** —— 兩者差在 `IEc`，而 loader 沒有任何命令讀得到 CP0，所以提示字元下那個字仍然只有 `讀`。*(以下為只有單一來源時寫的：)* 🔄 **`0`（清掉），單一來源，2026-08-25 從桌面補上 —— 而證據 2026-08-23 就已經在這個 repo 裡了。** `docs/loader-phy-and-switch.md` §2 的中斷四層表第 1 列寫著 `Status.IM[7:2]` 未遮罩且 **`BEV` 清掉**，設在 `0x80406694`、由 `0x80408634` 呼叫、**在開機路徑上且從未被重新遮罩**；而這一格與 `PROGRESS.md` 的 gate 前提條件同時寫著「沒追過」。**一個事實只有一個擁有者，而它從來沒走到兩個依賴它的檔案** —— 與 `43ec0e0` 同一個缺陷類。🔴 **這是 *讀* 不是 *量*，而且鏈條不隔離 `BEV`**：`REG-25` 量到 tick 以 100.0018 Hz 前進，只證明中斷**有被服務**，一台 `BEV=1` 的機器透過 ROM 向量服務它會給出一模一樣的讀數。**loader 沒有任何命令讀得到 CP0**，所以量測需要 payload；`H2`（`probe2`）已延到 `R1g-4b`，所以 `R1g-4a` 之後這一格仍然是 *讀* | 讀 | 讀 | A `0x80406694`（單一來源）；`REG-25`/`CLK-04` 是端到端的旁證，不是隔離證據 | `docs/loader-phy-and-switch.md` §2 |
| `CPU-28` | 對建置旗標的後果 | 用 `-march=mips1`。**`-march=mips32` 會安靜地編錯** —— 不 fault、不警告 | 推 | — | 由 `CPU-14` 推出 | `plan/` §3.4、`CLAUDE.md` |
| `CPU-36` 🆕 | **CP0 有沒有 select 欄位** | 🔴 **沒有 —— 這顆核心忽略 `mfc0` 的 bits 2:0，量 2026-08-25b。** 256 個 stub（rd 0..31 × sel 0..7，編碼見 `tools/rlxprobe/exc.S` 的 `CP0STUB`）跑完：`moves = 8` 而那八列**全部是 rd 1 的八個 select**，`rows.suppressed = 0`，`rows.printed = 39`。也就是說除了 rd 1 之外，每一個暫存器的八個 select 都回**完全相同**的 `(v1, v2, state)`，被印出判準抑制掉；rd 1 之所以八列都印，是因為它的值在兩次讀之間會變，不可能等於自己稍早那一列。R3000 式解碼，量到的不是假設的 | 量 | 讀（"select" 這個名字是 MIPS32 的） | 量 `bench/2026-08-25b/H2a.log`、`H2g.log`（兩條通道） | `RUNSHEET.md` §Results B4 `R1g-4b` |
| `CPU-37` 🆕 | **讀一個沒實作的 CP0 暫存器會怎樣** | 🔴 **回 0，不 trap。`traps = 0`，256 列全部**（量 2026-08-25b）。架構上這是 UNDEFINED 而不是例外，而這顆的選擇現在是量到的。⚠️ **`S_ZERO` 分不開「有實作而且讀 0」與「沒實作而匯流排回 0」**，payload 自己也這麼說；能分開的是 `CPU-38` | 量 | — | 量 `H2a.log` | 同上 |
| `CPU-38` 🆕 | **`mfc0` 會不會寫目的暫存器** | 🔴 **一定會。`nowrite = 0`，256 列全部**（量 2026-08-25b）。census 每一列讀兩次、兩次用不同的 prime（`0xC0DE00nn`、`0xD1CE00nn`），所以「`mfc0` 退休但沒寫 `rt`」有一個專屬狀態 `S_NOWRITE`，而它一次都沒出現。**這是讓所有 `zeros=208` 變成真的 0 的那一格** —— 稽核的 Must-fix 4 問的就是這件事，而答案是用一支為了偵測它而造的儀器排除的，不是用假設 | 量 | — | 量 `H2a.log`；正控制：同一次 `moves = 8`，機制證明得出變化 | 同上 |
| `CPU-39` 🆕 | **CP0 20（`CCTL`）的讀側** | 🔴 **真的讀 0。** `probe1` 的 `XCT0` 列 2026-08-25 讀到 `00000000` 但**分不開「讀 0」與「目的沒被寫」**；2026-08-25b 的 row `0xa0` 一樣 `00000000` 而 `CPU-38` 證明 `mfc0` 一定寫 `rt`，所以分開了。**寫側早就是量**（`CPU-19`：cells 2/3/6 證明 `mtc0 $t,$20` 有效果）。**合起來：CP0 20 是一個唯寫的命令暫存器、讀回 0** —— 這正是 `R5b` 的 MTD 驅動需要的那一句 | 量 | 推（`CCTL` 這個名字來自 Lexra 文件 + `notes/cache-model.md`） | 量 `bench/2026-08-25/H1b.log`（讀 0）+ `bench/2026-08-25b/H2a.log`（`nowrite=0`）+ `H1` cells 2/3/6（寫側） | `notes/cache-model.md` |
| `CPU-40` 🆕 | **CP0 rd 6** | **`0x00000004`，`S_VALUE`，兩個 prime 都一樣**（量 2026-08-25b，row `0x30`）。**沒有解釋。** R3000 沒有 rd 6；R4000 家族的 `Wired` 在 rd 6，而 4 是一個講得通的 wired 數。**一次讀數、零個第二來源** —— 記成開著的，不是記成 `Wired` | 量（值） | — — **這個暫存器叫什麼名字沒有任何來源**，所以名字欄是空的而不是「未定」：未定要有候選 | 量 `H2a.log`、`H2g.log` | §17 |
| `CPU-41` 🆕 | **`rfe` 有沒有正確彈 KU/IE 堆疊** | 🔴 **有。`status_end == status == 0x1000FC00`，逐位元相同**（量 2026-08-25b）。事前推導：R3000 進例外時把三層 `(KU,IE)` 左移、`rfe` 右移並把 `p→c`、`o→p`，所以 `status_end` 只能在 bits 5:4 與 `status` 不同，而且第一次例外之後就等於 `status` 的 bits 3:2 —— 這裡兩者都是 0，於是兩個字必須完全相同。**任何其他位元不同都會否證這顆核心的 R3000 `rfe` 模型**，那比整份 census 值錢。這一格是免費的：兩個字本來就在報告與區塊字 6／30 裡 | 量 | 讀（R3000 手冊的 `rfe` 語意） | 量 `H2a.log` + 事前推導寫在 `bench/2026-08-25b/PREDICTIONS-b4-block1.md` | `RUNSHEET.md` §Results B4 `R1g-4b` |
| `CPU-42` 🆕 | **CP0 `Count`(9) / `Compare`(11)** | 🔴 **`Count` 沒有實作。** row `0x48`（rd 9）讀 `00000000` 且 `S_ZERO`；`rlx_count_delta` 在 100,000 圈三指令迴圈兩端讀到 `count.before = count.after = 00000000`，`count.delta = 0`，`count.traps = 0`（量 2026-08-25b）。**兩個目的暫存器都先被寫了不同的 prime（`C0DE0009`/`D1CE0009`）**，所以「`mfc0` 沒寫目的」會產生 `0x10F00000` 而不是 0；加上 `CPU-38`（`nowrite = 0`，256 列全部），**這個 0 是真的 0**。rd 11 `Compare` 同樣在 `zeros` 裡。🔴 **後果：`R5-0` 的 SoC timer 驅動是前提而不是加分，`R1c` 失去第一條計時路線** —— 這是 `R1-gate` 存在要回答的四個決定之一（計畫 `F50b`）。正控制：同一次 row `0x08`（`Random`）是 `S_MOVES`，機制證明得出變化 | 量 | 讀（R3000 沒有這兩個，它們從 R4000 才有） | 量 `bench/2026-08-25b/H2a.log`、`H2g.log`（兩條通道） | `RUNSHEET.md` §Results B4 `R1g-4b` |
| `CPU-43` 🆕 | CP0 r20 —— `DWB`（只寫回 D-cache，不作廢） | `0x100` | 讀 | 讀 | **×2** —— B `arch/rlx/mm/cache-rlx.c` 的編碼表明寫、🆕 A 這台的 kernel `0x8000CA94`／`0x8000CAC0`。🔴 **這個命令到 2026-08-26 為止，這個 repo 沒有任何一列記過它**；這台的 loader 從不發它 🆕 **2026-08-26 多一票**：`arch/rlx/include/asm/rlxregs.h:635` 的 `CCTL_DWB 0x00000100`（⚠️ 與 `cache-rlx.c` 同一棵樹，**不是第二票**）。🔴 **這個命令的「效果」在這個 repo 裡從來沒有被任何東西量過**，`probe3` 的 cell `c-F`／`c-E2` 是第一次 —— 而 `c-F` 同時是 cell `c-C`（`CCTL 0x001` 整顆 D-cache 不寫回作廢）的安全前置：先 `DWB` 才不會掉髒 line。🆕 🔴 **2026-08-26 建成，而建的過程發現 `c-E2` 原本量不到這一格**：LX4189 §5.2 說 uncached read 會把常駐的 line 作廢，而 `c-E` 的最後一步正是 uncached load —— 接在它後面的 `c-E2` 發 `DWB` 時髒 line 已經不在了，讀回 `P0` 會被記成「`0x100` 不寫回」。**那是被執行順序製造出來的否證。** 三個 E 格子現在各跑各的完整序列。`docs/probe3-cells.md` §1.4 ① | `notes/cache-model.md` |
| `CPU-44` 🆕 | **這顆上到底有沒有在執行 `cache` 指令**（🔄 本欄原寫「MIPS-II 的 `cache`」，見值欄末） | 🔴 **這台自己的 kernel 裡有 37 條，全部是 D 側**（讀 2026-08-26，解壓後的 kernel）：`0x8000CA40`–`0x8000CD4C` 一段，op 欄只有 `0x11`（`DInval`）／`0x15`（`DWBInval`）／`0x19`（`DWB`），base 只有 `v0`／`a0`，offset 只有 `0x00`…`0x70` 八個、stride `0x10` —— **八條蓋 128 bytes，就是 16 byte line 的假設**。同一支掃描器在全檔另外命中 15 條，位址全部 ≥ `0x802BA660`、op 欄十種、offset 是任意 16 位元值：**code 與 data 被三個彼此獨立的性質完全分開**。**I 側（op `0x10` `IInval`）全檔零條**，I 側一律走 `CCTL 0x002`。⚠️ **「在二進位裡」不等於「會執行」**，而這一格要的是後者；判決它的是把一條 `cache 0x11` 放進 `R1h` 的 payload，看它 retire 還是吃 Reserved Instruction —— 兩種結果都有價值，而且都不花一次電源循環。🆕 **2026-08-26：payload 建成了。** `probe3` 的映像裡有 **5 條 `cache`**（Group X 四條，`c-D` 的 leaf 一條）與 **8 條 `mfc3`**，`make show` 印出這兩個數字，suite 逐一點名。🔴 **qemu 對這一格兩個方向都答不了，而且它的答案看起來像確認**：量(qemu) 2026-08-26，`0x10`／`0x11`／`0x15`／`0x19` 全部 retire、`n=0`、scratch 三個字一個都沒動 —— TCG 根本不解 op 欄。🔄 **2026-08-27：那八條 `mfc3` 的標籤修好了（`hazlint` 1.2），而原本記在這裡的理由本身也是錯的。**原文寫「opcode `0x13` 從 MIPS-II 起才是 COP1X」——**不是**。量(binutils 2.42)：`mfc3` 在 `-march=mips1` 與 `mips2` 都編得過（`4c020000`），`mips3`／`mips4` 被拒；`lwxc1` 要到 `mips4` 才編得過。讀(MIPS IV Rev 3.2 A 8.3.4)：**COP3 是 MIPS I 與 MIPS II，MIPS III 拿掉它，MIPS IV 把那個 opcode 收去給 COP1X**（`TC-08`）。結論沒變——這顆是 MIPS-I（`Config.M = 0` 量），所以 `0x13` 是 COP3——gate（load-delay 檢查）也從頭到尾沒錯。🔴 **而修法刻意沒有把那八條從 ISA 清單上拿掉**：同一段規格說 COP3 在 MIPS I 是 *optional and implementation-specific*，所以「它是 MIPS-I」不是「這顆會執行它」的證據，而那正是 `m-imem`／否證 M 還開著要量的事。八條現在標成 `mfc3`／`MIPS-I COP3`，逐條印出位址與解碼。⚠️ 這**不**代表這是 MIPS32 核心：`Config.M = 0` 是量的  🆕 **2026-08-27：那 37 條為什麼長成那樣，現在是讀出來的而不是觀察到的。**`arch/rlx/mm/cache-rlx.c` 對 `CPU_RLX4181` 定義 `CONFIG_CPU_HAS_DCACHE_OP`（4181/5181/4281/5281 都有），但 `CONFIG_CPU_HAS_ICACHE_OP` **只給 4281/5281** —— 所以這一顆 **D 側走 `cache` 指令、I 側走 CCTL `0x2`**，本列量到的「I 側全檔零條」是那個 `#ifdef` 的直接後果。op 欄只有 `0x11`／`0x15`／`0x19` 同樣對得上（WBC 那一支）。🔴 **而本列講「code 與 data 被三個彼此獨立的性質完全分開」要修**：那 15 條裡的第一條 `0x802BA660` 在 `.iram` 裡（`__iram = 0x802B8000`，讀自這台 kernel 自己的 `0x8000226C`），而 `.iram` **是混著真程式碼、literal pool 與資料的**。逐條裁決過：`0x802BA660` 周圍是一張遮罩表（`0x00010000`、`0xFFFEFFFF`、`0xBFFFFFFF`…），仍然是資料，**所以 37 這個數字不動** —— 改的是它為什麼不動的理由。⚠️ 而 `.iram` 裡有 MIPS16（`CPU-48`），那一段不能用 4 byte 掃描去數  ⚠️ **`MIPS-II` 是錯的標籤，2026-08-28 改**：量(廠商 binutils，`tools/isa-probe.sh`)，`cache` 在 `-march=mips1` **與** `-march=mips2` **與** `lx4180` 都被拒絕，在 `rlx4181` 起才接受 —— `CACHE` 是 MIPS-III／MIPS32，在這族核心上是**擴充**。這是同一類缺陷的第三次（前兩次是 `0x13` COP1X→COP3、`0x33` pref→lwc3），而這一次是**前一天自己量到、隔天才被讀回去對上標籤的**。⚠️ 它**不是**這顆是 MIPS32 核心的證據：`Config.M = 0` 是量的。 | 讀 | 讀 | A —— 這台 flash dump 切出的 `r0-vendor-kernel.bin`（sha256 `396561a0…`，987,138 bytes，`RUNSHEET.md` `P4` 已記）在 offset `0x2808` 解 LZMA 得到的 3,374,772 bytes（sha256 `cf0d60a8…`）＋ B `cache-rlx.c` 的 op 編碼表 ＋ 🆕 B `cache-rlx.c` 的 `#ifdef` 結構 ＋ 🆕 `tools/isa-probe.sh` | `notes/cache-model.md`、`docs/rlx-cache-and-cp0.md` |
| `CPU-45` 🆕 | **D-cache 對「記憶體被 CPU 以外的東西改掉」的一致性** —— 它 read-allocate 嗎？有沒有一條命令能作廢一條 clean line？ | *(未定)* —— 🔴 **`R1-gate` 一個字都沒量這個方向。** `probe1` 量的是 CPU→記憶體，而且只量到 write **miss** 那一格 | — | — | 判決它的實驗寫在 `docs/rlx-cache-and-cp0.md` § ②：cells A–E，用 KSEG0/KSEG1 別名當 DMA 代理，**不需要 DMA 引擎也不需要驅動**。⚠️ 代理是模型不是實物，所以 cell E 是它的正控制 | gate R1h |

## 3. 記憶體 —— SDRAM

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `MEM-01` | 型號 | **Winbond W9825G6KH-6** —— 256 Mbit，16M × 16 | 量 | 讀 | P；`-6` 解碼為 6 ns / 166 MHz 速度等級 | `upstream/notes/hardware-inspection.md` §3 |
| `MEM-02` | 絲印／日期碼 | `Winbond · W9825G6KH-6 · 1837H · 6824506000` —— 2018 年第 37 週，全板最新的一顆 | 量 | 推 | P | 同上 §6 |
| `MEM-03` | 匯流排寬度 | **16 bit** | 量 | 讀 | 開機 banner 的 `[16bit]`，且這顆本身是 ×16 | `upstream/notes/uart-findings.md` §2 |
| `MEM-04` | 實裝容量 | **32 MiB** | 量 | 量 | **×2** —— P，以及 loader 的 `ramSize: 32M` | 同上 |
| `MEM-05` | loader 偵測到的 | `32M` | 量 | — | C、S | `bench/2026-08-23/A-catch.log` |
| `MEM-06` | Linux 實際拿到的 | **26,052 kB**（25.4 MiB） | 量 | — | C —— 在運行中的裝置上讀 `/proc/meminfo` | `upstream/test-ledger.md` `P5-5` |
| `MEM-07` | 記憶體控制器 | `MCR`，`0xB8001000` | 讀 | 讀 | D §7.4.1 | `SOURCES.json` |
| `MEM-08` | SDRAM timing 設定 | *(留白)* —— loader 設的，從沒讀出來過 | — | — | — | gate R5 |
| `MEM-09` | 上電 strapping 腳 | 是 DRAM 那幾支 —— `MA[10:8]` 在 44/52/53，`RAS#`/`CAS#` 在 51。**不是 `SF_*` 那幾支** | 讀 | 讀 | D §6.1 | `notes/power-and-programmer.md` §3 |
| `MEM-10` | 暖重置後 SDRAM 內容是否存活 | 🔴 **存活，但不是每一個字。** `0x80A00000` 的兩字 canary `5EA72D2B A5A5A5A5` 撐過**三次**暖重置、逐位元組不變（`D2c`、`D2f`、`D2g`）。**`0x81000000` 不能當 canary** —— 它的 word 1 每次開機都被寫成 `00000144`，見 `MEM-14`。所以 `C-8` 的第二個鑑別器成立：**在 `0x80A00000`，不在 `0x81000000`** | 量 | — | 量 `D2c`/`D2f`/`D2g` | `RUNSHEET.md` `D2c` |
| `MEM-11` | `0x81000400` 區的 32 位元組週期結構 | 🔴🔴 **寫它的是 vendor kernel，不是 loader —— 2026-08-24 推翻，`C-17` 的前提整個換掉。** 16 小時斷電後同一位址是高熵偏壓雜訊（`X1`）；幾秒斷電、且緊接在 kernel 跑過之後，整張表原封不動回來（`X1-24d`、`X1-24f`）。**連線建立被否證**（`X1c` 與 `X1b` 逐位元組相同，而 `X1b` 不含該結構，所以蓋出來會看得見）。🔴 **`IPCONFIG` 的否證理由被改正**：不是因為 `X1-post` 與 `X1-24d` 相同 —— 那個對照**本來就帶著結構**，不會失敗 —— 而是 `24c` 全場沒有下過 `IPCONFIG`，且 `X1-24d` 在 `24d` 的 `IPCONFIG` 之前就讀到它。🔴 **哪一次執行寫的仍然未定**：`0x81000400` 在 21:28:23 到 22:10:35 之間沒有被讀過，而那個窗口裡有三次暖重置與 `G6` 的 kernel 開機。*原文*：「不是 scratch，是 loader 的網路緩衝區池（推）」 | 量 | 推 | 量 ×6 | `PROGRESS.md` `C-17` |
| `MEM-12` 🆕 | `0x81800000` 的第二個結構 | `81C09988 81810000 00000058 81800058` / `0000000F FFFFFFFF 00002534 00000001` —— 內容不同，一樣自我指涉：`+0x08` = `0x58`，`+0x0C` = `0x81800058` = 基底 + `+0x08` 的值。第一個字 `0x81C09988` 指到 **28.04 MiB**。所以 `0x81000000`–`0x81C09988` 至少住著兩個互指的活結構，**這不是填值樣式** | 量 | 推 | S `C7-pre2`（2026-08-24b） | `RUNSHEET.md` `C7-pre2` |
| `MEM-13` | `0x80A00000` 讀起來沒被碰過 | 16 小時斷電後 48 個字：**沒有對齊的指標、沒有自我指涉、沒有週期**（`G0-head`/`G0-mid`/`G0-tail`，每格 16 字）。🔴 **而這個判準只在長斷電後成立**：同一位址在幾秒斷電後讀回我自己上一次拔電用 `FLR` 寫進去的 16 個字，逐位元組不變 —— 見 `MEM-15`。**「有指標」在短斷電後代表殘留，不代表有東西在用它** | 量 | — | 量 ×4 | `RUNSHEET.md` `G0` |
| `MEM-14` 🆕 | `0x81000000` word 1 被每次開機寫成 `00000144` | 🔴 **三次重現。** 寫 `DEADBEEF` → 重置 → `00000144`；寫 `F00DFACE` → 重置 → `00000144`；冷開機直接讀也是 `00000144`（`X3`、`D2b`、`D2e`、`X3-24d`）。word 2 兩次都活著，所以**不是衰減**。`0x144` = 324，意義不明。**stage 2 的 900 個 `lui` 裡 `lui …,0x8100` 是 0 個**，所以位址是執行期算的、或寫它的東西在 stage 1 | 量 | — | 量 ×4 + 讀 | `PROGRESS.md` `C-17` |
| `MEM-15` 🆕 | 短時間斷電後 DRAM 保留**內容** | 🔴 **保留，而且有正控制。** `0x80A00000` 在拔電後讀回我上一次拔電用 `FLR` 寫進去的 flash loader 頭部，**16 個字逐位元組相同** —— 寫入者不是推論出來的，是我自己下的指令。對照：**16 小時斷電後零殘留** —— part two 寫下的 23 個特徵字（`C7A0000n`/`C7B0000n`）一個都沒留（`X1`、`X2`）。**所以任何 canary 跨拔電的結論都必須說明斷電多久**。🆕 **2026-08-25 加一個跨暖重置的點，而且它比 canary 強**：`0x81000400` 的 **16 個字在 `J BFC00000` 的 ROM 向量重置前後逐位元組相同**（`H3a-early` vs `H3a-rb`），而那 16 個字是 DRAM 上電偏壓垃圾 —— **canary 是有人選的值，偏壓垃圾不是誰選的**，所以它排除「重置路徑剛好重寫成同樣的東西」這個解釋。🔴 **同一天也量到冷上電的偏壓不是決定性的**：`0x80A00000` 今天讀 `55617135 00F73F55 11744D3C E1553515`，對 2026-08-24 的 `55617135 0077BF55 11744D3C E1553515` —— **四個字裡三個相同、第 1 個字差兩個位元**。所以「這塊區域看起來像雜訊」**不是**一種鑑別，magic／nonce／封印才是。<br>🔴 **2026-08-25b 把區間收緊了，而且是用選定值收的。** 2026-08-25 15:01 之後 `0x80A00000` 存著 `probe1` 的完整區塊 —— 137 個字、magic `524C5831`、nonce `9D34F1C7`、13 列資料與有效封印。約 **3.9 小時**斷電後再讀（`H0d-a`），**word 0 是 `55617135`，不是 `524C5831`** ——**沒有保留**。所以區間從「幾秒有／16 小時無」縮成「**幾秒有／3.9 小時無**」，而這次的載體是一個 548 位元組的選定值，比 `MEM-10` 的兩字 canary 強。🔴 **同一天四個電源循環也把「上電偏壓不是決定性的」從 n=2 做到 n=4，而且帶兩個控制**：`0x80A00000` 八個字跨三次冷上電 **4／25／27 個位元（共 256）不同**（`24b`／`24c`／`25`），正控制（自己對自己）0／256、負控制（對 loader 寫過的 `H0a`）123／256；再加 `25b` 的 `H0d-a` 對 `25` 是 17／256、`H0d-b` 是 11／256、`H0c` 前七個字是 20／224。**所以任何跨電源循環的偏壓比對都是錯的**，而這正是 `H0c` 今晚非重讀不可的理由 —— 見 `RUNSHEET.md` §Results B4 `R1g-4b` | 量 | — | 量 `G0-head-24d` vs `G8pre-rd0`；🆕 量 `bench/2026-08-25/H3a-early.log` vs `H3a-rb.log`、`H0d-a.log` vs `bench/2026-08-24b/G4-addr-probe.log` | `PROGRESS.md` `C-17`、`RUNSHEET.md` §Results B4 |
| `MEM-16` 🆕 | 上電偏壓，未初始化 DRAM | **89.5 % 跨 16 小時斷電可重現** —— `0x80A00000` 的 8 個字，256 bits 只有 **27 bits** 不同。🔴 **虛無假設是量出來的，不是 50 %**：在這塊板上跨擷取的字對比較給出 **55.98 %** 相同，所以 89.5 % 要對照的是 56 % 而不是 50 %，其中一個字只差 **1 bit**。**而且有 1 KiB 週期**：同一次擷取裡 `0x81000000` 對 `0x81000400` 差 20/128 bits，`+0x08` 完全相同。*推*：DRAM 單元物理偏壓 + row 結構。**後果**：未初始化 DRAM 在這塊板上不是熵源，而且一個沒寫過的值跨開機看起來會是穩定的 | 量 | 推 | 量 ×2 | `RUNSHEET.md` `G0` |

## 4. Flash

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `FLS-01` | 型號 | **Eon（cFeon）EN25QH32B**，SOP-8，板上編號 `U19` | 量 | 讀 | P | `upstream/notes/hardware-inspection.md` §1 |
| `FLS-02` | 絲印／日期碼 | `cFeon · QH32B-104HIP · X703811 · 1750HKB` —— 2017 年第 50 週 | 量 | 推 | P | 同上 |
| `FLS-03` | 容量 | **4 MiB / 32 Mbit** | 量 | 量 | **×3** —— P、JEDEC 容量位元組、`0x350000` 讀回全 `FF` | 同上、`upstream/notes/flash-layout.md` |
| `FLS-04` | **JEDEC ID** | **`0x1C7016`** —— 廠商碼 `0x1C`（Eon），容量位元組 `0x16` = 2²² | 量 | 讀 | S `B2`，2026-08-23，**同一次輸出裡帶四個事前算好的控制** | `RUNSHEET.md` `B2`、`docs/loader-flash-write.md` |
| `FLS-05` | 封裝本體寬度 | **150 mil** | 量 | — | **×1** —— 一把尺，對照前一天凍結的預測。**沒有留下 artefact**，沒人能覆核 | `upstream/notes/hardware-inspection.md` §1 |
| `FLS-06` | sector 大小 | `0x1000`（4 KiB） | 量 | 推 | 值在裝置上的描述子 `+24` 讀到 —— 但它來自 loader 的**未知晶片 fallback**，不是來自這顆 flash | `docs/loader-flash-write.md` §2 |
| `FLS-07` | page 大小 | `0x100`（256 B） | 讀 | 推 | A —— fallback 的字面值 | 同上 |
| `FLS-08` | block 大小 | `0x10000`（64 KiB） | 量 | 推 | 描述子 `+16`；同樣是 fallback | 同上 |
| `FLS-09` | loader 認定的容量 | `0x00400000` = `1 << 22` | 量 | 讀 | 描述子 `+12`；**這是給不認得的晶片用的寫死預設值**。對這台正確純屬巧合 | 同上 |
| `FLS-10` | loader 的晶片表 | 32 筆，`0x20` 間距，在 `stage2` 的 `0x0d764`。**沒有一筆對得上 `1c7016`** → banner 印 `chipName: UNKNOWN` | 讀 | 讀 | A、C | `upstream/notes/loader-chip-table.md` |
| `FLS-11` | 記憶體映射視窗 | **`0xBD000000`**（KSEG1，非快取） | 量 | 讀 | loader 執行 `FLW` 時自己印出 `offset 0x003f0000<0xbd3f0000>` | `upstream/notes/uart-pinout.md` §4 |
| `FLS-12` | loader 用的第二個映射 | `0xB8000000 + 偏移`，`check_image()` 用 `lhu`/`sh` 成對搬 | 讀 | 讀 | A | `docs/loader-command-semantics.md` §a |
| `FLS-13` | 實際的抹除粒度 | `FLW` **整組命令裡沒有 erase**，而且寫 `FF` 真的能讓它回到 `FF`，所以它一定自己在抹 —— 指向 4 KiB sector 的 read-modify-erase-program 循環 | 量（行為） | 推 | C | `upstream/notes/uart-pinout.md` §4 |
| `FLS-14` | 完整 dump | 4,194,304 bytes，`sha256 a800059a…10f37ea`，用 `FLR`+`DB` 花 105 分鐘讀完，**0 次區塊重讀** | 量 | — | C | `upstream/notes/dump-vs-official.md` §1 |
| `FLS-15` | flash 的第二支儀器 | *(留白)* —— 從來沒有燒錄器讀過這顆 | — | — | — | `notes/power-and-programmer.md` §4，`⊘` |
| `FLS-16` | 夾具供電下的 `VCC` | **1.70 V**，穩定，三種供電組態都一樣 | 量 | — | C `T-85` | `notes/power-and-programmer.md` §1 |
| `FLS-17` | 同一次的 `WP#` / `HOLD#` | **1.79 V** —— 比 `VCC` **高** 90 mV，所以整片板的 3.3 V 網路本身就在 1.79 V | 量 | 讀 | C `T-85` + 這顆的腳位功能 | 同上 §2 |
| `FLS-18` | SPI 命令集 | `WREN 06` `WRDI 04` **`RDID 9F`** `RDSR 05` `WRSR 01` `READ 03` `FASTREAD 0B` `SE 20` `BE D8` `CE 60` `PP 02` `DP B9` `RDP AB` | 讀 | 讀 | B，而 `RDID` 的值 D 自己的範例也再說一次 | `docs/loader-flash-write.md` §2 |

## 5. 乙太網路 —— 交換器、PHY、埠

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `NET-01` | 交換器 | 5 埠 10/100，整合在 SoC 裡 | 量 | 讀 | 開機 log 的五個介面、板上三顆網路變壓器、D | `upstream/notes/hardware-inspection.md` §7.1 |
| `NET-02` | 網路變壓器 | `U&T UTH20T02M` ×2（各兩埠）+ `UTH16T01M` ×1；日期碼 `1818` | 量 | — | P | 同上 |
| `NET-03` | 網路孔 | **4 × LAN（橘）+ 1 × WAN（黃）** | 量 | — | P | 同上 |
| `NET-04` | 埠 ↔ 介面 ↔ VLAN | `eth1` 埠 0 vid **8**（WAN，別名 `peth0`）· `eth4` 埠 1 · `eth3` 埠 2 · `eth2` 埠 3 · `eth0` 埠 4，後四者都是 vid **9** | 量 | 讀 | C —— 廠商 kernel 自己的開機行、member port 位元遮罩 | `upstream/dumps/uart-boot.log` |
| `NET-05` | 會回應 MDIO 的 PHY 位址 | **0、1、2、3、4** | 量 | 量 | S `E4`、`E7`、`E8` —— 這台裝置有史以來第一批 MDIO 交易 | `RUNSHEET.md` `E4`–`E8` |
| `NET-06` | PHY 識別碼 | **`0x001CC880`**（MII reg 2 = `0x001C`，reg 3 = `0xC880`），**五個位址完全相同** | 量 | 讀 | S。**沒有任何來源預測得出這個值** —— 它是一次量測，不是一次確認 | `docs/loader-phy-and-switch.md` §6 |
| `NET-07` | 位址 1 | 回同一個識別碼 → loader 的 `PORT1` 跳過位址 1 是**關於埠**，不是關於 PHY。一支驅動涵蓋五個。🔴 **2026-08-24 換掉的是原因，不是這個結論。** 上機當中提出過「埠 1 沒有網路孔，所以沒東西要 patch」，被 `E11e` 否證：第五個孔一插，`PSRP1` 讀 `0x000010F9`，bit 4 `LinkUp` 起來且是那次唯一起來的一埠 —— **埠 1 有孔**（`NET-13`）。那個說法建立在一個「在桌邊被回報、沒被量」的孔數上，而 `E11e` 的否證條件事前就指名了這個結果。**剩下的候選是說明字串自己的字**：`PORT1: port 1 patch for FT2` 讀成「**為**埠 1 打的 patch」——套在另外四埠上，好讓埠 1 在 FT2 裡單獨受測；`0x8040B890` 的四個位元組正是 `{0, 2, 3, 4}`，埠 1 不在裡面。*推*，否證路徑見 §17 | 量 | 推 | S `E8`、`E11e` | `docs/loader-phy-and-switch.md` §4、`RUNSHEET.md` `E8` |
| `NET-08` | 位址 5–31 | *(留白)* —— `F1`/`F2` 未跑，而 `F1` 正是那格會終結一次上機的 | — | — | — | `RUNSHEET.md` §F |
| `NET-09` | `ExtPHYID` 對應表 | `PCRP0`–`PCRP4` 的 bit 30:26 讀到 **0、1、2、3、4** | 量 | 讀 | S `E9` + D Table 64 | `docs/loader-phy-and-switch.md` §7 |
| `NET-10` | `0xBB804118` / `0xBB80411C` | `0x00000000` / `0x187F0038` —— 後者 bit 30:26 = **6**。照 `PCRP` 每埠 4 位元組的間距推下去，這兩個就是 `PCRP5` 與 `PCRP6`。🆕 **2026-08-24 在 `PSRP` 側加上一個不變性結果**：`DW BB804128 8` 的第 7、8 個字在今天八次讀取裡全是 `0000007A`（bit 4 LinkUp、bit 3 全雙工、速度碼 `10`，**高於** `01` = 100M），第 6 個字全是 `000000E2`，而**五次實體換孔動了 `PSRP0`–`PSRP4`，這三個字一次都沒動** —— 一個有五次獨立機會被否證、五次都沒被否證的假說。🔄 **2026-08-25 加五次讀取（`E13-*`、`E11f`），共 13 次，而其中的關鍵是：這五次在另一個電源循環上。** 先前八次全在同一次開機裡，所以「跨換孔不變」與「跨開機不變」在今天以前是同一組八個讀數；現在它們被分開了。*推*：交換器內部／CPU 側的埠 | 量 | 推 | S `E9`、`E10b`、`E11a`–`E11e`。**間距是推的；手上沒有任何來源明寫這兩個位址**。`E9` 的判讀只涵蓋到第 6 個字，這兩個字是同一份 transcript 裡沒被寫下來的部分 | `RUNSHEET.md` `E9`、`docs/loader-phy-and-switch.md` §7 |
| `NET-11` | `PSRP` 位元配置 | 8 `LinkDownEventFlag`（閂鎖，讀取即清）· 7 NWayEnable · 6 RxPause · 5 TxPause · **4 LinkUp** · 3 Duplex · 1:0 速度（`01` = 100M）。🔴 **2026-08-24：bit 8 的「讀取即清」從讀來的變成量到的，而讓它成立的是那個控制。** `E11a2` 重讀一個沒動過的埠，bit 8 **還在**，當場被記成「bit 8 黏住、`DW` 不會清」—— **該判讀已撤回**。`E11c2` 是為分開這兩個模型而做的：讀的是**孔已經空掉**的 `PSRP0`，不可能再有新的 down 事件，而一次讀取就把 bit 8 從 1 帶到 0；`E11e` 第三次重現。`E11a2` 的解釋是**連線還在收斂時的第二次 autoneg 閂鎖** —— 線上的真事件，不是儀器毛病。🆕 **bit 6 與 5 第一次被逼著動**：對端從 PC NIC 換成 RTL8153 後 `ANLPAR` 由 `0xC1E1` 變 `0xCDE1`（差 `0x0C00`，bit 11 `ASM_DIR` 與 bit 10 `PAUSE`），同一時間 `PSRP` 由 `0x1099` 變 `0x10F9`（差 `0x0060`，bit 6 與 bit 5）—— **兩邊各只差它們指名的那一對，其他位元全同**。斷線狀態下五埠全是 `0x10E0`、bit 6 與 5 都是 1，所以預設是「開」，是不宣告 PAUSE 的對端把它們清掉。**沒定的是哪一個是 Rx、哪一個是 Tx**：兩個一起動，這個分派仍然只有 B 一個來源。<br>🆕 **2026-08-25 兩件新的，而第一件會走進驅動**：① 🔴 **速度與雙工欄位不受 `LinkUp` 把關。** 一個協商過、之後被拔掉的埠讀 `…E9` —— bit 4 清，**bit 3 與 bit 0 仍然報告 100M 全雙工**。對照在**單一擷取內部**：`E13-posX-lan3` 裡四個曾經協商過的埠都是 `E9`，而 `PSRP4`（這個電源循環唯一從沒協商過的埠）是 `E0`。**一支不先讀 bit 4 就讀速度的驅動，會在一個空孔上回報 100M 全雙工。** ② **bit 8 的讀取即清，第一次在 link 往「下」掉的事件上量到**：`E13-pos1-wan` 在拔線後立刻讀到 `PSRP2 = 000011E9`，`E11f-psrp2-empty` 中間什麼都沒動、讀到 `000010E9`。`NET-11` 原本的三次觀測全是 link 往上收斂時的，那種情況下「第二次真的閂鎖」與「讀取沒清掉」不可分辨；**拔線進一個空孔是這張桌子能造出來最乾淨的 down 事件**。兩件事之後又各自通過兩次獨立測試：`E13-posX-lan1` 與 `E13-posX-lan3` 各自**事先推導**了五個埠字裡的三個，六個推導字全中 | 讀 · 🔄 量（bit 8、6、5）· 🆕 量（bit 3、1:0 的保留態） | 讀 | B 給 7:0 的內容；D Table 65 確認 bit 8 與該欄位；S `E10b`、`E11a2`、`E11c2`、`E11e`、`E12b` | `docs/loader-phy-and-switch.md` §7 |
| `NET-12` | 連線狀態，量到的 | `PSRP0`–`PSRP4` = `10E0 10E0` **`1099`** `10E0 10E0` —— **恰好一埠 up**，100M 全雙工 | 量 | 讀 | S `E10`，並由對端 Windows 回報 `Up, 100 Mbps` 在裝置外佐證 | `RUNSHEET.md` `E10` |
| `NET-13` | 哪一個 RJ45 是哪一個埠號 | 🔴 **未定 —— 2026-08-24 重新打開，而它一天之內錯了兩次。** 錯因：五次換孔的孔別是**事後指派**的，而寫入時假設了「依實體位置依序走過」。**量到的是暫存器，不是孔別。** `E11a`–`E11e`（24b）五次讀數各只有一埠 bit 4 起來，集合覆蓋 {0,1,2,3,4}；`E10c`/`E10d`（24c）量到一條線從點亮 `PSRP1` 的孔移到點亮 `PSRP2` 的孔。**兩次的孔別都是事後指派的。** `E10d` 尤其不能當反證：舊圖與線性圖在孔 2 上**預測相同**，而 `PREDICTIONS-block0b.md`（早於該擷取 7 分 08 秒）正是**用舊圖**推出它的期望值 `000010F9` 並命中 —— 一個不會失敗的對照。另有一個更省的解釋未被排除：`E11e`（04:17）把線留在點亮 `PSRP1` 的孔，16 小時斷電期間沒人動它 —— 那樣 `E10c` 反而是舊圖的**確認**。🔴 **兩個方向的導出句都收回**：「照絲印取 index 的驅動會在剛好一個孔上出錯」與它的收回，今天都沒有證據。**站得住的只有兩件**：五個 RJ45 對埠 {0,1,2,3,4} 是雙射（word 6 = `0x000000E2` bit 4 恆為 0，跨 7 次換線不動，而 0–4 每一埠都至少亮過一次）；因此**任何「孔 N = 埠 N」在孔以 1..5 編號時都不可能成立，寫這句話之前必須先講清楚編號基底**。**決定它的實驗**：換孔時把孔別寫進 `--out` 檔名，四次移線四份擷取。<br>🔄 **2026-08-25 關閉，而關掉它的圖不是上面那兩張的任何一張。** 那兩張都是**位置 → 埠**、而且位置都是事後貼的；這一張是**絲印 → 埠**，也就是驅動與使用者實際看到的那一張，**每一點的標籤都由操作者在它自己的擷取之前口頭指定**：**WAN → 0**、**LAN1 → 1**、**LAN2 → 2**、**LAN3 → 3**（四點皆 **量**），**LAN4 → 4**（**推**，由雙射消去；操作者放棄第五次移線，此格照實標記）。五次讀取每一次都**恰好一埠** bit 4 起來，雙射至今 13 次沒被否證。🔴 **`LAN1 → port 1` 是重點**：port 1 正是 `PORT1` 的 patch 清單（`0x8040B890` = `{0,2,3,4}`）跳過的那一埠，而它背後是哪個孔，在今天以前從來沒有在標籤先寫好的情況下被指名過。**位置圖仍然未定，而它現在缺的是看一眼機殼絲印順序，不是一次暫存器讀取** —— 檔名裡的 `posX` 記的就是這個缺口 | 量 ×4 + 推 ×1 | — | 量 `bench/2026-08-25/E13-pos1-wan.log`、`E13-posX-lan1.log`、`E13-posX-lan2.log`、`E13-posX-lan3.log` + **D** Table 62；舊的 `E10c`/`E10d`/`E11a`–`e` 讀數為真但孔別不可用 | `docs/loader-phy-and-switch.md` §7、`RUNSHEET.md` §Results B4 |
| `NET-24` 🆕 | MDIO 位址空間，32 點掃描 | 🔴 **位址 0–4 回 `0x001c`；位址 5–31 回 `0x0000`。** 一個 `MDIOR 2` 掃完（`F2`，32 行，1042 bytes，大小逐位元組對得上）。**`docs/loader-phy-and-switch.md` §6 的事前預測「0–4 有、5–31 沒有」32 點命中**，而內建反證（32 行全同 = 匯流排在回音）有真的機會觸發卻沒觸發。**不存在的位址回 `0x0000` 不是 `0xFFFF`** —— `F1` 的風險論證裡「匯流排被拉高所以回 `0xFFFF`」那半被否證，「交易會完成」那半成立 | 量 | 讀 | 量 `F1` + `F2` | `docs/loader-phy-and-switch.md` §6 |
| `NET-14` | 讀到的 MII 暫存器 | `BMSR` 有連線 `0x78ED` / 無連線 `0x78C9` · `BMCR` `0x1100` · `ANLPAR` `0xC1E1`（對端是 PC NIC）。🆕 **2026-08-24 加兩筆 `ANLPAR`**：對 RTL8153 USB GbE 對端讀 **`0xCDE1`**（`E12b`，`PHYR 1 5`）；對一個**沒連線**的埠讀 **`0x0001`**（`E12e`，`PHYR 0 5`）—— 只剩 802.3 selector，能力位元全清。後者同時是 `E12b` 的控制：兩個值不同 ⇒ 這個暫存器**是每埠一份**；若整組共用一個值，`NET-11` 那個流量控制的比對會整個作廢。而且沒連線的埠是**被清掉**，不是留著上一個對端的宣告 | 量 | 讀 | S `E12`、`E12b`–`E12e`，名稱來自 MII 標準 | `RUNSHEET.md` `E12`、`docs/loader-phy-and-switch.md` §7 |
| `NET-15` | MDIO 控制器 | `MDCIOCR` `0xBB804004` —— bit 31 `COMMAND`、`PHYADD[4:0]` 28:24、`REGADD[4:0]` 20:16、`WRDATA[15:0]` 15:0 · `MDCIOSR` `0xBB804008` —— bit 31 `STATUS`、`RDATA[15:0]` | 讀 | 讀 | **×3** —— A、B（`asicregs.h` 與 `rtl8651_getAsicEthernetPHYReg()`）、**D Table 57–59** | `docs/loader-phy-and-switch.md` §1 |
| `NET-16` | 一次 MDIO 讀取**不是**唯讀 | `phy_read()` 會寫 `MDCIOCR`，**而且會設 `GIMR` bit 8**，且從不還原 | 讀 | 讀 | A，並在裝置上以因果方式確認 | 同上 §2、`RUNSHEET.md` `C5` |
| `NET-17` | MDIO 等待沒有 timeout | `0x80402FD8` 的 `bltz v1`，沒有 timeout、沒有迴圈上限。一個永不回應的位址會終結整次上機 | 讀 | 讀 | A | 同上 §1 |
| `NET-18` | MDIO 的 erratum 延遲 | **每一次**讀取都 `delay(10)`，而且沒有版本判斷 —— B 把同一個延遲關在 `RTL8196C_REVISION_A` 之後 | 讀 | 讀 | A + B | 同上 §1 |
| `NET-19` | `PITCR` `0xBB804100` | 讀到 **`0x00000000`** = `UTP (10/100M embedded PHY)`。會把 bit 0 設起來的那段 strap 分支，**在這片板上沒有跑** | 量 | 讀 | S `E9` + D Table 63 | 同上 §7 |
| `NET-20` | `PCRP0` | `0x007F0039` —— `EnForceMode` 是 0，所以 loader 的 force-mode 設定同樣沒跑 | 量 | 讀 | S `E9` + D Table 64 | 同上 §7 |
| `NET-21` | loader 碰過的交換器暫存器 | 48 個 `lui …,0xbb80` 站點收斂成 13 個相異位址，**全部落在 `0xBB804xxx`**：`4000 MACCR` · `4004 MDCIOCR` · `4008 MDCIOSR` · `4100 PITCR` · `4104 PCRP0` · `414C P0GMIICR` · **`4234` 無名** · `4418 SWTCR0` · `4428 FFCR` · `4A08 PVCR0` · `4D00 SWTACR` · `4D08 SWTAA` · `4D3C TCR7` | 讀 | 混合 —— 4 個有 D + B，8 個只有 B，**1 個兩者皆無** | A | 同上 §7、`C-15` |
| `NET-22` | `0xBB802xxx`（B 的 MII 記憶體映射影子） | **這顆不能用** —— D 沒有這個區塊，而且 loader 那 48 個站點沒有一個落在裡面 | 讀 | 讀 | A + D | `docs/loader-phy-and-switch.md` §7 |
| `NET-23` | 網卡驅動怎麼稱呼這顆 | `chip name: 8196C, chip revid: 0` —— **錯的，而且它自己在兩行前就自我否定**：它宣告自己正在探測 RTL8186 | 量 | — | C | `upstream/notes/uart-findings.md` §2 |

## 6. 無線電

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `RF-01` | 型號 | **Realtek RTL8188ER** —— 1T1R 802.11n，2.4 GHz，獨立一顆 | 量 | 讀 | **×1 —— 只有 P。** 驅動 banner 印的是版本，不是料號 | `upstream/notes/hardware-inspection.md` §4 |
| `RF-02` | 絲印 | `RTL8188ER · I210QP1 · GF08` | 量 | — | P | 同上 |
| `RF-03` | 驅動 banner | `Realtek WLAN driver - version 1.6 (2013-02-21)`、`Adaptivity function - version 7.1` | 量 | — | C | `upstream/dumps/uart-boot.log` |
| `RF-04` | 它怎麼接到 SoC | *(留白)* —— 從沒追過 | — | — | — | gate R10d |
| `RF-05` | 天線 | 絲印上有 `ANT1` 與 `ANT2`；**只裝一支**，一條線走到板子右緣 | 量 | 推 | P | `upstream/notes/hardware-inspection.md` §7.3 |
| `RF-06` | 校正資料 | 在 flash `0x006000` 的 `H601` 裡。**回復原廠設定不會還原它** —— 永久禁寫 | 讀 | 讀 | A、C | `plan/` D4、`CLAUDE.md` |
| `RF-07` | WPS | 開機時就活著 —— `WiFi Simple Config v2.18-wps1.0` | 量 | — | C | `upstream/notes/uart-findings.md` §3 |

## 7. 板子 —— 電源、連接器、排針

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `BRD-01` | 穩壓器 | `LSC · LSP5526 · 181525`，8 腳，旁邊有 `D3`。**未識別**；降壓只是從位置推的。輸出腳從沒量過 | 量（油墨） | 推 | **×1** | `upstream/notes/hardware-inspection.md` §5 |
| `BRD-02` | 外部供電 | **只有一條 3.3 V**；核心 1.0 V 由內建 SWR 或 LDO 產生 | 讀 | 讀 | D §1 | `notes/power-and-programmer.md` §2 |
| `BRD-03` | 電源輸入 | 板子左上角的圓形插孔，旁邊有一顆按鈕 | 量 | — | P | `upstream/notes/hardware-inspection.md` §7.1 |
| `BRD-04` | `J2` | 緊鄰插孔的 2 腳排針；機殼開關的辮子線插在這裡。**「串在 DC 迴路上」是從照片來的佈局推論**，用通斷檔就能定案 | 量 | 推 | P | 同上 §7.4 |
| `BRD-05` | 那顆按鈕 | 🔴 **2026-08-24 定案：是 `PABCD` bit 5 上的一支 GPIO，低準位有效（有上拉），不是 `RESET#`。** `DW B8003500 1` 放開時回 `FFFFFFDF 00000000 FF000000 0000003C`、按住時回 `FFFFFFDF 00000000 FF000000 0000001C` —— **XOR = `00000020`，只有 bit 5**。`PABCD_CNR`（`0xB8003500`）與 `PABCD_DIR`（`0xB8003508`）在按與放之間逐位元組不變，`DIR` bit 5 = 0 是輸入；`B7c.log` 裡沒有 `Booting...`、沒有 banner，按住期間板子沒有重置。**是一個動了的位元回答的，不是一個「沒有」** —— `D3` 那格永遠只能產生「沒有」。原本那句「三個『沒有』指向 `RESET#`」留在這裡當記錄，**而其中一個根本不是『沒有』**：loader 的 init 自己把這支腳設成 GPIO 輸入，`0x80408DE4` 會讀它 | 量 | 讀 | **×3** —— S `B7a`／`B7b`／`B7c`、A（`0x804083AC` 清掉 bit 5，由 `main` `0x80406778` 無條件呼叫）、D §8.3 給名字 | `notes/power-and-programmer.md` §3、`C-14` |
| `BRD-06` | `RESET#` | SoC 第 49 腳，**與 `LED_PORT3` 和 `GPIOB[5]` 共用** —— 所以在這個設計上它可能是一支 LED 驅動腳 | 讀 | 讀 | D §6、§6.1 | `notes/power-and-programmer.md` §3 |
| `BRD-07` | `SF_CS0#` / `SF_SCK` | 第 45 與 48 腳，是 SoC 的**輸出**。D 沒有任何一句說它們在 `RESET#` 拉低時會 tri-state | 讀 | 讀 | D | 同上 §3 |
| `BRD-08` | UART 排針 | 4 腳 2.54 mm，**出廠就焊好**，絲印寫 `UART`，在板子下緣 LED 排旁邊。**整個 W02 沒有任何一處需要焊接** | 量 | 量 | P + 逐腳掃描 | `upstream/notes/uart-pinout.md` |
| `BRD-09` | UART 腳位 | 第 1 腳 **`VCC 3.3 V`** · 2 **`TX`**（板 → 你）· 3 **`RX`** · 4 **`GND`** | 量 | 量 | 每腳兩個來源；`RX` 是在 ESC 打進去中斷了開機之後才從論證變成量測 | 同上 §1、§5 |
| `BRD-10` | 主控台參數 | **38400 8N1**，邏輯準位 **3.3 V** | 量 | 量 | 最窄脈衝 26 µs 對 38400 的 26.042 µs（差 0.16%），再用同一份 capture 裡的 52 µs 脈衝自我檢查，最後由解碼成可讀 ASCII 確認 | 同上 §2 |
| `BRD-11` | EJTAG | D 說是 5 訊號 P1149.1。**板上沒有定位過**，也沒有轉接器 | 文 | 讀 | D §1 | `upstream/docs/lab-inventory.md` §3 |
| `BRD-12` | 未貼裝的焊墊 | `U6` 周圍是空的。未調查 | 量 | — | P | `upstream/notes/hardware-inspection.md` §7.1 |

## 8. 位址地圖

除非另外標記，每一列都是「讀」；固定住多數位址的是 loader 自己的用法。

| id | 範圍／位址 | 是什麼 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `MAP-01` | `0x80000000` | KSEG0 —— 快取的 RAM | 文 | 文 | MIPS 架構。**這個 repo 裡沒有任何檔案擁有這一列** —— `spec-check.py` 的 C5 就是這樣找出來的 | — |
| `MAP-02` | `0xA0000000` | KSEG1 —— 非快取別名。**loader 自己跑在這裡**：它在 `0x804004a8` 跳到自己下一條指令的 KSEG1 別名 | 讀 | 讀 | A | `notes/cache-model.md` |
| `MAP-03` | `0xB8000000` | SoC 周邊暫存器空間 | 讀 | 讀 | A + D | `plan/` F7 |
| `MAP-04` | `0xB8001000` | 記憶體控制器（`MCR`） | 讀 | 讀 | D §7.4.1 | `SOURCES.json` |
| `MAP-05` | `0xB8001200` | SPI flash 控制器 | 讀 | 讀 | **×3** —— A、B、D §7.4.5–7.4.9 | `docs/loader-flash-write.md` §2 |
| `MAP-06` | `0xB8002000` | UART（DLAB 切換在 `0xB8002100`/`2000`） | 讀 | 讀 | D §9.3、A | `SOURCES.json` |
| `MAP-07` | `0xB8003000` | 中斷控制器 | 讀 | 讀 | D §8.1、A、S | 見 §10 |
| `MAP-08` | `0xB8003100` | 計時器／看門狗 | 讀 | 讀 | D §8.2、E、A、S | 見 §10 |
| `MAP-09` | `0xB8003500` | GPIO —— `PABCD_CNR 3500`、`PABCD_DIR 3508`、`PABCD_DAT 350C` | 文 · 🔄 量 | 讀 | D §8.3。🔄 **2026-08-24 第一次在這台上讀到**（`B7a`／`B7c`）：三個偏移的行為與名字相符，讀數的擁有者是 `BRD-05`。原本這一格寫的是「本專案從未碰過」 | `SOURCES.json`、`notes/power-and-programmer.md` §3 |
| `MAP-10` | `0xB8B01000` | PCIe host 模式。**不是網路 MAC** | 文 | 讀 | D §10.3 | `SOURCES.json` |
| `MAP-11` | `0xBB804000` | 交換器核心 | 讀 | 讀 | A + D §11.1 | `docs/loader-phy-and-switch.md` §7 |
| `MAP-12` | `0xBD000000` | SPI flash 的記憶體映射，KSEG1 | 量 | 讀 | loader 自己在 `FLW` 那行印的 | `upstream/notes/uart-pinout.md` §4 |
| `MAP-13` | `0xBFC00000` | reset 向量 —— `J BFC00000` 就是重置命令 | 讀 | 讀 | A | `docs/loader-command-semantics.md` §f |
| `MAP-14` | `0x80400000`–`0x8040DD10` | loader 自己的映像與 `.data`；`.bss` 從 `0x8040DD10` 起，堆疊在其下 | 讀 | 讀 | A；載入基底是從指標序列還原出來的，不是假設的 | `upstream/notes/loader-chip-table.md` §1 |
| `MAP-15` | `0x80500000` | 這台的 kernel 映像被搬去的位置 —— 就是它自己容器標頭裡的 `startAddr` | 量 | 讀 | S `B3`/`B4` + A | `RUNSHEET.md` `B3` |
| `MAP-16` | `0x81000000` | *(選定)* bench 用的 scratch —— 進 SDRAM 16 MiB，遠離 loader 與已就位的 kernel。🔴 **2026-08-24 這個選擇的理由被否證**：`C7-pre` 在 `0x81000400` 量到一張活的描述子表（`MEM-11`），而 `0x81000000` 在同一個區域裡。原本的論證只排除了兩個**已知**的東西（loader 的映像、已就位的 kernel），沒排除 loader 在執行期自己配出來的任何東西。part one 寫在一個活結構上而什麼都沒壞，那是運氣不是設計。**這一列留著**，因為 part one 的 `C1`–`C4` 就是在這個位址上量的 | — | — | 這是選的，不是裝置性質；否證見 `MEM-11` | `RUNSHEET.md` §C |
| `MAP-17` 🆕 | `0x80A00000`–`0x80AF1002` | *(選定)* `§G` 的上傳位址與 `§D` 的 canary —— 在已就位 kernel 的結尾（`0x805F1002`）之上、16 MiB 那些結構之下。`G4-addr-probe` 在頭部讀到八個字，**沒有指標形狀的字、沒有自我指涉、沒有週期**（`MEM-13`）。**選它的否證條件寫在選它之前**：`G0` 要讀頭、中、尾，**任何一個指標形狀的字，位址就重選** | — | — | 這是選的；支撐它的量測在 `MEM-13` | `RUNSHEET.md` `G0` |

## 9. 在這台裝置上讀過或寫過的暫存器

**只有真的有讀數的列才標「量」。** 值欄是這台在 `<RealTek>` 提示字元下回的東西，
未特別註明者皆為 2026-08-23。

| id | 位址 | 名稱 | 讀數 | V | N | 來源 | 擁有者 |
|---|---|---|---|:-:|:-:|---|---|
| `REG-01` | `0xB8003000` | `GIMR` | `0x00008100` —— 在提示字元下 **bit 8 `TCIE` 與 bit 15 `SWIE` 就已經是 1** | 量 | 讀 | S `E3`、D、B | `docs/loader-phy-and-switch.md` §2 |
| `REG-02` | `0xB8003004` | `GISR` | `0x88000004`；看到 bit 8 `TCIP` 在被遮住時閂鎖、遞送之後清掉 | 量 | 讀 | S `C5` | `RUNSHEET.md` `C5` |
| `REG-03` | `0xB800300C` | `IRR1` | `0x30050004` —— loader 寫的是 `0x00050004`；bit 28 的 `SWIRS` = 3 | 量 | 讀 | S `E3` + A | 同上 |
| `REG-04` | `0xB8003010` | `IRR2` | *(未讀)* | — | 讀 | D | — |
| `REG-05` | `0xB8003100` | `TC0DATA` | `0x0022E0A0` = 142,858 << 4 —— **所以計數欄位是 bit 31:4** | 量 | 量 | S `E2b` + A 的編譯進去的值 | `docs/loader-phy-and-switch.md` §2 |
| `REG-06` | `0xB8003104` | `TC1DATA` | `0x00000000` | 量 | 讀 | S `E2b` + `SOURCES.json` 的位移表 | `RUNSHEET.md` `E2b`、`SOURCES.json` |
| `REG-07` | `0xB8003108` | `TC0CNT` | `0x0010B960` | 量 | 讀 | S `E2b` + `SOURCES.json` 的位移表 ⚠️ 🆕 **n=1** —— 全 repo 只有這一次讀過 `0xB8003108`（2026-08-23）。**「這個暫存器是內部計數器的活鏡子」目前只有 D §8.2 一句話撐著**，而 `probe3` 的整組計時儀器站在它上面。最便宜的補強是 prompt 上 `DW B8003108 1` 讀兩次（`LDR-07` 進位成四個字，一次同時回 `TC0CNT`／`TC1CNT`／`TCCNR`／`TCIR`）—— 見 `docs/probe3-cells.md` `P1` | `RUNSHEET.md` `E2b`、`SOURCES.json` |
| `REG-08` | `0xB800310C` | `TC1CNT` | `0x00000000` | 量 | 讀 | S `E2b` + `SOURCES.json` 的位移表 | `RUNSHEET.md` `E2b`、`SOURCES.json` |
| `REG-09` | `0xB8003110` | `TCCNR` | `0xC0000000` —— 與 `timer_init` 寫的完全一樣 | 量 | 讀 | S `B7` + A | `RUNSHEET.md` `B7` |
| `REG-10` | `0xB8003114` | `TCIR` | `0x80000000` | 量 | 讀 | S `B7` + A | 同上 |
| `REG-11` | `0xB8003118` | `CDBR` | `0x000E0000` —— 除頻欄位 **14** | 量 | 讀 | S `B7` + A + D | 同上 |
| `REG-12` | `0xB800311C` | `WDTCNR` | `0xA5000000` —— `WDTE[7:0]` = `0xA5`，**停止**樣式；**上電重置後 `WatchDogIND` bit 20 = `0`**。🔴 **這是硬體 reset 預設值，不是 loader 寫的**：2026-08-24 桌面查證，loader 對 `WDTCNR` 只有兩個寫入點（`0x804012F8`、`0x804092E8`），兩個都是 `sw zero` 後緊接自跳迴圈，之後沒有任何指令執行。`B7` 原本的判讀暗示相反，已更正 | 量 | 讀 | **×4** —— S `B7`、D §8.2.9 Table 27、E、B | `RUNSHEET.md` `D2`、`docs/loader-command-semantics.md` §f |
| `REG-13` | `0xB8001200`–`0x1210` | `SFCR` `SFCR2` `SFCSR` `SFDR` `SFDR2` | 🔴 **第一次在裝置上讀到，量 2026-08-25b。** 冷上電 `3FC00000 0BA08000 D8050000 FFFF0002`、watchdog 重置後 `3FC00000 0BA08000 D8050000 FFFF0000`（`SPI-cold` / `SPI-warm`，`DW B8001200 4`）。**`SFCSR = D8050000` 依 D table 10 解碼**：`CSB0`=1、`CSB1`=1（兩個 chip select 都不啟用）、`LEN`=`01`（2 bytes，**不是重置值 `11`**）、`SPI_RDY`=1、`IO_WIDTH`=serial、`CHIP_SEL`=0、`CMD_BYTE`=**`0x05`**，也就是 SPI 的 `RDSR`。`SFCR2` 最高位元組是 **`0x0B`**，`Fast Read`。**所以 loader 不是把控制器留在重置狀態，是留在輪詢狀態。** ⚠️ `SFDR2`（`0x1210`）仍未讀：`LDR-07` 把字數往上進位到 4 的倍數，`DW B8001200 1..4` 都只印四個字，要讀第五個得換起始位址。⚠️ **`DW` 只發 load**；這個控制器是靠**寫** `SFDR` 下命令（`sw` `0x9F000000`，見 owner 檔的 `RDID` 反組譯），所以這是唯讀的 | 🔄 **量 ×2** | 讀 | **×3** —— A（`SFCSR` 19 處參照、`SFDR` 14 處）、B、D；🆕 量 `bench/2026-08-25b/SPI-cold.log`、`SPI-warm.log` | `docs/loader-flash-write.md` §2 |
| `REG-14` | `0xB8001208` bit 27 | `SPI_RDY` | *(未讀)* —— 但**這台自己的 `ComSrlCmd_RDID()` 就是在等這個位元**，那是程式碼倚賴語意，比一份文件宣稱語意更硬 | — | 讀+ | A + D table 10 | 同上 |
| `REG-15` | `0xBB804100` | `PITCR` | `0x00000000` | 量 | 讀 | S `E9` + D Table 63 | `docs/loader-phy-and-switch.md` §7 |
| `REG-16` | `0xBB804104`–`0x4114` | `PCRP0`–`PCRP4` | `007F0039` `047F0039` `087F0039` `0C7F0039` `107F0039` | 量 | 讀 | S `E9` + D Table 64 | 同上 |
| `REG-17` | `0xBB804128`–`0x4138` | `PSRP0`–`PSRP4` | `10E0` `10E0` **`1099`** `10E0` `10E0` | 量 | 讀 | S `E10` + D Table 62 + B | 同上 |
| `REG-18` | `0xBB80413C` | B 稱 `PSRP5`；**D 的 Table 62 跳過 `0x3C`** | `0x000000E2` | 量 | — | S `E10`；兩份文件不一致，而 D 是這顆 | `RUNSHEET.md` `E10`、`docs/loader-phy-and-switch.md` §7 |
| `REG-19` | `0xBB804140` | D 稱 `PSRP6` | `0x0000007A` | 量 | 讀 | S `E10` + D Table 62 | `RUNSHEET.md` `E10` |
| `REG-19b` | `0xBB804144` | 依 `PSRP` 間距是下一埠；**沒有來源明寫** | `0x0000007A` | 量 | 推 | S `E10` | `RUNSHEET.md` `E10` |
| `REG-20` | `0x8040DBC0` | loader 命令表的 `?` 那一列 | `8040B070 00000000 80409A9C 8040B074` —— 名稱指標、`argc`、handler、說明指標，16 位元組間距 | 量 | 讀 | S `B1` —— 這是整份 runsheet 其他位址的全域控制 | `RUNSHEET.md` `B1` |
| `REG-21` | `0x8040FBD4` | flash 晶片描述子 | `001C7016 1C701600 16000000 00400000 / 00010000 00000040 00001000 00000400` | 量 | 讀 | S `B2` + A | `docs/loader-flash-write.md` §2 |
| `REG-22` | `0x8040DBA4` | `gCHKKEY_HIT` | 從上電前就串流 ESC 時是 **`1`** —— 於是 `check_image()` 短路，checksum 迴圈從沒跑過 | 量 | 讀 | S `B5` + A + B | `C-13`、`C-16` |
| `REG-23` | `0x8040D4A0` | `AUTOBURN` | **上電後 `1`** —— 提示字元下自動燒錄是**開著的**；🔄 **`AUTOBURN 0` 之後量到 `0`**（`C6`），而**每一次重置都會把它打回 `1`** | 量 | 讀 | S `B6`、`C6` + A 的初始值 | `RUNSHEET.md` `B6`、`C6` |
| `REG-24` | `0x8040DD3C` | 正在測試的候選偏移，偏置 `0x05000000` | `0x05060000` —— **值對了而判讀被否證**：它存的是**正在試**的候選，不是被接受的那個 | 量 | 讀（已更正） | S `B3` + A | `RUNSHEET.md` `B3` |
| `REG-25` | `0x8040DCE8` | 計時器 tick 計數 | 61.842 秒內 `0x0000473A` → `0x00005F52`（2026-08-23）。🔄 **2026-08-24 同一次開機四次讀數：252,061 · 277,390 · 428,675 · 485,370**，`CLK-02`／`CLK-04` 就是從它們算的。🆕 **它同時是一個會期連續性的證人，而且證詞有預測值**：主控台轉接器掉出 USB 匯流排之後 `CONT2` 讀到 428,675，一台在那段空窗裡重置過的板子最多只會讀到約 60,000 —— **板子整段時間都在跑**。這個鑑別器不依賴 DRAM 保存，`D2b` 依賴 | 量 | 讀 | S `E1`／`E2`（2026-08-23）、`E1b`／`E2b`／`CONT2`／`CONT3`（2026-08-24）+ A | `docs/loader-phy-and-switch.md` §2、`bench/README.md` |
| `REG-26` 🆕 | `0xB8003500` | `PABCD_CNR` | `0xFFFFFFDF` —— 🔴 **bit 5 是整個 32 位元字裡唯一被清掉的位元**，而反組譯說 `0x804083AC`（由 `main` `0x80406778` 無條件呼叫）清的就是 `0xB8003500` 與 `0xB8003508` 的 bit 5。一個位元被指名，矽片上正好只有那一個位元是清的 | 量 | 讀 | S `B7a`／`B7c`（2026-08-24）+ A + D §8.3 | `notes/power-and-programmer.md` §3 |
| `REG-27` 🆕 | `0xB8003508` | `PABCD_DIR` | `0xFF000000` —— bit 5 = 0，**輸入**。按住與放開之間逐位元組不變 | 量 | 讀 | S `B7a`／`B7c` + A + D §8.3 | 同上 |
| `REG-28` 🆕 | `0xB800350C` | `PABCD_DAT` | 放開 `0x0000003C` · 按住 `0x0000001C` —— **XOR `00000020`，只有 bit 5**，低準位有效。判讀見 `BRD-05` | 量 | 讀 | S `B7a`／`B7c` + D §8.3 | 同上 |
| `REG-29` 🆕 | `0xB8000000` | 晶片識別 | `0x8196E001`（同一次的 w2 `00000002`、w3 `00100200`，兩個都無名）。判讀見 `CPU-32` | 量 | 讀 · 推 | S `B7b` + A（`CPU-03` 的比較常數 `0x8196E000`） | `RUNSHEET.md` `B7b` |
| `REG-30` 🆕 | `0xB800000C` | 按鈕來源的多工選擇 | `0x0000000F` —— 低 nibble 是 `0xF`，**不是 13**。`0x80408DE4` 只在那個 nibble 等於 13 時才改從 `[0x8040DD4C]+0x44` 取按鈕狀態，所以那條分支沒被走，`B7a`／`B7c` 讀到的就是 loader 讀的那支腳。**沒有這一格，`B7a` 量到的是某支腳，不必然是*那*支** | 量 | 讀 | S `B7b` + A | `notes/power-and-programmer.md` §3、`RUNSHEET.md` `B7b` |

## 10. 時脈、計時器、看門狗、中斷

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `CLK-01` | CPU 時脈 | **400 MHz** | 量 | — | banner、D | `bench/2026-08-23/A-catch.log` |
| `CLK-02` 🔄 | **周邊 Lexra 匯流排時脈**（D：System_clock (Peripheral Lexra Bus)）—— 🔴 **舊名「計時器基底時脈」2026-08-26 撤回，見來源欄** | 🔴 **量到 200.0049 MHz，±0.0015 MHz（2080 秒基線下 ±7 ppm）**，對照編譯進去的 `0x0BEBC200` = 200,000,000 —— 差 **+24 ppm**，在一般晶振的容差之內。**取代 2026-08-23 的 199.48 MHz（差 0.26%），而倒下的是儀器不是板子**：那次的區間是**手計時**的 61.842 秒，人讀錶大概好到 ±0.15 s，在 61.842 s 上就是 **±0.25%** —— 正好是當時被當成「這片板子的性質」報出來的那個偏差。舊值留在這裡，連同它為什麼倒 | 量 | 讀 | 由三個在矽片上讀到的項推導：tick（`CLK-04`）、`CDBR`（14）、`TC0DATA`（142,858）。2026-08-24 的區間取自四份 capture 的 `.log` mtime 而不是手錶；三條互不共端點的基線給到五位有效數字都一樣 🔴 **2026-08-26：這一列的「名稱」對不上它自己引的那份資料表，而這是會讓下一個讀者反乘 14 倍的那種缺陷。** D §8.2.8 Table 26 明寫「Base clock = System_clock (Peripheral Lexra Bus)/N」—— 在 D 的用語裡 **base clock 是除頻器的輸出**（14.286 MHz，見 `CLK-17`），而這一列量的 200.0049 MHz 是**輸入**，D 叫它 System_clock (Peripheral Lexra Bus)。**名稱應為「周邊 Lexra 匯流排時脈」。** 🆕 **同時這一列多一個這個 repo 沒引過的來源**：D §9.4 Table 50「Value of divisor latch=[base clock/(16 x baud rate)]-1. **The base clock is 200MHz.**」—— UART 那條鏈，與計時器鏈完全無關，卻給出同一個數字 | `docs/loader-phy-and-switch.md` §2 |
| `CLK-03` | 它與 `CLK-01` 的關係 | 🔄 **2026-08-25 拿到第一個實驗約束，而且是免費的。** `H1b` 的 `--esc-after` 把 payload 報告與重置後開機放進同一份擷取，`Δ = t(重置後第一個 byte) − t(`rlxprobe: end` 最後一個 byte)` = **123.7 ms**。三個事前登錄的候選：**130.4 ms（400 MHz 且 3 cycles/迴圈）存活**，低 5.1 %，在本儀器已證實的散布內；**172.3 ms（4 cycles）否證，差 1.39 倍**；**256.2 ms（200 MHz 且 3 cycles）否證，差 2.07 倍**。得 `f/CPI = 1.408 × 10⁸ iterations/s`。⚠️ **它量的是 `f/CPI` 不是 `f`** —— 400 MHz 配 6 cycles 與 200 MHz 配 3 cycles 無法區分 —— **所以存活的是那個組合**，`CLK-01` 的 400 MHz 仍然只是 banner 印出來的字串。n=1 | 量 ×1 | — | 量 `bench/2026-08-25/H1b.timing`；`tools/rlxprobe/uart.S` `rlx_reset` 的 16,777,216 圈三指令迴圈是**讀** | gate R1 |
| `CLK-04` | 計時器 tick | 🔴 **量到 100.0018 Hz**（2026-08-24，四次 `DW 8040DCE8 1`，區間取自 `.log` mtime）。**取代 2026-08-23 的 99.74 Hz**，理由與 `CLK-02` 同一個：那 0.26% 是手錶。**每一條不含 `E1b` 的基線都給到五位有效數字的 `100.0018`** —— 567 s、1513 s、2080 s 三條，而 `CONT2`→`CONT3` 與 `E2b`→`CONT2` 連端點都不共用。殘差只有一個名字：`E1b` 的 mtime 距它取樣 tick 的那一刻約 15 ms，那是 CP2102 latency timer 的尺度（1–16 ms），短基線上由它支配 —— 253 秒那條因此讀出 +84 ppm | 量 | 讀 | S `E1b`／`E2b`／`CONT2`／`CONT3`（2026-08-24）+ A；被取代的舊值來自 S `E2`，61.842 秒內 6,168 個計數 | 同上 |
| `CLK-05` | `delay(10)` | 一個 tick = **10 ms**，與 B 的 `mdelay(10)` 相符 | 讀 | 讀 | A + B | 同上 |
| `CLK-06` | 除頻欄位的語意 | `CDBR` 的 14 就是除數本身 —— 若是 15，基底會變成 213.7 MHz，那不是任何人會做的時脈 | 量 | 推 | 推導 | 同上 |
| `CLK-07` | 看門狗逾時選擇 | `OVSEL[3:0]` = `0000` = 2¹⁵ 個基底時脈 tick —— 十個檔位裡最短的 | 讀 | 讀 | D Table 27 | `docs/loader-command-semantics.md` §f |
| `CLK-08` | 看門狗的實際牆鐘時間 | 🔴 **量到了，兩個 `OVSEL` 點，而區間要寫成區間。** 逾時 = **1122.5–1149.9 ms**（`OVSEL=1001`，2²⁴）與 **549.6–577.4 ms**（`OVSEL=1000`，2²³）—— 下界是「命令回音的 read#0 → 最後一個 ESC 回音」，上界再加一個心跳；心跳被工具自己的 `drain(0.02)` 量化在 **20.35 / 20.32 ms**。算出來是 **1174.376 / 587.188 ms**，兩者都短。**分頻／未分頻沒有疑義** —— 未分頻候選 83.9 ms，差 14 倍。🆕 **與時間戳完全無關的獨立確認**：兩格回音的 ESC 個數是 **56 對 28 = 恰好 2.000**，這確認了 `0x240000`／`0x40000` 的 `OVSEL` 分裂欄位解碼，不依賴任何一個時間戳。殘差見 `CLK-08b`。🔄 **2026-08-25 在 2.118 ms 的心跳上重量（`H3c`），區間收成單點**：**1118.133 ms** 與 **557.583 ms**，比值 **2.0053**，兩者都落在上面的舊區間內；ESC 回音 **528 : 262 = 2.0153**，同樣不用任何時間戳 | 量 | 讀 | 量 `D4`、`D4c`（20.35 ms 格點）+ 🆕 量 `H3c-D4`、`H3c-D4c`（2.118 ms 格點）+ **D** Table 27 | `RUNSHEET.md` `D4`、§Results B4；`bench/2026-08-24c/D4.timing`、`bench/2026-08-25/H3c-D4.timing` |
| `CLK-08b` 🆕 | 看門狗實際數的時脈 | 🔴 **14.9650 MHz ±0.02，2026-08-25 關閉，而殘差是「比例」不是「固定」。** `H3c` 把心跳從 20.35 ms 換成 **2.118 ms**（`CLK-16` 說的就是這個格點），兩個 `OVSEL` 點量到 **1118.133 / 557.583 ms**。設 `L` 為延遲、`c` 為「武裝 → 提示字元最後一個 byte」的共用偏移：**固定延遲模型無解** —— 由 `D4` 得 `L+c = 56.243 ms`，由 `D4c` 得 `29.605 ms`，同一個量兩個值，差 26.638 ms。比例模型 `2²⁴/(A₄+c) = 2²³/(A₄c+c)` 有唯一解 `c = 2.967 ms`、`f = 14.9650 MHz`。**`c` 不是自由參數**：它跨越 loader 的提示字元，而提示字元必須先被送出去，兩份擷取裡命令回音到第一個 ESC 之間都是 **10 bytes = 2.604 ms**，所以 `c ≥ 2.604`；解出的 2.967 只多 0.363 ms（讀取延遲）。**看起來漂亮的 15.000 MHz（= 200.0049 × 3/40）需要 `c` = 0.32 與 1.64 ms 兩個互相矛盾的值，而且兩個都小於提示字元自己的傳輸時間 —— 排除。** 🔴 **被否證的是 `f_timer/14 = 14.2861 MHz`，差 4.75 %**，而計時器基頻本身量到 ±7 ppm，所以這個差距是它的約 6800 倍：**看門狗數的不是分頻後的計時器時脈**。舊的 14.53–15.26 MHz 區間收窄約 35 倍且新值落在其中。**1.495 那個比值是 20.35 ms 格點的假象，撤回。** 14.965 MHz 與 200.0049 MHz 之間的整數關係**未定**（`/13`=15.385、`/14`=14.286、`400/26`=15.385、`400/27`=14.815，沒有一個在 1 % 內）—— 見 §17 | 量 | — | 量 `H3c-D4` + `H3c-D4c`（兩點兩未知數，加上 `c` 的物理下界） | `RUNSHEET.md` §Results B4；`bench/2026-08-25/H3c-D4.timing` |
| `CLK-16` 🆕 | ESC 心跳的**實得**週期 | 🔴 **要求 20.00 ms 實得 20.35／20.32；要求 2.00 ms 實得 2.32 ms；要求 0.1 µs 實得 29 µs。**差額原本記成主機端一個約 **0.32 ms** 的固定額外開銷。🔴 **那個「固定」在真實序列埠上被否證，2026-08-25**：同一天的三份裝置擷取，要求 20.00 ms 實得 **20.29**（+0.29 ms，`A-catch`）、要求 2.00 ms 實得 **2.118 / 2.115**（+0.118 ms，`H3c`）。**額外開銷不是固定的加項**（0.29 對 0.118），**也不是固定的乘項**（+1.45 % 對 +5.9 %）。舊的三個點全在 pty 上量的，這三個在 `/dev/ttyUSB0` 上，所以兩組不衝突 —— **衝突的是「固定常數」這個模型**，它現在只在 pty 上成立。**這是每一份用 ESC 回音量出來的區間所被量化到的格點**，所以它由 `console-capture.py` 逐次擷取量測並寫進該次的 `.meta.json`，而不是由讀者假設成請求值。29 µs 是這台主機上這個迴圈的地板 | 量 ×3（pty）+ 🆕 量 ×3（裝置） | — | 量，`tools/test-console-capture.sh` P11／N15／N16；`bench/2026-08-25/H3c-D4.meta.json` | `tools/console-capture.py` |
| `CLK-09` | 上電後的 `WatchDogIND` | **`0`** | 量 | 讀 | S `B7` + D | `RUNSHEET.md` `B7` |
| `CLK-10` | 看門狗重置後的 `WatchDogIND` | 🔴 **`A5000000` —— bit 20 是清的**，兩次獨立量測（`D2` 在 `J BFC00000` 之後、`D2d` 在真正的逾時之後）。**這個位元撐不到 prompt**，所以 `C-8` 不能用它 —— `CLK-10` 原本預測 `A5100000`，被否證。*推*：loader 讀完就清掉（write-1-to-clear），因為它**印**了那句話，見 `CLK-13` | 量 | 讀 | 量 `D2`、`D2d` + **D** | `RUNSHEET.md` `D2` |
| `CLK-11` 🆕 | 誰寫過 `WDTCNR` | **loader 從來沒寫過它，除了兩處 `sw zero` 之後原地死迴圈** —— `0x804012F8`（`reboot.......` 那條路）與 `0x804092E8`（`J BFC00000`）。所以 `B7` 量到的 `A5000000` 是**硬體重置預設值**，不是 loader 寫進去的，而 `D2` 的路徑上沒有任何軟體，它直接讀硬體 | 讀 | 讀 | A，三種搜法互補：`0x311c` 立即值（2 筆）、`TC_BASE 0x3100`＋位移 28（0 筆）、非 `sp` 的 `sw …,28(reg)`（3 筆，全落在別的周邊）。**正控制：同一個方法找得到 `0x80408F34` 的 `CDBR = 0x000E0000`，而那個值 `B7` 在裝置上量到過** | `RUNSHEET.md` `D2` |
| `CLK-15` 🆕 | `Booting...` **之後**的靜默 | **冷上電 348.0–356.9 ms（n=7，平均 351.9，全距 2.5 %）· 暖重置 338.2–347.6 ms（n=7，平均 342.6，全距 2.7 %）** —— 🔴 **2026-08-25 第二節桌面：兩群不重疊，而本列原本寫「冷暖重置皆同」。**`tools/boot-timeline.py` 把十四份含開機文字的擷取逐一量過，**冷／暖是用 loader 自己的 `C-8` 那一行分的（`ramSize: 32M` 之後是 `Reboot Result from Watchdog Timeout!` 還是一個空格），不是用 artifact byte 分的** —— 用 artifact byte 分會讓「artifact 只出現在冷開機」變成定義上的真理。🔴 **而決定性的比較是同一個電源循環之內**，因為那樣就沒有日期、溫度與板子的差異：`2026-08-24c` 冷 352.1 對同循環三次暖重置最大 347.6（**+4.5 ms**）、`2026-08-25` 冷 355.7 對同循環四次暖重置最大 341.1（**+14.5 ms**）。兩個獨立的電源循環，方向一致。**所以本列原本記的 3.5 % 未解釋散布有一部分現在解釋掉了：那是兩群被當成一群。**拆開之後群內散布是 2.5 % 與 2.7 %，合併是 5.4 %（n=14）。**冷比暖慢的機制未定**，候選是 SPI NOR 控制器的除頻在冷上電與 watchdog／ROM 重置之後不同、或 SPI NOR 自己的上電喚醒 —— **決定它的實驗不用上機成本**：在冷開機後與暖重置後各讀一次 `SFCR`（`0xb8001200`）等四個暫存器並比對，兩個 `DW`，零風險。🔴 **錨點是定義的一部分，而本列原本沒寫。** 這段靜默從哪一個位元組起算有四個講得通的選法，彼此差到 1.7 ms：`Booting...` 的最後一個 `.`、其後 CR LF 的 LF、再其後那個 NUL、或 `Booting` 的 `B`。**本列的九份原始數字只有「NUL → `chipName` 的第一個位元組」重現得出來**（那九份在該錨點下是 0.3447 .. 0.3569，對上本列公佈的 344.7–356.9），其餘三個錨點都重現不出來 —— 所以那就是本列一直在量的東西，現在寫下來了。`tools/test-boot-timeline.sh` `B1` 就是這個比對，而它同時要求另外三個錨點**不**吻合，否則「錨點 C 是本列用的那個」只是猜的。🔴 **而同一天發現 `Booting...` 之前也有一個 ≈345 ms 的靜默，只在冷上電時出現**（0.340 / 0.349 s，n=2；暖重置是 0.001 / 0.010 s 且沒有那個 artifact byte）—— **兩個等長且相鄰的區間**，見 `CLK-14`。本列只擁有**之後**那一個。🔴 **2026-08-25 定案，而 §17 為它寫下的實驗指錯了 binary。** `Booting...` **不是 stage 2 印的** —— `Booting` 這個字串在 `stage2.bin` 裡 0 次、在 4 MiB dump 的 flash offset `0x3DE` 出現一次，由 **stage 1** 在 `0xBFC003A4–0xBFC003D0` 用一個沒有 THRE 輪詢的 byte 迴圈直接寫 `0xB8002000` 印出來。所以那 350 ms **跨了一個 stage 邊界**，讀 `0x80400000–0x80400440` 永遠讀不到它。**主導項是 stage 1 在 `0xBFC001D0–0xBFC001EC` 的複製迴圈**：把 flash `0x0004F0–0x0056AB`（20,924 bytes、5,231 圈）一次一個字搬進 `0x80100000`，而迴圈的 8 條指令**與**它的來源字每一個都是從 memory-mapped SPI NOR 來的未快取 KSEG1 讀。**解壓縮是用量測排除的**，不是用論證：同一組 LZMA 參數解 976,877 → 3,374,772 bytes 的 kernel 花 **1.0132 / 1.0233 s**（`bench/2026-08-24c/G6.timing`、`24d/G7.timing`），依輸出位元組與依輸入位元組兩種換算各給 **17.1 / 18.1 ms**，約 5 %。DRAM read-window 訓練掃描（`0xBFC001FC`，≤32 圈）與 BSS 清零各在 2 ms 以下 —— **原本這一列猜的「DRAM init／校準」是錯的**。🔴 **兩個沒藏起來的鬆端**：`Booting...` 之後那個 NUL **不是**那個 byte 迴圈寫的（迴圈在 NUL 上 `beqz` 退出），來源未定；而剩下的群內散布（冷 2.5 %、暖 2.7 %）對固定時脈的指令執行來說仍然太大，未解釋 —— 🔄 **原本寫的是 3.5 %，那個數字裡有一部分是冷暖混在一起** | 量 ×14 | 讀 | 量 ×14（`tools/boot-timeline.py`，冷 7 暖 7，含兩個同循環冷暖對） + 讀（stage1／stage2／dump）+ 量（解壓縮換算） | `docs/loader-command-semantics.md` |
| `CLK-13` 🆕 | 重置成因，在開機文字裡 | 🔴 **loader 印 `Reboot Result from Watchdog Timeout!`**，位置在 `ramSize: 32M` 之後；冷開機那一行是**一個空格**。🔄 **六次量到**（`D1`、`D4`、`D4c`，加 2026-08-25 的 `H1b`、`H3a`、`H3c-D4`、`H3c-D4c` —— 其中 `H3c` 那兩次**有分辨力**：看門狗是用 `EW` 從 prompt 武裝的，loader 之後一條指令都沒執行，那行仍然出現）。**它讀的是硬體位元不是軟體旗標**：`D4`/`D4c` 的看門狗是用 `EW` 從 prompt 武裝的，loader 之後一條指令都沒執行，那行仍然出現。**`C-8` 的鑑別器就是這一行** —— 免費，每一份擷取都帶著 | 量 | 讀 | 量 ×3 | `RUNSHEET.md` `D4` |
| `CLK-14` 🆕 | 重置 → console 第一個 byte | **2.07 ms**（`D1`，`J BFC00000`：最後一個重置前 byte 在 t=0.015723，第一個重置後 byte 在 t=0.017792，同一份擷取）· **≤21.1 ms**（`D4`）· **≤19.2 ms**（`D4c`）—— 後兩者是**上界**，由 ESC 回音心跳的 20 ms 格點設限。🔄 **冷上電 2026-08-25 量到了，而量它的是一個原本被當成雜訊的位元組。** 擷取先開、電源後上時，CP2102 會交出**一個 framing artifact byte**（今天 `0x00`、`bench/2026-08-24c` `0xFF` —— 同一條線的兩個靜止極性，這也是為什麼兩次不同；裝置印出來的字元兩次會一樣）。那個 byte 是「線被驅動起來」的時刻，而下一個 byte（`\r\nBooting...` 的 `\r`）在 **0.349 / 0.340 s 之後**，n=2。暖重置沒有 artifact byte 也沒有這個間隔（`H1b` 0.001 s、`H3a` 0.010 s）。🔴 **所以 `Booting...` 前後各有一個 ≈345 ms 的靜默，相鄰而等長**，`CLK-15` 擁有後面那一個。✅ **2026-08-25 第二節桌面定案：那次改判三個數字裡送對了兩個、送錯了一個。**`tools/boot-timeline.py` 逐份重量之後對得起來：**340 = `bench/2026-08-24c/A-catch` 的 artifact byte → 下一個位元組 = 0.3404 s**，那是**本列**的量，不是 `CLK-15` 的；**348 與 345 分別是 `D1` 的 0.3476 與 `D4` 的 0.3447**，都是 `Booting` 之後的靜默，屬於 `CLK-15`。而 `CLK-15` 公佈的區間下界 344.7 就是 `D4` 那一筆，**所以那個區間本身從來沒有被 340 汙染過**——被貼錯的是「三個數字都是同一件事」這句話，不是區間。🆕 **同一次重量把本列的暖重置母體從 n=3 加到 n=6，而新的三筆是更緊的上界**：`H3a`（`J BFC00000`）**2.8 ms**、`H3c-D4` **3.2 ms**、`H3c-D4c` **3.8 ms** —— 後兩者跑在 2.115 ms 的 ESC 心跳格點上，而 `D4`／`D4c` 的 ≤21.1／≤19.2 是 20 ms 格點的產物。**`H1b` 被排除，而排除它是本列的重點**：它送的是 `J 80500000`，開機文字之前那個最大間隔 0.1237 s 是 `probe1` 自己的延遲迴圈，那是 `CLK-03` 的量 —— 把它放進本列就是再犯一次同樣的錯。`tools/test-boot-timeline.sh` `B3` 是這條排除規則的控制。🔴 **原本這一列寫 340 / 348 / 345 ms —— 三個真實存在但被貼錯名字的數字**，見 `CLK-15`；而寫上去的當下就跟本表自己的 `D1b` 矛盾（0.592 s 減掉 `Booting`→橫幅的 0.583 s 只剩約 9 ms，不可能是 348 ms）。**這正是 `D1b` 存在的目的：一個量測戴著另一個量的名字** | 量 | — | 量 ×1 精確 + ×5 上界（其中三筆在 2.115 ms 格點）+ 冷上電 ×2 | `RUNSHEET.md` Results — seating 2 part three、`tools/boot-timeline.py` |
| `CLK-12` 🆕 | tick 計數器的零點，以及 `timer_init` 落在開機時間軸的哪裡 | 🆕 **計數器開機時從 ≈0 起算。** 以 `f = 100.0018` 把 `CONT3` 外推回去，tick 0 落在 **03:32:51.54**；那次開機的第一個 UART byte 在 **03:32:50.13**。所以 **`timer_init`（`0x80408F20`）在開機後 1.41 s 執行** —— 在 banner（+0.585 s）之後、Ethernet/PHY init（+2.246 s）之前，也就是 `IRQ-04` 的第 3 層在網路初始化**之前**就已武裝。兩條互相獨立的推導（單次讀數 ÷ 經過時間，以及差分）一致。**否證**：非零起點會把外推的零點推到第一個 UART byte **之前**，或推到提示字元之後 | 量 | 讀 | S `E1b`／`E2b`／`CONT2`／`CONT3` 的 tick 值 + `A-catch.timing` 的開機時間軸 + A（`timer_init` 的位址） | `docs/loader-phy-and-switch.md` §2 |
| `CLK-17` 🆕 | 🔴 **`TC0CNT` 的遞增速率 —— D 用語裡的 “Base clock”** | **14,286,057 Hz**，週期 **69.9983 ns**。🔴 **這個數字在 2026-08-26 以前沒有自己的一列**，只隱含在 `CLK-02`／`CLK-04`／`CLK-06` 的推導裡 —— 而 `probe3` 的每一個計時格子都要拿它來除。**一個 payload 實際要除的數字不可以只活在一條算式裡。** 推導只用兩個量到的量：`CLK-04` 的 100.0018 Hz × `REG-05` 的 `TC0DATA = 142,858`。**不需要 CDBR、不需要除數語意、不需要 200 MHz 那個數字**。⚠️ `f = sysclk/CDBR = 200.0049/14` 給到六位有效數字一樣，**但那不是獨立驗證** —— 200.0049 MHz 本身就是由 tick × 14 × 142,858 導出來的，是同一條式子的移項。🔴 **不要用 14.9650 MHz**：那是 `CLK-08b` 的**看門狗**時脈，同一顆晶片上的另一個時脈，差 4.75 %，而 `CLK-08b` 正是把 `f_timer/14` 當作看門狗時脈這件事**否證掉**的那一列。用錯會在每個快取數字裡塞進 4.75 % 的誤差。⚠️ **回繞 142,858 tick = 9.9998 ms，而且模數不是 2 的冪** —— `after − before` 遮成 28 bit 是錯的，要 `if (d<0) d += 142858`，而且只對單次回繞有效 🆕 **2026-08-26：儀器建成了，而 qemu 上這一組整組是死的 —— 那是量到的，不是猜的。** 量(qemu) `qemu/2026-08-26/probe3.txt`：`0xB8003108` 每次都讀回 **`FFFFFFFF`**，Malta 那個位址什麼都沒有。值不等於任一個 prime，所以 load **確實寫了**目的暫存器 —— **「那裡沒有東西」和「暫存器凍住」是兩個不同的說法**，payload 自己分得開並印出是哪一個。**否證 T 因此多了第三種狀態**，而那一種在寫格子表的時候不存在 | 推 | 🔄 **讀** —— 名字取自 D §8.2.8「Base clock=System_clock (Peripheral Lexra Bus)/N」 | 由兩個量的量導出：量 `CLK-04`、量 `REG-05` | `docs/probe3-cells.md` §6.3 |
| `IRQ-01` | IRQ 15 | `eth0` —— loader 的 TFTP 是中斷驅動的，handler 在 `0x804023B0` | 讀 | 讀 | C，靜態 + 暫存器讀回 | `plan/` F40 |
| `IRQ-02` | IRQ 8 | `timer` | 讀 | 讀 | C | 同上 |
| `IRQ-03` | IRQ 27 | `SPEED` | 讀 | — | C | 同上 |
| `IRQ-04` | 一個計時器中斷需要的四層 | `Status.IM[7:2]` 未遮罩且 `BEV` 清掉 · `Status.IE` = 1 · `TCCNR`/`TCIR` 已武裝 · `GIMR` bit 8 | 讀 | 讀 | A。**第 4 層原本寫的值是錯的，是量測改正了它** —— 見 `REG-01` | `docs/loader-phy-and-switch.md` §2 |

## 11. 開機載入器

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `LDR-01` | Banner | `---RealTek(RTL8196E)at 2014.04.22-16:22+0800 v1.3 [16bit](400MHz)` —— 2026-08-17、-18、-23 三次逐位元組相同 | 量 | — | C、S | `bench/2026-08-23/A-catch.log` |
| `LDR-02` | 第二階段 | `stage2.bin`，56,592 bytes，`sha256 f88869d1…c9c1b4ee`，載入基底 **`0x80400000`** | 讀 | 讀 | A；基底是從 `0x20` 間距的指標序列還原的，而還原器在 0 個或 2 個候選時都拒絕回答 | `upstream/notes/loader-chip-table.md` §1 |
| `LDR-03` | 命令集 | `?` 印出 **17 條** —— `HELP` 自己反而回 `Unknown command !` | 量 | 量 | S，且靜態還原的表與裝置逐行相同 | `bench/2026-08-23/A-catch.log` |
| `LDR-04` | 命令表 | 在 `0x8040DBC0`，16 位元組間距，`{name*, argc, handler, help*}` | 量 | 讀 | S `B1`、`B9` + A | `RUNSHEET.md` `B1` |
| `LDR-05` | 大小寫 | **不敏感** —— dispatcher 先跑 `strupr` | 讀 | 讀 | C | `plan/` F37 |
| `LDR-06` | 斷詞器 | 20 個槽，**只用空白**當分隔，每行把所有槽清零，而且**先存 `argv[i]` 再測分隔字元** —— 所以**一個前導空白會讓 `argv[0]` 變成空字串** | 讀 | 讀 | A `0x80407248`，並在 2026-08-23 於裝置上實際引爆 | `docs/loader-command-semantics.md` §f |
| `LDR-06b` | 一行能載多少 | 🔄 **2026-08-24 更正，被 `LDR-06c` 推翻。** 斷詞器的 20 個槽不是綁束條件，**行緩衝的 128 bytes 才是**：`EW <addr> ` 佔 12 字元，每個值 9 字元，所以 `11 + 9n < 128` → **一行 12 個字 = 48 bytes，119 個字元**。1 KiB 的裸機探針要 **22 行，不是 15 行**。原本那句只算了槽數。🔴 **2026-08-24b 這個數字從「算出來的」變成「在矽片上跑過的」**：`C7a` 就是那條 119 個字元、十二個值的 `EW`，十二個字全部依序落地；`C7b` 的 127 個字元也完整回來，是它的邊界控制。🔄 **2026-08-25 樣本數從 7 變成 54，零例外**：`A-catch` 在 180 秒的 ESC 串流裡回音 **6968 = 128 × 54 + 56** 個 byte —— 54 條連續的滿行，最後 56 byte 的殘餘由 `console-capture` 自己寫的終止 CR 沖掉（`Unknown command !` 55 次、prompt 56 次），**所以這同時是 `terminate_esc_line` 在真矽片上而不是在 pty 上的正控制** | 量 | 讀 | S `A-catch`（2026-08-23／24／🆕 25）、`C7a`／`C7b` + A（斷詞器與行讀取器兩處） | `RUNSHEET.md` `C7`、`docs/loader-command-semantics.md` §f |
| `LDR-06c` | loader 的輸入行緩衝長度 | **128 bytes**。🔴 **2026-08-24b 在第二次電源循環上重現，而且它是一個分割、不是「128 這個數字又出現了」**：`A-catch` 回音 730 個 ESC，分成 `[128 ×5, 90]`，接著 `flush` 把那 90 吃掉並回**恰好一個** `Unknown command !`；`B7c` 是 985 = 7 × 128 + 89，`flush-b7c` 同樣收乾淨。兩次獨立的電源循環、十二次未終止的填滿。**否證**：一份 ESC capture 的回音位元組數不是 `128 × n + r`（`n` 是 `Unknown command !` 的行數、`0 ≤ r < 128`），或殘量沒被一個裸 CR 變成剛好一行 | 量 | 讀 | **×2** —— S：`A-catch` 裡剛好 128 個 ESC 後回 `Unknown command !`，**同一個數字七次**，2026-08-24b 再五次加七次；A：命令迴圈 `0x80409190` 的 `memset(buf,0,128)` 與 `0x804091A0` 的 `readline(buf,128,1)` | `docs/loader-command-semantics.md` §f |
| `LDR-06d` 🆕 | 🔴 **剛好 128 個字元的行沒有終止符** | `readline`（`0x8040708C`）三個出口只有 CR 那條寫 NUL（`0x804070FC`）；LF 那條與長度到底那條（`0x80407194`，`count < 128`）都不寫。呼叫端的 `memset` 只救得了**短於** 128 的行，因為那才留得下零。**所以斷詞器（`0x80407248`）會掃過 `sp+143`，進到 8 bytes 堆疊空隙，再進到存起來的 `s0`（`sp+152`）**。堆疊框 184 bytes，緩衝在 `sp+16` | 讀 | 讀 | A | 同上 |
| `LDR-07` | `DB` / `DW` 的進位陷阱 | 位址十六進位，**長度十進位**。`DW` 數的是「字」，一行四個 —— 🔴 而迴圈的 `i` 以 4 為步進、條件是 `i < N`，每一趟印一整行，所以 **`DW <addr> N` 印的是 `4 × ceil(N/4)` 個字，不是 `N` 個**：`1`、`2`、`3`、`4` 都印四個，`10` 印十二個。**進位是向上的，所以長度給小了不會自己說** —— 回讀永遠是整數行，看起來就像完整的。`C7` 原本寫 `DW 81000400 3` 去回讀十二個剛寫進去的字：印四個，**而 `C7` 正是那格在獵「截斷」的，那會用它自己的讀取命令製造出它存在目的的那個假陽性**。另外在 bit 31 為 0 時會把位址強制推進 KSEG0。🆕 **2026-08-25 兩件新的**：① **`DW` 不會把起始位址向下對齊。** `H0a2` 給的是 `0x8040054C`（本專案第一次餵它一個非 16-byte 對齊的位址），印出來的第一個位址就是 `8040054C`，後續行以 `+0x10` 遞增 —— block 0 事先把「若印出 `80400540` 則代表會向下對齊」寫成一個待決問題，答案是不會。② **回覆長度公式 `len(cmd) + 2 + 47 × lines + 9` 今天 15 中 15**：block 0 九格全中，加上 `71 / 1543 / 11 / 1671 / 213 / 118`。⚠️ **而它中不了的那一次是人算錯**：block 3 預測 214、量到 213，因為 `DW 81000400 16` 是 14 個字元不是 15。✅ **2026-08-25 第二節：公式變成了 `tools/reply-size.py`，而母體從手數的 15 變成 121。**對 `bench/` 全部 150 份擷取中「有下指令且沒有串 ESC」的那些跑：**121 中、0 個不明**。每個指令族的常數是**從擷取擬合出來的**，不是從 loader 原始碼讀的、也不是在終端機上數字元的：`DW` 47×⌈N/4⌉（n=91）、`EW`／`EB` 無輸出（n=11）、`Y` 23（n=6）、`PHYR` 68（n=5）、`FLR` 79 **而且不回 `<RealTek>`**（n=6，所以它的尾巴少 9 個位元組）。**`DB`／`J`／`MDIOR` 明確不建模**，各自寫下理由與樣本數 —— 一個把「沒看的東西」算進「0 個不明」的掃描，就是本專案一直在抓的那種沒有正控制的掃描。🔴 **兩個原本會被當成「不中」的，現在各有名字**：`CONT` 的 24 位元組是 `ECHO-ONLY`（有回音、沒執行，`C-19` 的簽名），`A0-reopen-control` 的 44 位元組是 `UNKNOWN-COMMAND`。**獨立確認**：工具對 `DW 80A00000 137` 算出 1671，而那正是 `H1c` 量到的位元組數，兩條路互不相干。工具的十二個控制項裡 `C8` 就是那次人算錯（`DW 81000400 16` → 213 而不是 214） | 量 | 讀 | S `B8`（`A` 沒有任何輸出）、`B9`（`10` 印三行）、`C7-pre`／`C7a-rb`（`28` → 七行、`16` → 四行）、🆕 `bench/` 全部 121 份可建模的擷取（`tools/reply-size.py check bench/`）+ A `0x804094B4` | `docs/loader-command-semantics.md` §f、`RUNSHEET.md` `B8`、`B9` |
| `LDR-08` | `EW` | 4 位元組寫入，**不做邊界檢查**，**完全靜默**，接受多個值並依序寫成連續的字，未對齊的位址會**向上**進位。🔴 **2026-08-24b 再加一條：`EW` 寫的恰好是 `argc − 1` 個字，不是「至少」。** `C7a` 送十二個值、`C7b` 送十一個，兩格的**溢寫控制字**（`0x81000430`、`0x8100046C`）都與寫入前逐位元組相同，而兩個控制的否證條件都在格子跑之前就寫下來了 | 量 | 讀 | 🔄 **2026-08-24 全部升成量測**：S `C1`（靜默、多值）、`C2`（依序落位）、**`C3`（`EW 81000102` 把值放到 `0x81000104`）**、`C7a`／`C7a-rb`、`C7b`／`C7b-rb` + A | `docs/loader-command-semantics.md` §f |
| `LDR-08b` 🆕 | loader 的十六進位解析 | **`strtoul` 式，不是固定寬度。** `C7b` 的前六個參數寫成**十個字元**（`00C7B00001`），回讀是 `C7B00001` —— 前導零被吃掉、不截成八位、不溢位。**一個固定八位的讀法會取 `00C7B000`、把 `01` 留在後面，之後每一個參數都位移。** 反組譯在 `0x80409678` 有 `strtoul(_,_,16)`，與結果相容，但**這個 repo 裡沒有任何檔案事先把位數變成一個主張** —— 八位是慣例，不是量測。**沒測到的**：值本身超過 32 bit 的參數，一個都沒送過 | 量 | 讀 | S `C7b`／`C7b-rb` + A `0x80409678` | `docs/loader-command-semantics.md` §f |
| `LDR-09` | `EB` | 1 位元組寫入，不檢查邊界，靜默，**完全不進位** —— **與 `LDR-08` 相反，而那個不對稱才是要記住的東西** | 量 | 讀 | 🔄 S `C4`（`EB 81000200 41 42 43` → 逐位元組落在 `…200/201/202`）+ A | 同上 |
| `LDR-10` | `EH` | **程式碼在 `0x804096F4`，但到不了** —— 它的表列在 B 裡被 `#ifdef REMOVED_UNUSED` 關掉，三種搜法都回 0，而同樣的搜法對照組分別回 32 與 2 | 讀 | 讀 | A + B | 同上 |
| `LDR-11` | 能寫任意記憶體的路徑 | **四條** —— `EB`、`EW`、`FLR` 的第一個參數（整個 flash 區段）、以及 `LOADADDR` 之後的 TFTP 寫入（MB 級） | 讀 | 讀 | A | 同上 |
| `LDR-12` | 六個 handler 未測 `argc` 就取用 `argv[0]` | `EB` `EW` `LOADADDR` `FLR` `PHYR` `PHYW` —— 其中任何一條不帶參數就打，代價是一次電源循環 | 讀 | 讀 | A、`loader-unpack.py --commands` | 同上、`RUNSHEET.md` 的禁打清單 |
| `LDR-13` | `PHYR`/`PHYW` 對 `MDIOR`/`MDIOW` | **四條命令三種參數慣例**，而且 `MDIOR` 的說明字串連自己的參數個數都寫錯：它只讀 `argv[0]`，用**十進位**解析，當成*暫存器*，然後自己掃 PHY 位址 | 讀 | 讀 | A。`MDIOR`/`MDIOW`/`PORT1` **只有 A 一個來源** —— B 裡的 `MDIOR` 是一條同名的 PCIe slave port 命令 | `docs/loader-phy-and-switch.md` §3 |
| `LDR-14` | `PORT1` | 一支**產測程式** —— 不讀 `argv`、不讀 `argc`，把一張 Gray code 表打進位址 `{0,2,3,4}` 的 PHY vendor 暫存器 19：**光是 payload 就 612 次寫入，外加周邊 272 次**，而且無法中止 | 讀 | 讀 | A；B 只佐證了 page select 的慣用寫法 | 同上 §4 |
| `LDR-15` | ESC 視窗 | **約 4.886 秒**，從 banner 到 `Jump to image start`，而且它在上電當下就開 —— ESC 必須在那之前就已經在串流 | 量 | — | C，2026-08-18 | `RUNSHEET.md` `A1` |
| `LDR-16` | 中斷手法的副作用 | 排隊的 ESC 會毒到**下一條**命令，讓它回 `Unknown command !`。先送一個裸 CR 讀到提示字元再說。🔄 **2026-08-24 第三次重現（`A0`），而重現它花掉了一格** —— 這條規則在這張表裡，卻**不在 `RUNSHEET.md` 的執行程序裡**，所以它被重新發現而不是被遵守。已寫進 seating 2 的程序 | 量 | 讀 | C、S（三次） | `upstream/notes/uart-pinout.md` §4、`RUNSHEET.md` seating 2 |
| `LDR-17` | 映像定位 | **它會掃。** 先固定試 `0x010000`/`0x020000`/`0x030000`，再以 `0x10000` 為步長掃 `[0x030000, 0x060000]` 並跳過那三個 —— 共六個候選。rootfs 則是 `0x0E0000`/`0x0F0000`/`0x130000` 再 `0x100000`/`0x110000`/`0x120000` | 讀 | 讀 | A + B 的 `check_image_header()` | `docs/loader-command-semantics.md` §a、`C-1` |
| `LDR-18` | `check_image()` | 在 `0x80407D50`：簽章 `cs6c` → 1、`cr6c` → 2，然後對 RAM 副本做 16 位元半字加總，必須為零。只有 `2` 能滿足呼叫端 | 讀 | 讀 | A + B | 同上 |
| `LDR-19` | 這個檢查是靜默的 | 這個 build **把 `no sys signature` / `sys checksum error` 編掉了**，卻留著 rootfs 的那兩句。當初把「字串不在」讀成了「檢查不在」 | 讀 | 讀 | A | 同上 |
| `LDR-20` | `doBooting()` | 在 `0x80408690`。**壞映像直接進救援提示字元 —— 不等 ESC，也不印任何訊息** | 讀 | 讀 | A，與 B 逐分支相符 | 同上、`C-4` |
| `LDR-21` | `gCHKKEY_HIT` | 在 `0x8040DBA4`。為 `1` 時 `check_image()` 直接回「沒有映像」。它唯一的寫入者讀 `0xB8002014` bit 24 與 `0xB8002000` 的最高位元組 —— 那是 UART 路徑，**但沒有對照 D 確認過** | 量（值） | 推（機制） | S `B5` + A + B | `C-13` |
| `LDR-22` | 是誰在任何 `FLR` 之前就填了 `0x80500000` | *(留白)* —— 提示字元下 RAM 就已經是 flash `0x060010` 的內容，而 `check_image()` **不可能**是填它的。🔴 **2026-08-25b 加一個量到的性質，而它是結構性的：這個複製在 watchdog 重置之後也會跑，不只是冷上電。** `probe2` 跑完、`rlx_reset` 重置、loader 重開之後，第二次 `J 80500000` 印的是 `decompressing kernel:` 並把原廠韌體開到 userspace（`bench/2026-08-25b/H2a2.log`）—— payload 已經被蓋掉了。**所以同一個電源循環內不重新上傳就不可能把 payload 跑第二次**，而這件事之前沒有任何檔案寫過那個 | 量（那些位元組） | — | S `B4` | `C-16` |
| `LDR-23` | `AUTOBURN` | 初始值 `1`，全映像**只有一條指令**讀它（`0x80401B9C`），在上傳完成的路徑上。**上傳完成而事前沒打 `AUTOBURN 0`，就會被燒進 flash** | 量 | 讀 | S `B6` + A + C | `RUNSHEET.md` `B6` |
| `LDR-23b` | 關掉它的語法，以及它到底有沒有生效 | **`AUTOBURN 0`（空白，不是冒號）。有效。** 🔄 2026-08-24：`AUTOBURN: 0` → `Unknown command !`；`AUTOBURN 0` → `AutoBurning=0`；回讀 `0x8040D4A0` = `00000000`（`B6` 當時是 `00000001`）。**說明字串自己印的形式不是語法，在裝置上證實了**；`IPCONFIG:` 對 `IPCONFIG ` 是同型態的第二例 | 量 | 讀 | **×2** —— S `C6` 的回音（loader 說它以為的事）+ S `C6` 回讀 `0x8040D4A0`（`0x80401B9C` 那條指令實際會看的字） | `RUNSHEET.md` `C6`、`bench/2026-08-24/C6-rescue.json` |
| `LDR-24` | `LOADADDR` | 一個十六進位參數，**不檢查邊界、不確認**；全域在 `0x8040D3A8`，初始值 `0x80500000` | 讀 | 讀 | A | `docs/loader-command-semantics.md` §b |
| `LDR-37` 🆕 | 🔴 **檔名 `boot.img` 會把寫入指標設成 `0x80000000`** | 名稱比對打到 `0x8040A6A8`（`"boot.img"`；`0x8040A6A0` 是 `"nfjrom"`）之後走到 `0x80401250`，把 `0x80000000` 同時存進 `LOADADDR` 全域（`0x8040D3A8`）與執行中的 TFTP 寫入指標（`0x8040DD10`），而 `0x80401A10` 的 memcpy 從那裡往上走。**它會蓋掉 UTLB refill 向量與一般例外向量**，在傳輸中途把 loader 自己的例外處理毀掉。**`boot.img` 與 `nfjrom` 進不可打清單。** 由一次反駁稽核翻出來 —— 原本的讀法宣稱「沒有任何東西寫低位 RAM」，而它自己承認它的掃描看不到執行期算出來的基底位址 | 讀 | 讀 | A | `RUNSHEET.md` 不可打清單 |
| `LDR-25` | TFTP | 是**伺服器**不是客戶端；編譯進去的 IP 是 **`192.168.1.6`**，不必打 `IPCONFIG` 就會回應 | 讀 | 讀 | C，指令級 + 線上的 DATA opcode | `plan/` F42 |
| `LDR-26` | 兩個會繞過一切的檔名 | **`nfjrom` 與 `boot.img`** 會把載入位址強制成 `0x80000000`，並在傳輸結束當下直接執行 | 讀 | 讀 | C | `plan/` F44 |
| `LDR-27` | `burn()` | 在 `0x80401318` —— 它是映像解析器與分派器。接受八種區段簽章：`boot` `sqsh` `w6cp` `jw6c` `cwmp` `ksap` `ALL1` `ALL2` | 讀 | 讀 | A | `docs/loader-flash-write.md` §1 |
| `LDR-28` | `burn()` 的邊界 | **只有上界** —— 晶片描述子裡的容量，而且寫太長是被*截斷*而不是被拒絕。**下界根本不存在**，而 `boot` 是它接受的簽章之一 | 讀 | 讀 | A | 同上 |
| `LDR-29` | `0xDEADC0DE` | 緊接在 4 KiB 對齊映像之後的標記；loader 找到它就把燒錄長度加 4。**是誰寫下這個標記並未確立** | 讀 | 讀 | A | 同上 |
| `LDR-30` | `FLW` | 四個參數；會印出記憶體映射位址；只用一個 `.` 回應；確認提示是 `(Y)es, (N)o->` —— 而 `FLR` 的是 `(Y)es , (N)o ? -->` | 量 | 讀 | C（2026-08-17 實際執行過）+ A + B | `upstream/notes/uart-pinout.md` §4 |
| `LDR-31` | `FLR` | `FLR <dst_RAM> <src_flash> <len>`，三個都是十六進位，**三個都不檢查邊界**；它同時會寫 TFTP 長度全域 `0x8040DD28` | 讀 | 讀 | A | `docs/loader-command-semantics.md` §f |
| `LDR-32` | `DB` 的吞吐 | 81 個字元載 16 個位元組 —— **5.06 倍膨脹**，38400 下上限約 759 B/s，**實測 723 B/s**；讀完 4 MiB 約 95 分鐘 | 量 | 讀 | C | `upstream/notes/uart-pinout.md` §4.1 |
| `LDR-33` | `J BFC00000` | 就是重置 —— 它寫 `WDTCNR = 0` 然後在中斷全遮的狀態下空轉，所以只有看門狗能離開那個迴圈 | 讀 | 讀 | **×4** —— A（兩處）、B（帶廠商自己的註解）、D、E | `docs/loader-command-semantics.md` §f |
| `LDR-34` | 任何地方都沒有 kernel 命令列 | 13 個命令列形狀的關鍵字，**0 命中**，而同一次執行找齊了全部 17 條命令當控制。沒有環境變數機制、沒有儲存、也沒有對應命令 | 讀 | 讀 | C，並在本 repo 重跑一次 | 同上 §d、`C-2` |
| `LDR-35` | 從主控台碰不到的東西 | 沒有 16 位元寫入、沒有記憶體填值、除了 `MDIOW`/`PHYW` 外沒有暫存器命令、**沒有任何讀寫 CP0 的辦法** | 讀 | 讀 | A + B | 同上 §f |
| `LDR-36` 🆕 | 一則回覆有多重 | `bytes = len(命令) + 2`（回音與它的 `\n\r`）` + 47 × 行數`（`AAAAAAAA:` 九個字元 + 4 ×（tab + 八位十六進位）+ 2）` + 9`（`<RealTek>`）。`DW` 的行數是 `ceil(N/4)`；**靜默的寫入命令行數是 0**，所以 `EW`／`EB` 的整則回覆就是 `len(命令) + 11`。六格各自在跑之前預測、六格全中：`DW 8040DBC0 1` = **71** · `DW BB804128 8` = **118** · `DW 81000400 16` = **213** · `DW 81000400 28` = **354** · `C7a` = **130** · `C7b` = **138**。**適用範圍**：`DW` 與靜默寫入，不是每一條命令（`PHYR 1 5` 回的是 87 bytes 的散文），而且只對 `console-capture.py` 的 `.log` 成立 —— 那裡面就是連接埠交出來的位元組。**用途**：一個不花錢、而截斷過或讀到舊資料的 capture 過不了的完整性控制。少 47 是掉了一行、少 9 是掉了提示字元；`CONT` 那 24 bytes 就是它開火的那一次 | 量 | 讀 | S 2026-08-24b 的每一份 capture + A | `docs/loader-command-semantics.md` §f |

## 12. 這台實際在跑的韌體

**不是本專案分析過的那兩份映像中的任何一份。** 是第三個 build，
而這台自己的 flash 之外，任何地方都沒有它的副本。

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `FW-01` | 版本字串 | **`TOTOLINK-CX-N150RT-V2.1.6-B20171121.1002`** —— `/etc/version`，41 bytes | 讀 | 讀 | A，並由 `boa` 編譯進去的 `Model No. N150RT (Firmware V2.1.6)` 佐證 | `upstream/notes/dump-vs-official.md` |
| `FW-02` | 二進位檔的建置日期 | **2018-01-10**，四個元件一致到分鐘 | 量 | 讀 | C | `upstream/notes/uart-findings.md` §1 |
| `FW-03` | Kernel | `Linux version 2.6.30.9 (admin@office.hopeiot) (gcc 4.4.5-1.5.5p2) #1526 Wed Jan 10 14:50:54 CST 2018` | 量 | — | C，從運行中的裝置讀出 | `upstream/test-ledger.md` `P5-5`、`upstream/PROGRESS.md` |
| `FW-04` | Kernel 命令列 | 編譯進去的是 `console=ttyS0,38400 root=/dev/mtdblock1`，**沒有 `init=`**；`Kernel command line` 這個字串不存在，所以開機 log 永遠印不出命令列 | 讀 | 讀 | C | `docs/loader-command-semantics.md` §d |
| `FW-05` | init／shell | BusyBox `v1.13.4 (2018-01-10 14:56:45 CST)`。**主控台上沒有 getty、沒有 shell** —— 打 `\r` 會回顯只是因為 tty 層在回顯 | 量 | 讀 | C | `upstream/notes/uart-pinout.md` §3 |
| `FW-06` | Web 伺服器 | `Boa/0.94.14rc21`，建於 `Jan 10 2018 at 14:57:54`，**485,012 bytes**，`sha256 19fe29d7…`，`pid=350, port 80`，以 **root** 執行 | 量（banner）· 讀（大小、雜湊） | 讀 | C、A | `upstream/notes/dump-vs-official.md` |
| `FW-07` | 開機起來的其他 daemon | `WiFi Simple Config v2.18-wps1.0` · `MiniIGD v1.09.1` · `IEEE 802.11f (IAPP) … (v1.8)` · `Realtek FastPath v1.03` · `sysconf init gw all` · `wan_disconnect: StartDnsSpoof` | 量 | — | C | `upstream/notes/uart-findings.md` §3 |
| `FW-08` | 根檔案系統 | **SquashFS 4.0，LZMA**，128 KiB 區塊，**567 inode**，20 fragment，`bytes_used` 1,876,033 | 讀 | 讀 | A | `upstream/notes/flash-layout.md` §3 |
| `FW-09` | libc | **uClibc 0.9.30.3** | 讀 | 讀 | A —— 這個 build 上映射的是 `/lib/libuClibc-0.9.30.3.so` | `upstream/notes/mips-ret2libc.md` |
| `FW-10` | 開機時的網路緩衝參數 | `SKB_BUF_SIZE=2408`、`MAX_SKB_NUM=256`、`NUM_RX_PKTHDR_DESC=428`、`eth_skb_free_num=694` | 量 | — | C | `upstream/dumps/uart-boot.log` |
| `FW-11` | MTD | `/dev/mtdblock0` 是整顆（`apmib` 讀的就是它）；`/dev/mtdblock1` 是 rootfs | 讀 | 讀 | A + C | `upstream/notes/firmware-upgrade-path.md` |
| `FW-12` | 映像容器 | `IMG_HEADER_T`，**16 位元組，big-endian**：`signature[4]`、`startAddr`、`burnAddr`、`len`。`w6cg` 與 `cr6c` 連標頭一起寫進 flash；rootfs **沒有**標頭 | 讀 | 讀 | A + C，並以三個 build 的區段接續驗證 | `upstream/notes/anatomy-n150rt.md` |
| `FW-13` | 升級路徑到底檢查什麼 | 一個 4 位元組 tag、一個**無金鑰**加總、一個型號字串。就這樣 | 讀 | 讀 | C，`UpgradeByData` 指令級讀完 | `upstream/notes/firmware-upgrade-path.md` |
| `FW-14` | 型號相容 tag | `TOTOLINK-N150RT-V2.1.0` —— 這是硬體／產品標籤，**不是韌體版本**；兩份廠商映像雖然標成 2.1.2 與 3.4.0，裡面都是這個字串 | 讀 | 讀 | C | `upstream/notes/anatomy-n150rt.md` |
| `FW-15` | 設定檔格式 | `COMPCS` 目前設定 · `COMPDS` 預設設定 · `COMPHS` 硬體設定 —— 是 `apmib` 表的**壓縮**（不是加密）TLV 序列化 | 讀 | 讀 | A + B | `upstream/notes/mib-and-config-dat.md` |
| `FW-16` | 出廠的 shell 帳號 | `/etc/passwd.org` 裡的 `root:123456`、`onlime_r:12345`（uid 0）；這台的 `TELNET_ENABLED = 0` | 讀 | 讀 | C。屬於型號層級，且已載於 CVE 紀錄 | `upstream/notes/credentials.md`、`upstream/notes/compcs-decode.md` |
| `FW-17` | 預設 SSID 命名 | `TOTOLINK N150RT` 加上 MAC 末六碼（在沒改過的機器上） | 讀 | 讀 | C | `upstream/notes/compcs-decode.md` |
| `FW-18` | 利用緩解機制 | **一個都沒有** —— 沒有 canary、沒有 RELRO、沒有 PIE、沒有 FORTIFY，而且每一支帶 `PT_GNU_STACK` 的 ELF 都標成 `RWE` | 讀 | 讀 | C，逐一數過每支 ELF | `upstream/notes/anatomy-n150rt.md` |
| `FW-19` 🔄 | 手上另外兩份可比對的 build | V2.1.2 與 V3.4.0 —— **這台一份都沒跑過**。🔄 **2026-08-27：這一列原本寫「V2.1.2（2015-08-25）」，而那是廠商的發布日期，不是建置日期。** 量：`v2.1.2` 的 BusyBox banner 是 `(2015-08-11 17:26:34 CST)`，樹裡每支 binary 的 mtime 是 `2015-08-11 17:36`，兩個獨立來源相差十分鐘。六棵樹的**建置**日期是 `2015-08-11`／`2016-05-16`／`2018-01-10`／`2018-03-30`／`2019-03-15`／`2020-10-30`，其中 V3.4.0 的 `2020-10-30` 本來就是建置日期，所以這一列原本在同一格裡混了兩種日期。**`notes/lwl-mystery.md` 的表頭寫「build」而值是 `2015-08-25`，同一個錯，一起改** | 讀 | 讀 | C ＋ A（banner 與 mtime，量 2026-08-27） | `upstream/notes/dump-vs-official.md` §3、`tools/binsim-corpus.tsv` |
| `FW-20` 🆕 | rootfs 裡引用 `system()` 或 `popen()` 的 ELF 數 | **31 / 55**（檔案總數 161）。含 `boa`（兩個都有）、`sysconf`、`miniigd`、`wscd`、`udhcpd`（`system`）、`dnsmasq`（`popen`）。🔴 **這是一個上界** —— 方法是掃 `.dynstr` 的完整字串，一個命中可能是訊息字串而不是匯入符號；判定要走 `PT_DYNAMIC` 的 `DT_SYMTAB`。**為什麼不用 `readelf --dyn-syms`**：這些 binary 沒有 section header，那條路對全部 55 個都回傳 0，而 0 正好是通過 | 讀 | 讀 | A，正控制（`malloc`/`strcpy`/`socket` 兩邊都被看到）與負控制（不存在的符號名兩邊都 0）皆成立 | `notes/rootfs-census.md` |
| `FW-21` 🆕 | rootfs 裡的 shell 表面 | **75** 個檔案帶 `#!`，其中 **36** 個是 `.sh`（`firewall.sh`／`init.sh`／`lan.sh`／`connect.sh`／`ip_qos.sh` 等）；**50** 個 symlink 指向 `busybox` | 讀 | 讀 | A | `notes/rootfs-census.md` |
| `FW-22` 🆕 | 這顆 busybox 怎麼建立行程 | **`execv`／`execve`／`execvp`／`vfork`／`fork`／`daemon`；`system` 與 `popen` 兩個都不引用。** 而且它**沒有編進任何 `udhcp*` applet** —— 廠商用的是獨立的 `/bin/udhcpd`，而那顆引用 `system` | 讀 | 讀 | A | `notes/rootfs-census.md` |

## 13. flash 內容，從這台讀出來的

| id | 偏移 | 內容 | 長度 | V | N | 來源 | 擁有者 |
|---|---|---|---|:-:|:-:|---|---|
| `FLM-01` | `0x000000` | 開機載入器，MIPS 程式碼 —— `0b f0 00 04` = `j`，接著就是晶片 ID 比較 | — | 量 | 讀 | C | `upstream/notes/flash-layout.md` |
| `FLM-02` | `0x006000` | **`H601`** —— 硬體設定：MAC 位址與射頻校正 | — | 讀 | 讀 | A | 同上 §6 |
| `FLM-03` | `0x008000` | `COMPDS` —— 出廠預設 | `0x1D39`（7,481） | 讀 | 讀 | A | 同上 |
| `FLM-04` | `0x00A000` | 寫入的零 —— 是配置過的，不是抹除狀態 | — | 讀 | 推 | A | 同上 |
| `FLM-05` | `0x00C000` | **`COMPCS`** —— 現行設定；`config.dat` 就是它 | `0x1D36`（7,478） | 讀 | 讀 | A | 同上 |
| `FLM-06` | `0x00E000` | 寫入的零 | — | 讀 | 推 | A | 同上 |
| `FLM-07` | `0x010000` | **`w6cg`** 標頭 + bzip2 網頁資源 | `0x043A14`（277,012） | 讀 | 讀 | A | 同上 |
| `FLM-08` | `0x053A24`–`0x05FFFF` | 填充 —— 單一重複值 | 50,652 | 讀 | 讀 | A，是量出來的不是假設的 | `upstream/notes/dump-vs-official.md` §2 |
| `FLM-09` | `0x060000` | **`cr6c`** 標頭 + kernel；`startAddr = 0x80500000` | `0x0F1002`（987,138） | 讀 | 讀 | A，且 loader 自己印的 `Jump to image start` 與之相符 | `upstream/notes/flash-layout.md` |
| `FLM-10` | `0x060028` | kernel 的自解壓 stub —— `3c 10 80 5f` = `lui s0,0x805f` | — | 讀 | 推 | A。**上游 RUNBOOK 把這個字放在 `0x060030`；在這裡量到的是 `0x060028`** | `PROGRESS.md` Corrections |
| `FLM-11` | `0x151012`–`0x17FFFF` | 填充 —— 單一重複值 | 192,494 | 讀 | 讀 | A | `upstream/notes/dump-vs-official.md` §2 |
| `FLM-12` | `0x180000` | **SquashFS 4.0 / LZMA**，沒有容器標頭 —— 它必須正好落在分割區邊界上 | 使用 1,876,033 | 讀 | 讀 | A | `upstream/notes/flash-layout.md` §2 |
| `FLM-13` | `0x34A041` | 映像結束 —— **3.29 MiB**。公開規格說的 2 MB 不可能成立 | — | 讀 | — | A，且 W01 早在硬體到手三週前就從廠商容器推出「≥ 4 MB」 | 同上 §1 |
| `FLM-14` | `0x350000`–`0x3FFFFF` | **抹除狀態**，全 `FF`，整條尾巴 | — | 量 | 讀 | C，完整 dump | `upstream/notes/dump-vs-official.md` §2 |
| `FLM-15` | SquashFS superblock 的 `mkfs_time` | `0x80AD1C00` —— 位元組反轉後是 1,879,424，對照 `bytes_used` 的 1,876,033。**那個欄位裝的是位元組反轉的大小，不是時間戳**；三個 build 都是如此 | — | 讀 | 讀 | A + C（三個 build） | `upstream/notes/flash-layout.md` §4 |

## 14. 工具鏈與 libc

| id | 項目 | 值 | V | N | 來源 | 擁有者 |
|---|---|---|:-:|:-:|---|---|
| `TC-01` | 建置這份原廠韌體的東西 | `gcc 4.4.5-1.5.5p2`、uClibc `0.9.30.3`、Linux `2.6.30.9` | 量 | — | C，kernel 自己的 banner | `upstream/test-ledger.md` `P5-5` |
| `TC-02` | 候選的 GPL drop | `rtl819x-toolchain` 的 `toolchain/rsdk-1.5.5-5281-EB-2.6.30-0.9.30.3-110714` —— **banner 每一欄都對得上。在 R2a 量到之前它是假說** | 推 | — | `SOURCES.json` | gate R2 |
| `TC-02a` 🆕 | `R2a/b/d-1` 的矩陣對 `TC-02` 做了什麼 | 🔴 **`TC-02` 不動，仍然是 推 —— 而這是結論不是延後**：語料庫裡六棵全是**出貨映像**，GPL drop 是一份原始碼＋工具鏈釋出，出貨映像的兩兩相似度定不出建置機器上放的是哪一份。矩陣做到的是**排除**與**設界**：③（2019／2020）那一代的 drop 被排除（`boa` 0.0650／0.0664，加上 `TC-10` 兩支獨立儀器）；這台的建置者就是 79 天後建 `n200re-3.2.0` 的那個東西（`TC-12`）；而工具鏈在 2015-08 → 2018-03 之間沒有動過（`TC-12`，推），所以一份 2011 年日期的 rsdk 在 2018 年還在用並不反常 —— **這一句很弱，而且是 推**。真正會動它的是 `R2a/b/d-4` | 推 | — | `notes/which-drop.md` §5，2026-08-27 | gate R2 |
| `TC-03` | 那份 drop 的但書 | 它的目標板是 ALFA AIP-W512，不是 TOTOLINK 這片。DDR timing、PHY 設定、GPIO 腳位都可能不同，**而弄錯不一定會當機** | 推 | 推 | `SOURCES.json` | gate R2 |
| `TC-04` | `boa` 觀察到的 ELF flags | 到 2018-03-30 為止是 `0x1007` … pic … mips1；2019-03-15 起是 `0x1005` … mips1 | 讀 | 讀 | A + C，橫跨六個 build | `notes/lwl-mystery.md` |
| `TC-05` | rlxfw 自己要用什麼建 | *(留白)* —— T-vendor 對 T-modern 仍然是 gate R2 的決定。`-march=mips1`、big-endian、o32、soft-float 已定案，libc 未定。🔄 **2026-08-28：這一格的「環境」那一半不再留白，「選哪一條」那一半還是。** 建置環境已成立且不需要容器：三份 rsdk 在 Ubuntu 24.04 上原生跑得動，唯一缺的是 i386 `libz.so.1`，配方與版本釘在 `TC-14`；能不能建出東西由 `TC-16` 回答。**要選哪一條工具鏈仍未定，而 `TC-15` 為這個選擇加了一條硬條件** | — | — | — | `plan/` §5、gate R2；環境那一半見 `notes/vendor-toolchains.md` |
| `TC-06` | 🔴 `mips-linux-gnu-gcc-12` (12.4.0) 下 `-march=mips1` 的實際條件 | **單獨給 `-march=mips1` 會被拒絕**：`cc1: error: '-march=mips1' requires '-mfp32'`。`-msoft-float` 也滿足它，而且對一顆沒有 FPU 的核心那才是誠實的旗標。`-march=r3000` 同病 | 量 | 讀 | 2026-08-24 在本機量到；`plan/DAY-ZERO.md` 第 6、8 項記的建置行少了這個旗標，照抄不會編譯 | `tools/test-hazlint.sh` `E2` |
| `TC-07` | 同一段 C 在兩個 `-march` 下的 `movz`/`movn` | `-march=mips1 -msoft-float` **0 個**；`-march=mips32 -msoft-float` **1 個**。兩個 `.o` 的 load-use 違規都是 0 | 量 | 讀 | 同上，而且「`.o` 真的產生了」本身是一個控制 —— 沒編出來的 0 跟編出來的 0 長得一樣 | 同上 |
| `TC-08` 🆕 | opcode `0x13` 屬於哪個 ISA level，以及工具鏈怎麼認 | 🔴 **COP3 是 MIPS I 與 MIPS II；MIPS III 拿掉它；MIPS IV 把 opcode 收去給 COP1X。** 量(binutils 2.42，2026-08-27)：`mfc3 $2,$0` 在 `mips1`／`mips2` 編出 `0x4c020000`，在 `mips3`／`mips4` 被拒；`lwxc1 $f0,$4($5)` 在 `mips1`／`mips3` 被拒，`mips4` 編出 `0x4ca40000`。同一個 reallocation 也動到 `0x33`／`0x3B`：MIPS I 是 `LWC3`／`SWC3`，MIPS IV 把 `0x33` 給了 `PREF` ——量：`-march=mips1` **拒絕** `pref`、**接受** `lwc3`（`0xcc820000`）。⚠️ **而規格同一段說 COP3 在 MIPS I／II 是 *optional and implementation-specific***，所以「這是 MIPS-I」對「這顆會不會執行 COP3」一句話都沒說 —— 判它的是 `CPU-46` 的 `m-imem`。🔴 順帶量到本專案自己記錯的一件事：這個 Makefile 的 ARCH 行下 **`-march=mips1` 其實接受 `mfc3`／`mtc3`／`cfc3`／`bc3t`，只拒 `cache`** —— `cells.S` 註解 6 原本寫「兩個 mnemonic 都被拒」 | 量 | 讀 | 量於 binutils 2.42 ＋ 讀 MIPS IV Instruction Set Rev 3.2 A 8.3.4（`SOURCES.json` `spec-mips-iv-rev3.2`） | `tools/test-hazlint.sh` `E3`、`tools/hazlint` |
| `TC-09` 🆕 | `.comment` 在六棵 extracted 樹裡剩下什麼 | 六棵樹裡**只有 `v2.1.2` 與 `n300rt-2.1.6` 的 `boa` 還帶 `.comment`**，內容是 `GCC: (GNU) 3.3.2` 加 **`GCC: (GNU) 4.4.5-1.5.5p2`** —— 後者與 `TC-01` 從 kernel banner 量到的值一字不差，而且是**第二個獨立 artefact**（userland 對 kernel）。其餘四棵（含**這台**）連 section header table 都被拿掉了，`.comment` 是 non-alloc、不在任何 `PT_LOAD` 裡，所以整段沒了 —— 檔案大小正好等於兩個 `PT_LOAD` 的 `p_filesz` 相加（`unit-2018`：471,108 + 13,904 = 485,012）。🔴 **所以這一格對「這台是誰建的」什麼都沒說**，`TC-02` 仍然是 **推**。⚠️ **而它是這個語料庫的陷阱**：六棵樹各有一支 `bin/acltd`，10,032 bytes，**六棵同一個 sha256**（`1f3cd73b…`），它帶 `GCC: (GNU) 3.2.3-1.2.11` 與 `3.3.2` —— 對 `unit-2018` 整棵 rootfs 下 `grep 'GCC: (GNU)'` 只會答 **3.2.3**，而那是一支**沿用五年、三條產品線都沒重建過的預編二進位**的編譯器，不是這台的 🔄 **2026-08-28 第二次讀，結論一格未動**，新增三件小事：那個戳記在 `lib/libapmib.so` 裡也在，而 `libapmib` 是廠商程式碼不是工具鏈附的函式庫，所以它是 build 戳記不是工具鏈戳記；逐棵樹保有 section header 的 ELF 檔數量出來了 —— 1/55、1/62、**63/63**、**64/64**、1/50、1/50；⚠️ **廠商自己的 `mips-linux-readelf`（binutils 2.16.94）沒有 `-p` 這個選項**，它回一段 usage，於是 grep 它的輸出會把這件事記成「沒有 `.comment` section」。兩支 readelf 用 `-S` 問都說那個 section 在。這一題要用 host 的 readelf。| 讀 | 讀 | A，六棵 `$FWRE_WORK/extracted/*/squashfs-root`，量 2026-08-27，重讀 2026-08-28 | `notes/binsim.md` |
| `TC-10` 🆕 | 六棵樹的**容器指紋**分割 | **2+2+2，完全由 ELF 中繼資料讀出，沒有用到任何相似度**：① `v2.1.2`＋`n300rt-2.1.6` —— `e_flags 0x1007`（含 `pic`）、8 個 program header、無 `DT_MIPS_PLTGOT`、**section header table 還在**、3 個 `DT_NEEDED`；② `unit-2018`＋`n200re-3.2.0` —— 同上但 **section header table 被拿掉**；③ `n300rt-3.4.0`＋`v3.4.0` —— `e_flags 0x1005`（**沒有 `pic`**）、10 個 program header、**有 `DT_MIPS_PLTGOT`**、5 個 `DT_NEEDED`（多 `libcjson`、`libmtdapi`）。🔴 **這和 `notes/lwl-mystery.md` 從 unaligned 指令數得到的 176／144／0 是同一個 2+2+2**，兩個互相獨立的儀器讀檔案的不同部分。`TC-04` 已經持有其中 `e_flags` 那一半 | 讀 | 讀 | A，同上 | `notes/binsim.md` |
| `TC-11` 🆕 | `binsim` 的定義、`k` 的選法，與三個門檻 | **定義**：`binsim(A,B)` = 兩檔 code window `[DT_INIT, DT_FINI)` 上正規化 MIPS 運算元 token 的 **7-gram 集合 containment**；Jaccard 一律並列印出，不取代它。**`k=7` 是事前寫好的規則選的**，規則只讀答案已知的錨點：word-permutation（同一組指令、次序打散）必須 < 0.05，1 % 指令換掉後必須 ≥ 0.50，取滿足全部條件的最小 `k`。🔴 **`k=4` 的 null 是 0.4398，差九倍** —— token 字母表只有 52 個，96,490 個窗只產生 7,333 個相異 4-gram。**門檻**（讀 2026-08-27）：`BASE` = **0.9818**（`boa`，`unit-2018` 對 `n200re-3.2.0`）、**`CROSS`（同一棵樹的 `boa` 對 `busybox`，六棵 0.1132–0.1581）= 0.1581**。🔄 **`FLOOR` 換過了，而且換了兩次。** `plan/router-rebuild-plan.md:1128` 寫的 `boa unit-2018/v3.4.0` = 0.0650 **低於 `CROSS`**，被語料庫推翻。`R2a/b/d-1` 第一次改成 `busybox unit-2018/v3.4.0` = 0.1646（同樣那兩棵樹、換一支原始碼固定的程式），**而那個答案也是錯的，同日被對抗審查殺掉**：🔴 **containment 的分母是比較小的那個特徵集**，`busybox` 是 42,297 個 gram、`boa` 是 28,887，差 1.46 倍，所以那兩個數字是在**兩個不同的分母**上讀的。讀，在同一個分母上次序會反過來：無共用原始碼、同一個編譯模型是 **0.1551–0.1581**（`boa` 對 `busybox`／`pppd`／`wscd`，分母都是 28,887），而**同一份上游原始碼跨編譯模型**是 **0.1212**（`pppd` `unit-2018`／`v3.4.0`，分母 28,601）—— **比前者低**。所以在 `boa` 的尺度上，語料庫裡沒有任何一格在「無共用原始碼的水準」之上、`BASE` 之下可以當底，最緊的正確底**就是那個水準本身**：**`@floor boa unit-2018 busybox unit-2018` = 0.1581**（manifest 為此長出五欄的跨程式格式）。`E7` 斷言它是那個分母下三個程式裡最高的一格；`E8` 量「只換編譯模型要付多少」，那是判準前面那條 **VOID 前置條件**的依據而不是論證。`k` 從 2 到 14 這一格都是族群裡最高的，`k`=1 被 `pppd` 超過（那裡參考樣本自己也翻了面）。🔄 **噪音底不是 0.0000，那句是循環論證**：那 16 對是用它接著要評分的窗的逐位元組相等挑出來的，1.000 是算術。改用同源但窗不同的一對（`busybox` `n300rt-2.1.6`／`unit-2018`，Jaccard 0.999196）估計 = **8.0e-4，推**。span：`boa` 92.8 pp、`busybox` 83.5 pp、`acltd` **0.0 pp**（那是正控制，void 判決必須在它身上發射）。🔄 **這一列的 `V` 原本標 `量`，那是錯的** —— §0 的圖例寫「量＝在這台裝置上量到的」，而這裡每一個數字都是從六棵樹的檔案算出來的，一次也沒有碰過裝置。改成 `讀`，與同樣讀這六棵樹的 `TC-04`／`TC-09`／`TC-10`／`TC-12` 一致 | 讀 | — | A ＋ 本專案 `tools/binsim.py`（35 個控制：24 個合成 ＋ 11 個要真材料，`tools/test-binsim.sh` 96 案） | `notes/binsim.md`；`FLOOR` 那一格由 `notes/which-drop.md` 擁有 |
| `TC-12` 🆕 | 六棵樹的**相似度分群**、這台的最近鄰，以及它排除掉什麼 | **三群 = ①`v2.1.2`＋`n300rt-2.1.6`／②`unit-2018`＋`n200re-3.2.0`／③`n300rt-3.4.0`＋`v3.4.0`**，與 `TC-10` 的容器指紋逐格相同。**這台的最近鄰是 `n200re-3.2.0`，`boa` containment 0.9818**，第二名 `n300rt-2.1.6` 0.8951 —— 差 **8.67 pp**，是重現誤差（8.0e-4，推）的 108 倍，而且 `k` 從 2 到 16 排名不變。🔴 **產品線被這個語料庫自己否證掉了**：讀 `/etc/version`，N150RT 三群裡都有、N300RT 在①和③，**群內最低一格 0.9740 高過群間最高一格 0.8951（十五格無重疊）；同一群裡兩個不同產品是 0.9863／0.9818，而同一個產品（都是 N150RT）跨群是 0.8860（①↔②）與 0.0650（②↔③）—— 0.8860 是產品線在這個語料庫裡最強的一次，仍然低於每一格群內值**；廠商版本號同樣否證 —— 這台與 `n300rt-2.1.6` 都標 V2.1.6 卻落在不同群 —— 那一格是 0.8951，是群間最高的一格，仍然低於每一格群內值。剩下真正共線的只有日期與 SDK 世代，而那是同義反覆。🔴 **而 `busybox` 是工具鏈的示蹤劑**：讀，六棵樹同一版 `BusyBox v1.13.4`，所以它只在 code generator 的輸出動的時候動 —— ①↔② 它是 **0.9995–1.0000** 而 `boa` 掉到 0.877–0.895，**所以那一步是 `boa` 的原始碼改了、code generator 的輸出沒改**（把它轉到 `boa` 上是 **推**，假設一棵樹一條工具鏈；而「編譯器換了」與「同一個編譯器換了旗標」這裡也分不開）；①②↔③ 兩者一起垮（0.1646–0.1695 對 0.058–0.068），那一步分不開。最利的一格是 `n200re-3.2.0` 的 `busybox` 比這台少 1,869 個 code word，而它 40,915 個 7-gram **全部**是這台 42,297 個的子集（containment 正好 1.0000）。第三支儀器 `G(boa_t) ∩ G(busybox_t)`（兩支沒有共用原始碼的程式共有的 gram）跨樹比較給 **4+2**：①②之間 containment 0.932–0.969、對③只有 0.099–0.103，**而它把「同一支程式的兩個 build」這件事**衰減**掉，不是完全免疫** —— `IDIOM(t1) ∩ IDIOM(t2)` 展開後分子裡確實有 `G(boa_t1) ∩ G(boa_t2)`。⚠️ 這一格對 `TC-02` 什麼都沒說 —— 六棵都是**出貨映像**，不是 GPL drop | 讀 | 讀 | A，六棵 `$FWRE_WORK/extracted/*/squashfs-root` ＋ 本專案 `tools/binsim.py --corpus`，讀 2026-08-27 | `notes/which-drop.md` |
| `TC-13` 🆕 | 廠商組譯器對每個 Lexra 核心接受什麼 | 🔄 **這個 id 在 `CPU-04` 裡被引用了，卻沒有自己的列 —— 2026-08-28 補上，而它引用的內容早就量過了。**`tools/isa-probe.sh` 逐條把指令丟給廠商 binutils 的 opcode 表，八個 `-march` 欄各一次：`movz`／`movn` 在 `rlx4181` 接受、`mips1` 拒絕；`cache` 同理；**`sync` 在 `rlx4181` 拒絕、`rlx5281` 接受，與板級 config 完全一致**；🔴 **而 `ll`／`sc` 與板級 config 相衝 —— 組譯器對 `rlx4181` 接受，`boards/rtl8196e/config.in` 寫 `ARCH_CPU_LLSC=n`，這台 kernel 的 2.85 MB 程式區一條都沒有。記成衝突不做調和** ⚠️ **一個來源（B）。** 這是廠商組譯器的說法，不是矽片的說法，也沒有第二個獨立來源對它 —— 依 §0 的門檻這一列的**值**只到「工具鏈這樣認為」為止。 | 讀 | 讀 | B，廠商 binutils 的 opcode 表 ＋ 本專案 `tools/isa-probe.sh`（48 案） | `notes/vendor-kernel-isa.md` §6 |
| `TC-14` 🆕 | 三份 rsdk 各自被設定成哪一顆核心，以及要什麼才跑得起來 | **`rsdk-linux-gcc` 是 RSDK Wrapper 2.0，三份都是**，而它只接受一個 `-march`：`rsdk-1.3.6-4181` → `4181`、`rsdk-1.3.6-5281` → `5281`、`rsdk-1.5.5-5281` → `5281`。其餘一律 `FATAL: -march mismatch`，exit 1 **而且不寫輸出檔**。🔴 **目錄名裡的數字不是編譯器的預設** —— 兩份 1.3.6 的 `mips-linux-gcc` 都報 `_MIPS_ARCH "4180"`，`as` 還逐位元組相同；**問核心要問 wrapper，而 kernel 也正是呼叫它**（`arch/rlx/Makefile` 的 `CROSS_COMPILE := rsdk-linux-`）。wrapper 自己會說：`Configured for: -march=5281 -EB -msoft-float -fuse-uls -ffix-bdsl`。🔴 **`-fuse-uls` 是 1.5.5 的 wrapper 注入的，三份 drop 的 build system 一個字都沒提**（控制：同一組 grep 在同一份語料庫裡找得到 `CPU_HAS_ULS` 48 個檔、`march=5281` 1141 個、`ffix-bdsl` 2 個）。**執行期**：1.5.5 的 gcc／cpp／xgcc 是靜態連結所以會動，**binutils 十七支全部倒在一個 i386 `libz.so.1` 上**（`as` `ld` `xld` `ar` `ranlib` `nm` `objcopy` `objdump` `readelf` `size` `strings` `strip` `addr2line` `c++filt` `gprof`）—— 這正是「`--version` 印得出來」為什麼是個壞 DoD。兩條配方都驗過，**先驗沒有系統變更的那條**：`lib32z1` `1:1.3.dfsg-3.1ubuntu2.1`（deb sha256 `91ab7d60…`，57,380 bytes；`libz.so.1.3` sha256 `0c100479…`），解開用 `LD_LIBRARY_PATH` 或直接裝，**都不需要 `dpkg --add-architecture i386`**。補完後 30 支裡 27 支解得開，剩下三支是 gdb／gdbtui／insight，要 `libX11`／`libncurses.so.5`／`libexpat.so.0`，**建置用不到，記在這裡是為了以後那個零讀起來是「已知且不需要」而不是新問題** ⚠️ **一個來源（B）**，而且是同一個 tarball 裡的三份工具鏈。這一列講的全部是廠商工具的行為，不是這顆晶片的性質。 | 讀 | 讀 | B，三份 rsdk 自己的執行檔與 `RSDK_LOGFILE` 輸出，讀 2026-08-28 | `notes/vendor-toolchains.md` §2–3 |
| `TC-15` 🆕 | **load delay slot：廠商自己的工具鏈怎麼分核心**，以及拿錯的 rsdk 建這塊板子會怎樣 | 🔴 **兩支實作互相獨立、但出處同一份的廠商儀器給出同一條界線**（`notes/vendor-toolchains.md` §8 說明為什麼那是一個廠商的一次決定被讀了兩遍，不是兩個來源）。① 編譯器：同一份原始碼、同樣 `-O2`，`-march` 掃六格，load 後面補 `nop` 的條數 —— `4180`／`4181`／`5181` 是 121／121／121（1.3.6）與 90／90／91（1.5.5），`5280`／`5281`／`4281` 那一側 1.3.6 是 **0**、1.5.5 是 **1（0.26 %）**，而 violations 是 **107–162**（另一側全部 0）—— **violations 才是利的那一欄，nop 數只是它的影子**。② 組譯器：rsdk-1.5.5 的 `as` 內建 load-use 檢查（`possible LOAD-USE: regno=%d`、`warn-possible-load-use`、`load_delay_nop`），1.3.6 的 `as` **一個字串都沒有**；在 `.set noreorder` 下餵它 `lw $31` 接 `jr $31`，`4180`／`4181`／`5181`／`mips1` 會警告，`5280`／`5281`／`4281`／`mips2` 不會。🔄 **2026-08-28 對抗審查：「1.3.6 沒有這個模型」是 noreorder 模式下沒有正控制造成的假零。** 在 gas 預設的 `.set reorder` 下，**兩個世代都會替 `4181` 補上那個 `nop`、都不會替 `5281` 補**（1.3.6 `8c9f0000 00000000 03e00008`／1.5.5 同）。所以字串量的是**診斷**不是模型，分割線因此站在三個讀數上而不是兩個；死掉的是「組譯器只警告不修」那一句。**分割完全相同：`4180 4181 5181 mips1` 曝露 load delay slot，`5280 5281 4281 mips2` 不曝露。**⚠️ **組譯器只警告，不補 `nop`。**🔴 **代價是可量的**：同一份原始碼、同一份 `.config`，`arch/rlx/kernel/traps.o` 用 1.3.6-4181 建是 134 loads／30 nop／**0 violations**，用 1.5.5-5281 建是 150 loads／0 nop／**28 violations**；`mm/cache.o` 是 0 對 **16**；`bsp/setup.o` 是 0 對 **5**。同一支儀器量 `users/dhrystone/dhry_1.c`：`{4180 4181 5181}` 那一側 violations **全部 0**，`{5280 5281 4281}` 那一側是 **107–162** —— **violations 這一欄比 nop 數更利，而它不在這張表的第一版裡**。**三個檔 49 條，而其中兩個是例外處理與快取管理。**🔴 **而真正該比的是整份映像，今天從同一份原始碼建出了兩份**（三列同一支儀器，**範圍切在 MIPS16 之下** —— 最低的 `[MIPS16]` 符號是 `0x8016c844`／`0x8015c200`，所以 `[0x80000000, 0x80158000)` 三份都是 32-bit 程式碼，同一個界服務三份，而且 `hazlint` 不需要任何 override 就認）：我建的 `-march=4181` 是 61,568 loads／17,423 nop（**28.30 %**）／**4 violations**；`-march=5281` 是 65,740／117（**0.18 %**）／**21,185**；**這台自己的 kernel 是 63,298／19,419（30.68 %）／**0****。🔄 **這一格第一版是整份映像加 `--allow-mips16`（256／36,264／168），那是壞的讀法而且理由是錯的** —— 當時寫「切界的話三份切的不是同一個界」，實際上同一個界就服務三份；那個模式 `hazlint` 自己叫「不是保守的答案，是沒有答案」，而在那個模式下數到的東西大半是**資料被當成程式碼**（`vmlinux` 只有一個可執行 `PT_LOAD`，`__ex_table`／`.rodata`／`.data` 全在裡面）。舊數字留著，因為負結果不刪。⚠️ **不要和 `K4b` 的 128,440／40,182（31.28 %）／58 混用**，那又是另一個範圍。⚠️ **那 4 條沒有解釋**，61,568 個 load 裡的四個點位，這裡沒說它們是什麼。**推**：建這台韌體的工具鏈把這顆核心當成 load delay slot 曝露的。⚠️ **這是讀兩個編譯器，不是讀矽片**；矽片那一題是 `R1a`／`CPU-14` | 讀 | 讀 | **×2** —— B 兩支廠商儀器（編譯器排程器＋組譯器檢查器，後者在舊世代裡不存在）；A 這台自己的 kernel 經 `tools/hazlint` | `notes/vendor-toolchains.md` §5 |
| `TC-16` 🆕 | 這三條工具鏈到底建不建得出東西 | **建得出，而且是完整的 kernel。**`tools/tc-smoke.sh` 的四階梯（binutils 起得來／`.c`→`.s`→`.o`→靜態連結的 32-bit MSB MIPS `EXEC`／`arch/rlx` 用到的九條指令組出同樣 48 bytes，sha256 `298d5f2a…`，三份一致／連結出來的程式在 `qemu-mips` 下跑出它自己算的數）**三份全部走到第四階**。之上三次真的建置：廠商自己的 `users/dhrystone` 用它自己的 Makefile，三份工具鏈都 `make` rc=0、動態與靜態都連得出來、在 `qemu-mips` 下跑完而且**每一個內部自檢值都等於它自己寫的期望常數**；`arch/rlx` 的四個 object 兩份工具鏈都 rc=0；**完整的 `vmlinux` 724 個 object，兩份工具鏈都 rc=0** —— 1.3.6-4181 是 3,340,287 bytes（text 2,656,040、entry `0x80003600`），1.5.5-5281 是 3,166,710 bytes（text 2,497,352、entry `0x80003420`），兩個都是 `ELF 32-bit MSB, MIPS-I, statically linked`。🔴 **唯一擋路的是 host 工具不是交叉工具鏈**：`kernel/timeconst.pl:373` 的 `defined(@val)` 在 Perl 5.22 之後被拿掉，這台 host 跑 5.38.2，改一行就過 —— 這就是計畫 `R-6`（「2.6.30 在現代主機上建不動」）的答案。⚠️ **建出來的 `vmlinux` 沒有跑過**，裝置上沒有、模擬器裡也沒有 ⚠️ **一個來源（B）＋這台桌面上的一次建置。** 它說的是「這條工具鏈建得出來」，不是「建出來的東西對這顆核心是對的」—— 後者是 `TC-15`。 | 讀 | 讀 | B 三份 rsdk ＋ `rtl819x-toolchain` 的 `linux-2.6.30` 與 `boards/rtl8196e`；本專案 `tools/tc-smoke.sh`（31 案）與 `tools/vendor-tripwire.sh`（24 案） | `notes/vendor-toolchains.md` §4 |
| `TC-17` 🆕 | 每一份 drop 自己的 `.config` 指定的是哪一條 rsdk | 讀三份 drop 各自的頂層 `.config`：`rtl819x-toolchain` 是 **rtl8196e** 板、`rsdk-1.3.6-4181`、`RTL8196E_88E_GW`，產生於 2013-06-29；`saturn49-wecb` 與 `wecb-vz-gpl` 都是 **rtl8198** 板、`rsdk-1.3.6-5281`、`RTL8198_SPI_SQUASHFS`，兩份的產生時間同為 2012-08-15。控制：每一份都有三行 `CONFIG_RSDK_*`，剛好一行 `=y`。🔴 **三份沒有一份選 1.5.5，而 `TC-01` 的 banner 是 `4.4.5-1.5.5p2`** —— 這是矩陣做不到的鑑別器，因為它讀的是 drop 不是映像：**手上這三份 drop，照它們自己的設定，建不出這份韌體**。⚠️ **它不指認誰建的**：`.config` 可以改，建置機器上也可以有 tarball 沒附的工具鏈；drop 做不到的是提供一份它沒有的 release，而三份裡沒有任何一份帶 1.5.5p2，也沒有任何一份帶設定成 4181 的 1.5.5，**而板級 config 是 `ARCH_CPU_RLX4181=y`、1.5.5 的 wrapper 拒絕 4181**（`TC-14`）。⚠️ **三份裡有兩份是 RTL8198 的 drop**，只有 `rtl819x-toolchain` 打的是這顆 SoC。**`TC-02` 仍然是 推**，改變的是它現在是對著一個指名並查過的替代選項在推 ⚠️ **一個來源（B）。** 讀的是 drop 自己的設定檔，所以它排除的是「照這份 drop 現在的設定」，不是「這份 drop 的原始碼」。 | 讀 | 讀 | B，三份 drop 各自的頂層 `.config`，讀 2026-08-28 | `notes/which-drop.md` |

## 15. rlxfw 的設計標的

**這些是決定，不是量測。** 放在這裡是因為設計時會跟硬體數字一起看，
而且其中兩條是絕對的。

| id | 項目 | 值 | 擁有者 |
|---|---|---|---|
| `TGT-01` | `0x000000`–`0x005FFF` | **永久禁寫。** 變磚不可回復，而且沒有備機 | `CLAUDE.md`、`plan/` D4 |
| `TGT-02` | `0x006000`–`0x007FFF`（`H601`） | **永久禁寫。** 這台的 MAC 與射頻校正，回復原廠設定不會還原 | `CLAUDE.md`、`plan/` D4 |
| `TGT-03` | `0x008000`–`0x00FFFF`（`COMPDS`/`COMPCS`） | 有經過驗證的備份才准寫 | `plan/` D4 |
| `TGT-04` | `0x010000`–`0x3FFFFF` | kernel 與 rootfs —— **R8 起**可寫，前提是先做過一次救援演練 | `plan/` D4 |
| `TGT-05` | 零 flash 寫入 | **主線在 R9 之前一個 flash 位元組都不寫。** 要寫必須拿到明確的同意 | `CLAUDE.md` |
| `TGT-06` | 為什麼寫 kernel 區是可回復的 | loader 佔 `0x000000`–`0x005FFF` 且永遠不被寫；它在提供 ESC 視窗**之前**就把映像搬進 RAM；檢查失敗會直接進提示字元。所以不論 `0x010000` 之後變成什麼樣，救援路徑都還在 | `plan/` D4、`docs/loader-command-semantics.md` §a |
| `TGT-07` | 預計的 A/B 佈局 | 🔄 **2026-08-25 更新，這一列落後計畫兩版**（它還帶著沒有 `rlxboot-rescue` 的舊佈局）。現行：`0x010000` `rlxboot` 64 KiB（預算 ≤ 32 KiB）· **`0x020000` `rlxboot-rescue` 64 KiB，唯讀，更新流程永不寫** · `0x030000` 保留 · `0x040000` slot A 1,728 KiB · `0x1F0000` slot B 1,728 KiB · `0x3A0000` state 384 KiB。**slot 映像刻意不帶 loader 認得的簽名頭。⚠️ 邊界仍是暫定的**：sector 大小要等 JEDEC ID，`bank_offset` 還沒讀 | `plan/` §9、D6 |
| `TGT-08` | 每個 slot 的預算 | 🔄 **2026-08-25 更新，同上**。kernel（LZMA）≤ 830 KiB、rootfs ≤ 830 KiB、餘裕 68 KiB。原廠 kernel 是 987 KiB **含一堆不需要的**，砍到 700 KiB 是目標 | `plan/` §9 |
| `TGT-09` | RAM 預算 | 以 `MEM-06` 的 26,052 kB 為底：kernel ≤ 8 MiB、userspace ≤ 6 MiB、更新緩衝 1.92 MiB、DMA ring + RX 池 ~600 KiB、餘裕 ≥ 8 MiB | `plan/` §9 |
| `TGT-10` | `0x010000`–`0x060000` 之間任一個 64 KiB 邊界 | 都是**原廠 loader 自己就找得到**的 slot，而且它偏好最低的那個 —— 所以 A/B 方案不需要改 loader | `docs/loader-command-semantics.md` §a |

## 16. 公開規格，以及它錯在哪

列出來是為了讓它被否證，而不是被半記半忘。來源 **X**：
TechInfoDepot 的 TOTOLINK N150RT 條目。

| id | 公開規格說 | 實際量到 | 是怎麼抓到的 |
|---|---|---|---|
| `PUB-01` | SoC **RTL8196C** | **RTL8196E**（`CPU-01`） | 晶片絲印，然後是開機碼自己對 ID 的比較 |
| `PUB-02` | Flash **2 MB** | **4 MiB**（`FLS-03`） | **廠商自己的韌體塞不進去** —— 在硬體到手三週前，就從容器的 burn 位址推出來了 |
| `PUB-03` | RAM **16 MB** | **實裝 32 MiB**（`MEM-04`） | 封裝絲印，然後是 `ramSize: 32M` |

**這三條原本都寫在本專案自己的計畫書裡，是從規格表抄來的，不是從板子上讀來的。**
其中最可查證地錯的那一條，光靠韌體就被證明不可能 —— 而那時裝置還沒到桌上。

## 17. 留白清單 —— 各由什麼來填

上面每一格留白都在這裡出現一次，並附上能填它的實驗。
**一格留白如果沒有出路，那格本身就是這張表的 bug。**

| id | 缺什麼 | 什麼能定案 | owning gate |
|---|---|---|---|
| `CPU-25` 殘留 🆕 | **關聯度** —— 大小與 line 由 `bspcpu.h` 填上了（`I 16 KiB`／`D 8 KiB`／line 16／無 L2），但三份 GPL drop 裡**沒有任何一處**講 direct-mapped 還是 set-associative | LX4189 資料表說 D-cache direct mapped、I-cache 可二路，但那是**姊妹核心的文件**，而這一格已經被 `CPU-46` 警告過一次（I-MEM 16 KiB 與 I-cache 16 KiB 同大小，任何「大小」量測都分不開）。能定案的是一次不需要隔離的 eviction walk，而它要先能區分 I-MEM 與 I-cache | R1h |
| `CPU-48` 殘留 🆕 | **那些 MIPS16 區段到哪裡結束** —— 從呼叫端知道 23 個進入點落在 `0x802B8118`–`0x802BC9F8`，但每一個函式的長度、以及 `.iram` 裡是否也混著 32-bit MIPS，都沒有確立 | 從每個進入點做 MIPS16 反組譯直到終止指令，並與 `__iram` 的界對照。⚠️ **這件事必須在任何工具再去掃那一段之前做**，因為 `hazlint` 1.3 現在整段拒絕，而拒絕不等於知道裡面是什麼 | R2、之後 R6 |
| `TC-02` 材料 🆕 | **手上三份 drop 都建不出這個映像。** 原本寫「有兩個具體的鑑別器 —— `imem-dmem.S` 的 `IMEM0_SIZE`（drop 4096／binary 16384）與 MIPS16（五份出貨 config 沒有一個開，binary 有）」。🔄 **2026-08-28：MIPS16 那一個被建置本身否證了。** 拿那五份 config 之一（`RTL8196E_88E_GW`）把 `rtl819x-toolchain` 的 kernel 建出來，`vmlinux` 裡就有 MIPS16 —— `readelf -s` 標了 **39 個 `[MIPS16]` 符號**，廠商自己的 objdump 把 `0x80006bf4` 的 `7409506d` 讀成 `jalx <irq_to_desc>`。**config 沒開不代表建不出來，所以那不是鑑別器。**`IMEM0_SIZE` 還是。而 `TC-17` 補上一個比兩者都強的：三份 drop 自己的頂層 `.config` 選的都是 1.3.6 世代的 rsdk | 這**不會**讓 `TC-02` 離開 推：它把候選縮小，但出貨映像的相似度仍然定不出建置機器上放的是哪一份。能用的是拿到第四份 drop 時先對 `IMEM0_SIZE` 與 `TC-17` 這兩個鑑別器 | R2a |
| ~~`CLK-15`~~ ✅ | ~~`Booting...` 之後那 350 ms 的靜默是什麼~~ —— **2026-08-25 關閉，見上表**。原本這一格寫的實驗是「讀 `0x80400000`–`0x80400440`」，而那段程式碼裡沒有 `Booting...`：它是 stage 1 印的 | 它與重置方式無關（冷、暖皆同，n=9，全距 3.5 %），所以是 loader 自己在做一件固定的事。**讀 `0x80400000`–`0x80400440` 之間的開機路徑**：`Booting...` 的 `dprintf` 到 `chipName:` 的 `dprintf` 之間那段程式，靜態就能定案，不必上機 | R1 |
| `CLK-15` 殘留 🔄 | 那個 NUL 是誰寫的，以及**拆掉冷暖之後剩下的** 2.5 %／2.7 % 群內散布為什麼那麼大 | NUL：讀 stage 1 在 `0xBFC003D4` 之後那一段，以及 16550 的 FIFO 行為。散布：🔴 **2026-08-25b 跑了，而這個實驗的機制不成立。** 讀 `tools/console-capture.py:373`：`drain()` 用 `select()` 在位元組抵達的當下打時戳，與 ESC 週期無關。卡住 `CLK-08b` 的 20.35 ms 格點量的是 **ESC 回音之間**的間隔；`CLK-15` 的兩個端點都是**裝置自己送的位元組**。而且 n=1 本來就量不出散布。**實際得到的是一個偏差檢查，而它回了陰性**：2.32 ms 格點上的冷開機 `booting` 是 **348.8 ms**，**落在**本列公佈的冷區間 348.0–356.9 之內，所以細格點沒有把數字拉走。🆕 **真正要查的儀器項是 chunk 邊界**：`.timing` 每一次 `read()` 寫一行、時戳綁在 chunk 的第一個 offset 上，所以一個跟後面十個位元組一起交付的首位元組會帶著整組的抵達時間。**那是桌面工作，九份 `.timing` 都在磁碟上**，不用上機 | R1-gate 順手 |
| `CLK-15` 冷暖差 🔄 | 冷上電比暖重置慢 4.5–14.5 ms，機制未定 —— 🔴 **2026-08-25b 排除了 SPI 除頻這條。** `SFCR`／`SFCR2`／`SFCSR` 冷暖**逐位元組相同**（只有 `SFDR` 由 `FFFF0002` 變 `FFFF0000`，而那正好證明這四個字不是凍住的常數，是這一格活著的控制）。**下一個候選是 SPI NOR 自身的上電喚醒**，而決定它的實驗不在暫存器裡：NOR 的 `tVSL`／deep-power-down 喚醒時間要從 `REG-21` 的 JEDEC ID `1C 70 16` 查 EON 的 datasheet，桌面。🆕 同一次也加了第三個同循環冷暖對：**+6.0 ms**（348.8 對 342.8），落在既有的 +4.5 與 +14.5 之間。*(原文的實驗：)* | **兩個 `DW`，零風險，順手**：冷開機後與暖重置後各讀一次 SPI 控制器 （`SFCR 0xb8001200`／`SFCR2 0xb8001204`／`SFCSR 0xb8001208`／`SFDR 0xb800120c`）並比對。主導項是 stage 1 從 memory-mapped SPI NOR 逐字搬 20,924 bytes，所以除頻或喚醒延遲會直接落在這一段。**若四個暫存器完全相同，SPI 除頻這條就被排除**，下一個候選是 SPI NOR 自身的上電喚醒 | R1-gate 順手 |
| ~~`CPU-25` 幾何~~ 🔄 | *(合併到下方 `CPU-25` 那一列，2026-08-26)* | 🔴 **這是同一格的第二個副本，而且它的 gate 欄還停在已經結案的 `R1d`。** 兩列都在把同樣三個數字記成留白，而 `spec-check.py` 的 C1（id 唯一）只掃 §1–§16，所以 §17 的重複 id 對它是不可見的 —— 這一列是被人讀出來的，不是被工具抓到的。**保留的那一句是 `GEOM=1` 的量測更正**：`rlx_r3k_size` 每次呼叫約 16 個字散在 1 MiB 的框裡，**1 MiB 是外框不是體積**，而它在這顆不隔離的核心上只能回 `0` —— 那正是它自己「核心不回答」的值 | ✅ 併入下列 |
| ~~`CPU-35`~~ ✅ | ~~`Status.IsC` 在這顆上到底有沒有隔離快取~~ —— **2026-08-25 答了：沒有。** `probe1` cell 4 兩個 victim 都是 `V_CORRUPT`，`sb $0` 進了真記憶體（`240222b2 → 000222b2`、`03e00008 → 00e00008`，每字最高位元組清零、stride 4）。**qemu 的 24Kf 一天前就是這樣，而這顆矽片同意它** —— 這是本專案第一次 qemu 的行為與裝置一致而不是相反。⚠️ **量到的是行為不是位元**：`Status` 兩個位元是否實作要讀回 `Status`，那是 `probe2` | 量，`bench/2026-08-25/H1b.log` | ✅ 已關 |
| ~~`NET-13`~~ ✅ | ~~哪一個 RJ45 是哪一個埠號~~ —— **2026-08-25 關閉，見上表。** 關掉它的是**絲印 → 埠**（WAN→0、LAN1→1、LAN2→2、LAN3→3 皆量，LAN4→4 推），不是原本設計的**位置 → 埠**。🔴 **原本這一格寫的實驗有一個沒被說出來的歧義**：「孔別寫進檔名」裡的孔別是**位置**，而操作者看到的是**絲印**，兩者只有在機殼按 `WAN LAN1..LAN4` 順序印刷時才能換算 —— 而那個順序這個 repo 從來沒有記錄過。檔名現在寫 `pos<N>-<絲印>` 兩者都帶 | ——（已關，見左） | ✅ 已關 |
| `NET-13` 位置圖 🆕 | 機殼上五個孔的**實體順序**對應哪些絕印標籤 —— 因此也就是位置 → 埠 | **看一眼機殼**，從 WAN 孔沿著機殼把絲印依序抄下來。不是暫存器讀取，不用上電，不花電源循環。舊的 `0,2,3,4,1` 與線性圖兩張位置圖仍然作廢，因為它們的位置是事後貼的 | R6，桌面 |
| `PSRP` 保留態的起點 🆕 | 速度／雙工欄位從 `E0`（沒協商過）變成 `F9` 的那一刻 | 2026-08-25 量到的四次都是保留態的**持續**，沒有一次是**開始**：`PSRP4` 是那個電源循環唯一沒協商過的埠，而操作者放棄了第五次換線。**任何一次把線插進一個沒協商過的埠的擷取都免費補上這一點**，不必為它專門安排 | R6，順手 |
| ~~`CLK-08b`~~ ✅ | ~~看門狗數的時脈比 `base/CDBR` 快多少，以及殘差是固定還是比例~~ —— **2026-08-25 關閉：14.9650 MHz，比例。** 這一格開的處方（更細的心跳、不是第三個 `OVSEL` 點）**照原樣執行而且成立**：`--esc-period 0.002` 實得 2.118 ms，兩個舊點就分出了勝負，**固定延遲模型無解**。這一格猜的 3 % 盲窗風險沒有實現 | ——（已關，見左） | ✅ 已關 |
| `CLK-08b` 殘留 🆕 | **14.965 MHz 與 200.0049 MHz 之間是什麼整數關係** | `f_timer/13` = 15.385、`/14` = 14.286、`400/26` = 15.385、`400/27` = 14.815 —— **沒有一個在 1 % 以內**，所以看門狗數的不是分頻後的計時器時脈，而它數什麼未定。⚠️ 這一格不能靠再量一次逾時解決（那只會再量到 14.965）：要的是 **D** 的時脈樹，或一個把看門狗與計時器綁在同一次量測裡的實驗 | R4 |
| ~~`CLK-14` 殘留~~ ✅ | ~~`Booting...` 前後那兩個 ≈345 ms —— 兩個等長且相鄰的靜默，而 `CLK-14` 原本的 340/348/345 ms 在 2026-08-25 被改判給其中一個~~ —— **2026-08-25 第二節關閉，見上表：340 屬本列、348／345 屬 `CLK-15`，而且順手把冷暖分了群** | **桌面，九份擷取都在磁碟上**：把 `CLK-15` 引用的那九份逐一分開量「artifact byte → `Booting`」與「`Booting` → `chipName`」兩段，冷開機才有前段、暖重置沒有。**改判送對了哪一個，靠這個決定，不用上機** | R1-gate 順手 |
| 回覆長度公式該是工具 🆕 | `len(cmd) + 2 + 47 × lines + 9` 今天 15 中 15，而唯一沒中的一次是**人把命令字元數算錯** | 一支腳本，吃命令字串吐位元組數，寫進預測檔的數字由它產生。**`check-predictions.py` 驗的是順序不是算術**，而今天兩個算術錯都出在現場寫的區塊、沒有一個出在桌上寫的區塊 | 下一次上機前 |
| `CPU-04` 🔄 | 核心是 RLX4181 還是 RLX5281 —— **值關了，名字沒關**。`PRId = 0x0000CD01` 量 2026-08-25b，`Config.M = 0` 也量到（`Config` 讀 `00000000`，而 `nowrite=0` 證明那是真的 0）。🆕 🔴 **2026-08-26：第一個資料點進來了，而禁令一動也不動。** `SOURCES.json` `ds-lexra-lx4189`（2026-08-26 抓下，sha256 `6afb1415…`）Table 2 明寫 **LX4189 的 `PRID` 讀 `0x0000c401`**。這是**核心廠商自己的文件**，也是這個專案手上第一個 Lexra `PRId` 值。⚠️ **它只說一件事：`0xCD01` 不是 LX4189。** 一個點不是一張指派表，`0xC4` 到 `0xCD` 之間怎麼編碼沒有任何來源，**`RLX4181` 與 `RLX5281` 兩個名字仍然都不可寫**。**剩下的不是一次量測，是一份把 `PRId` 值對到 Lexra 型號的來源** —— 四票的 4181 全部只講名字。決定它的：Lexra 的 `PRId` 指派表，或任何一棵 rlx kernel 樹裡以 `PRId` 值分支的 `cpu_probe`。桌面 | 裸機探針讀 `PRId` 與 `Config.M`。`/proc/cpuinfo` 試過了，它印的是一個十進位數字。🔄 **2026-08-25：預測要收窄，而且要在上機前寫進 `bench/PREDICTIONS-*`** —— 四份獨立來源指向 4181 族（`CPU-04`）。🔴 **如果 `PRId` 落在 5281 段，那比落在 4181 段更值錢**：它會同時否證一份 Realtek 自己的公開 datasheet 與兩棵公開的 kernel 樹。**一個先寫下來、方向明確的預測，比一個「未定」有資訊量得多** 🆕 **2026-08-26 進來兩個新輸入，而禁令不動。**① 🔴 **`refs/RTL8196E-VEx-CG_Datasheet_1.1.pdf` §1 自己寫「The RTL8196E-VE1/2/3-CG … An **RLX4181** CPU is embedded」**，而 `SOURCES.json:195` 早就逐字引了 —— 廠商文件，就在這個 repo 裡。⚠️ **而 `SOURCES.json` 同一筆的 caveat 說這台不是那個版本**（那個版本 DRAM 是 MCM 內嵌，這台外接 W9825G6KH，`MEM-01` 量），所以這一段是**關於家族的**；公開的 `RTL8196E-CG` 資料表講同一句話，兩份文件兩個版本。② 第三方（hackpascal）的 `cpu.h` 有一張 PRId 表把 imp `0xCD` 對到 LX4380／RLX4181，**單一來源**。🔴 **兩個都不是「把 `0xCD01` 這個值對到型號」的那張表**，鎖要三段才通：**a.** 這台是 `RTL8196E-VEx-CG`？（量 `0xB8000000 → 0x8196E001` ＋絲印，**`-VEx` 後綴未驗**）**b.** 該零件內含 RLX4181（讀×1，廠商）**c.** RLX4181 的 `PRId` = `0xCD01`（讀×1，第三方）。三段各自的強度寫在這裡，**`CLAUDE.md` 的禁令維持不動。🔄 **2026-08-28：那張表在 2026-08-27 找到了，就在手上的 GPL drop 裡** —— `PRID_IMP_RLX4181 = 0xcd00`，`CLAUDE.md` 的禁令解除，見上表 `CPU-04`。**這一列改 🔄 不是 ✅**，因為殘留還在：一個來源三份逐位元組副本、沒有任何程式碼在讀那張表、`0xdc01`／`0xdc02` 破壞前四格的編碼紀律，而四個佐證裡只有一個不是 Realtek 下游。原文（保留，因為它當時是對的判斷）：**。③ 🆕 **`R1h` 會給第一個矽片上的能力量測**：`cache 0x10` retire 或吃例外 —— `cache-rlx.c` 把 I 側 op 只給 4281／5281，而這台 kernel I 側零條。**那是能力，不是名字** | `R1-gate`（R1e） |
| `CPU-15` | 矽片到底有沒有 `lwl`/`lwr`/`swl`/`swr` | 裸機 Reserved Instruction 處理器下的一條 `lwl` | R1a |
| `CPU-17` | `movz`/`movn` 是矽片實作還是被靜默模擬 | 同一個處理器，一條指令 | R1a / `C-12` |
| ~~`CPU-24`~~ ✅ 🔄 | CP0 r20 的 `0x010` 與 `0x020` | 🔴 **2026-08-26 結案，桌面，零風險 —— 而擋在前面的一直是搜尋方向而不是資料。`0x010 = IMEM0FILL`、`0x020 = IMEM0OFF`**，四個來源、其中兩個彼此獨立，細節在 `CPU-24` 上表與 `notes/cache-model.md`。**它們不是快取命令**，是一塊 16 KiB 本機指令 scratchpad 的生命週期控制（新列 `CPU-46`）。這一格原本寫「剩下兩條路，一條被刻意放棄：① 一份會講的文件；② 從 payload 寫出去看效果」—— **① 成立了，而且那份 Realtek 的命名檔就在已經在手的 GPL drop 裡**（`arch/rlx/include/asm/rlxregs.h`，與 `cache-rlx.c` 同一棵樹）。②「不做」的理由是**當時它們無名**，那個前提沒了：`probe3` 會發 `0x020` 當控制組（因為 I-MEM 與預測的 I-cache 同大，沒有別的東西能分開它們），仍然不發 `0x010`，新理由寫在 `docs/probe3-cells.md` §8 | ✅ 已關 |
| `CPU-24` 殘留 🆕 | CP0 r20 剩下的位元 —— bit 6／7，以及 `0x400`／`0x800` | 🔴 **頭條那一對關了，這顆暫存器沒有全部畫完，而剩下的殘留是「來源互相矛盾」不是「沒有來源」，這兩件事的處置不一樣。** 🆕 **2026-08-26：LX4189 資料表從「只引用」變成「手上有，帶 sha256」**（`SOURCES.json` 從 `reference_only` 移到 `documents`，`6afb1415…`，檔案不進 repo）。**矛盾沒有因此解決** —— 拿到全文之後 bit 6／7 仍然是 `IROMOn`／`IROMOff`，仍然和 `rlxregs.h` 撞。**能拿到文件與文件說得清楚是兩件事。** **bit 6／7**：LX4189 資料表說 `IROMOn`／`IROMOff`，Realtek 的 `rlxregs.h` bit 6 說 `IMEM0ON` 而 bit 7 空著，第三方 `lxregs.h` 又把 `ILock` 定義成 `0xc0` —— 而 `ILock` 在 LX4189 的圖裡是 bits 3-2，所以第三方那一份**自己和自己撞**（`ILock` 與它自己的 `IROM0ON\|IROM0OFF` 同一組位元）。推：那是第三方 header 的 bug，不是不同的矽片。**別把 `0xc0` 帶進程式碼。** **`0x400`／`0x800`（`DMEM0ON`／`DMEM0OFF`）**：只有 `rlxregs.h` 一票，LX4189 的圖裡整個不存在（它是 write-through 的部件，LMI 配置不同）。**兩格都不進 payload**：一個值不會靠單一來源進程式碼，而互相矛盾的兩個名字之間，量測分不出來哪一個對 —— 這裡缺的是文件不是實驗。⚠️ 而這台上**沒有任何軟體發過這四個位元**，所以連「位置」這種弱證據都沒有 | 一份會講的文件；桌面，零風險 |
| `CPU-31` | `HI`/`LO` 與 CP0 的 hazard 規則 | 裸機上一組 `mult`→`mflo`、`mtc0`→`mfc0`，中間補 0/1/2 個 `nop`，看讀值何時開始正確。`CPU-29`／`CPU-30` 已經指出原廠一個都沒補 | R1b |
| `CPU-25` 🔄 | ~~快取大小、line 大小~~、**關聯度** | 🔄 **2026-08-27：三項裡的兩項由 `boards/rtl8196e/bsp/bspcpu.h` 填掉了（讀，不是量），剩下的是關聯度，另見上面新增的 `CPU-25` 殘留那一列。** 下面這段仍然成立，因為它講的是**在矽片上**怎麼量，而填上去的那兩項是讀的：**不再是「重現 `r3k_cache_size()`」** —— 那支在這顆核心上只會回 0，見上一列與上表。改成 `probe3` 的 eviction walk。🆕 **2026-08-26：現在它有一個要被否證的預測，而不是一片空白。** 這台自己的 kernel 宣告 **D-cache `0x2000` = 8 KiB**（`0x8000CAAC` 等三處 `sltiu … 0x4000` = `cpu_dcache_size × 2`）與 **16 byte 的 D line**（八條 `cache` op 蓋 128 bytes）。⚠️ **三個強度不一樣**：line 大小只靠二進位就讀得到；8 KiB 要靠 `cache-rlx.c` 才解得開那個常數；**I-cache 大小這條路完全讀不到**，因為 I 側這個 build 沒有 per-line op。⚠️ 而它們全都是「build 相信的東西」，不是矽片。**寫在 walk 之前，所以是否證條件；寫在之後就只是描述**  🔴 **2026-08-26 兩處更正。**① **來源盤點錯了，而且是往「來源更多」的方向錯**：`refs/` 那份資料表第一頁就給了 I 16 KiB／D 8 KiB，`SOURCES.json:195` 早就逐字引著 —— 所以**沒有任何來源的那一格是關聯度**，不是 I-cache 大小。② 🔴 **這支 walk 的機制與這個預測講的是不同的快取**：`probe1` cell 1／cell 5 量的是 **I 側**，而 8 KiB／16 B 這個預測是 **D 側**。`probe3` 帶**兩支** walk，D 側那支由 cell A 在執行期閘控。③ 🔴 **新的混淆源 `CPU-46`**：16 KiB I-MEM 與預測的 16 KiB I-cache 同大。④ 🆕 🔴 **2026-08-26 傍晚：「關聯度任何來源都沒有」這句話翻了。** LX4189 §5.1 Table 18 給 `ICACHE` 兩種形式 —— *"Direct mapped or two-way set associative"* —— 而 `DCACHE` 只有一種，*"Direct mapped"*（Table 25 七個組態全是 direct mapped）。所以 **I 側搜尋空間是 {1, 2}，D 側預測是 direct-mapped**，`w-assoc`／`v-assoc` 從盲掃變成有預測可否證：**量到 K = 4 就否證這條血緣**。⚠️ **LX4189 不是這顆**（Table 2 沒有 TLB，這顆有 32 個 entry，`CPU-08` 量），是讀 ×1 的旁證。⑤ 🆕 **line 大小的上界也錯了**：LX4189 §5.1／§5.6 說 line 可組態成 **16／32／64／128 位元組**，所以格子表原本「STALE 跑到 +192 就作廢」的門檻會把一條真的 128 位元組 line 記成 void —— 探針延伸到 +320，門檻移到 +256。格子表：`docs/probe3-cells.md` §1.4 | R1h，桌面＋一次上機 |
| `CPU-19` 殘留 🆕 | 兩件事：① D-cache 是 **write-through** 還是 **write-back 但不做 write-allocate**；② `Status.IsC`／`SwC` 到底有沒有被實作成位元 | ① `probe1` 的六格分不開，因為兩格的 store 都打在 D-cache 沒持有的 line 上；判決它的是 `docs/rlx-cache-and-cp0.md` § ② 的 **cell E** —— 先 load 再 store 同一個位址，然後 uncached 讀回。🔴 而**廠商自己的 `boards/rtl8196e` 設定是 `CONFIG_ARCH_CACHE_WBC=y`**（write-back），所以這一格不是「沒人想過」，是**有一票投反面**。② 原本指名 `probe2`／`R1g-4b`，**而 `probe2` 依它自己的稽核要求，全檔沒有任何一條 `mtc0` 到 CP0 12** —— 那個實驗跑了而且不含這一格。要的是一次 `Status` 寫入後的讀回  🆕 **2026-08-26 兩格都排進 `probe3` 了**：① 是 cell `c-E`（先 load 再 store 同一條 line，然後 uncached 讀回），🆕 🔴 **2026-08-26 傍晚：② 的「控制位元」定案了，而且連期望值都不再是留白。** LX4189 §3.4.1 的 `STATUS` 圖把 **27-23、21-16、7-6 全列為「0」欄位**，散文寫 *"The 0 fields are ignored on write and are 0 on read"* —— 於是 **控制位元取 bit 6 與 bit 24**（兩顆，位在暫存器兩端，**一顆看不到部分遮罩**），而**同一張圖把 bit 16 自己也放在「0」欄位裡**：在那顆 Lexra 上 `IsC`／`SwC` 根本不存在，所以裝置端的預測是 **bit 16 讀回為 0**，讀 ×1。⚠️ **LX4189 沒有 TLB 而這顆有 32 個 entry**，它是同廠的另一顆，不是這顆。⚠️ 也要照實說：bit 6／24 在 MIPS32 的圖裡**有畫**（`SX` 是 MIPS64 的 64 位元位址致能、`MX` 是 DSP ASE），只是這顆 `Config.M = 0` 量到不是 MIPS32 核心。② 是 cell `s-isc`（**只寫 IsC 不寫 SwC** —— 對調期間的 I-fetch 行為無任何來源，而這顆已量到不照 R3000 走 IsC，所以不能用 R3000 語意推 SwC 安全）。期望值與否證條件：`docs/probe3-cells.md` §6.5、§6.7 | R1h |
| `CPU-44` 殘留 🆕 | `cache` 指令在這顆矽片上會不會 retire —— **「在二進位裡」與「會執行」是兩句話** | 一條 `cache 0x11` 放進 payload，在已經量到可用的 handler 底下執行。retire 或吃 Reserved Instruction，**兩種結果都是答案**，而且都不花一次電源循環。順帶：`cache 0x15`／`0x11` 若能跑，它就是 `CPU-45` 的候選解  🆕 **2026-08-26：qemu 在這一格上完全不能當對照，而且它的答案會長得像確認。** 量(qemu)：`cache 0x10`／`0x11`／`0x15`／`0x19`／`0x1b` 全部 retire，**而且 op 欄 32 個值全部 retire** —— qemu 根本不解碼 op 欄。所以裝置若也 retire，qemu 那一欄什麼都沒說。⚠️ **`0x1b` 不進 payload**：Lexra 叫它 `DWB_IInval`，MIPS32 那個編碼是 **Hit Writeback Secondary**，兩邊直接衝突，而它也是全 image 零出現的那一條 —— 二進位判不了它。`docs/probe3-cells.md` §6.4 | R1h |
| `CPU-45` 🆕 | D-cache 對「記憶體被 CPU 以外的東西改掉」的一致性 —— read-allocate 嗎？哪一條命令能作廢一條 clean line？ | `docs/rlx-cache-and-cp0.md` § ② 的 cells A–D：用 KSEG0/KSEG1 別名當 DMA 代理（`probe1` 已經證明這條機制在這顆上成立），**不需要 DMA 引擎、不需要驅動**。⚠️ 代理是模型不是實物：cell A 回 fresh 時分不開「不 read-allocate」「別名被 snoop」「line 被逐出」，所以要 `probe1` 那招兩個 victim 隔很遠當控制  🔴 **2026-08-26 收窄：cell E 只在它的兩個分支之一裡是 cell A 的正控制。** E 若量到 store 被扣住，line 就是常駐的，read-allocate 為真，A 的 *fresh* 只能是「別名被 snoop」；**E 若量到 write-through，它對常駐與否一句話都沒說**，A 的 *fresh* 仍是二選一，而**這兩個只有真的 bus master 分得開** —— 對 `R6` 來說它們等價，前提是真的 DMA 寫從快取那一側看起來像 uncached CPU store，**而那個前提要等 `R6` 用真引擎重測**。cells 加到七格（多了 A′、F、E2），且 **A 為負時 B／C／D／F 不可否證，記 void 不記 pass**。🆕 🔴 **2026-08-26 傍晚：cell A 第一次有了期望值，而且多了一格 `c-G`。** LX4189 §5.2 兩句話 —— *"a write to a KSEG1 address has no affect on the contents of the data cache"* 給 `c-A` 預測 **`l2 = P0`（有 stale line）**，讀 ×1；*"perform an uncached read … If the location is resident in the data cache it will be invalidated"* 則是**一條不用任何 `CCTL` 的 per-line 作廢原語**，而這個 repo 從來沒有測過它 —— 那就是新的 `c-G`，五個 load、零條新指令，一樣掛在 `c-A` 的閘門後面。⚠️ 同一節還說 *"Caches do not snoop the system bus"*：**核心廠商自己的文件就說別名代理與真 bus master 在這一點上不一樣**，所以 §6.5 那句「要等 `R6` 用真引擎重測」不是謹慎，是有文件根據的。`docs/probe3-cells.md` §1.4、§6.5 | R1h |
| ~~`CPU-27`~~ ✅ | ~~提示字元下的 `Status.BEV`~~ —— **2026-08-25b 量到了，見上表：`Status = 0x1000FC00`，bit 22 = 0**，而 `break` 陷進我們裝在 `0x80000080` 的 handler 並回來，是「核心真的從那裡取指」的直接證據。⚠️ **殘留**：量到的是 payload 在 `J` 清掉 `IE` 之後看到的 `Status`，不是提示字元下那一個；兩者只差 `IEc`，而沒有 loader 命令讀得到 CP0，所以提示字元下那個字仍然只有 `讀`。*(原文：)* 🔄 **追過了，是 `0`，單一來源**（`0x80406694`）—— 見上表。**還缺的是量測**，而唯一能量它的是 `probe2`，已延到 `R1g-4b`。否證條件寫在 payload 裡：`BEV != 0` 時 `probe2` 拒絕安裝並重置。🔴 **2026-08-25 更正：本格原本寫「並蓋 `0xBE71BAD1`」，而那個常數在任何 payload 原始碼裡都不存在** —— 量：`grep -rin be71bad1 tools/` 零命中，同一支 grep 找 `DEADC0DE` 命中 `probe1.c:117` 與 `probe2.c:127`，所以這個搜尋是會失敗的。而且「結果字 23」現在是 `H_CNT_BEFORE`。**真正的拒絕簽章是兩個互相獨立的訊號**：區塊字 2 的 `progress=00000010`，與字 6 `status` 的 bit 22 —— 比一個魔術數字強，因為兩個都要對。同一個錯誤同時在 `RUNSHEET.md` 兩處，一個事實有三個擁有者而沒有一個是程式碼 | ——（已關，見左） | R1g-4b |
| `MEM-08` | SDRAM timing | 讀 loader 對記憶體控制器的寫入 | R5 |
| `MEM-11` 🆕 | `0x81000400` 那張 32 位元組週期的表是**誰**配的、拿來做什麼 | 讀建表的那段常式 —— 桌面工作，不用上機。`0x400`（=1024）與開機時印的 `---Ethernet init Okay!` 一起指向 loader 的網路緩衝池，但那是推的。**這一格關係到 `§G`**：若那張表就是 TFTP 自己的緩衝，往它上面上傳會是「用一次傳輸蓋掉這次傳輸自己的緩衝」 | R6 |
| `MEM-12` 🆕 | `0x81800000` 那個結構是誰配的，`0x81C09988` 為什麼落在 28 MiB | 同一份桌面工作。兩個結構互指，所以八成是同一個配置器 —— 而「八成」正是這一格還開著的理由 | R6 |
| `REG-04` | `IRR2` 從沒被讀過 | 一次 `DW B8003010 1`，零風險，順手 | R6 |
| `REG-13` 🔄 | SPI 控制器的暫存器 —— **四個已讀，第五個還沒**。`SFCR`/`SFCR2`/`SFCSR`/`SFDR` 冷暖各一次，量 2026-08-25b，見上表。`SFDR2`（`0x1210`）仍未讀，因為 `LDR-07` 的向上進位讓 `DW B8001200 N` 只印四個字 —— 要讀它得換起始位址（`DW B8001204 4` 會蓋到 `0x1204`–`0x1210`）。**命令序列仍然只有一個來源**，而那才是寫 flash 之前的門檻 | ——（見左欄） | R5b |
| `REG-14` | `SFCSR` bit 27 `SPI_RDY` 的實際行為 | 這台自己的 `ComSrlCmd_RDID()` 就在等它，但沒人看過它翻 | R5b |
| `FLS-15` | flash 的第二支儀器 | 一組 `VCC` 線可以斷開的夾具，**外加**觀察到 `SF_CS0#`/`SF_SCK` 在 reset 時是浮接的 | `⊘` —— `notes/power-and-programmer.md` §5 |
| `FLS-06`–`FLS-08` | 這顆真正的 sector／page／block 大小 | EN25QH32B 的 datasheet，或 `RDID` 之後的後續命令。今天這三個值是 loader 的 fallback 預設 | R5b |
| `NET-08` | PHY 位址 5–31 | `PHYR 5 2`，然後 `MDIOR 2` —— **而 `PHYR 5 2` 正是那格會終結一次上機的** | `RUNSHEET.md` §F |
| `NET-07` 🆕 | `PORT1` 為什麼跳過位址 1 —— **「埠 1 沒有網路孔」已被 `E11e` 否證**（`NET-13`），原因這一格重新開著 | 讀那段常式實際寫了什麼：page 1 的 vendor 暫存器 19（`0x8040B84C` 的 17 字 Gray code 表），以及寫到 PHY 4 暫存器 20 的 `0xB20` 或上 `1<<phy` —— `1<<phy` 是整支常式裡唯一出現「每埠選擇」的地方。若那一對是為每個受測埠自己而設而不是把另外四埠讓開，這個讀法就倒。**不能靠執行命令來做**：`PORT1` 不吃參數、也停不下來，所以是對 A 的桌面工作 | `C-15` / R6 |
| `NET-10` | `0xBB804118`/`0x411C` 是什麼 | 值已量到；照 `PCRP` 間距推是埠 5 與埠 6，而**間距是推的**，沒有來源明寫。🆕 `PSRP` 側的第 6–8 個字在五次換孔中完全不動，是同一個問題的另一面，一樣沒有來源明寫那幾個位址 | R6 / `C-15` |
| `RF-01` | 無線電型號的第二來源 | 驅動印的是版本，不是料號 | R10d |
| `RF-04` | 無線電怎麼接到 SoC | 從沒追過 | R10d |
| `BRD-01` | `LSP5526` 是什麼 | 量它的輸出腳對地 —— 三十秒的事，而且它是板上唯一與分析無關的一顆 | — |
| `CLK-03` | 200.0049 MHz 與 400 MHz 的關係 | 未量；÷2 是一種讀法，不是一次量測。（原本這一格寫的是 199.48 MHz，見 `CLK-02`） | R1 |
| `CLK-08` | 看門狗的牆鐘逾時 | 🔄 **`D4` 在 2026-08-24 那次上機沒有跑，所以這一格還是空的。** 最短那檔量不到（164 µs／2.30 ms），最長那檔量得到（84.1 ms／1.177 s，相差 14×）—— 四個數字用改正後的 `CLK-02` 重算過，在這個精度下都不動。**要修的是實驗，不是儀器。** `EW B800311C 240000` 武裝 `OVSEL`=`1001`，`D1` 當控制（它的區間是開機時間加上一個 ≈0 的逾時），`D4 − D1` 就是逾時本身，開機時間對消 | `C-8` |
| `CLK-10` | `WatchDogIND` 到底有沒有鑑別力 | `D2` 讀整個字，預測 `A5100000`；讀回 `A5000000` 就是沒有 | `C-8` |
| `MEM-10` | 暖重置後 SDRAM 內容存不存活 | 🔄 **兩處改路，2026-08-24。** canary 從 `0x81000000` 移到 `0x80A00000`（`D0b`／`D2c`），因為前者在 `MEM-11` 那張活表裡，重跑一次初始化就會給出第三種讀數；否證條件從 `D3` 的按鈕改到 `§G` 的電源循環（`D2b-cold`），因為 `BRD-05` 證明那顆按鈕**不給冷開機** | `C-8` |
| `LDR-22` | 是誰在任何 `FLR` 之前填了 `0x80500000` | 值是量到的，而它被宣稱的機制已被否證 | `C-16` |
| `LDR-21` | `gCHKKEY_HIT` 的寫入者 | 桌面十五分鐘，對照 D | `C-13` |
| `TC-02` | 那份 GPL drop 是不是就是這一份 | 建出來比對 | R2a |
| `TC-05` | rlxfw 自己的工具鏈 | T-modern 的 timeboxed spike | R2 |

## 18. 刻意不放在這裡的東西

**任何識別「這一台」而非「這個型號」的東西。** 這條規則在檔案被加進來之前
就適用，不是推上去之後才補。

| | 為什麼 |
|---|---|
| MAC 位址 | 在 flash `0x006000` 的 `H601` 裡，也印在 PCB 背面的條碼標籤上。兩者已被確認逐位元組相同 |
| 序號 | 在正面的 QR 與數字標籤上 —— QR 會被自動解碼，而且大幅縮圖之後仍然可解 |
| WPS PIN | 經由 `HW_WLAN0_WSC_PIN` 可達，而且開機 log 有可能印出來 |
| 射頻校正 | `H601` 的其餘部分 |
| flash dump 本身 | 它既是廠商韌體，又同時裝著上面全部。`SOURCES.json` 記錄怎麼取得公開輸入；dump 不是其中之一 |
| 兩份 datasheet | `refs/README.md` —— 引用、記錄雜湊，永不轉載 |

`tools/audit-bench-log.py` 會對任何要進 `bench/` 的檔案掃描以上這些形態，
而且**每一條樣式都先跑過一份合成的正控制**，除非全部命中，否則它拒絕回報乾淨。
